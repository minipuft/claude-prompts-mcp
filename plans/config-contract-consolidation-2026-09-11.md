---
title: "Config contract consolidation — one owner for the shape of config.json"
date: 2026-09-11
status: active
initiative_branch: feat/config-jsonc
tracking: https://github.com/minipuft/claude-prompts-mcp/issues/287
publish: push+merge
tags: [config, schema, validation, cli, contracts]
---

# Config Contract Consolidation — One Owner For The Shape Of `config.json`

**Status**: ACTIVE — T0 gate passed 2026-09-11 (row 0.5 open); T1 in progress
**Owner**: minipuft
**Created**: 2026-09-11

## Now (2026-09-19, step 5 boundary green — PR opening)

- **Goal**: one owner for `config.json`'s shape. Steps 1–4 merged (#307, #312, #322, #325). Step 5 (R34) is branch `feat/config-jsonc`, linked worktree `../claude-prompts-mcp-jsonc` with its own real `server/node_modules` (R65), `main` `ed73dcd9` merged in (changelog kept both sides, lint baseline re-measured).
- **Slice**: T7 rows 7.1–7.11 and 7.13 ✓, 7.12 ✗ killed. Rows 7.9–7.11 and 7.13 were opened by handoffs and the boundary: a reader no row named, two bundles that could not start, a README promise `--init` never kept, four suite-only gates. Boundary on one snapshot (675ba641): `validate:all` 67/67 · `test:all` unit 3880 / integration 1020 / e2e 283, 0 failed · `verify:mcp` 18/18 · `cli/` 88/88 · `pytest hooks/tests` 314 · live drive on a built server: a commented `config.jsonc` loads and validates by name, a persisted toggle inserts three lines and keeps 330 of 330 comment lines, a `cpm config set` reaches the running server in 2.5 s, its `list` is byte-identical to a strict `config.json` twin (115 lines), both names refuse startup naming both paths, the same commented text as `config.json` refuses (control), a version-less 4.x file translates under either name. `git diff origin/main --name-only` holds only `server/`, `cli/`, `hooks/`, `docs/`, `CHANGELOG.md`, `README.md`, `.husky/pre-commit` and this plan.
- **Next decision**: open the PR (`feat(config)!`), squash-merge when the required checks pass and `mergeStateStatus` is CLEAN (`publish: push+merge`); then remove the worktree and branch, run `npm ci --prefix server` in the main checkout (peers' `validate:lockfile-sync` fails until they merge `main`), and cut step 6 (`$id`, R29, rows 3.1–3.3). #232 stays held (R31); row 2.16's survey before any `persist` extension.
- **Constraint in force**: no edit to a projected `CLAUDE.md` section (AGENTS.md 32767/32768, F-T4-31). `gh pr create` from this worktree needs the main checkout detached at the branch tip first (F-T4-36).

## Why this exists

`config.json` has four authorities on its shape and no test that any two agree.

| Authority                                              | Governs                       | Checked against |
| ------------------------------------------------------ | ----------------------------- | --------------- |
| `server/config.schema.json` (577L, draft-07)           | editor hints + one CI check   | nothing         |
| `Config` + `DEFAULT_*` (`shared/types/core-config.ts`) | the shape the runtime reads   | nothing         |
| `validateConfigInput` (440L switch, 57 keys)           | what `cpm config set` accepts | nothing         |
| `adoptInertSpellings` + `validateAndSetDefaults`       | silent renames + defaulting   | nothing         |

**Root cause: there is no type for the on-disk shape.** `loadConfig` does
`JSON.parse(content) as Config` — an unchecked cast of the _file_ to the _resolved runtime_ type.
The two genuinely differ (`maxVersions`→`max_versions`, flat `frameworks.*Frequency/*Target`→
nested `injection.*`, `advanced.sessions`→`chainSessions`, `gates.directory`→`definitionsDirectory`).
Because no type expresses the file shape, the JSON Schema had to be hand-written, and nothing can
check it against anything. Every symptom below is that one fact leaking.

### Measured symptoms (2026-09-11, all probed with controls)

| #   | Symptom                                                                                                                                                                                                                                | Probe                                                                                                                                                                                              |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1  | Schema strictness is root-only: `additionalProperties:false` at root, unset in all subsections (**authored 26, measured 27** on 2026-09-11 — the plan undercounted by one; the count is now asserted by the gate rather than restated) | `gates.enabld`, `server.prot`, `resources.logs.maxEntrys`, `verification.isolation.tmeout` → all VALID; root `gatez` + `server.port:"nine"` → REJECTED                                             |
| S2  | Nothing validates at runtime — only `scripts/validate-config-schema.ts`, only against the repo's own file. A config at `MCP_CONFIG_PATH` is unchecked                                                                                  | `rg validateConfigAgainstSchema` → 1 script, 0 runtime callers                                                                                                                                     |
| S3  | CLI ↔ schema disagree on 23 of 75 keys (5 CLI-only, 18 schema-only)                                                                                                                                                                    | key-set diff; `frameworks.systemPromptTarget`, `gates.evaluation.defaultMode`, `versioning.maxVersions` → `Unknown configuration key` from `cpm config set`, all three present in the shipped file |
| S4  | `versioning` is spelled inversely on the two surfaces — schema camelCase, CLI snake_case, runtime snake_case, shim folds camel→snake                                                                                                   | `versioning.max_versions` ACCEPTED / `versioning.maxVersions` REFUSED by the CLI                                                                                                                   |
| S5  | `GET /health` reports `version: "1.0.0"`; package is `4.0.1`                                                                                                                                                                           | resolved `getServerConfig()` → `{"version":"1.0.0","port":9090}` — port from file, version from `DEFAULT_CONFIG`                                                                                   |
| S6  | 2 CLI-settable keys have zero readers: `gates.enforcePendingVerdict`, `resources.prompts.defaultRegistration`                                                                                                                          | `rg` → only the CLI's own allowlist + switch                                                                                                                                                       |
| S7  | Shipped file contradicts shipped schema: `logging.level:"debug"` vs schema default `info`; ships in `package.json` `files`                                                                                                             | read both                                                                                                                                                                                          |
| S8  | Shipped file carries inert `gates.mode:"on"` beside `gates.enabled:true` — undeclared, deleted at load, passes CI only because of S1                                                                                                   | schema key diff + `adoptInertSpellings`                                                                                                                                                            |
| S9  | `$id` is a non-fetchable GitHub path; `$schema` is relative, so it breaks under `MCP_CONFIG_PATH`/`MCP_WORKSPACE`; dialect is draft-07                                                                                                 | read both                                                                                                                                                                                          |

**Non-finding, kept so it is not re-derived:** `versioning.maxVersions`/`autoVersion` in the shipped
file are NOT dead — `adoptInertSpellings` folds them to snake_case before defaulting. Probed: a file
with `maxVersions: 7` resolves to `max_versions: 7`. The shim is load-bearing; do not delete it as
part of a cleanup. **Retired by row 6.1 (step 4):** the `version`-keyed translation replaces it, carrying
the same fold, so the fixture above still resolves to 7.

**Correction to the first appraisal:** `unevaluatedProperties: false` is a no-op here. AJV 8.20 under
draft-07 with `strict: false` compiles and ignores it — probed `{a:{typo:1}}` still ACCEPTED. Per-section
`additionalProperties: false` is the working equivalent (this schema has no `allOf`/`$ref` composition).

## Intent Declaration

**Work Type**: refactor (with 2 embedded bug fixes: S5, S6)
**Confidence**: high for T0–T3, medium for T4, low for T5 (needs a ruling)
**Scope**: `server/config.schema.json`, `server/config.json`, `src/infra/config/`,
`src/cli-shared/config-input-validator.ts`, `src/shared/types/core-config.ts`,
`src/mcp/tools/system-control/handlers/config-action-handler.ts`. Risk: low through T3 —
no key shapes change. T5 is a major-version change.
**Problem Statement**: Four authorities, no agreement test, silent defaulting on typos →
one generated authority, strictness that fires, and a warning the operator sees.

## Existing Systems Audit

**Reuse before creation.** Nothing new gets built that these already do.

| Existing                                                                  | Reused how                                                                                                                |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `validateConfigAgainstSchema` (94L)                                       | Already returns `{valid, errors[]}` with AJV instance paths + an mtime-keyed compile cache. T1 is a caller, not a rewrite |
| `warnAnalysisSectionDeprecated` + `warnedAnalysisDeprecated`              | The warn-once-per-process pattern T1 must copy — hot reload re-enters `loadConfig`                                        |
| module-level `logger` (`createLogger(getDefaultLoggerConfig())`, line 15) | No chicken-and-egg: a logger exists before config loads. T1 needs no new wiring                                           |
| `system_control config validate` action                                   | The existing operator-facing surface for config health. T1's schema result lands here, not in a new action                |
| `scripts/validate-config-schema.ts`                                       | Already in `run-validation-suite.js:178`. Gains no new wiring; its strictness improves via T0                             |
| `adoptInertSpellings` table                                               | Already the migration mechanism. T2 adds entries; it does not build a second one                                          |

**New files**: one (`config-file-schema.ts`, T4) — justified inline at its row.

## Consequence Tracing

| Change                                   | READS                                                                                  | WRITES                                                                    | DECIDES                                        | VIEW/PROJECTION                                 |
| ---------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------------------- | ----------------------------------------------- |
| per-section `additionalProperties:false` | `validate-config-schema.ts`; T1's runtime call                                         | hand edits; `cpm config set`; `system_control config set`                 | CI pass/fail; T1's warning                     | —                                               |
| schema validation at load                | `ConfigLoader.loadConfig`                                                              | —                                                                         | warning emission only — must NOT gate startup  | stderr (STDIO-safe); `config validate` response |
| `server.version` (S5)                    | `mcp/http/api.ts:144` (`/health`), `runtime/application.ts:386`, `config-utils.ts:405` | `DEFAULT_CONFIG.server`; `cpm config set server.version`                  | `config-utils` marks config invalid when falsy | HTTP `/health` JSON body                        |
| removing 2 zero-reader CLI keys (S6)     | none                                                                                   | `config-operations.ts:430,437`; `config-input-validator.ts:55,38,154,161` | —                                              | `cpm config list` output                        |

`/health`'s `version` field is a consumer-observable HTTP response value. It is not named in
CLAUDE.md §Public API Contract (which lists MCP tool surface, CLI surface, resource formats,
hook contract, symbolic command language). Treated as out-of-contract here — see OQ-2.

## Implementation Table

Status vocabulary: `☐ (as of <date> · flips when <observation>)` · `✓ <date> · <receipt>` · `⊘ (verified <date> · <reason>)` · `✗ KILLED (<date> · <reason> · revives if <observation>)`.
Row ids compile to node ids (`0.1` → `t0-1`); `Depends` holds row ids within the tier.

### T0 — Make the gate that exists actually fire

| #    | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | File                                                                                            | Change              | Depends  | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0.1  | ✓ 2026-09-11 · `server/config.json` — `gates.mode` gone, `logging.level: info`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `server/config.json`                                                                            | Edit                | —        | Delete inert `gates.mode:"on"` (S8) and ship `logging.level: "info"` (S7, R3). Must precede 0.3 or 0.3 fails CI                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 0.2  | ✓ 2026-09-11 · `server/config.schema.json` — all 3 declared, each marked DEPRECATED with its OQ (DEV-T0-2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `server/config.schema.json`                                                                     | Extend              | —        | Declare `server.version`, `gates.enforcePendingVerdict`, `resources.prompts.defaultRegistration` — the CLI-writable keys that would otherwise fail 0.3 (S3). Provisional; T2 decides their fate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 0.3  | ✓ 2026-09-11 · `server/config.schema.json` — 27/27 strict                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `server/config.schema.json`                                                                     | Extend              | 0.1, 0.2 | Per-section `additionalProperties:false` (S1). NOT `unevaluatedProperties` — proven no-op under draft-07/AJV 8.20                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 0.4  | ✓ 2026-09-11 · `server/scripts/validate-config-schema.ts` 8/8 · `package.json` self-test script · `scripts/run-validation-suite.js` converse flipped + `reads` corrected (DEV-T0-3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `scripts/validate-config-schema.ts`                                                             | Extend              | 0.3      | Self-test with a positive control. Without it 0.3 is an unverified claim                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 0.5  | ✓ 2026-09-15 · `server/tsconfig.scripts.json`, `server/package.json`, `server/scripts/run-validation-suite.js` (commit da1e1184). `typecheck:scripts` exits 0; a planted type error in a new script exits 2 naming the file; `validate:suite-membership` reports 59 in SUITE; knip ratchet no regressions · worker sonnet, shared-tree                                                                                                                                                                                                                                                                                                                                                                                                        | `server/tsconfig.scripts.json`, `server/package.json`, `server/scripts/run-validation-suite.js` | **Create** + Extend | 0.6, 0.7 | **Rewritten 2026-09-14 from discovery (notes R19).** Authored: "a typecheck gate reads `scripts/`". Measured: `tsconfig.json` includes only `src/**/*` and `tsconfig.test.json` adds only `tests/**/*`; typechecking the 23 `.ts` scripts yields 15 errors in 4 files. A new `tsconfig.scripts.json` follows `tsconfig.test.json`'s one-project-per-target precedent, because `tsconfig.json`'s `rootDir: src` serves the build. A plain zero-error `typecheck:scripts`, not a ratchet — a ratchet absorbs a backlog too large to fix, and 15 is not one. The SUITE entry is what puts it in CI. Its `reads` is `['spawn']`, measured 2026-09-14 by calling `deriveSubstrate` (`scripts/lib/substrate.js`) on `tsc --noEmit --project tsconfig.scripts.json`, with `validate:format` → `[spawn, tracked]` and `typecheck:tests:ratchet` → `[file, spawn]` as controls matching their declarations                                                                                                                                      |
| 0.6  | ✓ 2026-09-15 · `server/scripts/table-contracts-reader.ts`, `server/scripts/validate-prompts.ts` (commit 947915bc). Planner re-ran `validate:table-contracts`, `validate:no-phantom-columns`, `validate:prompts` and their self-tests, all exit 0; scripts typecheck reports 0 under `scripts/` · worker sonnet, shared-tree                                                                                                                                                                                                                                                                                                                                                                                                                   | `server/scripts/table-contracts-reader.ts`, `server/scripts/validate-prompts.ts`                | Edit                | —        | 7 of the 15 (6 + 1), measured twice: TS2532/TS2345-shaped narrowing. Fix by narrowing, never by casting to silence — each script must still exit and print as before                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 0.7  | ✓ 2026-09-15 · `server/scripts/validate-shipped-frameworks.ts`, `server/scripts/verify-action-inventory.ts` (commit 947915bc). Planner re-ran `validate:shipped-frameworks`, its self-test and `verify:action-metadata`, all exit 0; scripts typecheck reports 0 under `scripts/` · worker sonnet, shared-tree                                                                                                                                                                                                                                                                                                                                                                                                                                | `server/scripts/validate-shipped-frameworks.ts`, `server/scripts/verify-action-inventory.ts`    | Edit                | —        | 8 of the 15 (4 + 4), same shape and same constraint as 0.6                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 0.8  | ✗ KILLED (2026-09-14 · `checkJs` over the 50 `.js`/`.mjs` scripts yields 695 diagnostics, 426 of them implicit-`any` parameters: annotation work on untyped JavaScript, not defects, and not this initiative's concern · revives if a `.js` script is converted to `.ts`, or a defect in one is traced to a type error `checkJs` would have caught)                                                                                                                                                                                                                                                                                                                                                                                           | —                                                                                               | —                   | —        | Typecheck the JavaScript scripts too                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 0.9  | ✓ 2026-09-15 · `server/scripts/validate-prompts.ts` (commit d604e869). Planner re-ran on the committed tree: `typecheck:scripts` exit 0; `validate:prompts` and its self-test exit 0 with output byte-identical to the pre-fix baseline; eslint 0 on the file. Worker: reverting the `include` narrowing fails the new self-test case (`object is not iterable`); `validate:suite-membership` and the knip ratchet exit 0 · worker sonnet, shared-tree                                                                                                                                                                                                                                                                                        | `server/scripts/validate-prompts.ts`                                                            | Edit                | —        | **Found re-verifying the second `main` merge (DEV-T0-5).** `main` (b3e8df31) added `collectIncludedGateIds`, which casts `gateConfiguration.include` and a chain step's `inlineGateIds` to `unknown` and iterates `x ?? []`. That expression types as `{}`, so the merged tree's scripts typecheck reports TS2488 at lines 238 and 242. `main`'s CI never saw it, because this gate is not on `main` yet. The same cast asserts `chainSteps` is an array, which YAML does not guarantee: a mapping there throws `not iterable` and aborts the whole run. Narrow all three with `Array.isArray` and skip a non-array. `validateFile` runs `validatePromptYaml` over the same files, and the schema declares all three as arrays (`prompt-schema.ts:205`, `:351`; `workflow-ir/node-schema.ts:160`), so a malformed shape is already reported by name — and an include that goes uncounted makes its gate report as ORPHAN, loud rather than silent. Pin it with a self-test case: a prompt whose `include` is a mapping must not throw. |
| 0.10 | ✓ 2026-09-15 · `server/scripts/validate-prompts.ts` (commit 3f87bd08). Planner re-ran on the tree: `typecheck:scripts` exit 0; `validate:prompts` and its self-test exit 0 with output byte-identical to the pre-0.9 baseline; eslint 0. Worker: reverting either guard fails the self-test (`Cannot read properties of null`, reading `gateConfiguration` / `activation`); `validate:suite-membership` and the knip ratchet exit 0. A null gate is reported, not skipped: the only other reader of every `gate.yaml`, `validate:gate-index`, crashes on it (row 0.11). The re-run enumeration finds all four casts of a `yaml.load` result in the file guarded · worker sonnet, shared-tree                                                  | `server/scripts/validate-prompts.ts`                                                            | Edit                | 0.9      | **Found by row 0.9's worker enumerating the class.** `collectIncludedGateIds` reads `doc.gateConfiguration` and `findOrphanGates` reads `gate.activation` straight off `yaml.load`, which returns `null` for a file containing only `null` or `~`. Both throw, and because `run()` builds its result by spreading `findOrphanGates`, the throw also discards the schema problem `validateFile` already found for that file — a stack trace in place of a named error. `findDroppedGates` is the file's own guard for this (`?.` directly after the cast). A null prompt is skipped, since `validateFile` reports it. A null gate is skipped only if a step in `validate:all` is measured reporting it; otherwise this check is its only reader and reports it. The worker re-runs the enumeration afterwards, and the row closes the class in this file only when no unguarded dereference of parsed YAML remains                                                                                                                      |
| 0.11 | ✗ KILLED (2026-09-15 · `scripts/generate-gate-index.js` (`validate:gate-index`) throws on a `gate.yaml` whose content is only `null`: `classifyGate` reads `gate.id.startsWith` off a spread of `null`, measured by row 0.10's worker. Loud, not silent — the step exits non-zero with a stack trace, and since row 0.10 `validate:prompts` names the same file in the same `validate:all` run. A JavaScript script outside this slice, and 0.8 killed typechecking those · revives if a null or non-mapping `gate.yaml` is seen in a real tree, or the script is converted to TypeScript; whoever fixes it also rewrites the comment above `findOrphanGates`'s mapping check in `validate-prompts.ts`, which cites this crash as its reason) | —                                                                                               | —                   | —        | Report a null `gate.yaml` from the gate index generator instead of crashing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

### T1 — Warn at load, keep serving

Rulings this tier depends on: notes R9–R13. Dispatch surface is the `Agent` tool, which binds `model` only, so each row's declared effort is intent and the session's effort is what runs. Branch mode for the whole slice is `shared-tree`.

| #    | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | File                                                                                                                                                                                                                                                                 | Change                                                                                                                                                                                                                  | Depends                                                                                   | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1  | ✓ 2026-09-14 · `server/src/infra/config/index.ts`, `server/src/runtime/context.ts`, `server/src/infra/config/config-schema-validator.ts` — planner re-drive of a built server with `CI` and `NODE_ENV` unset: typo config → exit 0, one stderr line naming `/gates` and `enabld`, stdout 0 bytes; clean config → none. Config unit tests 32/32, self-test 9/9, no lint rule grew. Warning channel per R20 · worker opus, shared-tree, artifact spot-read                                                                                                                                                                                                                           | `src/infra/config/index.ts`, `src/runtime/context.ts`, `src/infra/config/config-schema-validator.ts`                                                                                                                                                                 | Extend                                                                                                                                                                                                                  | 1.6                                                                                       | **Rewritten 2026-09-14.** Authored: "call `validateConfigAgainstSchema` after parse". Three premises did not survive re-measurement (notes R9–R11). `ConfigLoader` receives the schema path by injection — `runtime/context.ts:115` passes the package root's `config.schema.json`, no `import.meta.url` depth arithmetic. It validates the RAW parsed file inside `loadConfig` (`index.ts:286`), before `validateAndSetDefaults` (`:704`), whose first act is `adoptInertSpellings` (`:707`). One `logger.warn` per error path, repeated only when the error set changes (R16): hot reload re-enters `loadConfig`, so an unchanged file stays quiet and a new mistake is still reported. Each warning names the offending key — AJV's `additionalProperties` message does not, so `formatAjvErrors` appends it (R17). No port change: `serverRoot` is already in scope at `context.ts:115`. An unavailable schema warns once as unavailable (R10). Stores the last result for 1.3 — absorbs 1.2                                                                                                                                                                                                                      |
| 1.2  | ✗ KILLED (2026-09-14 · merged into 1.1: same file and strictly sequential, so two workers on it is a merge conflict the plan authored · revives if 1.1's handoff shows the stored result needs its own design)                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | —                                                                                                                                                                                                                                                                    | —                                                                                                                                                                                                                       | —                                                                                         | Store the last validation result on the loader                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 1.3  | ✓ 2026-09-15 · `server/src/mcp/tools/system-control/handlers/config-action-handler.ts`, `server/src/shared/types/config-manager.ts`, `server/src/infra/config/config-schema-validator.ts`, `server/tests/unit/mcp-tools/prompt-engine/prompt-engine-validation.test.ts` (commit 9f2b330e). Worker live drive over MCP with declared params only: typo config → mismatch report naming `/gates` and `enabld`; clean → matches; no operation → unchanged listing; removing the branch reverts to the dump. Planner read the diff and ran the batch gates (R24). An independent planner drive is folded into the PR-boundary live drive · worker sonnet, shared-tree                  | `system-control/handlers/config-action-handler.ts`, `src/shared/types/config-manager.ts`, `src/infra/config/config-schema-validator.ts`, `tests/unit/mcp-tools/prompt-engine/prompt-engine-validation.test.ts`                                                       | Extend                                                                                                                                                                                                                  | 1.1                                                                                       | **Re-cut 2026-09-14 after a live drive (notes F-T1-7, R14).** Authored: `validate` with no key reports the stored result, falling back to `list`. Measured: the handler's `config` object never arrives over MCP, so every config call returns the full dump. The reachable surface is the DECLARED top-level `operation`: when `action:config` arrives with `operation:"validate"` and no `config` object, report the loader's stored schema result — valid, the invalid paths, or unavailable. Every other config path is unchanged; declaring the missing inputs is row 2.5. The `ConfigManager` interface gains 1.1's `getSchemaValidation()` as a REQUIRED member (R18); `undefined` renders as "not validated", never as success. No test exercises this handler today, so the row's check is a live drive of a built server Read `getSchemaValidation()`; never re-validate `getConfig()` — filling defaults adds a root `transport` the schema does not declare, which 1.1's mutation measured. Four files, one over the default bound, by R22: `shared/` may not import `infra/`, so `ConfigSchemaValidationResult` moves into `src/shared/types/config-manager.ts` and the validator imports it from there. |
| 1.4  | ✓ 2026-09-15 · `server/tests/unit/infra/config/config-schema-warning.test.ts`, `server/scripts/validate-config-schema.ts` (commit 6ea2ba32). Config tests 34/34 re-run by the planner on the final tree; both mutations turn the tests red (worker); header corrected · worker sonnet, shared-tree                                                                                                                                                                                                                                                                                                                                                                                 | `tests/unit/infra/config/config-schema-warning.test.ts`, `server/scripts/validate-config-schema.ts`                                                                                                                                                                  | Create                                                                                                                                                                                                                  | 1.1                                                                                       | **Path corrected: authored `tests/unit/config/`, measured sibling dir `tests/unit/infra/config/`.** Copies `legacy-key-migration.test.ts`'s `managerFor` + `console.warn` spy. Cases: nested typo → one warning naming the path and the config still returned; clean config → zero warnings (positive control); unavailable schema → one unavailable warning; second `loadConfig` on the same file → no repeat; a changed error set on reload → warns again (R16). Mutations that must fail it: drop the validate call; move it after `adoptInertSpellings` The case list is 1.1's reload table (notes, handoff 1.1). Warnings reach `console.warn` directly (R20), so the spy observes them outside jest's test env too. The row also corrects `scripts/validate-config-schema.ts`'s header, which still says no runtime path calls the validator — false since 1.1.                                                                                                                                                                                                                                                                                                                                                 |
| 1.5  | ✓ 2026-09-14 · `server/package.json` carries the schema in `files` (npm pack: True) · `server/config.schema.json` declares the snake_case pair deprecated (ajv: VALID; `max_versionz` REJECTED) · worker sonnet, shared-tree, artifact spot-read                                                                                                                                                                                                                                                                                                                                                                                                                                   | `server/package.json`, `server/config.schema.json`                                                                                                                                                                                                                   | Extend                                                                                                                                                                                                                  | —                                                                                         | **Found re-measuring 1.1.** The schema is absent from the published tarball, so load-time validation would read nothing in any install (R9). Add it to `files`. Declare `versioning.max_versions` and `versioning.auto_version` deprecated, because the CLI writes that spelling today (R13)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 1.6  | ✓ 2026-09-14 · `server/src/infra/config/config-schema-validator.ts` takes `schemaPath` and returns `status: valid                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | invalid                                                                                                                                                                                                                                                              | unavailable`·`server/scripts/validate-config-schema.ts` self-test 9/9 on the planner's re-run, mutation killed · eslint on the validator 13 at HEAD → 12, no rule grew · worker sonnet, shared-tree, artifact spot-read | `src/infra/config/config-schema-validator.ts`, `server/scripts/validate-config-schema.ts` | Extend                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | —   | **Found re-measuring 1.1.** The validator resolves the CONFIG's `$schema` relative to the config file — ENOENT under `MCP_CONFIG_PATH`, and a URL once 3.2 lands — and reports any schema read failure as an invalid config. It takes the schema path as a parameter, and its result distinguishes valid · invalid · unavailable (R9, R10) |
| 1.7  | ✓ 2026-09-15 · `CHANGELOG.md` (commit db76c36c). Three `[Unreleased]` entries — Added (startup schema check, shipped schema, `config validate` report), Changed (shipped log level, `gates.mode` dropped), Fixed (every schema section rejects undeclared keys); additions only; no plan vocabulary; each claim tied to the code the worker read · worker sonnet, shared-tree                                                                                                                                                                                                                                                                                                      | `CHANGELOG.md`                                                                                                                                                                                                                                                       | Extend                                                                                                                                                                                                                  | 1.1, 1.3                                                                                  | **Found pre-reading the PR boundary.** The repository keeps a hand-edited `[Unreleased]` section that `scripts/merge-unreleased-changelog.js` folds into each release, and nothing in T0 or T1 has written to it. Behavior changes that need an entry, in the reader's register: a misspelled key inside any config section now fails schema validation; the shipped `config.json` logs at `info`; the package ships `config.schema.json`; the server warns at startup when `config.json` does not match the schema and keeps running; `system_control action:config operation:validate` reports that result. Sections per `/changelog-generator`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 1.8  | ✓ 2026-09-15 · `server/src/infra/logging/index.ts`, `server/tests/unit/infra/logging/stdio-console-output.test.ts` (commit 1778723b). Under STDIO outside CI, ERROR/WARN reach stderr and INFO/DEBUG need `enableDebug`; CI and non-STDIO unchanged (R36). Restoring the old gate fails 3 of the 7 tests. From source: a clean start writes 0 B to stderr, and a config with an `analysis` section writes one deprecation line and 0 B to stdout. Doc comments corrected twice on acceptance to match the code · worker sonnet, shared-tree (DEV-T1-8)                                                                                                                             | `server/src/infra/logging/index.ts`                                                                                                                                                                                                                                  | Extend                                                                                                                                                                                                                  | 1.13, 1.14, 1.15, 1.16                                                                    | **Ruled 2026-09-15 (notes R28): `warn` and `error` reach stderr, `info` and `debug` stay file-only, once 1.13–1.16 leave a clean start clean.** **Found by row 1.1's worker, confirmed by the planner.** Under STDIO, `logToConsole` writes to the console only when `CI=true` or `NODE_ENV=test`. Its comment gives "avoid corrupting STDIO protocol" as the reason, but warn and error go to stderr, which the protocol leaves to the server. So every `logger.warn`/`logger.error` — 400 call sites in 111 files, including the `analysis` deprecation notice — is invisible to a real STDIO user and visible under jest, which sets `NODE_ENV=test`. Fixing it in the logger closes the class in one place; call-site fixes would not. Row 1.1 routes around it (R20)                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 1.9  | ✓ 2026-09-15 · `server/src/infra/config/index.ts`, `server/src/infra/config/config-schema-validator.ts`, `server/scripts/validate-config-schema.ts` (commit 1fbd7c03). One `export interface`; every import names `config-manager.js`; typecheck 0, scripts typecheck 0, config tests pass, lint fell in `index.ts` · worker sonnet, shared-tree, scope amended mid-row (DEV-T1-2)                                                                                                                                                                                                                                                                                                 | `server/src/infra/config/index.ts`, `server/src/infra/config/config-schema-validator.ts`, `server/scripts/validate-config-schema.ts` (added 2026-09-15, DEV-T1-2)                                                                                                    | Refactor                                                                                                                                                                                                                | —                                                                                         | **Found accepting 1.3 (its deviation 1).** The validator re-exports `ConfigSchemaValidationResult` because `infra/config/index.ts` imports the type from it and that file was outside 1.3's list. That is a second import path for one symbol, which the handbook's module-organization rule asks to avoid, and the gate the handbook cites for it no longer exists (notes F-T1-12). `index.ts` imports the type from `#shared/types/config-manager.js` directly; the validator drops the re-export. Cut on the merged tree, because `index.ts` changed on `main` **Scope amended mid-row (DEV-T1-2):** the planner's consumer inventory covered `server/src` only; `scripts/validate-config-schema.ts` also imports the type from the validator, and row 0.5's new scripts typecheck reads that file.                                                                                                                                                                                                                                                                                                                                                                                                                |
| 1.10 | ✓ 2026-09-15 · `server/tests/unit/mcp-tools/system-control/config-action-handler.test.ts` (commit b558339d). 6/6; swapping the not-checked text for the success text fails case 1 · worker sonnet, shared-tree                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | one new test file beside the existing system-control handler tests                                                                                                                                                                                                   | Create                                                                                                                                                                                                                  | —                                                                                         | **Found accepting 1.3 (its concern).** Nothing tests `ConfigActionHandler`, and 1.3's live drive covered only valid and invalid. Not-validated matters most: it must never render as success                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 1.11 | ✓ 2026-09-15 · `server/tests/unit/infra/config/config-schema-warning.test.ts` (commit e17b679b). 2/2 on the merged tree; a wrong expected string fails the new assertion · worker sonnet, shared-tree                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `server/tests/unit/infra/config/config-schema-warning.test.ts`                                                                                                                                                                                                       | Edit                                                                                                                                                                                                                    | —                                                                                         | **Found re-verifying the `main` merge (R23).** `main` changed `loadConfig`'s fallback from `console.info` to `console.error` for "Using default configuration", and step 6 of row 1.4's sequence test asserts `console.info` was called — so the merged tree runs 45 of 46. Git merged both sides cleanly; this is exactly the case R23's re-verification exists for                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 1.12 | ✓ 2026-09-15 · `server/src/infra/config/index.ts` (commit 33f2e622). One directive line removed; `lint:ratchet` OK at 3092 errors / 897 warnings, no regressions; typecheck 0; config tests 34/34 · worker sonnet, shared-tree                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `server/src/infra/config/index.ts`                                                                                                                                                                                                                                   | Edit                                                                                                                                                                                                                    | —                                                                                         | **Found by the batch gates after the `main` merge (DEV-T1-3).** `main` changed `no-console` from `warn` to `['error', { allow: ['warn', 'error'] }]`, so row 1.1's `// eslint-disable-next-line no-console` above `writeSchemaWarning`'s `console.warn` now suppresses nothing, and eslint reports an unused directive — the ratchet's null-rule warnings go from 5 to 6. Remove the directive; keep the doc comment's reason for not using the logger, which is still true                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 1.13 | ✓ 2026-09-15 · `server/src/mcp/tools/tool-description-loader.ts`, `server/tests/tool-description-loader.test.ts` (commit a7ba2009). Static JSON import; the disk read, the not-found warning and `configPath` are gone. Restoring HEAD fails the new test (`defaults` where `contracts` is expected); the planner re-ran it, 3/3 with the ESM flag. The built-server check runs at the PR boundary · worker sonnet, shared-tree (DEV-T1-6)                                                                                                                                                                                                                                         | `server/src/mcp/tools/tool-description-loader.ts`                                                                                                                                                                                                                    | Edit                                                                                                                                                                                                                    | —                                                                                         | **Found in the owner interview (notes F-T2-6).** The loader reads `<serverRoot>/src/tooling/contracts/_generated/tool-descriptions.contracts.json`, a directory that exists neither in the repo nor in the package. The generator writes `src/mcp/contracts/schemas/_generated/`, and the package ships no `src/`, so every server has served the in-memory default descriptions. Read the generated descriptions through the bundle, a static import esbuild inlines, instead of a disk path; delete the not-found branch if nothing can reach it afterwards                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 1.14 | ✓ 2026-09-15 · `server/src/engine/frameworks/framework-state-store.ts`, `server/tests/unit/frameworks/framework-state-store.persistence.test.ts` (commit 47a86c5b). A scope with no saved state logs at debug and a corrupt row still warns; merging the two branches back fails the absent-state test. Planner read the diff and ran the unit suite on the shared tree, 239 suites passing · worker sonnet, shared-tree (DEV-T1-4)                                                                                                                                                                                                                                                | `server/src/engine/frameworks/framework-state-store.ts`                                                                                                                                                                                                              | Edit                                                                                                                                                                                                                    | —                                                                                         | **Found in the owner interview (notes F-T2-7).** `Invalid framework state, falling back to defaults` fires when nothing was ever saved, because a missing row and an invalid row share one branch. Report absence as absence, at debug; keep the warning for a row that exists and fails `isValidPersistedState`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 1.15 | ✓ 2026-09-15 · `server/src/modules/chains/manager.ts`, `server/src/mcp/tools/prompt-engine/core/prompt-executor.ts`, `server/src/mcp/tools/index.ts`, `server/src/runtime/module-initializer.ts`, `server/tests/unit/chain-session/chain-session-hook-projection.test.ts` (commit 03170550). The database opens before the tools are built and reaches the store as `databasePort`; the no-port warning stays for an empty server root. Making the store ignore the option fails the new no-warning test; the planner re-ran typecheck and the store test (15/15). The built-server drive reads the `chain_runs` row back at the PR boundary · worker opus, shared-tree (DEV-T1-7) | `server/src/modules/chains/manager.ts`, `server/src/mcp/tools/prompt-engine/core/prompt-executor.ts`, `server/src/mcp/tools/index.ts`, `server/src/runtime/module-initializer.ts`                                                                                    | Edit                                                                                                                                                                                                                    | —                                                                                         | **Found in the owner interview (notes F-T2-7).** The chain session store warns "persistence disabled" from its constructor, then `prompt-executor.ts:324` late-binds the port and re-arms persistence (the 2026-08-11 fix). A missing-dependency check ahead of a wiring call is an ordering bug: pass the `DatabasePort` at construction, so the store is never observable without it. The check reads the row back, because the 2026-08-11 defect was a store that looked healthy and saved nothing. Four files, one over the default bound, because the port is created in `runtime/` and consumed three layers down: `module-initializer.ts:117` opens the `SqliteEngine` singleton before `:312` builds the tools, so the port can travel with construction. It is an optional `databasePort` store option, since 36 test sites in 20 files construct the store without one                                                                                                                                                                                                                                                                                                                                      |
| 1.16 | ✓ 2026-09-15 · `server/src/runtime/application.ts` (commit 17f97ef1). The trace gated on `!testEnvironment`, true for every normal launch; it now gates on `verbose`, and the one ungated writer uses the same helper. Worker, from source: a quiet start writes 0 B to stderr, `--verbose` prints 21 trace lines, and restoring HEAD reproduces the 13 measured lines. `src/index.ts` was already gated. The built-server check runs at the PR boundary · worker sonnet, shared-tree (DEV-T1-5)                                                                                                                                                                                   | `server/src/runtime/application.ts`, `server/src/index.ts`                                                                                                                                                                                                           | Edit                                                                                                                                                                                                                    | —                                                                                         | **Corrected on acceptance (DEV-T1-5): the authored "route the trace through the logger at debug" was wrong — the logger drops every console level under STDIO, so the trace keeps its own writer, gated on `verbose`.** **Found in the owner interview (notes F-T2-1).** A clean start writes 13 `DEBUG:` lines straight to stderr through `console.error` (`application.ts:107`, `debugLog` in `index.ts`), outside the logger, while the logger suppresses every warning. Route the trace through the logger at debug where a logger exists, and print the lines that run before it only under `--verbose` or `--debug-startup`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 1.17 | ✓ 2026-09-15 · the files named in this row (commit 9bbc747c). The loader test runs under `tests/unit` (3/3); `validate:test-directory-membership` fails naming a planted `tests/tool-description-loader.test.ts` and passes the real tree; its self-test covers 6 rules; suite membership 60, knip and lint ratchets clean; tests-typecheck baseline 368 → 364 · worker sonnet, shared-tree (DEV-T1-9)                                                                                                                                                                                                                                                                             | `server/tests/unit/mcp-tools/tool-description-loader.test.ts` (moved from `server/tests/`), `server/scripts/validate-test-directory-membership.js`, `server/package.json`, `server/scripts/run-validation-suite.js`, `server/.typecheck-tests-ratchet-baseline.json` | Move + Create                                                                                                                                                                                                           | 1.13                                                                                      | **Found by row 1.13's worker (notes F-T2-13).** The loader's only test sits directly under `tests/`, while every test script and CI job runs a named subdirectory (`test:unit`, `test:coverage`, `test:integration`, `test:e2e`), so CI has never run it — which is how a wrong path survived. It is the only such file today (239 unit, 64 integration, 11 e2e, 1 at the root). Move it, and add a check to `validate:all` so the next stray suite fails instead of silently never running                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 1.18 | ✗ KILLED (2026-09-15 · row 1.15 added one positional parameter each to `PromptExecutor`'s constructor and `createPromptExecutor` (8 → 9) and `createMcpToolRouter` (9 → 10); all three already broke `max-params`, and the violation counts did not change. An options object touches their test call sites and is outside step 1 · revives if another positional parameter is added to any of the three, or delivery step 3 rewrites these constructors)                                                                                                                                                                                                                          | —                                                                                                                                                                                                                                                                    | —                                                                                                                                                                                                                       | —                                                                                         | Convert the three signatures to options objects                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 1.19 | ✓ 2026-09-15 · `server/src/infra/config/index.ts`, `server/tests/unit/infra/config/config-schema-warning.test.ts` (commit 2a7a81e6). `writeSchemaWarning` is gone; both schema messages go through the module logger's `warn` with the same text and the same once-per-change logic. The test requires the `[WARN] ` prefix, and restoring the direct `console.warn` fails it. From source: a misspelled `gates` key writes one `[WARN] [CONFIG] … does not match its schema: /gates … (enabld)` line and 0 B to stdout; a clean config writes 0 B · worker sonnet, shared-tree                                                                                                    | `server/src/infra/config/index.ts`, `server/tests/unit/infra/config/config-schema-warning.test.ts`                                                                                                                                                                   | Edit                                                                                                                                                                                                                    | 1.8                                                                                       | **Found accepting 1.14 (notes F-T2-11).** R20 sent the schema warning around the logger because STDIO dropped every logger level. 1.8 removes that reason, which leaves two channels for one kind of message. Send it through the module logger's `warn` with the same text, rewrite the doc comment (which also cites a `setupConsoleRedirection` that does not exist), and point the warning test at the logger                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 1.20 | ✓ 2026-09-15 · `CHANGELOG.md` (commit d187cb8f). Three `### Fixed` entries at the top of `[Unreleased]`: a clean STDIO start writes nothing to stderr; warnings and errors reach stderr under STDIO, the schema warning now prefixed `[WARN]`; tools are described by their contract text in every install. Each claim checked against its commit; prettier, `validate:preview-vocabulary` and `validate:no-methodology-vocab` exit 0; no plan vocabulary · worker sonnet, shared-tree                                                                                                                                                                                             | `CHANGELOG.md`                                                                                                                                                                                                                                                       | Extend                                                                                                                                                                                                                  | 1.8, 1.17, 1.19                                                                           | **Found cutting step 1's PR boundary.** The step changes what users see: tools are described by their contract text, a normal start writes nothing to stderr, the startup trace needs `--verbose`, and warnings and errors now reach stderr under STDIO. Row 1.17 is CI-only and gets no entry. Sections per `/changelog-generator`, reader voice, each claim tied to the code the worker reads — the same shape as row 1.7                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 1.21 | ✓ 2026-09-15 · `server/package.json`, `server/.typecheck-tests-ratchet-baseline.json` (merge commit 9c3f48a7). `package.json` keeps 1.17's two scripts and `main`'s rewritten `validate:format`/`format`, byte-equal to `main`'s; the baseline's per-code, per-file and total counts all sum to 364, and `typecheck:tests:ratchet` reports exactly 364 with no slack. Planner checked for markers and unmerged paths before concluding the merge · worker sonnet, shared-tree (DEV-T1-10)                                                                                                                                                                                          | `server/package.json`, `server/.typecheck-tests-ratchet-baseline.json`                                                                                                                                                                                               | Merge                                                                                                                                                                                                                   | 1.8, 1.17                                                                                 | **Found merging `main` (notes F-T2-15).** A trial merge on `6e0814f0` was clean, but merging `main` after 1.8 and 1.17 conflicted in two files 1.17 touched: `package.json` (its new scripts sit beside `main`'s rewritten format scripts) and the tests-typecheck baseline (`generatedAt` and `totals`). Keep both sides' scripts; take the baseline total both sides' entries actually sum to                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 1.22 | ✓ 2026-09-15 · `server/tests/unit/mcp-tools/tool-description-loader.test.ts` (commit 284627ce). The expected tool list is read from the contract, asserted non-empty and compared exactly; a planted extra name fails it; 3/3 on the merged tree. No other test expects a `skills_sync` tool description · worker sonnet, shared-tree                                                                                                                                                                                                                                                                                                                                              | `server/tests/unit/mcp-tools/tool-description-loader.test.ts`                                                                                                                                                                                                        | Edit                                                                                                                                                                                                                    | 1.21                                                                                      | **Found re-verifying the `main` merge (notes F-T2-20).** #299 folded `skills-sync.json` into `system-control.json`, so the generated descriptions no longer list a `skills_sync` tool, and 1.13's rewritten test hardcoded that name: 1 of 3 fails on the merged tree. The loader itself is right — source `contracts`, `prompt_engine` description equal, `validate:contracts` exit 0. Read the expected list from the contract so the next contract change cannot break the test the same way                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 1.23 | ✓ 2026-09-15 · `server/src/runtime/module-initializer.ts` (commit a07e070d). `openToolsDatabase(runtimeDbPath, serverRoot, logger)` calls `getInstance` with `{ dbPath: runtimeDbPath }`, safe because the constructor resolves `config.dbPath ?? <package default>`. `validate:db-claim-order` and its self-test exit 0, and dropping the argument fails it again. From source: 0 B on stdout and stderr, `state.db` created under `MCP_RUNTIME_ROOT`, the package-directory `state.db` untouched. The rebuilt-server check reruns at the PR boundary · worker sonnet, shared-tree                                                                                                | `server/src/runtime/module-initializer.ts`                                                                                                                                                                                                                           | Edit                                                                                                                                                                                                                    | 1.15                                                                                      | **Found at step 1's PR boundary (notes F-T2-21).** `validate:all` failed 1 of 60 on `c0cc52d7`: `validate:db-claim-order` rule 1 requires every `SqliteEngine.getInstance()` in this file that supplies no `dbPath` to sit below the `claimStateDatabase()` call, and 1.15's `openToolsDatabase` helper calls it with no `dbPath` at line 137, above the claim at 232. Runtime order is already right (the claim runs first), but the check is a source-order check by design. Pass the claimed `runtimeDbPath` into `openToolsDatabase` so its call supplies the resolved path, which removes the dependence on order. Moving the helper below the claim was rejected: it would add an undeclared third inheriting site under `module-initializer.ts`'s accepted-inheritor entry, whose reason names only two. A new exception was rejected: excusing a new call is what the check exists to catch                                                                                                                                                                                                                                                                                                                   |
| 1.24 | ✓ 2026-09-15 · `CHANGELOG.md` (merge commit 55cda489). `### Fixed` opens with `main`'s #305 entry, then this branch's three; `git diff origin/main -- CHANGELOG.md` is exactly those three added lines; no markers, each entry once, `git diff --check` and prettier clean. The planner confirmed the auto-merged `prompt-executor.ts` keeps both #305's framework lookup and 1.15's `databasePort` (typecheck 0, tests typecheck 364, chain-store and parser-registry tests 24/24) · worker sonnet, shared-tree                                                                                                                                                                   | `CHANGELOG.md`                                                                                                                                                                                                                                                       | Merge                                                                                                                                                                                                                   | 1.20                                                                                      | **Found merging `main` a third time (notes F-T2-24).** #305 and 1.20 both added entries at the top of `### Fixed`; keep both, `main`'s first                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

**Dispatch** — tier · effort · failure shape, per `/claude-code` §Per-row tier declaration:

| Row  | Tier   | Effort | Failure shape                                                                                                                                                                                                                                           | branch_mode | Surface |
| ---- | ------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------- |
| 1.5  | sonnet | low    | wrong output — two exact edits, content given                                                                                                                                                                                                           | shared-tree | Agent   |
| 1.6  | sonnet | high   | wrong output — signature and result shape specified; the self-test must stay green                                                                                                                                                                      | shared-tree | Agent   |
| 1.1  | opus   | high   | wrong approach — ordering against adoption, warn-once under hot reload, injection rather than a guard that skips                                                                                                                                        | shared-tree | Agent   |
| 1.3  | sonnet | high   | wrong output — surface ruled, fallback stated                                                                                                                                                                                                           | shared-tree | Agent   |
| 1.4  | sonnet | medium | wrong output — behavior pinned by 1.1, mutations named                                                                                                                                                                                                  | shared-tree | Agent   |
| 1.7  | sonnet | medium | wrong output — the behaviors are enumerated in the row; the risk is session vocabulary leaking into release notes                                                                                                                                       | shared-tree | Agent   |
| 2.6  | sonnet | high   | wrong output — the precedence rule is ruled in OQ-7; the risk is changing which key wins for a config that already works                                                                                                                                | shared-tree | Agent   |
| 1.9  | sonnet | low    | wrong output — a two-file import move with the target named                                                                                                                                                                                             | shared-tree | Agent   |
| 1.10 | sonnet | medium | wrong output — the render text is fixed by 1.3; the risk is a test that cannot fail                                                                                                                                                                     | shared-tree | Agent   |
| 1.11 | sonnet | low    | wrong output — one assertion follows a logging change already on `main`                                                                                                                                                                                 | shared-tree | Agent   |
| 1.12 | sonnet | low    | wrong output — one directive line; the check is the whole-project ratchet                                                                                                                                                                               | shared-tree | Agent   |
| 1.8  | sonnet | high   | wrong output — re-tiered 2026-09-15 from opus: the level-by-transport table is ruled in the brief (notes R36), so nothing is left to derive; the risk is a line on stdout, or a test that never sees STDIO outside CI because jest sets `NODE_ENV=test` | shared-tree | Agent   |
| 0.6  | sonnet | medium | wrong output — each diagnostic is located and named; the risk is changing a script's behavior while silencing its types                                                                                                                                 | shared-tree | Agent   |
| 0.7  | sonnet | medium | wrong output — same shape as 0.6                                                                                                                                                                                                                        | shared-tree | Agent   |
| 0.5  | sonnet | high   | wrong output — a new project file plus a SUITE entry whose `reads` substrate is re-derived and has already failed once in this plan (DEV-T0-3)                                                                                                          | shared-tree | Agent   |
| 0.9  | sonnet | medium | wrong output — fix and skip-not-throw ruling are given; the risk is changed script output, or a self-test case that cannot fail                                                                                                                         |
| 2.7  | sonnet | high   | wrong output — the member names, the three source labels and the two env overrides are all given by measurement; the risk is a provenance label that disagrees with the value the server runs on                                                        | shared-tree | Agent   |
| 2.8  | sonnet | high   | wrong output — R37 fixes the read shape and the removals are enumerated; the risk is contract, schema, generated metadata and snapshot drifting apart, which this repo gates                                                                            | shared-tree | Agent   |
| 2.9  | sonnet | medium | wrong output — a named construct deleted across a measured consumer list; the risk is a site left behind that keeps the write path reachable                                                                                                            | shared-tree | Agent   |
| 2.10 | sonnet | high   | wrong output — the cases are named, but the row must prove the control can fail; the risk is a test that passes against a surface that was never removed                                                                                                | shared-tree | Agent   |
| 2.11 | sonnet | medium | wrong output — the false claim and its line are located; the risk is prose that describes the old writable surface                                                                                                                                      | shared-tree | Agent   |     | shared-tree | Agent |
| 0.10 | sonnet | medium | wrong output — both sites and the sibling guard are named; the risk is a skip that no other step reports, or a self-test case that cannot fail                                                                                                          | shared-tree | Agent   |
| 1.13 | sonnet | high   | wrong output — the defect and the target are named; the risk is a check that passes on the in-memory defaults                                                                                                                                           | shared-tree | Agent   |
| 1.14 | sonnet | medium | wrong output — one branch split in two; the risk is silencing the corrupt-row warning along with the absent one                                                                                                                                         | shared-tree | Agent   |
| 1.15 | opus   | high   | wrong approach — construction order across the runtime composition, where the last change here left persistence silently dead                                                                                                                           | shared-tree | Agent   |
| 1.16 | sonnet | medium | wrong output — the lines and their writers are named; the risk is losing the trace under `--verbose`                                                                                                                                                    | shared-tree | Agent   |
| 1.17 | sonnet | high   | wrong output — a new validator whose suite `reads` substrate is re-derived and has failed once in this plan (DEV-T0-3); the risk is a check that cannot fail                                                                                            | shared-tree | Agent   |
| 1.19 | sonnet | medium | wrong output — one call site and its test move to a channel 1.8 proves; the risk is a test still spying on the old channel                                                                                                                              | shared-tree | Agent   |
| 1.20 | sonnet | medium | wrong output — the behaviors are listed in the row; the risk is session vocabulary leaking into release notes                                                                                                                                           | shared-tree | Agent   |
| 1.21 | sonnet | low    | wrong output — both resolutions are given as exact text; the risk is a baseline total that double-counts a change both sides made                                                                                                                       | shared-tree | Agent   |
| 1.22 | sonnet | low    | wrong output — the stale literal and its replacement are named; the risk is an assertion that passes on an empty list                                                                                                                                   | shared-tree | Agent   |
| 1.23 | sonnet | medium | wrong output — the argument to thread and its source are named; the risk is a call that trips `getInstance`'s divergence guard, or a path passed as `undefined` where it was omitted before                                                             | shared-tree | Agent   |
| 1.24 | sonnet | low    | wrong output — both sides' entries and their order are given; the risk is a reflowed or dropped bullet                                                                                                                                                  | shared-tree | Agent   |

### T2 — Reconcile the CLI against the schema (23 keys)

| #    | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | File                                                                                                                                                                                                                                                                                                                                        | Change | Depends   | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2.1  | ✗ KILLED (2026-09-15 · absorbed by 4.4: R31 lands the 5.0 shape in the same release, so a hand-edited CLI key list would be regenerated by 4.4 before it shipped, and R35 makes that generated list the only one · revives if 4.4 is killed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `src/cli-shared/config-input-validator.ts`                                                                                                                                                                                                                                                                                                  | —      | —         | Add the 18 schema keys the CLI refuses (S3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2.2  | ✗ KILLED (2026-09-15 · absorbed by 4.1: the `ConfigFile` type states the one spelling, and R33's translation adopts the other · revives if 4.1 is killed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `config-input-validator.ts`, `adoptInertSpellings`                                                                                                                                                                                                                                                                                          | —      | —         | camelCase canonical (R5); snake_case stays accepted (S4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2.3  | ✗ KILLED (2026-09-15 · absorbed by 4.1: `ConfigFile` omits both zero-reader keys and `server.version`, so the generated schema and CLI list drop them · revives if 4.1 is killed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `config-input-validator.ts`, `config-operations.ts`                                                                                                                                                                                                                                                                                         | —      | —         | Remove `gates.enforcePendingVerdict` + `resources.prompts.defaultRegistration` (S6, OQ-3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2.4  | ✗ KILLED (2026-09-15 · absorbed by 4.3 and 4.4: with the schema and the CLI list generated from one type, a drift check replaces an agreement gate between two hand-kept lists · revives if 4.4 is killed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `scripts/`                                                                                                                                                                                                                                                                                                                                  | —      | —         | Agreement gate: CLI key set ≡ schema leaf key set                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2.5  | ✗ KILLED (2026-09-15 · split into rows 2.7–2.11 when step 2 was cut: one row mixed a new read primitive, a contract change, a deletion across four consumers, the surface's first tests and the docs · revives if all five are killed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `server/tooling/contracts/system-control.json`, `src/mcp/tools/schemas/system-control.schema.ts`, `system-control/handlers/config-action-handler.ts`                                                                                                                                                                                        | Extend | R27, R35  | **⚠ Premise falsified on `main` 2026-09-15 (notes F-T2-15):** #299 declared the nested `config` object, `backup_path` and `confirm`, so get, set, list, validate and restore all reach the handler over MCP on `main`. R27 and R35 still keep writes out of MCP, but this row now removes a reachable, unreleased surface rather than an unreachable one, and whether reads keep #299's nested `config` object or move to a flat `key` is re-asked when step 2 is cut. The original text follows. **Found live-driving 1.3 (notes F-T1-7).** `get`, `set`, `validate` and `restore` read `config.{operation,key,value}`, `backup_path` and `confirm`. The tool's input schema declares none of them, so an MCP client cannot reach any of the four, and the handler's `if (!configRequest) return list` makes each failure look like a successful dump. The annotations already claim `config restore` is reachable. Needs OQ-5 ruled first; regenerate contracts and the `tests/snapshots/mcp-input-schemas.json` snapshot in the same row |
| 2.6  | ✗ KILLED (2026-09-15 · replaced by R30: the flag owns transport and both config keys are removed in 5.0.0, so no config key is made to take effect; the removal is cut with delivery step 3 · revives if R30 is reversed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `server/src/infra/config/index.ts`, `server/config.schema.json`                                                                                                                                                                                                                                                                             | —      | —         | Make `server.transport` take effect (F-T1-10)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2.7  | ✓ 2026-09-15 · `server/src/infra/config/index.ts`, `server/src/shared/types/config-manager.ts`, `prompt-engine-validation.test.ts` (commit ba5de798). `ConfigValueWithSource` + `listConfigKeys()` are required members; the value comes from the merged config and the raw file decides only the label. Positive control, one run: `gates.enabled` → file, `frameworks.defaultFramework` → default, `server.port` under `PORT=9999` → environment/9999; `analysis.semanticAnalysis.llmIntegration.model: null` in the file → `gpt-4`/file, the divergence the correction round was sent for. `listConfigKeys()` 78 keys, throws without a schema path. `typecheck` 0, that test file 12/12. Residual class → 2.13 | `server/src/infra/config/index.ts`, `server/src/shared/types/config-manager.ts`, `server/tests/unit/mcp-tools/prompt-engine/prompt-engine-validation.test.ts`                                                                                                                                                                               | Extend | R37, R38  | The loader keeps the parsed file beside the merged config and gains `getConfigValueWithSource(key)` (value plus `file`, `default` or `env`) and `listConfigKeys()` derived from the packaged schema. Both become required `ConfigManager` members, per R18's precedent; measured blast radius: 1 implementation, 1 literal-typed mock (`prompt-engine-validation.test.ts:25`), 35 casts that need nothing. `loadConfig` discards the raw file today (`index.ts:312` then `:315`), and `PORT` (`:619`) and `LOG_LEVEL` (`:391`) are the only env overrides, neither reachable through the generic dot-walk                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2.13 | ✓ 2026-09-15 · commit `729ca53a`. Fourth label `'deferred'`; enumeration gate `config-value-source.test.ts` 5/5, mutation (forcing `'default'`) failed 2 of them, restored green. Coverage of 78 keys: 1 `file`, 28 `default`, 2 `environment`, 47 `deferred`. **Planner lapse: this stayed open after its commit, and row 2.8's worker read the stale marker as an unmet dependency (F-T2-30)**                                                                                                                                                                                                                                                                                                                   | `server/src/shared/types/config-manager.ts`, `server/src/infra/config/index.ts`, `server/tests/unit/infra/config/config-value-source.test.ts`                                                                                                                                                                                               | Extend | 2.7, R40  | Opened from 2.7's `concerns`. Measured: `validateAndSetDefaults` writes only `server`, `prompts`, `analysis`, `transport`, `frameworks` and `advanced` back into the loaded config, while 17 getters apply defaults at read time across 19 `?? DEFAULT_*` sites — so a dot-walk answers `undefined` for `gates.enabled` and most other keys a user has not set. Add a fourth source label for exactly that state (a key the schema declares, absent from both the file and the loaded config, defaulted by its owning subsystem when read) so no caller is handed a bare `undefined` to interpret, and a test that enumerates every schema key and fails when one answers with neither a value nor that label (R40: step 2 does not hand-build a section→getter map)                                                                                                                                                                                                                                                                        |
| 2.8  | ✓ 2026-09-15 · commit `d69cba37`. Handler serves `list`, `keys`, `validate`; every other operation, including a missing one, hits one generic refusal naming `cpm` — `rg` for `'get'`/`'set'`/`'restore'`/`'reset'` in the handler returns zero. Live drive of all five calls: `keys` listed 78, `get` and `set` refused, empty-args refused instead of dumping the config. `typecheck` 0, `validate:contracts` 0. Snapshot deferred to the PR boundary (F-T2-31); e2e scenarios → 2.14                                                                                                                                                                                                                            | `server/src/mcp/tools/system-control/handlers/config-action-handler.ts`, `server/tooling/contracts/system-control.json`, `server/src/mcp/tools/schemas/system-control.schema.ts`                                                                                                                                                            | Extend | 2.7, 2.13 | Reads keep #299's nested `config` object (R37) and gain `keys`. **`get` is held until step 4 (R41, owner):** it leaves the MCP surface with the writes and returns once the generated shape resolves more than 31 of 78 keys; `list` still shows the loaded values meanwhile. `set`, `reset` and `restore` leave the handler; `value`, `backup_path` and `confirm` leave the contract and the schema. A request for any removed operation is refused naming `cpm`, never answered with a listing. Regenerate `_generated/` and `tests/snapshots/mcp-input-schemas.json` inside the row, per CLAUDE.md's contract-agreement rule                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2.9  | ✓ 2026-09-15 · commit `2367487b`, at a THIRD of its briefed scope and rightly so (DEV-T2-4). Deleted: the dead `safeConfigWriter` getter on `action-handler-base.ts`, the orphaned field on `SystemControlContext`, and `shared-types.ts`'s `SystemControlAction` / `BaseSystemControlArgs` (zero consumers, measured). KEPT, premise falsified: `SafeConfigWriter` is live via `persistGateConfig`/`persistFrameworkConfig` → R42. `typecheck` 0, `validate:contracts` 0, `validate:arch` 0, knip improved (exports 489→487, files 12→11), `legacy-key-migration.test.ts` 33/33 unmodified                                                                                                                        | `server/src/mcp/tools/config-utils.ts`, `server/src/mcp/tools/system-control/system-control-router.ts`, `server/src/mcp/tools/system-control/core/types.ts`, `server/src/mcp/tools/system-control/core/action-handler-base.ts`, `server/src/mcp/tools/types/shared-types.ts`, `server/tests/unit/infra/config/legacy-key-migration.test.ts` | Delete | 2.8       | `SafeConfigWriter` and `config-utils.ts`'s 19-key `CONFIG_VALID_KEYS` — a stale strict subset of `cli-shared`'s 59 — are removed along with every consumer named here; the migration test compares `cli-shared`'s list instead. Five files, over the default bound deliberately: this is one construct's full enumeration, and a site left behind keeps the write path alive (cleanup-standards §Removal Checklist). The two key lists are NOT consolidated here (R39). Also sweep `types/shared-types.ts`'s `SystemControlAction` / `BaseSystemControlArgs`, which still describe the old `config`/`backup_path`/`get`/`set` shape — 2.8's worker measured `SystemControlAction` at zero consumers in `src`, so it documents a surface that no longer exists                                                                                                                                                                                                                                                                               |
| 2.10 | ✓ 2026-09-15 · commit `c596f44e`. 12 tests through a real `ConfigLoader` on a temp config: `list`, `keys` (success and the enumeration-throws path), `validate` in both forms, and refusals for `get`/`set`/`restore`/`reset`/`{}` asserted on `isError` alone so a reworded message cannot drop the guard. Mutation (restoring the `default: return handleConfigList()` fallback) failed exactly those 5; handler diff clean afterwards. It also caught the tests-typecheck ratchet at 365 → row 2.15                                                                                                                                                                                                             | `server/tests/unit/mcp-tools/system-control/config-action.test.ts`                                                                                                                                                                                                                                                                          | Create | 2.8       | First unit coverage of the handler's config paths: `list`, `keys` (including the enumeration failure surfacing as an error, never an empty list), `validate` in both its forms — the stored load-time result and the per-key candidate check — and the controls that `get`, `set`, `restore` and an EMPTY request are each refused rather than answered with a listing. The empty-request case is the regression guard for the old `if (!configRequest) return list` fallback. Only the schema-validate branch was tested before this slice                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2.11 | ✓ 2026-09-15 · commit `0ebe4ffe`. New `### Config Operations` section in the tool reference (the file gives every nuanced action its own section); `cli.md`'s re-export claim replaced with the measured truth — two lists, 19 and 59 keys, the smaller a strict subset, verified by counting both arrays and a set comparison, with no cross-import; one BREAKING changelog entry carrying the `persist` nuance. `validate:format` 0                                                                                                                                                                                                                                                                              | `docs/reference/mcp-tools.md`, `docs/guides/cli.md`, `CHANGELOG.md`                                                                                                                                                                                                                                                                         | Extend | 2.8       | Document the read-only MCP config surface and `cpm` as the only writer. `docs/guides/cli.md:419` says `config-utils.ts` re-exports `cli-shared`'s key list "for backward compatibility"; it never did, and 2.9 deletes the copy, so the line is replaced rather than amended. One `### Changed` entry, written in the reader's register                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2.12 | ✓ 2026-09-15 · commit `9433eacd`. One `schemaCache` entry `{ schema, validator }` behind `getOrLoadSchemaEntry`; `rg` for a schema read/parse in `infra/config` returns only `config-schema-validator.ts:53-54`. Read-count test: 1 expected, mutation bypassing the cache observed 2 and failed, restored green. 8/8 across both config test files, `typecheck` 0, `validate:arch` 0                                                                                                                                                                                                                                                                                                                              | `server/src/infra/config/config-schema-validator.ts`, `server/src/infra/config/index.ts`                                                                                                                                                                                                                                                    | Extend | 2.7       | Opened from row 2.7's `concerns`. `getCompiledValidator` (`config-schema-validator.ts:39`) caches an AJV `ValidateFunction` keyed by schema path and mtime, is not exported, and exposes no accessor to the parsed schema — so 2.7's `listConfigKeys()` re-reads and re-parses the same file on every call. Expose the parsed schema through that existing cache and have `listConfigKeys` consume it: one read, one parse, one invalidation rule                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2.14 | ✓ 2026-09-15 · commit `951eb894`. `sc-config` now drives `list`; `sc-config-get` replaced by `sc-config-keys` plus `sc-config-get-refused`; new `sc-config-write-refused`. The corpus CAN express a required failure (`error_contains` asserts `isError` plus text, as siblings already do) — no invented key. Loader gate proven by mutating a key to `errorContains`, which threw "asserts nothing". Found: the retired scenario had been asserting the nested `config.operation` field, which the handler never dispatched on — stale before this slice touched it. Suite itself needs `dist/` → PR boundary                                                                                                    | `server/tests/e2e/conformance/tool-surface.yaml`                                                                                                                                                                                                                                                                                            | Extend | 2.8       | Opened from 2.8's `concerns`. Two scenarios encode the old surface and will fail once this ships: `sc-config` (line 59) sends `{action: config}` with no operation and expects `ok: true`, which only passed because of the `if (!configRequest) return list` fallback 2.8 deleted, and `sc-config-get` (line 88) sends the nested `config: { operation: get, key: server.name }` and expects the value back. Rewrite both to the shipped surface — `list`, `keys`, `validate` — and add a scenario asserting a write request is refused, so the conformance suite carries the boundary rather than just tolerating it                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2.15 | ✓ 2026-09-15 · commit `aa631736`. `prompt-engine-validation.test.ts` imported `ConfigManager` from `infra/config/index.ts`, which only imports the type for its `implements` clause; repointed at `shared/types/config-manager.ts`, its definition. No re-export added — that would be the banned compat shim. Ratchet back to 364, 12/12 still pass. The file's other 5 errors are a pre-existing `jest.fn()` widening shape, unchanged before and after, left alone                                                                                                                                                                                                                                              | `server/tests/unit/mcp-tools/prompt-engine/prompt-engine-validation.test.ts`                                                                                                                                                                                                                                                                | Fix    | 2.10      | Opened from 2.10's `concerns`: the row's own count rose when 2.7 added two mock members, and the ratchet is keyed per file                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2.17 | ☐ (as of 2026-09-15 · flips when no shipped message claims configuration cannot be written over MCP, while the arbitrary-write refusal stays)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `server/src/mcp/tools/system-control/handlers/config-action-handler.ts`, `server/tooling/contracts/system-control.json`, `server/src/mcp/tools/schemas/system-control.schema.ts`                                                                                                                                                            | Fix    | 2.8, R42  | The refusal shipped in `d69cba37` ends "Writes never reach MCP", which R42 makes false: `persist: true` writes `config.json` through `persistGateConfig`/`persistFrameworkConfig`. Reword to the real boundary — a caller cannot name the key or the value — keeping the pointers to `cpm config set`/`reset` and `cpm config get`. Two conformance scenarios assert on the substring `not served over MCP`; if that changes, the worker reports rather than editing another row's file                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2.16 | ☐ (as of 2026-09-15 · flips when every runtime-state operation is classified as able or unable to persist itself under a server-chosen key, with the unsafe ones named)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `server/src/mcp/tools/system-control/**` (survey; files named by the survey)                                                                                                                                                                                                                                                                | Survey | R42       | Owner's suggestion when ruling R42: extend the `persist: true` shape to operations that do not need closing for security. Enumerate every `system_control` operation that mutates runtime state, and for each say whether the server can choose and validate its config key (safe, like gates and frameworks) or whether persisting would require a caller-named key (not safe, stays out). Discovery only — implementation rows are cut from its result, after this slice's PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2.18 | ✓ 2026-09-15 · commit `1f70645b`. Opened from the PR boundary, where `lint:ratchet` failed `validate:all`: `no-unused-vars` 17→18 and `preserve-caught-error` 0→1, both this branch's. Removed an import left unused after 2.12's refactor, and attached `cause` to the key-listing throw so the reason survives. Ratchet back to OK (3029 errors, 874 warnings); the file's other unused-var reports are inside the baseline and were left alone. All three failure paths still throw, verified by a drive                                                                                                                                                                                                        | `server/src/infra/config/index.ts`                                                                                                                                                                                                                                                                                                          | Fix    | boundary  | A thrown error that discards what it caught defeats the "cannot enumerate, never empty" contract the row it came from exists for                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2.19 | ✓ 2026-09-15 · commit `d830cc5a`. Opened from the boundary, where `validate:conformance-coverage` reported `system_control.config` with no scenario — 2.14's rewrite had removed the last one sending that parameter. Two scenarios now drive its one remaining shape, a per-key candidate that passes and one refused with its reason; no exception entry was added. Renaming the parameter key made the original failure reappear verbatim, and the planner's live drive confirmed both asserted strings match the server's output byte for byte                                                                                                                                                                 | `server/tests/e2e/conformance/tool-surface.yaml`                                                                                                                                                                                                                                                                                            | Extend | 2.14      | An exceptions list was the offered way out; it would have recorded a reachable, easily-tested parameter as untested                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2.20 | ✓ 2026-09-15 · commit `e2c36bf5`. The coverage validator's own hygiene check reported `system_control.backup_path` as `subject-missing` — this slice removed the parameter and left the entry excusing it. Entry deleted; sibling entries checked against the tool's verdict rather than by reading, and `confirm` correctly stays. `validate:conformance-coverage` 0 (41 accepted exceptions), `validate:suite-membership` 0, knip ratchet improved (exports −2, files −1), `typecheck:scripts` 0. Re-adding a bogus entry reproduced the failure, proving the check still fires                                                                                                                                  | `server/scripts/validate-conformance-coverage.js`                                                                                                                                                                                                                                                                                           | Delete | 2.8       | cleanup-standards §A Status Outlives What It Described, caught by the gate that owns it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

### T3 — Make the schema reachable (no dependency on the tiers above; may run in parallel)

| #   | Status                                                                                                           | File                        | Change | Depends | Task                                                                                                                               |
| --- | ---------------------------------------------------------------------------------------------------------------- | --------------------------- | ------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 3.1 | ☐ (as of 2026-09-11 · flips when the `$id` URL returns the schema)                                               | `server/config.schema.json` | Edit   | —       | `$id` → a fetchable raw URL; nothing resolves the current GitHub path (S9)                                                         |
| 3.2 | ☐ (as of 2026-09-11 · flips when a config loaded from `MCP_CONFIG_PATH` outside the server root still validates) | `server/config.json`        | Edit   | 3.1     | `$schema` → the absolute `$id`. The relative fallback in the validator stays for offline use                                       |
| 3.3 | ☐ (as of 2026-09-11 · flips when a `docs/` page owns `config.json` and states the precedence chain)              | `docs/`                     | Create | 3.1     | No page owns it today — 10 guides mention it, none documents it. Precedence (defaults → file → `LOG_LEVEL`/`PORT`) is undocumented |

### T4 — One generated authority (R6: TS-first)

| #    | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | File                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Change                 | Depends  | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 4.1  | ✓ 2026-09-16 · `server/src/shared/types/config-file.ts` (622 lines, commit on this branch). Planner spot-read: header states the file/runtime boundary, `version: 5` literal, 60 `@default` and 15 range tags, one import (`core-config.js`) keeping it out of `no-orphans` — the worker's positive control deleted that import and `validate:arch` went red. `typecheck` 0, eslint 0, `validate:arch` 514 modules 0 errors. Enumeration: 79 schema leaves = 57 kept + 10 reshaped + 12 removed, plus 3 new (`version`, `systemPrompt.enabled`, `gateGuidance.enabled` — the last dropped by R47 in row 4.3). Four concerns ruled R47–R49 and F-T4-1 · worker opus, `Agent`, shared-tree                                                                                                                                                                                                                                                                                                                                                                                                                                               | `src/shared/types/config-file.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Create**             | —        | **The only new file.** `ConfigFile` = on-disk shape, distinct from `Config` = resolved runtime shape. Cannot extend `Config`: they describe different objects, and conflating them is the root cause. Also carries the schema's non-TS constraints (`minimum`, enum descriptions, defaults) as JSDoc the generator reads                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 4.2  | ✓ 2026-09-16 · `server/src/infra/config/index.ts`, `server/tests/unit/infra/config/config-file-normalize.test.ts` (11 cases), `config-value-source.test.ts` (commit 645a16cd). Planner spot-read: `parseConfigRecord` → `unknown`, `asConfigFile` the one documented boundary (`index.ts:337`), `normalizeConfigFile` pure per-section mappers (`:529`), zero `as Config`, zero flat-key and zero `advanced` reads; the interim flat-key case carries its flip stamp. Worker: typecheck 0, eslint 22 < baseline 65, tests-typecheck ratchet OK, mutation (drop `chainSessions`) fails 3 named tests, `validate:arch` 0 errors with the type's first real consumer. Concerns ruled R51–R52; `core-config.ts` edits handed to 4.6, the writer to 4.4, fixtures to 4.5 · worker opus, `Agent`, shared-tree                                                                                                                                                                                                                                                                                                                                | `src/infra/config/index.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Edit                   | 4.1      | `JSON.parse(...) as Config` → `normalize(parsed: ConfigFile): Config`, a typed function. The unchecked cast is what let all four authorities drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 4.3  | ✓ 2026-09-16 · `server/scripts/generate-config-schema.ts`, `server/package.json` (+`generate:config-schema`, devDep `ts-json-schema-generator` 2.9.0 exact), `server/config.schema.json` regenerated, `server/config.json` reshaped, `config-file.ts` (−`gateGuidance.enabled`), `core-config.ts` (6 JSDoc), `server/.prettierignore` (commits 2 on this branch). Planner spot-read: header records the inline/expose choice and the integer widening; schema head draft-07, `version` const 5; shipped file has `version`, `injection`, `chainSessions`, no `transport`. Worker: generator idempotent over 4 runs, `validate:config-schema` ✓ + self-test 9/9, resolved-leaf diff = 4.1's enumeration, hand-edit overwritten. Left for 4.9: 16 `number` where 13 were `integer` (R50); for 4.6: knip +21 exported types · worker sonnet, `Agent`, shared-tree                                                                                                                                                                                                                                                                         | `server/scripts/generate-config-schema.ts`, `server/package.json`, `server/package-lock.json`, `server/config.schema.json`, `server/config.json`, `server/src/shared/types/config-file.ts`, `server/src/shared/types/core-config.ts`                                                                                                                                                                                                                                                                                                                      | Create + Edit          | 4.1      | **Rewritten 2026-09-16 after 4.1's handoff.** Emit the schema from `ConfigFile` via `ts-json-schema-generator` 2.9.0 (R6), draft-07, per-object `additionalProperties:false`, deterministic output, `$id` unchanged until step 6. Same row: drop `gateGuidance.enabled` (R47), preserve `integer` on the 13 leaves 4.1 listed by the tool's mechanism or report, add the six schema descriptions as JSDoc on `IdentityLaunchDefaults` (R49), and reshape the shipped `server/config.json` to the 5.0 shape with its values unchanged so `validate:config-schema` stays green. The drift gate is row 4.9                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 4.4  | ✓ 2026-09-16 · `generate-config-schema.ts` (+`config-keys.ts` emitter), `validate-config-schema.ts` (drift covers the table, self-test 12), `src/cli-shared/_generated/config-keys.ts` (67 keys, `CONFIG_KEY_TABLE` total record), `config-input-validator.ts` (one lookup + 4 coercers, switch gone), `config-operations.ts` (`readConfigFile`, `setConfigValueAtPath`, `validateConfigDocument`, table-derived `describeLeaf`), `config-utils.ts` (`SafeConfigWriter` never reads `getConfig()`), router + handler, `config-input-validator.test.ts` + `config-file-write.test.ts` (91 cases), `config-action.test.ts`, `legacy-key-migration.test.ts`. Planner spot-read: table header + 67 entries, validator shape, writer path. Worker: idempotent, table mutation → exit 1 naming the fix, typecheck/scripts/tests-ratchet clean, `validate:arch` 0, `lint:ratchet` no regressions, knip +19 (down from +20), CLI 77/77. Commit blocked by the `_generated/` pre-commit guard (row 4.11); two reds left for 4.5. Concerns ruled R53 (bounds) and R54 (re-exports) · worker opus, `Agent`, shared-tree                           | `server/scripts/generate-config-schema.ts`, `server/src/cli-shared/_generated/config-keys.ts`, `server/src/cli-shared/config-input-validator.ts`, `server/src/mcp/tools/config-utils.ts`, `server/tests/unit/mcp-tools/system-control/config-action.test.ts`                                                                                                                                                                                                                                                                                              | Extend + Create + Edit | 4.3      | **Rewritten 2026-09-16 (R44).** Authored: "generate the allowlist from the same source". Measured: the standalone CLI (`cli/package.json`, Node ≥18.18) carries no `ajv` or `zod`, and `config-utils.ts` still holds a second `CONFIG_VALID_KEYS` + `validateConfigInput` + `createSafeConfigWriter` (F-T2-8), consumed by `system-control-router.ts` and `config-action-handler.ts`. So: the generator emits a leaf table (`path → {type, enum, minimum, maximum}`) into `_generated/config-keys.ts` on the `src/mcp/contracts/schemas/_generated/` precedent; `validateConfigInput` becomes one table-driven function and the 60-case switch goes; `CONFIG_RESTART_REQUIRED_KEYS` keeps `server.port` only (R30); `config-utils.ts` imports from `cli-shared` and defines no list of its own. The drift check from 4.3 covers the table too **R52 (4.2's finding):** `createSafeConfigWriter` (`config-utils.ts:266,280`) reads `getConfig()` and writes the RESOLVED shape back as the file — after 4.2 that would persist nested keys the operator never wrote and drop `hooks`. The writer moves to `cli-shared`, reads the on-disk file, sets one dotted key validated by the leaf table, and writes the file shape; `persistGateConfig`/`persistFrameworkConfig` (R42) keep working through it. `system_control config keys` must still answer with every leaf (69 measured 2026-09-16, zero `$ref`).                                                                                                                                                                                                                                                                                                                                                                               |
| 4.5  | ✓ 2026-09-16 · `server/config.json` = `$schema` + `version: 5`; `generateDefaultConfig` mirrors it; 9 `ConfigFile` members carry `@maximum` (R53) and the schema + key table regenerated byte-stable across the export trim; `config-utils.ts` exports nothing of the key surface (R54, its tests import `cli-shared` directly); `config-file.ts` exports only `ConfigFile` + `ConfigFileVersioning`; 3 fixture sites gained `version: 5`; `config-schema-warning.test.ts` and the `validate:config-schema` self-test build the sections they mutate (F-T4-26); enum storage already canonical and pinned. Planner spot-read: the two-key file, the bounds, the export lists. Worker: typecheck/scripts/tests-ratchet clean, knip `types` −6 against baseline, mutation (port max → 70000) fails the new case, scoped jest 126/126, `validation-self-tests` 57/57. Commit waits on 4.7 (F-T4-25) · worker sonnet, `Agent`, shared-tree, resumed once                                                                                                                                                                                   | `server/config.json`, `server/src/cli-shared/config-operations.ts`, `server/src/shared/types/config-file.ts`, `server/config.schema.json` + `server/src/cli-shared/_generated/config-keys.ts` (regenerated), `server/src/mcp/tools/config-utils.ts` (re-export block only), `server/tests/unit/infra/config/legacy-key-migration.test.ts`, `server/tests/unit/cli-shared/config-input-validator.test.ts`, every fixture `rg -l "validateConfigAgainstSchema\|config.schema.json" server/tests` names                                                      | Edit                   | 4.4, 4.6 | **R46 + R53 + R54 + F-T4-7.** (1) Shipped file → `$schema` + `version` only (`runtime/startup.ts:99` still needs it to exist); `generateDefaultConfig` writes the same object — its 15 stale leaves are the enumeration, and `legacy-key-migration.test.ts › generates a default config naming only keys the validator accepts` is the closure. (2) R53: the bounds the deleted switch enforced return as JSDoc on `config-file.ts`: `server.port` 1024–65535, the three `injection.*.frequency` ≤ 100, `verification.inContextAttempts` ≤ 10, `verification.isolation.timeout` ≤ 3600, `verification.isolation.maxBudget` ≤ 10, `versioning.maxVersions` ≤ 500, `chainSessions.*` 1–10080; regenerate; add the `server.port 70000` case. (3) R54: delete the re-export block in `config-utils.ts`; the tests import `cli-shared` directly. (4) Trim `config-file.ts` exports to `ConfigFile` plus what is imported elsewhere (knip). (5) Every schema-validating fixture carries `version: 5`. (6) Verify an enum matched case-insensitively is STORED as the canonical value. Verification: the planner's effective-config drive at the boundary plus `validate:config-schema` green, the named tests green, knip ratchet at or below +0 for `types` **Boundary drive 2026-09-16 (F-T4-34):** `system_control config list` on the two-key file and on the full-value 5.0 file agree on every key both show; the two-key answer omits `gates`, `phaseGuards`, `logging`, `verification` and `prompts.registerWithMcp`, the sections `normalizeConfigFile` carries only when the file sets them (F-T4-17). Behavioural equality holds at the getter surface (`config-defaults.test.ts`); `list` showing the resolved defaults is step 4's change to what `list` means, beside `get` (R41). |
| 4.6  | ✓ 2026-09-16 · `core-config.ts` (−`AdvancedConfig`, −`Config.advanced`, `FrameworkSettings.injection`, `definitionsDirectory` default fixed), `shared/types/index.ts`, `infra/config/index.ts` (`DEFAULT_CONFIG.server` → claude-prompts/9090, systemPrompt frequency 3, literal fallbacks → constants, `ResolvedConfig` collapsed), `config-defaults.test.ts` (2 cases, getter-surface equality), `config-value-source`/`legacy-key-migration`/`config-file-normalize` tests updated (5 literal sites), `hooks/lib/config_loader.py` + `hooks/README.md` + `hooks/tests/test_config_loader_defaults.py` (4 cases) — two commits on this branch. Planner spot-read: the three aligned values in `DEFAULT_CONFIG`, `injection?:` on `FrameworkSettings`, the Python fallback. Worker: typecheck 0, tests-typecheck ratchet OK, eslint 21 < 22, mutation (port back to 3456) fails the equality test naming `server.port`; ruff clean, pytest 4/4 with a mutation control. `server.version` KEPT: read by `runtime/application.ts:400` and `mcp/http/api.ts:144`, and not a file key · worker sonnet, `Agent`, shared-tree, resumed once | `server/src/infra/config/index.ts`, `server/src/shared/types/core-config.ts`, `server/tests/unit/infra/config/config-value-source.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                | Edit                   | 4.2      | **R32.** One default set, in code, aligned to the shipped values (F-T2-4 measured `server.port` 3456 vs 9090, `systemPromptFrequency` 2 vs 3, `server.name` differing). Every `DEFAULT_*_CONFIG` in `core-config.ts` is either the value `DEFAULT_CONFIG` composes or is deleted; a read-time getter default that disagrees with the file value is a finding, not a silent fix. R48: `hooks.expandedOutput` resolves to `true` when absent, matching the shipped value, in the Python reader (`config_loader.py:26,60`) — the one default whose owner is a hook, not `DEFAULT_CONFIG`. `DEFAULT_FRAMEWORKS_CONFIG.injection.systemPrompt.frequency` 2 → 3 and `DEFAULT_CONFIG.server` `{name, port}` → shipped values (F-T4-1). Pinned by a test that loads the shipped file and the empty object and asserts the resolved `Config` is deep-equal Also: export from `config-file.ts` only `ConfigFile` and the section types 4.2's mapping imports — the 25 exports regressed the knip ratchet by +21 types (4.3's finding) **From 4.2's concerns:** delete `AdvancedConfig` + `Config.advanced` (`core-config.ts:328-334,509-510`, `shared/types/index.ts:66`); replace `FrameworkSettings`' seven flat keys (`:295-307`) with `injection?: FrameworkInjectionConfig`; then collapse the `ResolvedConfig` alias (`index.ts:306`) to `Config`; drop the dead `SemanticAnalysisConfig` import. `server.version`: measure runtime readers; none → delete from `ServerConfig` and `DEFAULT_CONFIG` (R7). The `config-file.ts` export trim moves to row 4.5 (4.9 holds that file now).                                                                                                                                                                                                         |
| 4.7  | ✓ 2026-09-16 · `core-config.ts` (−`Config.transport`, −`GatesConfig.definitionsDirectory`), `infra/config/index.ts` (`assertServerTransportIsStdio` in `loadConfig`, `TransportConfigError`, `getTransportMode()` reads the flag), `application.ts` + `index.ts` (print once, exit 1, on the `PathSettingError` shape), `pipeline-builder.ts:134` reads `getTransportMode()`, `config-action-handler.ts` comment rewritten, `transport-config-refusal.test.ts` (4 cases with controls), `config-file-normalize.test.ts` — one commit on this branch. Worker: `rg` classification 32 → 30 hits, no reader of `Config.transport` left; typecheck 0, tests-typecheck ratchet OK, `validate:arch` 0, `lint:ratchet` no regressions, 72 suites / 862 tests green, mutation (refusal disabled) fails exactly the refusal test. Deviations accepted (R55); the two-parser finding is row 4.12 · worker sonnet, `Agent`, shared-tree                                                                                                                                                                                                           | `server/src/shared/types/core-config.ts`, `server/src/infra/config/index.ts`, `server/src/runtime/options.ts`, `server/src/runtime/cli.ts`, `server/src/runtime/application.ts`, `server/src/index.ts`, `server/src/infra/http/transport/index.ts`, `server/src/infra/logging/index.ts`, `server/src/shared/utils/request-identity-resolver.ts`, `server/src/mcp/tools/prompt-engine/core/pipeline-builder.ts`, `server/src/cli-shared/index.ts`, `server/src/mcp/tools/system-control/handlers/config-action-handler.ts` (one comment), plus their tests | Edit                   | 4.6, 4.4 | **R30, replaces 2.6; re-cut 2026-09-16 after 4.4 (which already dropped transport from the key table, the validator and `config-utils.ts`).** The `--transport` flag owns transport. `Config.transport` leaves `core-config.ts`; `normalizeConfigFile` stops setting it; every consumer reads the launch option instead (`pipeline-builder.ts:134` is the one reader 4.2 measured). A file still setting `server.transport` to anything but `stdio` refuses startup, in `createRuntimeFoundation` beside `assertUsablePathSettings`, naming `--transport=streamable-http`; a leftover `stdio` draws only the schema warning. Delete the `config-action-handler.ts:126` comment with the condition it describes. Also (F-T4-14): delete `GatesConfig.definitionsDirectory` and its assignment in `getGatesConfig()`. Enumeration: `rg -n "server\.transport                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | \.transport\b" src` re-run by the worker |
| 4.8  | ✓ 2026-09-16 · `docs/guides/injection-control.md` (nested keys in the example, the enable/disable table and three common configurations), `docs/reference/mcp-tools.md` (camelCase versioning keys), `CHANGELOG.md` (3 breaking + 3 added bullets, one per landed commit), `CLAUDE.md` §Core Principles 2 (+58 bytes of the 59 the projection had; AGENTS.md now 32767/32768) — one commit on this branch, AGENTS.md regenerated by the hook. Enumeration before/after pasted, every remaining hit staying with a reason; `validate:contributing` 54/54; prettier clean. The Command Reference rows for `generate:config-schema`/`validate:config-schema` could not fit (F-T4-31) · worker sonnet via `documentation_change`, `Agent`, shared-tree                                                                                                                                                                                                                                                                                                                                                                                     | `docs/guides/injection-control.md`, `CHANGELOG.md`, `CLAUDE.md` §Public API Contract if its `config.json` line is falsified                                                                                                                                                                                                                                                                                                                                                                                                                               | Edit                   | 4.5, 4.7 | Public documentation, routed through `>>documentation_change`. Keep the WHY; update every key, count or header the reshape falsifies. Breaking entries: `version` required, nested `frameworks.injection`, root `chainSessions`, `transport` keys removed, `server.version`/`gates.enforcePendingVerdict`/`resources.prompts.defaultRegistration`/`analysis` gone from the schema                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 4.9  | ✓ 2026-09-16 · `server/scripts/validate-config-schema.ts` (`checkSchemaDrift`, first check, self-test 9→11), `server/scripts/generate-config-schema.ts` (exports `generateConfigSchema`, `assertIntegerTagsOnNumberMembers` AST guard), `config-file.ts` (13 `@asType integer`), `config.schema.json` (13 `integer`, 3 `number`), `run-validation-suite.js` (`reads` + `spawn`, matching `validate:schemas`) — one commit on this branch. Planner spot-read: guard at `generate-config-schema.ts:94`, drift check at `validate-config-schema.ts:86` wired at `:286,:306`, 13 integer leaves. Worker: hand-edited description → exit 1 naming the fix; tag on a `string` member → generator exit 1; idempotent; typecheck + scripts typecheck 0; suite-membership 63. Design deviation accepted (F-T4-11): the drift check lives in the validator step rather than as `--check` on the generator · worker sonnet, `Agent`, shared-tree                                                                                                                                                                                                  | `server/scripts/validate-config-schema.ts`, `server/scripts/generate-config-schema.ts`, `server/src/shared/types/config-file.ts`, `server/config.schema.json`, `server/scripts/run-validation-suite.js` if the step's `reads` substrate changes                                                                                                                                                                                                                                                                                                           | Extend                 | 4.3      | Split from 4.3, plus R50. The existing step keeps validating the shipped file and gains the drift half on the `validate:contracts` shape: regenerate to a temp path, compare bytes, name the command that fixes it; self-test adds the hand-edit case. R50: the generator recovers `integer` by a deterministic post-pass driven by a JSDoc tag the library passes through (`extraTags`), never by a tag the library ignores; the 13 leaves 4.1 listed are the enumeration. `validate:suite-membership`, `typecheck:scripts` and the knip ratchet run, since the row edits a validator                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 4.11 | ✓ 2026-09-16 · `.husky/pre-commit` (guard block rewritten as a registry: `generator_for_artifact` names the generator, source regex and artifacts per `_generated` path; a staged artifact is accepted when its source is staged or when its generator reproduces it byte-for-byte; an unclaimed path is refused naming the registry; the regenerate-and-stage behaviour covers both generators). Planner spot-read: `sh -n` ok, registry function and both refusal messages present. Worker drove the hook four ways: stale artifact refused naming `generate:config-schema`, fresh regen-then-stage exit 0 through typecheck, hand edit refused, unclaimed file refused naming the registry. Commit deferred until the tree typechecks again (4.7 in flight) · worker sonnet, `Agent`, shared-tree                                                                                                                                                                                                                                                                                                                                   | the pre-commit guard that prints `Direct edits to generated files detected!` (located by `rg`), `.husky/pre-commit` if the call changes                                                                                                                                                                                                                                                                                                                                                                                                                   | Extend                 | 4.4      | **Found committing 4.4.** The guard owns every `_generated/` path for `generate:contracts`, so the new artifact of `generate:config-schema` cannot be committed at all — it names the wrong generator and the wrong fix. The guard becomes a registry: each generated path names its generator command, and a staged generated file is accepted when regenerating with that command changes nothing; a path under `_generated/` that no generator claims is refused. Self-test or a positive control: a hand-edited `config-keys.ts` is refused naming `npm run generate:config-schema`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 4.12 | ✓ 2026-09-16 · `infra/config/index.ts` (`getTransportMode()` returns a stored value set once by `setTransportMode()` from `application.ts`; no argv scan), `infra/http/transport/index.ts` (`extractTransportArg` reads `--transport value` and `--transport=value`), `runtime/options.ts` (`RuntimeLaunchOptions.transport`), `core-config.ts` (−`GateSystemSettings.definitionsDirectory`, −its default), 4 tests (+7 cases, space form pinned with controls) — two commits on this branch. Worker: typecheck 0, tests-ratchet OK, `validate:arch` 0, `lint:ratchet` no regressions, 57 suites / 722 tests, mutation (space form disabled) fails exactly the new test. The flip condition is only half met: `TransportRouter.determineTransport` still parses arguments because its callers were out of scope — row 4.13. One protocol breach self-reported (DEV-T4-3) · worker sonnet, `Agent`, shared-tree                                                                                                                                                                                                                         | `server/src/runtime/cli.ts`, `server/src/runtime/options.ts`, `server/src/infra/http/transport/index.ts`, `server/src/infra/config/index.ts`, `server/src/engine/execution/pipeline/pipeline-dependencies.ts` (or wherever `PipelineDependencies` is declared), `server/src/mcp/tools/prompt-engine/core/prompt-executor.ts`, `server/src/mcp/tools/prompt-engine/core/pipeline-builder.ts`, `server/src/shared/types/core-config.ts` (`GateSystemSettings.definitionsDirectory`, `DEFAULT_GATES_CONFIG`), plus tests                                     | Edit                   | 4.7      | **Found by 4.7.** `--transport` has two parsers: `parseServerCliArgs` (`runtime/cli.ts`, Node `parseArgs`, both forms) decides auto-quiet, while `TransportRouter.determineTransport` and now `getTransportMode()` scan argv for the `=` form only — so `--transport streamable-http` serves STDIO. One parser: the launch option carries `transport`, `TransportRouter` and the pipeline read it (thread through `PipelineDependencies` from `application.ts`'s `transportType`), and `getTransportMode()` returns what it was given rather than scanning argv. Second item: delete `GateSystemSettings.definitionsDirectory` and `DEFAULT_GATES_CONFIG.definitionsDirectory` (zero readers after 4.7; `GateGuidanceRenderer` reads `DEFAULT_GATES_CONFIG`, so run its tests). Pin with a test that the space form selects HTTP                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 4.13 | ✓ 2026-09-16 · `infra/http/transport/index.ts` (`determineTransport(transport)`; `extractTransportArg` and the `ConfigLoader` fallback deleted), `runtime/context.ts:141` and `runtime/startup-server.ts:61` pass `options.transport`, `options.ts` doc, `transport-mode-resolution.test.ts` (router driven by the option; the space/equals cases live once, at the parser's test), `application-startup.test.ts` (mock gains `transport`) — one commit on this branch. Planner spot-read: the router holds no `--transport`, `process.argv` or extraction. Worker: typecheck 0, tests-ratchet OK, `validate:arch` 0, `lint:ratchet` no regressions, 21 suites / 207 tests, mutation (router returns stdio) fails 6 of 7 router tests · worker sonnet, `Agent`, shared-tree                                                                                                                                                                                                                                                                                                                                                            | `server/src/runtime/context.ts`, `server/src/runtime/startup-server.ts`, `server/src/infra/http/transport/index.ts`, `server/src/runtime/application.ts`, `server/src/runtime/options.ts`, plus `server/tests/unit/infra/transport-mode-resolution.test.ts` and the tests of the two runtime callers                                                                                                                                                                                                                                                      | Edit                   | 4.12     | **Split from 4.12 (F-T4-33).** `parseServerCliArgs` is the one parser; `RuntimeLaunchOptions.transport` already carries its result. `TransportRouter.determineTransport` receives that value from its two callers (`runtime/context.ts`, `runtime/startup-server.ts`) and drops both its argument scan and its `configManager` fallback, which is dead in the real boot path (always `stdio`). `infra/` may not import `runtime/cli.ts` (`no-imports-into-runtime`), which is why the value must be passed in, not looked up. Enumeration: the `rg` in the flip condition; the row closes when the router contains no `--transport` string                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

### T5 — Lands in 5.0.0 (R31: `config.json` is in the Public API Contract, and the release in flight is the major)

| #   | Item                                                                                      | Why deferred                                                                                                                                   |
| --- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 5.1 | Flatten-fix `frameworks.*Frequency/*Target` → nested `injection.{...}.{frequency,target}` | Key reshape = breaking. Needs 4.5's `version` first → executed by rows 4.1/4.2 (step 3)                                                        |
| 5.2 | `advanced.sessions` → a name that says `chainSessions`                                    | same → executed by rows 4.1/4.2 (step 3)                                                                                                       |
| 5.3 | Delete `adoptInertSpellings` + the `analysis` section                                     | Its own stated retirement condition → executed by rows 6.1 and 6.4 (step 4), replaced by the R33 translation                                   |
| 5.4 | Split packaged default from user config (generated on first run)                          | Changes install-time behavior — see OQ-4 → R32 half executed by rows 4.5/4.6 (step 3); the `config.jsonc` half is executed by T7 (step 5, R34) |

### T6 — Delivery step 4: a 4.x file translates on load, every key resolves, `get` returns (cut 2026-09-16)

Rulings this tier runs on: notes R33 (translation keyed on `version`), R41 (`get` returns now), R40/F-T4-34 (`list` shows resolved values), R56–R59 (this cut). Surface `Agent`, `branch_mode: shared-tree`. Row 5.3 is executed here.

| #   | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | File                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Depends               | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 6.1 | ✓ 2026-09-16 · `infra/config/config-file-translation.ts` (new, `translateConfigFile`, 22 `MOVES` + 5 `DROPS`, pure), `infra/config/index.ts` (order parse → transport refusal → translate → notice → schema check → snapshot → normalize; fold, analysis defaults/mapper/warning and `AdoptedConfigFile` gone), `config-file-translation.test.ts` (new, the real 4.x shipped file as literal + hand-built 5.0 twin), four config tests rewritten (commit a3b66aad). Planner spot-read: the `version` gate at `:312`, the load order at `:554-575`, the notice text, the kept raw-file fallback for `hooks.expandedOutput`. Worker: typecheck 0; config tests 70 → 85; tests-ratchet 359 OK; eslint 21 → 19 in `index.ts`; `validate:arch` 516 modules 0; `validate:config-schema` byte-stable; knip ratchet improved (exports −2, types −2); mutation (schema check before translation) fails the ordering case. Left red for 6.2: `lint:ratchet` `import-x/order` 6 → 7 at `index.ts:25` (F-T4-38). Deviation accepted DEV-T4-4; findings F-T4-40–44 · worker opus, `Agent`, shared-tree                                                                                                                                                                                                                                                        | `server/src/infra/config/config-file-translation.ts` (new), `server/src/infra/config/index.ts`, `server/tests/unit/infra/config/config-file-translation.test.ts` (new), `legacy-key-migration.test.ts`, `config-file-normalize.test.ts`, `config-schema-warning.test.ts`, `config-value-source.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Create + Edit                                                                                                                                                                                                                                                                                                                                                                                                                                                            | —                     | **R33 + R58.** A pure `translateConfigFile(raw)` in its own module replaces `adoptInertSpellings`: a record with no `version` is a 4.x file and is translated to the 5.0 shape (`version: 5` set; flat `frameworks.*Frequency/*Target/styleGuidance` → `frameworks.injection.*`; `advanced.sessions.*` → `chainSessions.*`; `versioning.max_versions/auto_version` → camelCase; the eleven inert `mode` spellings → their booleans; removed keys `server.version`, `server.transport`, `gates.enforcePendingVerdict`, `resources.prompts.defaultRegistration`, `analysis` dropped), returning the record plus the list of what it translated and dropped. `version: 5` translates nothing; any other value is left untouched for the schema check to report. Load order becomes parse → transport refusal (raw, R30) → translate → schema check → snapshot → normalize, so a 4.x file draws no schema warning for a key the translation handled; the `rawFileConfig` snapshot is the translated record, so `getConfigValueWithSource` labels the user's key `file` under its 5.0 name and its legacy-spelling exception is deleted. One `logger.warn` per process names every translated and dropped key and says a `version: 5` file is silent. `normalizeAnalysis`, `DEFAULT_ANALYSIS_CONFIG`, `warnAnalysisSectionDeprecated` and the `analysis` assignment go; `AdoptedConfigFile` collapses to `ConfigFile`; `normalizeVersioning` reads camelCase only. Fixture: the pre-step-3 shipped file (`git show 83196fcd:server/config.json`, pasted as a literal) translates to zero schema errors where untranslated it draws at least one (control), and `normalizeConfigFile(translate(it))` deep-equals `normalizeConfigFile` of its hand-built 5.0 twin. Row 1.4's "check before adoption" mutation is rewritten to the new order: moving the schema check before the translation makes the 4.x case warn. `config-file-normalize.test.ts:86`'s flip stamp fires: a version-less file with `systemPromptFrequency: 7` resolves to 7 |
| 6.2 | ✓ 2026-09-16 · `infra/config/index.ts` (six new section defaults, `normalizeVerification`/`normalizeIdentity`, every mapper returns a value, `DEFAULT_CONFIG = normalizeConfigFile(EMPTY_CONFIG_FILE)`, 19 getter `??` sites gone, `getPromptsRegisterWithMcp(): boolean`), `core-config.ts` (sections required, `IdentityConfig`, `DEFAULT_PROMPTS_CONFIG`), `config-manager.ts` (`deferred` gone), `converter.ts` (hard-coded `true` gone), `pipeline-builder.ts` (phaseGuards literal and the `!identityConfig?.mode` guard gone), `request-identity-resolver.ts` (fallbacks pinned equal to the loader), handler (`is not set`), four tests (pinned undefined set of 13) (commit e10ede8e). Planner spot-read: `DEFAULT_CONFIG` at `:517`, the two new mappers, the pinned literal at the enumeration test. Worker: typecheck 0; unit 3675 pass / 1 fail (row 6.8); `validate:arch` 516/0; schema byte-stable; knip improved (types −12); mutation 1 (getter fallback reinstated) invisible, mutation 2 (loader default flipped) fails 3 normalize tests naming the key. Left for rows 6.6–6.8: three rename orphans (R60), `strict`/`gates.directory` (R61), 16 dead `?.` + 8 fixture errors + 1 assertion + 2 dead literals + the identity-scoping changelog line (R62). DEV-T4-6; findings F-T4-48–53 · worker opus, `Agent`, shared-tree | `server/src/infra/config/index.ts`, `server/src/shared/types/core-config.ts`, `server/src/shared/types/config-manager.ts`, `server/src/modules/prompts/converter.ts`, `server/src/mcp/tools/prompt-engine/core/pipeline-builder.ts` (dead `??` only), `config-value-source.test.ts`, `config-defaults.test.ts`, `config-file-normalize.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Edit                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 6.1                   | **R57 (R40's deferred half, F-T4-34, F-T4-15, F-T4-17).** `normalizeConfigFile` resolves every section — `gates`, `resources`, `logging`, `verification`, `phaseGuards`, `identity`, `prompts.registerWithMcp` join the ones already resolved — so `Config`'s section members become required, `DEFAULT_CONFIG` is `normalizeConfigFile({ version: 5 })`, and the getters keep only their name mapping (`frameworkGates` → `enableFrameworkGates`) and env overrides, never a second default. The read-time literals move into the loader's `DEFAULT_*` constants: `getGatesConfig`/`getResourcesConfig`/`getLoggingConfig`/`getVersioningConfig`/`getTelemetryConfig`/`getChainSessionConfig`/`getFrameworksConfig`/`getExecutionConfig`'s `??` sites, `pipeline-builder.ts:380`'s `phaseGuards` literal, `converter.ts`'s `return true` for `registerWithMcp` (the shipped 4.x value), and whatever `identity.mode`/`allowPerRequestOverride` default `request-identity-resolver.ts` carries — each measured before it moves, and a disagreement between two of them is a finding, not a silent pick. `'deferred'` leaves `ConfigValueSource`; `getPromptsRegisterWithMcp(): boolean`. `system_control config list` therefore prints every effective value. Enumeration test rewritten: every key `listConfigKeys()` returns answers `file`, `default` or `environment`, and the set answering `value: undefined` equals exactly the `ConfigFile` members with no `@default` tag (measured and pinned as a literal). `config-defaults.test.ts` compares raw `getConfig()` now, and drops its exclusion list. Mutation: a getter given back one `??` default disagreeing with the loader's must fail a named test                                                                                                                                                                                                                                                                                                                      |
| 6.3 | ✓ 2026-09-16 · `config-action-handler.ts` (`handleConfigGet`, refusal text without the held sentence), `system-control.schema.ts` (nested `operation` enum `validate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | get`), `tooling/contracts/system-control.json`, `_generated/`+`tests/snapshots/mcp-input-schemas.json`(regenerated, diff = the enum),`config-action.test.ts`12 → 15,`tool-surface.yaml` (`sc-config-get`, `sc-config-get-unknown-key`), `docs/reference/mcp-tools.md`§Config Operations (commit e7100216). Planner spot-read: the`get` case, both refusal strings, both scenarios, the doc example. Worker: mutation (`get`→`list`) fails all 4 new cases; typecheck 0; tests-ratchet 359 OK; `validate:contracts`+`validate:conformance-coverage`+`validate:tool-schemas`green after`capture:tool-schemas`; hermetic conformance 140/140; live: `server.name`→`claude-prompts`/`default`, unknown key refused naming `keys`, missing key refused. Findings F-T4-37/38 · worker sonnet, `Agent`, shared-tree | `server/src/mcp/tools/system-control/handlers/config-action-handler.ts`, `server/tooling/contracts/system-control.json`, `server/src/mcp/tools/schemas/system-control.schema.ts`, `server/src/mcp/contracts/schemas/_generated/` + `server/tests/snapshots/mcp-input-schemas.json` (regenerated), `server/tests/unit/mcp-tools/system-control/config-action.test.ts`, `server/tests/e2e/conformance/tool-surface.yaml`, `docs/reference/mcp-tools.md` §Config Operations | Extend                | — (∥ 6.1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **R41 + R59.** `get` returns over MCP in the shape R37 keeps: `config: { operation: "get", key }`. The handler answers from `getConfigValueWithSource(key)` — key, effective value as JSON, and source — refuses a key absent from `listConfigKeys()` naming `keys`, and refuses a missing key. The generic refusal drops its "`get` is held" sentence; `set`/`reset`/`restore` stay refused with the R42 wording (row 2.17's text is not this row's). Contract `config` description and the Zod `operation` enum gain `get`; regenerate `_generated/` and the snapshot in the row. `sc-config-get-refused` becomes `sc-config-get` (`server.name` → `claude-prompts`) plus `sc-config-get-unknown-key`; `validate:conformance-coverage` stays green. Unit cases: a `file` key, a `default` key, unknown key, missing key — asserted on `isError` plus the value text. Until 6.2 lands a never-set key answers `deferred`; the test pins only the two labels that exist in both worlds, and 6.2's handoff re-runs this file. Docs section rewritten in the reader's register (no `get` held sentence) |
| 6.4 | ✓ 2026-09-16 · `core-config.ts` (−4 types, −`Config.analysis`), `shared/types/index.ts` (−4 re-exports), `config-manager.ts` + `content-analyzer.ts` (comments rewritten to the removed state), `gate-services.test.ts` (vestigial `analysis` fixture and its flag gone, two identical cases merged), two config tests (`'analysis' in config` assertions) (commit 519b9c3a). Planner spot-read: no type or member left in the three type files; the fixture takes no flag. Worker: typecheck 0; 9 suites / 89 tests unchanged; tests-ratchet 359 OK; `validate:arch` 516/0; knip ratchet improved (exports −2, types −8); prettier clean. Enumeration: zero type-surface hits; the remaining string hits are the two config tests' 4.x fixtures that exercise the drop (flip condition corrected: type surface, not literals — F-T4-45). `test-helpers.ts` had no hit · worker sonnet, `Agent`, shared-tree                                                                                                                                                                                                                                                                                                                                                                                                                                     | `server/src/shared/types/core-config.ts`, `server/src/shared/types/index.ts`, `server/src/shared/types/config-manager.ts` (comment), `server/src/modules/semantic/content-analyzer.ts` (comment), `server/tests/unit/gates/services/gate-services.test.ts`, `server/tests/helpers/test-helpers.ts`, `server/tests/unit/infra/config/legacy-key-migration.test.ts` (the `analysis` describe)                                                                                                                                                                                                                                                                                                                                                                                                                  | Delete                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 6.1                   | **Row 5.3's second half.** Delete `LLMProvider`, `LLMIntegrationConfig`, `SemanticAnalysisConfig`, `AnalysisConfig` and `Config.analysis`, their re-exports, and the two comments that say the section is still parsed. The `gate-services.test.ts` fixture feeds an `analysis` object to `GateServiceFactory`, which reads nothing from it (`rg "\.analysis\b" src` finds no reader): delete the object and run the file to prove it. `ContentAnalysisResult.semanticAnalysis` and the frameworks' semantic types are a different concept and stay. Enumeration is the flip condition's `rg`, re-run by the worker; the knip ratchet and `validate:arch` run, since exports change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 6.5 | ✓ 2026-09-16 · `CHANGELOG.md` (BREAKING: `analysis` removed; Added: `config get`; Changed: version-less file translated on load, `list` shows every effective value; plus the older `version: 5` bullet's "analysis is unaffected" sentence corrected in the same PR), `docs/guides/cli.md` (§ removed-in-5.0 callout rewritten) (commit a2e3a94f). `troubleshooting.md` has no config-warning section (not invented); `injection-control.md` already nested. Planner spot-read: the four bullets and the callout. Worker: prettier clean; `validate:preview-vocabulary`, `validate:no-methodology-vocab`, `validate:contributing` exit 0; no plan vocabulary in added lines. Finding F-T4-47 · worker sonnet, `Agent`, shared-tree, resumed once                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `CHANGELOG.md`, `docs/guides/cli.md` (§ around line 330), `docs/guides/troubleshooting.md` and any other `docs/` page the worker's `rg -n "analysis                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | advanced.sessions                                                                                                                                                                                                                                                                                                                                                                                                                                                        | systemPromptFrequency | version.: 5" docs` names                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Edit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 6.1, 6.2, 6.3, 6.4 | Public documentation via `>>documentation_change`, reader's register, keep the WHY. Entries: BREAKING — the `analysis` section is removed; a file still carrying it loads, the section is dropped, one notice names the replacement. Changed — a `config.json` without `"version": 5` is read as a 4.x file and translated in memory on load, with one notice naming what moved; no user action; the translation is removed in 6.0.0. Added — `system_control(action:"config", operation:"get")` returns one key's effective value and where it came from. Changed — `config list` shows every effective value, defaults included. No `CLAUDE.md` projected-section edit (F-T4-31) |
| 6.6 | ✓ 2026-09-16 · `core-config.ts` (`VersioningConfig.maxVersions`/`autoVersion`, `ChainSessionConfig.timeoutMinutes`, −`GatesConfig.directory`), `index.ts` (defaults, mappers, placeholder gone), `version-history-service.ts`, `prompt-executor.ts`, 14 test files (fixtures, the pinned literal 13 → 10 with a positive case for the three keys, the `gates.directory` residue) (commit 84f48d15). Planner spot-read: the enumeration after (8 files, all the translation `from` side, 4.x fixtures or prose). Worker: typecheck 0; `test:unit` 3675/3675; 4 integration suites 99/99; both ratchets no regressions; knip −14; mutation (one consumer renamed back) fails typecheck naming it. `version-history.ts`'s `max_versions` is the CLI history request's own field, not a consumer (confirmed) · worker sonnet, `Agent`, shared-tree                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `server/src/shared/types/core-config.ts`, `server/src/infra/config/index.ts`, every file `rg -l "max_versions\|auto_version\|sessionTimeoutMinutes" server/src server/tests` names (20 + 6 measured 2026-09-16), `server/tests/unit/infra/config/config-value-source.test.ts` (pinned literal)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Rename                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 6.8                   | **R60.** The runtime `Config` adopts the file's leaf names — `VersioningConfig.maxVersions`/`autoVersion` and `ChainSessionConfig.timeoutMinutes` — so the dot-walk `get`/`list` use answers for those three keys with the value in use, and no file→runtime name map exists. One construct's enumeration, over the default bound on purpose; the predicate is the flip condition. The pinned undefined set shrinks from 13 to 10                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 6.7 | ✓ 2026-09-16 · `config-file.ts` (−`gates.directory`, `strict` describes its mode-dependent default, stale doc example corrected), `config.schema.json` + `config-keys.ts` regenerated (−1 property, −1 key), `config-file-translation.ts` (`DROPS` + `gates.directory`), `index.ts` (no file read of the key), `config-file-translation.test.ts` (`dropped` = `server.transport`, `gates.directory`; twin without the key), `CHANGELOG.md` (one BREAKING bullet) (commit d5f64444). Planner spot-read: the `DROPS` entry, the schema/table diff stat. Worker: generator idempotent; `validate:config-schema` ✓; typecheck 0; both ratchets no regressions; `validate:arch` 0; mutation (drop the `DROPS` entry) fails 2 of 10 translation tests. Residue to 6.6: `config-file-normalize.test.ts` `passes gates… as written` still sets `directory`, and `GatesConfig.directory` on the runtime type is dead (F-T4-56) · worker sonnet, `Agent`, shared-tree                                                                                                                                                                                                                                                                                                                                                                                      | `server/src/shared/types/config-file.ts`, `server/config.schema.json` + `server/src/cli-shared/_generated/config-keys.ts` (regenerated), `server/src/infra/config/config-file-translation.ts` (+ `DROPS`), `server/src/infra/config/index.ts` (the `gates.directory` default 6.2 added), `server/tests/unit/infra/config/config-file-translation.test.ts`, `server/tests/unit/cli-shared/config-input-validator.test.ts` if it names the key, `CHANGELOG.md` (one BREAKING bullet)                                                                                                                                                                                                                                                                                                                           | Delete + Edit                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 6.8                   | **R61.** `gates.directory` has zero readers (`getGatesDirectory()` resolves through `PathResolver`; F-T4-14 for its runtime twin) and after 6.2 answers `source: file` with a value the server ignores: remove it from `ConfigFile`, regenerate, add it to the translation's `DROPS` (a 4.x file loses it silently-with-notice; a `version: 5` file setting it draws the schema warning), and delete 6.2's `'resources/gates'` default. `gates.evaluation.strict`'s `@default true` is false — its effective default is `mode === 'judge'` (`judge-prompt-builder.ts:35`): replace the tag with a description stating that; the leaf stays in the pinned undefined set. Do not touch `config-value-source.test.ts` (6.6 owns it)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 6.8 | ✓ 2026-09-16 · six runtime files (16 dead `?.` gone), `03-identity-resolution-stage.ts` (provider non-null, fallbacks gone), `19-phase-guard-verification-stage.ts` (provider always returns a config, fallback gone), five test files (fixtures completed to full `Config`, one assertion, one premise-less case deleted), `CHANGELOG.md` (Fixed: executions scope to the launch workspace) — commits d713e4d2 + 3fd3087e. Planner spot-read: the ratchet outputs, the changelog bullet. Worker: typecheck 0; `lint:ratchet` no regressions (`no-unnecessary-condition` 418 → 402, positive control +2 on one restored line); tests-ratchet 359; `test:unit` 3673/3675 with the one remaining red owned by 6.6 (`gates.directory` fixture); findings F-T4-54/55/58 · worker sonnet, `Agent`, shared-tree, resumed once                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `runtime/context.ts`, `runtime/module-initializer.ts`, `runtime/application.ts`, `mcp/tools/index.ts`, `prompt-engine/core/prompt-executor.ts`, `system-control/system-control-router.ts` (16 dead `?.`), `stages/19-phase-guard-verification-stage.ts:67`, `stages/03-identity-resolution-stage.ts:88-91` (dead literals), `tests/unit/runtime/context.identity-overrides.test.ts`, `tests/unit/infra/config/legacy-key-migration.test.ts`, `tests/integration/gates/gate-judge-pipeline-wiring.test.ts`, `CHANGELOG.md` (one Fixed bullet, R62)                                                                                                                                                                                                                                                            | Edit                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 6.2                   | **Mechanical consequences of 6.2's required sections, enumerated by 6.2's worker (F-T4-50).** Delete the 16 optional chains eslint now flags, the two dead fallback literals, complete the three partial fixtures (+8 tests-typecheck errors), fix the one falsified assertion (`:174`, `gates.enabled` resolves to `true`), and add the changelog line for the identity-scoping fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

**Dispatch** — tier · effort · failure shape:

| Row | Tier   | Effort | Failure shape                                                                                                                                                                                    | branch_mode | Surface |
| --- | ------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | ------- |
| 6.1 | opus   | high   | wrong approach — the translation table is derived from two schema leaf lists the brief quotes, but the ordering against the schema check and the snapshot, and what the notice names, are design |
| 6.3 | sonnet | high   | wrong output — shape and refusals ruled (R37, R59); the risk is contract, Zod schema, generated metadata, snapshot and conformance drifting apart, which this repo gates                         |
| 6.2 | opus   | high   | wrong approach — which layer owns each default across eight getters and three out-of-loader literals, and `Config`'s optionality is a contract over 20+ readers                                  |
| 6.4 | sonnet | medium | wrong output — a named construct deleted across a measured list; the risk is touching the homonymous `semanticAnalysis` result field                                                             |
| 6.5 | sonnet | medium | wrong output — the entries are enumerated in the row; the risk is session vocabulary leaking into release notes                                                                                  |
| 6.8 | sonnet | medium | wrong output — every edit is named by file and line by the previous handoff; the risk is a fixture completed with a value that disagrees with the loader's default                               |
| 6.6 | sonnet | high   | wrong output — a rename across a measured 26-file enumeration; the risk is a consumer left reading the old name through a cast or a string key                                                   |
| 6.7 | sonnet | medium | wrong output — two named edits to one type plus a regeneration; the risk is a fixture or validator test that still names `gates.directory`                                                       |

### T7 — Delivery step 5: the user's config is `config.jsonc` (cut 2026-09-19)

Rulings this tier runs on: notes R34 (owner: `config.jsonc`, `jsonc-parser`, both-files refusal, hooks read it), R46 (the packaged `config.json` stays), R65–R73 (this cut). Surface `Agent` (binds `model` only), `branch_mode: shared-tree` inside the linked worktree `../claude-prompts-mcp-jsonc`. T5 row 5.4's remaining half is executed here.

| #    | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | File                                                                                                                                                                                                                                                                                                        | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Depends       | Task           |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | -------------- |
| 7.1  | ✓ 2026-09-19 · `config-file-format.ts` (new: `USER_CONFIG_FILENAMES`, `configFileFormat`, `parseConfigText`, `findWorkspaceConfigFiles`; pure), `jsonc-parser` ^3.3.1 in `server/package.json` + lock, 26 tests (commit 3fc4ad6d). Planner spot-read: the errors-array check, `allowTrailingComma`, `allowEmptyContent: false`, the line:column message. Worker: typecheck 0; tests-ratchet 359; `validate:lockfile-sync` OK; `validate:arch` 0 errors; eslint clean; knip ratchet 1158 no regressions; mutation (errors check removed) fails 5 named tests. Header comment carries plan ids — removal handed to 7.2 (F-T5-1) · worker sonnet, surface Agent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `server/src/shared/utils/config-file-format.ts` (new), `server/package.json` + `server/package-lock.json`, `server/tests/unit/shared/utils/config-file-format.test.ts` (new)                                                                                                                                | The one owner of "which file is the user's config and how its text parses" (R66, R67): `USER_CONFIG_FILENAMES = ['config.jsonc', 'config.json']`, `configFileFormat(path)` by extension, `parseConfigText(text, format)` — `jsonc` through `jsonc-parser` with the errors array checked (comments and trailing commas yes (R73 supersedes R67's comma half), nothing else beyond JSON, error names line:column), `json` through strict `JSON.parse` — and `findWorkspaceConfigFiles(dir)` returning the existing names in precedence order. Pure, no logger. Adds `jsonc-parser` as a server dependency                                                                                                                                                        | —             | format-owner   |
| 7.2  | ✓ 2026-09-19 · `runtime/paths.ts` (`resolveDefaultConfigPath` returns `origin: 'workspace' \| 'package'` so no caller compares a filename inside a label; workspace resolves through `findWorkspaceConfigFiles`; new `assertSingleWorkspaceConfig` refuses both names, naming both absolute paths; the usable-file check parses by format), `infra/config/index.ts` (`parseConfigRecord` parses by format — the one parse site, hot reload re-enters it; load order unchanged), `index.ts` (debug probe), `config-file-format.ts` (header without plan ids, F-T5-1 closed), +7 path tests, +3 loader tests (commit 327190a3). Planner spot-read: the refusal text, the `origin` split, the call order inside `assertUsablePathSettings`. Worker: 9 suites / 122 tests; typecheck 0; tests-ratchet 359; `lint:ratchet` no regressions (one `import-x/order` caught and fixed); `validate:arch` 0 errors; mutation (strict `JSON.parse` restored, one site at a time) fails exactly the 2 jsonc tests at each site — after the worker replaced a fixture whose values equalled the code defaults (F-T5-11). Live proof of the flip condition this row was cut with waits for the boundary drive on a built server · worker sonnet, surface Agent                                                                                                                                                                                      | `server/src/infra/config/index.ts`, `server/src/runtime/paths.ts`, `server/src/index.ts`, their unit tests                                                                                                                                                                                                  | Server readers go through 7.1 (R68): workspace resolution is `config.jsonc` → `config.json` → the packaged `config.json`; `assertUsablePathSettings` refuses a workspace holding both, naming both paths and the remedy, and its usable-file check parses by format; the loader parses by format before the transport refusal and translation (load order otherwise unchanged); `--config`/`MCP_CONFIG_PATH` take the format from the extension; `index.ts`'s debug probe resolves the same way                                                                                                                                                                                                                                                                | 7.1           | server-readers |
| 7.3  | ✓ 2026-09-19 · `generate-config-schema.ts` (third artifact from the same `createSchema()` run; self-asserts example-line count = leaf count; throws on a patterned string leaf with no default), `_generated/config-template.ts` (new, `CONFIG_JSONC_TEMPLATE`, 401 template lines, 66 example lines, every one ending in a comma), `validate-config-schema.ts` (third drift comparison; self-test 12 → 14 incl. parity with `generateDefaultConfig()`), `config-template.test.ts` (new, 70: one per key + uncomment-everything + controls), `.husky/pre-commit` (generated-file registry arm) (commit 49ab2b9e). Planner spot-read: the header text, the line grammar (`// ` + real indentation; `—` marks documentation), the registry diff, no plan ids. Worker: generator idempotent by sha1, the two older artifacts byte-unchanged; mutation (one character of the template) fails `validate:config-schema` naming the file; probe control (`port: 99`) fails 2 of 70; typecheck 0, `typecheck:scripts` 0, tests-ratchet 359; knip ratchet 1158 · worker opus, surface Agent                                                                                                                                                                                                                                                                                                                                                  | `server/scripts/generate-config-schema.ts`, `server/scripts/validate-config-schema.ts`, `server/src/cli-shared/_generated/config-template.ts` (new, generated), one new unit test                                                                                                                           | A third generated artifact (R69): `CONFIG_JSONC_TEMPLATE`, a string holding `$schema` and `version: 5` live and every settable leaf as a commented-out example grouped by section, each with its schema description, default where the schema has one, and enum values. Drift-checked by `validate:config-schema` with a self-test case. Property pinned by test for EVERY leaf: uncommenting that leaf's example (with its section braces) yields text that parses as JSONC and passes the schema check                                                                                                                                                                                                                                                       | —             | jsonc-template |
| 7.4  | ✓ 2026-09-19 · `config-operations.ts` (`resolveConfigPath` by precedence, neither → the `config.jsonc` path; ONE both-files detector `ambiguousConfigError` behind `readConfigFile`, `initConfig` and `resetConfig`, firing only for the two config basenames so an operator-named `MCP_CONFIG_PATH` is never refused for its neighbours; reads by format; new `writeConfigKeyAtomic` = `modify` + `applyEdits` on the text → temp, re-parse by format, rename; `writeConfigAtomic` kept for create/reset; `initConfig` writes the template verbatim, skips on either name; new `resetConfig(workspace): ConfigResetResult`), `config-utils.ts` (persist step 6 is the in-place writer; steps 1–5 and the reload unchanged), `version-history.ts` (workspace-id read by precedence and format), `cli-shared/index.ts` (+2 barrel exports, DEV-T5-1), tests +18 / new `safe-config-writer.test.ts` / +2 (commit 7017a718). Planner spot-read: `writeConfigKeyAtomic`, `writeConfigTextAtomic`, no plan ids. Worker: 276 tests / 11 suites + 140 integration / 8 suites; typecheck 0; tests-ratchet 359; `validate:arch` 0; `validate:config-schema` ✓ 14/14; cli typecheck 0; mutation (whole-document write) kills 5 named tests; live drive: init → set → diff is the inserted key only, comments preserved. `workspace-init.ts` unchanged — it writes no config; `cpm init` calls `initConfig` (R74) · worker opus, surface Agent | `server/src/cli-shared/config-operations.ts`, `server/src/cli-shared/workspace-init.ts`, `server/src/cli-shared/version-history.ts`, `server/src/mcp/tools/config-utils.ts`, their tests                                                                                                                    | Reads and writes on the file shape go through 7.1 (R70): `resolveConfigPath` follows R68 and refuses both-files; `set` and the server's persist writer edit in place with `jsonc-parser` `modify` + `applyEdits` for both formats, keeping atomic temp + rename and the post-write parse; `config init` and `cpm init` create `config.jsonc` from 7.3's template and skip when either name exists; `reset --force` backs up, then writes the template to `config.jsonc`, or today's minimal document to an existing `config.json` (no silent rename); `version-history.ts`'s workspace-id read resolves by R68. Byte-identity test uses a hand-authored fixture (comment, odd key order, block comment) plus a positive control                                | 7.1, 7.3      | config-writes  |
| 7.5  | ✓ 2026-09-19 · `cli/src/commands/config.ts` (`configReset` calls `resetConfig`; messages name the resolved basename), `init.ts`, `validate.ts` (result id = resolved basename), `cli.ts` + `guide.ts` help (`config.jsonc`), `create.ts`, new `cli/tests/integration/config-jsonc.test.ts` (7: init, set keeps a marker comment, list/validate, both-names refusal, reset ×2, strict-`.json` control) (commit a967b795). Worker: cli typecheck 0; 88/88 against an alias-patched bundle; mutation (old whole-document reset) fails the reset-keeps-comments test. **Against the tracked build the suite is 10/88: every `cpm` command crashes at load — F-T5-17, row 7.10.** `reset` now resolves the workspace before checking `--force`, matching its sibling subcommands (accepted, R75) · worker sonnet, surface Agent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `cli/src/commands/config.ts`, `cli/src/commands/init.ts`, `cli/src/commands/validate.ts`, `cli/src/cli.ts`, `cli/src/commands/guide.ts`, `cli/src/commands/create.ts`, `cli/tests/**` touched by them                                                                                                       | The standalone CLI names the file it acted on (R71): messages and help text say `config.jsonc` where they describe the 5.0 file and the resolved basename where they report on one; `cpm.js` bundles `jsonc-parser` (measure the bundle resolves it; Node 18.18 floor holds)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 7.4           | cli-surface    |
| 7.6  | ✓ 2026-09-19 · `file-observer.ts` + `artifact-kinds.ts` (both take the names from `USER_CONFIG_FILENAMES`), `config-action-handler.ts` + `system-control-router.ts` (replies name `basename(getConfigPath())` through getters that already existed), `infra/config/index.ts` (translation notice names the loaded file; the schema warnings already interpolated the full path and stay), `index.ts` `--help`, `injection.ts` and two contract descriptions (class-b prose found by the enumeration, DEV-T5-2), `config-file.ts` `version` JSDoc + regenerated schema and template (`config-keys.ts` byte-unchanged), `run-validation-suite.js` prose, `.eslint-ratchet-baseline.json` (−1 freed by 7.4, tightened), tests incl. new `system-control-router-persist.test.ts` (commit bd5d5de9). Planner spot-read: diff stat (21 files, +127/−46), the baseline delta (2903 → 2902, one rule 399 → 398). Worker: 21 suites / 244 tests; typecheck 0; tests-ratchet 359; `lint:ratchet` OK; `validate:arch` 0; `validate:config-schema` 14/14; `validate:contracts` ✓; mutation (literal restored in the validate reply) fails cases 2 and 4. Conformance e2e unrun (needs `dist/`) — boundary. Finding: the observer's name check never sees the workspace root; the config's own reload is `ConfigLoader`'s watcher (F-T5-12 answered) · worker sonnet, surface Agent                                                              | `server/src/modules/hot-reload/file-observer.ts`, `server/src/engine/gates/utils/artifact-kinds.ts`, `server/src/mcp/tools/system-control/handlers/config-action-handler.ts`, `server/src/mcp/tools/system-control/system-control-router.ts`, `server/tests/e2e/conformance/tool-surface.yaml`, their tests | Everything that recognises the config by NAME recognises both (R71): the observer's `isConfigFile`, `artifact-kinds`; the schema-warning, `validate` and persist replies name the basename of the file actually loaded instead of the literal `config.json`; conformance expectations follow. Plus two named edits from 7.3's handoff (F-T5-7, F-T5-8): the `version` JSDoc in `shared/types/config-file.ts` loses its TypeScript vocabulary (it now reaches users through the template) with all three artifacts regenerated, and `scripts/run-validation-suite.js`'s prose for `validate:config-schema` names the template and the parity case                                                                                                               | 7.2           | name-aware     |
| 7.7  | ✓ 2026-09-19 · `config_loader.py` (`get_config_path`: `MCP_WORKSPACE` → `CLAUDE_PLUGIN_DATA` → packaged `server/`, `config.jsonc` before `config.json`, first existing wins; pure string-aware `strip_jsonc_comments` for comments and trailing commas; failures fall back to defaults), `ralph-stop.py` (own path + parse gone, calls the loader), `test_config_loader_lookup.py` (new, 13), `test_ralph_stop.py` (new seam) (commit 232bd277). Planner spot-read: the lookup, the escape handling in both strippers, no plan ids left (`rg` empty after one resumed fix). Worker: `pytest hooks/tests` 314 passed, no stray files; ruff clean; pyrefly 0; mutation (string-unaware stripper) fails exactly the two string-awareness tests. No third Python reader; downstream repos hold only installed copies · worker sonnet, surface Agent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `hooks/lib/config_loader.py`, `hooks/ralph-stop.py`, `hooks/tests/test_config_loader_defaults.py`, `hooks/tests/test_ralph_stop.py`                                                                                                                                                                         | Python readers (R72): one lookup in `config_loader.py` — `MCP_WORKSPACE`, then `${CLAUDE_PLUGIN_DATA}`, then the packaged `server/` — trying `config.jsonc` then `config.json` in each and taking the first found; `.jsonc` text passes through a string-aware comment stripper (line and block comments and trailing commas, never inside a string); `ralph-stop.py` drops its own path + parse and calls the loader. A hook never refuses: both names present reads `config.jsonc`, unreadable falls back to defaults as today                                                                                                                                                                                                                               | —             | python-readers |
| 7.8  | ✓ 2026-09-19 · `CHANGELOG.md` (1 BREAKING, 1 Added, 3 Changed, 1 Fixed — the hooks item is Fixed because a 4.x workspace `hooks.expandedOutput` silently did nothing), `docs/guides/cli.md` (new `config.jsonc` section), `troubleshooting.md` (+2 entries: both-files refusal, a comment in `config.json`), `hooks/README.md` (lookup order), 14 more docs with class-a/b edits; 22 files / 67 hits classified, every remaining `config.json` is the packaged file, a client's own config, a 4.x note or the one still-read sentence (commit 1cd0c905). Planner spot-read: the six changelog bullets against the code they describe. Worker: prettier clean on 18 files; `validate:format`, `validate:contributing`, `validate:preview-vocabulary`, `validate:no-methodology-vocab`, `guidance:check` (AGENTS.md 32767/32768) exit 0; no plan vocabulary in added lines; `config init` absent. `>>documentation_change` has no document type for a changelog-plus-sweep and was cancelled (F-T5-21) · worker sonnet, surface Agent                                                                                                                                                                                                                                                                                                                                                                                                 | `CHANGELOG.md`, `docs/guides/cli.md`, `docs/guides/troubleshooting.md`, `server/README.md`, `docs/reference/mcp-tools.md`, plus any of the 22 measured docs whose instruction is now wrong                                                                                                                  | Reader-voice docs: CHANGELOG (Added: `config.jsonc` with commented examples and comment-preserving `cpm config set`; Changed: `init` writes `config.jsonc`, hooks read the workspace file; BREAKING: a workspace holding both names refuses startup), and each doc that tells a user to create or edit `config.json` names `config.jsonc` while saying `config.json` is still read. No projected `CLAUDE.md` section                                                                                                                                                                                                                                                                                                                                           | 7.2, 7.4, 7.7 | docs           |
| 7.9  | ✓ 2026-09-19 · `skills-sync/service.ts` (`resolveHarnessCovers` parses by format; fallback unchanged), `export-command.test.ts` (+2: a commented `config.jsonc` with a distinguishing value is honoured; the same text as `.json` yields the fallback), new `config-text-parse-guard.test.ts` (predicate: holds a config path AND calls `JSON.parse(`; 5 pinned files, each reason verified by opening the file; in-memory positive control) (commit 8eaacba7). Planner spot-read: the pinned map's five reasons. Worker: 5/5 + 3/3; typecheck 0; tests-ratchet 359; `validate:arch` 0; mutation A (strict parse restored) fails the named test, mutation B (one pinned entry deleted) fails the guard naming the file. `gates.harnessCovers` is untouched by the 4→5 translation · worker sonnet, surface Agent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `server/src/modules/skills-sync/service.ts`, its unit test, one new guard test under `server/tests/unit/shared/utils/`                                                                                                                                                                                      | A reader no row named (F-T5-16): `resolveHarnessCovers` (`service.ts:159-160`) runs a strict `JSON.parse` on `paths.serverConfigPath`, which now resolves `config.jsonc` first — a comment throws, the catch returns `[]`, and exports silently stop honouring `gates.harnessCovers`. Parse by format through the owner module. Close the CLASS, not the site: a guard test enumerates every `server/src` and `cli/src` file that both holds a config path (`serverConfigPath`, `getConfigPath(`, `configPath`, `resolveConfigPath`, `MCP_CONFIG_PATH`) and calls `JSON.parse(`, and compares the list to a pinned set where each entry states what its `JSON.parse` reads instead (measured today: 5 files, 1 defect)                                         | 7.2           | missed-reader  |
| 7.10 | ✓ 2026-09-19 · `cli/esbuild.config.mjs` (exported `resolveEsmEntry`: a per-package `alias` to the `module` entry found through `createRequire(...).resolve('jsonc-parser/package.json')`, no machine path; exported `assertNoUntraceableRequires(outfile)`: refuses a bundle holding a relative `require` whose target was not emitted, naming each specifier and the likely cause; dev budget 640,000 → 680,000 B for a measured 673,115 B, shipped budget 512,000 B untouched at 339,971 B), `server/esbuild.config.mjs` (same alias resolved inline, to keep that file's lazy import of the CLI config; the guard runs on `index.js` and `cpm.js`) (commit 0978b0f5). Planner spot-read: the commit holds exactly the two configs. Worker, all on tracked builds: `node dist/index.js --help` 0, `node dist/cpm.js config --help` 0, `verify:mcp` 18/18, `cli/` suite 88/88 with no `.tmp-*` left, built `cpm init` → marker comment → `config set gates.enabled false` → marker still line 1 and `get` prints `false`, built server on a commented workspace exits 0 with 0 B stderr; positive control: alias removed → the build fails naming `./impl/format`, `./impl/edit`, `./impl/scanner`, `./impl/parser`. Residue resumed: `cpm config init` names a command that does not exist (F-T5-18) · worker sonnet, surface Agent                                                                                               | `cli/esbuild.config.mjs`, `server/esbuild.config.mjs`, the bundle-budget test if one pins the constant                                                                                                                                                                                                      | Both bundles crash at load (F-T5-17, reproduced by the planner on `server/dist/index.js`): esbuild's node default resolves `jsonc-parser` through `main` to its UMD build, whose factory calls `require` through a parameter, so `./impl/format` and its siblings are never bundled. Resolve the package's ESM build in both configs with the narrowest setting that works, and keep the shipped-bundle budget (512,000 B) untouched; the unminified dev budget (640,000 B) rises only by what the measured artifact needs (R75). Leave something that fails when the NEXT dependency does this: a post-build check in the shared build path that refuses a bundle still carrying a relative `require` of a file that was not emitted, with a positive control | 7.1           | bundle-load    |
| 7.11 | ✓ 2026-09-19 · `server/src/index.ts` (`--init` handling extracted to `handleInitFlag`, which calls `initConfig` after a successful `initWorkspace` and prints what it reports; both `--help` lines), `init-workspace.e2e.test.ts` (+2: the created `config.jsonc` equals the template byte for byte; a directory already holding `config.json` is left identical and gains no `config.jsonc`), `README.md:165`, `CHANGELOG.md` (one Fixed bullet). Worker: e2e 4/4 on a build; typecheck 0; tests-ratchet 359; eslint on `index.ts` identical to HEAD's (the extraction kept cognitive complexity at baseline, DEV-T5-3); `validate:arch` 0; `validate:readme`, `validate:format`, `validate:contributing` exit 0; drive: first `--init` exits 0 and prints `Created config.jsonc at …`, a second run exits 1 on the existing workspace without touching the config — the same gating as `cpm init`; mutation (call removed) fails the template-equality test · worker sonnet, surface Agent                                                                                                                                                                                                                                                                                                                                                                                                                                        | `server/src/index.ts`, its unit/integration test, `README.md`, `CHANGELOG.md`                                                                                                                                                                                                                               | `README.md:165` promises that `--init=<path>` scaffolds "three example prompts plus `config.json`"; measured, the flag calls `initWorkspace` alone and writes no config (F-T5-22) — only `cpm init` does. Make the promise true (R76): the `--init` branch calls `initConfig(targetPath)` after `initWorkspace`, reports the file it created or found, and its two `--help` lines say so; the README line names `config.jsonc`; one Fixed changelog bullet                                                                                                                                                                                                                                                                                                     | 7.4           | server-init    |
| 7.12 | ✗ KILLED (2026-09-19 · `cpm init -w <path>` ignores `-w` and reports on the cwd's workspace (F-T5-20): an argument-parsing defect in `cpm init` that predates this plan, touches no config behaviour, and sits in the first-run surface the tutorial plan owns — the live relay failed (that session had exited), so the note is left in that plan's project memory · revives if `cpm init` gains a config-related flag in this plan)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `cli/src/commands/init.ts`                                                                                                                                                                                                                                                                                  | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | —             | killed         |
| 7.13 | ✓ 2026-09-19 · boundary fallout from the first `validate:all` on the merged snapshot (63/67): knip ratchet exports 470 → 468 (lowered, no category rose), `unreached-methods` baseline loses `SafeConfigWriter.getConfigPath` (now called; nothing added), `docs/reference/module-catalog.md` regenerated (`hot-reload` gains a `shared-utils` edge from row 7.6's import), and `system-control-router-persist.test.ts`'s header names its stand-in writer and points at `safe-config-writer.test.ts` instead of claiming a type-only import as its subject (commit 675ba641). Planner: second `validate:all` 67/67 · worker sonnet, surface Agent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `server/.knip-ratchet-baseline.json`, `server/.unreached-methods-baseline.json`, `docs/reference/module-catalog.md`, `server/tests/unit/mcp-tools/system-control/system-control-router-persist.test.ts`                                                                                                     | Four gates only the whole suite runs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 7.6           | boundary-gates |

**Dispatch** — tier · effort · failure shape:

| Row  | Tier   | Effort | Failure shape                                                                                                                                                                                        | branch_mode | Surface |
| ---- | ------ | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------- |
| 7.1  | sonnet | high   | wrong output — the contract is ruled (R66, R67); the risk is `jsonc-parser`'s fault-tolerant `parse` returning a partial value when the errors array goes unread                                     | shared-tree | Agent   |
| 7.3  | opus   | high   | wrong approach — example values and layout are derived from a schema the brief does not quote, and "uncomment any leaf and it validates" constrains the comment layout                               | shared-tree | Agent   |
| 7.7  | sonnet | high   | wrong output — lookup order is ruled (R72); the risk is a comment stripper that eats `//` inside a string such as a `$schema` URL                                                                    | shared-tree | Agent   |
| 7.2  | sonnet | high   | wrong output — resolution order and the refusal are ruled (R68); the risk is the format check running after the transport refusal or translation, or a refusal that names one path                   | shared-tree | Agent   |
| 7.4  | opus   | high   | wrong approach — four write paths (set, persist, init, reset) over two formats with atomicity and backups; which keeps a whole-document rewrite is a design call the row bounds but does not spell   | shared-tree | Agent   |
| 7.5  | sonnet | medium | wrong output — strings and help text enumerated by file; the risk is the separate `cli/` suite and the bundle, which server gates cannot see                                                         | shared-tree | Agent   |
| 7.6  | sonnet | medium | wrong output — four named sites; the risk is a conformance expectation or snapshot still pinning the literal `config.json`                                                                           | shared-tree | Agent   |
| 7.8  | sonnet | medium | wrong output — entries enumerated in the row; the risk is session vocabulary in release notes                                                                                                        | shared-tree | Agent   |
| 7.9  | sonnet | high   | wrong output — the fix is one call; the risk is a guard whose predicate misses the next reader, so the row names the predicate and requires a positive control                                       | shared-tree | Agent   |
| 7.10 | sonnet | high   | wrong output — cause and fix are measured by 7.5's worker and reproduced by the planner; the risk is a fix that loads but changes resolution for every other dependency, or a guard that cannot fire | shared-tree | Agent   |
| 7.11 | sonnet | medium | wrong output — one call added beside a sibling that already makes it (`cpm init`); the risk is the README charter gate and an init that overwrites an existing config                                | shared-tree | Agent   |

## Rulings (defaulted — flip any of these and I'll re-cut the table)

| #   | Ruling                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | Warn-then-serve, not fail-fast (operator ruling, 2026-09-11)                                                                                                                                                                                                                                                                                                                                   |
| R2  | Per-section `additionalProperties:false` under draft-07; NO dialect bump. `unevaluatedProperties` is a proven no-op here and 2020-12 buys nothing this schema uses                                                                                                                                                                                                                             |
| R3  | `logging.level` ships as `info` (row 0.1). Devs use `LOG_LEVEL=debug` — the env var is already read at `index.ts:358`, so no new override mechanism is needed                                                                                                                                                                                                                                  |
| R4  | The schema warning surfaces in BOTH stderr and `system_control config validate` — a startup warning scrolls away under STDIO                                                                                                                                                                                                                                                                   |
| R5  | `versioning` canonical spelling is camelCase; snake_case stays accepted via the existing shim                                                                                                                                                                                                                                                                                                  |
| R6  | T4 SSOT is the TS `ConfigFile` type; `config.schema.json` becomes generated from it via `ts-json-schema-generator` (operator ruling, 2026-09-11). Chosen over schema-first because T4.2's `ConfigFile → Config` normalizer is the change that fixes the root cause, and having both its input and output types compiler-checked is what makes that mapping verifiable rather than another cast |

## Open Questions

| #     | Question                                                                                                                                                                                                                | Default if unanswered                                                                                                                                                                                                                                                                                                                           |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OQ-1  | ~~Generation direction for T4~~                                                                                                                                                                                         | ✓ RESOLVED 2026-09-11 → R6: TS `ConfigFile` → generated schema                                                                                                                                                                                                                                                                                  |
| OQ-2  | ~~`server.version` (S5)~~                                                                                                                                                                                               | ✓ RULED 2026-09-11 to its default → notes R7: read from `package.json`, drop from the config surface. Implemented by the CLI reconciliation tier; row 0.2 declares it so the two surfaces stay consistent meanwhile                                                                                                                             |
| OQ-3  | ~~The 2 zero-reader CLI keys (S6)~~                                                                                                                                                                                     | ✓ RULED 2026-09-11 to its default → notes R8: delete, as row 2.3. Same interim reasoning as OQ-2                                                                                                                                                                                                                                                |
| OQ-4  | Does T5 get scheduled against a specific major, or declared permanently out of scope?                                                                                                                                   | ✓ RULED 2026-09-15 → notes R31: every row in this plan, T5 included, lands before 5.0.0 releases                                                                                                                                                                                                                                                |
| OQ-5  | Row 2.5: declare the config inputs as flat top-level `system_control` params, or the nested `config` object the handler already reads?                                                                                  | ✓ RULED 2026-09-15 → notes R27: neither for writes. Read over MCP (a flat `key` so `get` works), write through `cpm`; `set` and `restore` leave the MCP handler. **Reads re-ruled 2026-09-15 when step 2 was cut → notes R37**: reads keep #299's nested `config` object, superseding R27's flat-`key` half; R27's writes-leave-MCP half stands |
| OQ-6  | Should the logger write `warn` and `error` to stderr under STDIO outside CI (row 1.8)?                                                                                                                                  | ✓ RULED 2026-09-15 → notes R28: yes for `warn` and `error`, after a clean start stops emitting warnings that are not about user input (F-T2-1, F-T2-2) and the `DEBUG:` lines go through the logger                                                                                                                                             |
| OQ-7  | Row 2.6: who chooses the transport — the `--transport` flag alone, `server.transport`, or root `transport`?                                                                                                             | ✓ RULED 2026-09-15 → notes R30: the flag owns transport; both config keys are removed in 5.0.0; a removed key asking for anything but `stdio` refuses startup, naming the flag                                                                                                                                                                  |
| OQ-8  | Rows 3.1–3.2: where do `$id` and a config's `$schema` point, so an editor validates a config outside the package? The current `$id` returns 404                                                                         | ✓ RULED 2026-09-15 → notes R29: jsDelivr pinned to the major, `https://cdn.jsdelivr.net/npm/claude-prompts@4/config.schema.json`, with a check that the URL's major equals `package.json`'s                                                                                                                                                     |
| OQ-9  | Row 5.4: where do config defaults come from? Shipped `config.json`, code `DEFAULT_CONFIG` and `cpm`'s template disagree (notes F-T2-4)                                                                                  | ✓ RULED 2026-09-15 → notes R32: code owns them; the user-facing file carries no copy                                                                                                                                                                                                                                                            |
| OQ-10 | How does a 4.x `config.json` meet 5.0.0's removed and reshaped keys?                                                                                                                                                    | ✓ RULED 2026-09-15 → notes R33: translated in memory on load, keyed on `version`                                                                                                                                                                                                                                                                |
| OQ-11 | R32's addition: the owner wants the user's config to show every option as commented-out examples, and JSON has no comments. Accept comments (JSONC), ship a separate example file, or carry the examples in the schema? | ✓ RULED 2026-09-15 → notes R34: `config.jsonc`, with every option a commented-out example generated from the schema                                                                                                                                                                                                                             |

## Completion Criteria

| Tier | Criterion                                                                              | Validation                                                                                                                                                                                                                                                                                                                                                          |
| ---- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T0   | ✓ PASSED 2026-09-11 — a nested typo fails CI; the shipped file passes                  | Self-test 8/8, falsified twice (strip one section → reported it; swap all 27 for `unevaluatedProperties` → reported all 27). `typecheck` 0 · `lint:ratchet` 0 · `typecheck:tests:ratchet` 0 · `validate:all` 58/58 · `test:all` exit 0                                                                                                                              |
| T1   | ✓ PASSED 2026-09-15 — a nested typo loads, serves, and emits exactly one named warning | Planner drive of a built server, before and after the `main` merge: typo config → exit 0, one stderr line naming `/gates` and `enabld`, stdout empty; clean config → none. `config-schema-warning.test.ts` pins the reload sequence with a positive control                                                                                                         |
| T1   | ✓ PASSED 2026-09-15 — `system_control config validate` reports schema errors           | Planner live drive over MCP against a build of 66b635ef, beside the same drive against `main`: typo config → `❌ config.json does not match its schema…` plus `/gates: must NOT have additional properties (enabld)` (main: the whole configuration, 2,392 characters); clean config → `✅ config.json matches its schema.`; `config` with no operation still lists |
| T2   | CLI key set ≡ schema leaf key set modulo a declared exception list                     | new agreement gate (2.4) + its self-test                                                                                                                                                                                                                                                                                                                            |
| T3   | `$schema` resolves from a config outside the server root                               | load a config from `MCP_CONFIG_PATH` in a temp dir; observe validation still runs                                                                                                                                                                                                                                                                                   |
| T4   | `config.schema.json` regenerates byte-identical from `ConfigFile`                      | drift check, same shape as `validate:contracts`                                                                                                                                                                                                                                                                                                                     |
| all  | Repo gates                                                                             | `npm run typecheck && lint:ratchet && typecheck:tests:ratchet && test:all`, plus `validate:arch` for 4.1's new file                                                                                                                                                                                                                                                 |

## Sources & Inspiration

| Field                    | What                                                                                                                                                                                                                                                    |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Reference implementation | `tooling/contracts/*.json` → `src/mcp/contracts/schemas/_generated/` — this repo's existing answer to "who owns a schema". R6 rules the other way for config (TS-first): a generated contract JSON has no normalizer to type-check, whereas T4.2's does |
| Reference implementation | `warnAnalysisSectionDeprecated` (`infra/config/index.ts:815`) — the warn-once-at-load shape T1.1 copies verbatim                                                                                                                                        |
| Reference implementation | `validate:contracts` / `validate:domain-ownership` — the two-direction drift-gate shape T2.4 and T4.3 copy                                                                                                                                              |
| Prior decision           | `adoptInertSpellings` header (`infra/config/index.ts:48-65`) — records the same defect class across ten subsystems and its retirement condition                                                                                                         |
| Constraint               | CLAUDE.md §Public API Contract — `config.json` is in-contract, which is what puts T5 behind a major bump                                                                                                                                                |
| Constraint               | CLAUDE.md §Validation Gates — a new step goes into `validate:all` first, which CI runs whole                                                                                                                                                            |
| Open unknowns            | OQ-1..OQ-4 above                                                                                                                                                                                                                                        |
