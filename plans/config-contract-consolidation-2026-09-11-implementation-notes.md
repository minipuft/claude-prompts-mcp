---
title: "Config Contract Consolidation — One Owner For The Shape Of `config.json` — Implementation Notes"
plan: config-contract-consolidation-2026-09-11.md
date: 2026-09-11
status: active
tags: []
---

# Implementation Notes

Deviations, discovered constraints, unknowns, and re-measurements found while
executing the plan. Conservative option taken, logged, work continued.

## Rulings

| #   | Date       | Ruling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| R7  | 2026-09-11 | OQ-2 RULED to its default: `server.version` is read from `package.json` and dropped from the config surface. Implemented in T2, not T0 — T0 declares it so the schema and the CLI stay consistent in the interim                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| R8  | 2026-09-11 | OQ-3 RULED to its default: both zero-reader CLI keys are deleted, in T2.3. Same interim reasoning as R7                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| R9  | 2026-09-14 | Load-time validation reads the package's OWN `config.schema.json`, shipped in `files` and handed to the validator by path. The config's `$schema` stays an editor hint and is never read by the server. Measured: `npm pack --dry-run` lists no `config.schema.json`; `$schema` resolves beside the user's file, so ENOENT under `MCP_CONFIG_PATH`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| R10 | 2026-09-14 | An unreadable schema is reported once as unavailable, never as an invalid config. Measured: the validator's catch branch returns `valid:false` for a missing file, which under warn-then-serve would tell every install its config is wrong                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| R11 | 2026-09-14 | Validation reads the raw parsed file, before `adoptInertSpellings`. Adoption rewrites `maxVersions` to `max_versions`, so validating after it flags the shipped config. A warning on a legacy inert spelling is correct: that key does nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| R12 | 2026-09-14 | `system_control config validate` with no key reports the load-time result; with a key it is unchanged. If the tool contract requires `key`, the result goes on `list` instead. No contract change either way                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| R13 | 2026-09-14 | The schema declares the CLI's `versioning.max_versions` / `auto_version` as deprecated inside T1, so T1 does not warn on configs `cpm` itself wrote before row 2.2 flips the canonical spelling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| R14 | 2026-09-14 | Supersedes R12's surface. 1.3 reports the stored schema result when `action:config` arrives with top-level `operation:"validate"` and no `config` object — the only config input an MCP client can actually send. R12's `list` fallback is dropped: `list` is where every broken call already lands, so a result appended there would be indistinguishable from the failure it reports                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| R15 | 2026-09-14 | Row 1.6's lint concern is not this initiative's work. The validator's eslint problems predate the row, sit inside the `lint:ratchet` baseline, and the row reduced them (13 → 12). No row opened; revives if a later row in this plan raises the file's count                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R16 | 2026-09-14 | Refines 1.1's "warn once per process". A schema warning repeats whenever the set of errors differs from the last set warned, and stays quiet when hot reload re-reads an unchanged file. Once-per-process fits a deprecation notice, whose cause cannot change without a restart; applied to validation it would stay silent about every typo after the first, which is the silent-default failure T0 exists to end                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| R17 | 2026-09-14 | A schema warning names the offending key. AJV reports an undeclared key as `must NOT have additional properties` with the key only in `params.additionalProperty`, so today's formatted error is `/gates: must NOT have additional properties` — the section, never `enabld`. `formatAjvErrors` appends the key; the T0 self-test asserts on the path prefix and is unaffected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| R18 | 2026-09-14 | Row 1.3 adds `getSchemaValidation()` to `ConfigManager` as a required member, not an optional one. Measured blast radius: `ConfigLoader` is the only `implements`, exactly one test object is annotated `: ConfigManager` (`prompt-engine-validation.test.ts:25`), and the other 22 sites cast. An optional member would push an absence check into the handler — the `?.()`-under-success-text shape this repo has already fixed five times — to spare editing one mock                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| R19 | 2026-09-14 | Row 0.5 closes with a plain zero-error `typecheck:scripts` over the `.ts` scripts, added to `validate:all`, after rows 0.6 and 0.7 fix the 15 existing errors. Not a ratchet: `typecheck-tests-ratchet.js` hardcodes `PROJECT = 'tsconfig.test.json'` (line 26) and `startsWith('tests/')` (line 134), and generalizing it to hold a baseline of 15 buys a file that can rot in exchange for four small fixes. `checkJs` over the `.js`/`.mjs` scripts is killed for this initiative (row 0.8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| R20 | 2026-09-14 | Row 1.1's warning goes to stderr through `console.warn`, not the module logger, and the file log does not get a copy. Measured twice: under STDIO the logger prints nothing to the console unless `CI` or `NODE_ENV=test` is set (worker probe against an HTTP-transport control; planner re-drive with both unset). `loadConfig`'s catch path already writes with `console.error`, so this matches the function's own precedent. Mirroring into the logger would print twice under CI and jest. Revives if OQ-6 rules the logger should print warnings under STDIO — then the direct write retires                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| R21 | 2026-09-14 | R16 repeats per SET, and that is intended: when a second mistake appears, the whole current set warns again, including the one already reported. Each burst is then a complete picture of what is wrong now, rather than a diff the operator must assemble from scrollback                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| R22 | 2026-09-14 | Row 1.3 takes four files, one over the default bound. The interface member it adds names `ConfigSchemaValidationResult`, which is defined in `infra/`, and `shared/` may not import `infra/` (CLAUDE.md, `validate:arch`). The type's definition moves into `src/shared/types/config-manager.ts`, and the validator imports it from there directly rather than through a barrel. A separate row would only sequence a type move ahead of its single consumer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| R23 | 2026-09-15 | `main` gained 8 commits (#276–#283) since this branch's base `81748d6a`, including two breaking runtime changes to how a server treats the settings it was given. Three files changed on both sides — `server/src/infra/config/index.ts` (main: the fallback's `console.info` became `console.error`), `server/src/runtime/context.ts` (main: `assertUsablePathSettings()` runs before the config path is read), `server/scripts/run-validation-suite.js` (no SUITE entry changed) — and `git merge-tree` reports no textual conflict. #283 changed tool-result text at the registration layer and left the config handler alone, so row 1.3 needs no rework. Integration is a MERGE of `origin/main`, not a rebase: nothing is pushed yet, but receipts in these notes cite commit SHAs, and PRs squash. It runs only once the current batch is committed, because workers hold uncommitted edits and HEAD must not move under them. A clean textual merge is not evidence the behavior composes, so the merged tree gets the config unit tests and the typo/clean startup drive again. Row 1.7 is cut on the merged tree, because `CHANGELOG.md` changed on `main`                                                                                                                                                                 |
| R24 | 2026-09-15 | Rows 0.6, 0.7 and 1.4 edit scripts that `validate:*` steps run, so accepting them runs `validate:suite-membership` and `validate:knip-ratchet` as well as each row's own check: a script edit can change the substrate the suite declares for it, or add an import knip counts. Run once over the whole batch after the last worker finishes, because those gates read files other live workers are still editing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| R25 | 2026-09-15 | **Owner ruling: publish `push+merge`.** Once the PR boundary is green, the planner pushes `feat/config-contract-consolidation`, opens the PR from the repository template, and squash-merges when CI is green, with the repository's own squash body and no override. Recorded as the plan's `publish:` field. Per-act stops still apply whatever the ruling: a force push, and any diff reaching files outside this slice                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| R26 | 2026-09-15 | **Owner ruling: a public tracking Issue.** Created as https://github.com/minipuft/claude-prompts-mcp/issues/287 (label `enhancement`), stating the four sources of truth, their measured symptoms, what the first PR delivers, what stays open, and OQ-5/6/7 in plain terms with their defaults. The plan stays `active`, so the PR says "Part of #287" rather than a closing keyword                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| R27 | 2026-09-15 | OQ-5 RULED by the owner: `system_control` config is read over MCP and written through `cpm`. Row 2.5 declares a flat `key` parameter so `get` works, removes `set` and `restore` from the MCP handler, and stops the tool description saying "modify". Measured: `set` writes `config.json` atomically with a backup, over keys that include gate and phase-guard switches (`src/cli-shared/config-operations.ts`), and `restore` copies whatever file `backup_path` names over it. Over MCP either lets a model rewrite server policy. Neither is reachable today, so removing them narrows nothing a client can call                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| R28 | 2026-09-15 | OQ-6 RULED by the owner: under STDIO the logger writes `warn` and `error` to stderr outside CI, and `info`/`debug` stay file-only — once a clean start is clean. Measured (F-T2-1): outside CI a clean start writes 13 `DEBUG:` lines straight to stderr and no warnings; with the console on it writes 4 `[WARN]` lines, none about user input. Row 1.8 depends on one row per warning (fix it or re-level it), a row routing the `DEBUG:` lines through the logger, and F-T2-2's missing tool descriptions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| R29 | 2026-09-15 | New OQ-8 RULED by the owner: `config.schema.json`'s `$id` becomes `https://cdn.jsdelivr.net/npm/claude-prompts@4/config.schema.json`. Every npm release already publishes the file there, and the major-pinned path moves only within a major, which matches `config.json` being in the Public API Contract. A check asserts the URL's major equals `package.json`'s, so a major bump cannot leave it behind. Defaulted, flips if the owner objects: the shipped `server/config.json` keeps its relative `$schema`, since it sits beside the schema in both the repo and the package, and row 3.3's page tells users to use the URL in their own configs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| R30 | 2026-09-15 | OQ-7 RULED by the owner: the `--transport` flag owns transport. Root `transport` and `server.transport` leave the schema, the config types, the shipped `config.json`, `cpm` and `CONFIG_RESTART_REQUIRED_KEYS` in 5.0.0 — removed, not deprecated, because the release in flight is already a major (owner: "the release is already a major we can push this now"). A config that still sets either key to anything but `stdio` refuses startup with a message naming `--transport=streamable-http`, the posture the path settings took on 2026-09-14: serving STDIO to a config that asked for HTTP boots on something nobody asked for. A leftover `stdio` value draws only the schema warning. Launch facts: F-T2-3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| R31 | 2026-09-15 | OQ-4 RULED by the owner: "Everything in our plan should be implemented" before 5.0.0 releases — T2, T3, T4, all four T5 rows, and the rows R28 makes 1.8 depend on. Measured: #232 is an open "release 5.0.0", the manifest is at 4.0.1, and `main` carries 8 breaking commits since `v4.0.1`. Consequences taken as defaults: #232 stays unmerged until these land; R29's URL pins `@5`, the major it ships in; a PR that removes or reshapes a config key carries `!` and a BREAKING CHANGE note; 5.3 follows 2.2, because `adoptInertSpellings` (since `v3.2.0`) is what maps the shipped file's camelCase `versioning.maxVersions`/`autoVersion` to the snake_case the runtime reads                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| R32 | 2026-09-15 | OQ-9 (row 5.4) RULED by the owner: code owns config defaults. One default set, aligned first to today's shipped values (F-T2-4) so a user with no config sees no change, carried by T4's `ConfigFile` into the generated schema's `default`s. The user-facing file holds overrides, not a copy of the defaults. Owner addition: "config.json could just show examples commented out to guide the user on what's possible, easier at a glance" — JSON has no comments, so the form is OQ-11                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| R33 | 2026-09-15 | OQ-10 RULED by the owner, against the planner's recommendation of a `cpm config migrate` command: a 4.x `config.json` is translated in memory on load, with no user action. Consequences taken as defaults, each flips if the owner objects. (1) Row 5.3 replaces `adoptInertSpellings` with a 4→5 translation keyed on the file's `version` (row 4.5): a file without `version: 5` is translated and a version-5 file never is, which gives the translation the retirement condition the shim lacked — removal in 6.0.0. (2) Load order becomes translate → schema check → defaults, so a translated 4.x file draws no warning for keys the translation handled; row 1.4's mutation that pins "check before adoption" is rewritten against that order. (3) The translation reports once per process which keys it translated. (4) Removed keys are not translated: a `transport` asking for anything but `stdio` still refuses per R30, and `analysis` is dropped with the notice                                                                                                                                                                                                                                                                                                                                                   |
| R34 | 2026-09-15 | OQ-11 RULED by the owner: the user's config becomes `config.jsonc`. The server, `cpm` and the Python hooks read it with comments allowed; `config.json` is still read strictly, so 4.x files keep working through R33's translation. `cpm config init` writes a `config.jsonc` whose every option is a commented-out example generated from the schema, so the examples cannot drift, and `cpm config set` edits it in place without losing comments, adding `jsonc-parser`. Defaults, each flips if the owner objects: a workspace holding both files refuses startup and names both; the readers that change are F-T2-5's six TypeScript sites and two Python hooks. Measured: VS Code flags every comment in a `.json` file ("Comments are not permitted in JSON.") unless the user maps the file to JSONC, and accepts them in `.jsonc` with no setup                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| R35 | 2026-09-15 | Owner ruling, confirming the security reason behind R27: `cpm config` and `system_control config` share one implementation and the key list generated from the schema (row 4.4). Both offer the reads — list, get, keys and validate — and `get` returns a key's effective value with its source: the user's file, a default, or an environment override such as `PORT` or `LOG_LEVEL`. Writes stay in `cpm`: set, reset, and restore, which moves over from the MCP handler so `cpm`'s automatic backups can be restored. This removes F-T2-8's second key list (`mcp/tools/config-utils.ts`) and its write path. Follow-ups answered the same day: the four false startup warnings are fixed at their causes in delivery step 1 (F-T2-6, F-T2-7); with no `--transport` flag the server runs STDIO, whatever a config says; R29's jsDelivr URL stands (F-T2-9). The owner confirmed the ruling summary and the delivery order, which closes the interview                                                                                                                                                                                                                                                                                                                                                                          |
| R36 | 2026-09-15 | Planner ruling for row 1.8, completing R28: under STDIO outside CI, `error` and `warn` go to stderr and `info` and `debug` go there only with `--verbose` or `--debug-startup`; CI and non-STDIO behavior is unchanged, and nothing goes to stdout. Measured basis: `EnhancedLogger.logToConsole` drops every level under STDIO outside CI, so without this `--verbose` would show no logger output under STDIO at all; `--quiet` never reaches the logger (F-T2-11). With the table in the brief, 1.8 derives nothing, so it was re-tiered from opus to sonnet at high effort                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| R37 | 2026-09-15 | OQ-5's READS half re-asked when step 2 was cut (F-T2-15 falsified its premise) and RULED by the owner: `system_control` config reads keep #299's nested `config` object (`{ operation, key }`), superseding R27's flat-`key` half. Both shapes are unreleased, so neither breaks a caller; the object is the one `main` already declares, so step 2 leaves the read parameters alone and confines its contract change to removing the write operations and their `backup_path` / `confirm` fields. R27's and R35's writes-are-CLI-only half stands unchanged, on the owner's security reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| R38 | 2026-09-15 | Planner ruling for row 2.7: the read primitive lives on `ConfigManager`, and it derives both halves from authorities this initiative already owns — valid keys from the packaged `config.schema.json` (T0 ships it), provenance from the parsed file kept beside the merged config. Measured basis: `ConfigLoader` holds one `config` field (`infra/config/index.ts:263`), `loadConfig` assigns the parsed file at `:312` and `validateAndSetDefaults()` overwrites it in place at `:315`, so the raw file is discarded today and nothing records which key came from where. `get` must also report the two env overrides — `PORT` (`:619`) and `LOG_LEVEL` (`:391`) — because the handler dot-walks `getConfig()` and so currently answers `server.port` with a value the running server may not be using, which is the false-confidence shape this initiative exists to end                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R39 | 2026-09-15 | Planner ruling: step 2 does NOT consolidate the two config key lists. `mcp/tools/config-utils.ts`'s 19 keys are a strict subset of `cli-shared/config-input-validator.ts`'s 59 (measured: zero keys unique to the MCP copy), so the MCP copy is a stale snapshot, not a second design — but row 4.4 generates the one list from the `ConfigFile` type (R35), and hand-merging now is the rework that killed rows 2.1–2.4. Row 2.9 therefore deletes only what removing the MCP write path makes dead, and row 2.7 answers "is this a valid key" from the schema instead of from either list                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| R40 | 2026-09-15 | Planner ruling from row 2.7's second round: for a schema-declared key that neither the file nor the loaded config holds, step 2 reports that state explicitly rather than inventing a value. Two rejected alternatives, both of which would make a fourth disagreeing authority out of the three F-T2-4 already measured: reading the JSON Schema's own `default` keyword (the schema, code `DEFAULT_CONFIG` and `cpm`'s template already disagree, so a schema default can differ from what the process uses), and hand-writing a section→getter map (the getters are runtime-shaped, not file-shaped — #306's `getGatesConfig` renames `directory` to `definitionsDirectory` — so the map would be exactly the hand-kept correspondence row 4.4 generates, and R39 declined that once already). Measured basis: `validateAndSetDefaults` writes back 6 sections; 17 getters default at read time across 19 sites. Every key answering with the value the server would use is step 4's job, where one generated shape owns file names, runtime names and defaults together                                                                                                                                                                                                                                                          |
| R41 | 2026-09-15 | **Owner ruling: `get` over MCP is HELD until step 4.** Step 2 serves `list`, `keys` and `validate`, and removes every write path; `get` leaves the MCP surface with them and returns in step 4, once the generated shape can resolve nearly every key. Measured basis for the question (row 2.13's gate, a config file setting only `gates.enabled`, with `PORT` and `LOG_LEVEL` set): of 78 schema-declared keys just 31 resolve — 1 `file`, 28 `default`, 2 `environment` — and 47 answer `deferred`, so a shipped `get` would decline to answer 60% of the surface. The planner's interim default while the question was unanswered was the opposite (ship it, labelled honestly); the owner ruled the other way before any row acted on it, and this row supersedes that default rather than recording it as a decision. `list` still shows the loaded values, so nothing an operator can see today is lost. Rows 2.7 and 2.13 stay: their primitive is what step 4 consumes, and until then it has no production caller — recorded as F-T2-28 so it is not read as live wiring                                                                                                                                                                                                                                                  |     |
| R42 | 2026-09-15 | **Owner ruling: `persist: true` stays, and the boundary is narrower than step 2 claimed.** `system_control(action: "gates"\|"framework", operation: "enable"\|"disable", persist: true)` writes `config.json` over MCP through `persistGateConfig`/`persistFrameworkConfig` (`system-control-router.ts:229-279`, called from `gate-action-handler.ts:86,125` and `framework-action-handler.ts:290,331`), and row 2.9's worker refused to delete `SafeConfigWriter` on finding it. The owner's reading, accepted: this is not the risk R27/R35 were drawn against. The distinction is WHO NAMES THE KEY — a caller naming any key and value, or restoring a backup, is closed; an action asking the server to persist its own one setting, under a key the server chooses and validates, is not. So "no arbitrary config writes over MCP" is true and "no config writes over MCP" is false, and every shipped message, commit body and PR sentence must carry the first (row 2.17). The owner also asked to extend the safe shape to other operations that do not need closing — surveyed as row 2.16, implemented after this slice                                                                                                                                                                                                   |
| R43 | 2026-09-16 | Planner ruling for the step-3 cut: between step 3 and step 4, `main` carries the 5.0 file shape with no 4→5 translation. A 4.x file draws the R1 schema warning for its flat injection keys, `advanced.sessions` and `transport`, and runs code defaults for them. Accepted because R31 holds #232 until step 6, so no release ships the interim; flips if a release is cut between the two steps                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| R44 | 2026-09-16 | Planner ruling for row 4.4: the generated CLI key list is a build-time leaf table (`src/cli-shared/_generated/config-keys.ts`, on the `_generated/` contracts precedent), not runtime JSON Schema validation. Measured: the standalone CLI (`cli/package.json`) carries no `ajv` or `zod`, and bundling `ajv` into `cpm.js` to validate one key at a time buys nothing the table does not. Per-key `type`, `enum`, `minimum`, `maximum` come from the schema; `restartRequired` stays a hand list because the schema has no such notion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| R45 | 2026-09-16 | Planner ruling for row 4.1: `ConfigFile` carries `version: 5` as a required literal (R33's key) and omits the `analysis` section along with the R30, R7 and R8 keys. The loader's `analysis` deprecation shim (`warnAnalysisSectionDeprecated`) and `adoptInertSpellings` stay until step 4's row 5.3 replaces them with the version-keyed translation; a file that still carries `analysis` draws the schema warning and the existing deprecation notice, and both are correct                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| R46 | 2026-09-16 | Planner ruling for row 4.5 (R32's consequence): once `DEFAULT_CONFIG` equals the shipped values (row 4.6), the packaged `server/config.json` holds overrides only, and there are none — it becomes `$schema` + `version: 5`. Kept as a file because `runtime/startup.ts:99` refuses a package root without one. Verified by an effective-config drive: a built server started on the old file and one on the new file must answer `system_control config list` identically                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| R47 | 2026-09-16 | Planner ruling on 4.1's concern 1: `frameworks.injection.gateGuidance.enabled` is dropped from `ConfigFile`. The runtime's `FrameworkInjectionConfig.gateGuidance` (`core-config.ts:128`) has no `enabled` and nothing reads one, so the brief's rule 2 named a key with zero readers; a frequency of 0 is how gate guidance is off. Row 4.3 removes it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| R48 | 2026-09-16 | Planner ruling on 4.1's concern 2: `hooks.expandedOutput`'s `@default true` stands, and the Python reader's fallback moves to match it. The shipped file ships `true` and R32 says a user with no config sees no change; the only reader is `hooks/lib/config_loader.py:60`, which falls back to `False`, so once row 4.5 shrinks the shipped file the hook would silently flip. Row 4.6 aligns the reader and its test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| R49 | 2026-09-16 | Planner ruling on 4.1's description-drift finding: `IdentityLaunchDefaults` stays reused, and row 4.3 adds the six current schema descriptions as JSDoc on its fields in `core-config.ts`, so the regenerated schema loses no operator text. Two of six leaves would otherwise regenerate with no description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R50 | 2026-09-16 | Planner ruling on 4.3's concern 4: `integer` is recovered, not conceded. `ts-json-schema-generator` 2.9.0 emits `number` for every TypeScript number (`NumberTypeFormatter`, verified by the worker) and honours no tag for it, so row 4.9 adds a post-pass to `generate-config-schema.ts` driven by a tag the library passes THROUGH (`extraTags`), rewriting `{type:'number', <tag>:'integer'}` to `{type:'integer'}` and deleting the carrier key. A tag the library ignores is forbidden (the generator header says why). The 13 leaves are 4.1's list; a 14th `number` that was never `integer` stays `number`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| R51 | 2026-09-16 | Planner ruling on 4.2's question: `frameworks.enabled: false` keeps suppressing system-prompt injection. `normalizeInjection` reads `injection.systemPrompt.enabled ?? frameworks.enabled ?? true` (`index.ts:344`): the dedicated 5.0 leaf wins when set, and `frameworks.enabled` stays the master switch beneath it, because removing the fallback would silently re-enable injection for every file written as `frameworks: {enabled: false}`. Not a leftover conflation: one is the section switch, the other the channel switch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| R52 | 2026-09-16 | Planner ruling on 4.2's finding: config WRITES operate on the file shape. `createSafeConfigWriter` (`mcp/tools/config-utils.ts:266,280`) writes `getConfig()` — the resolved runtime object — back to disk, which after 4.2 would persist nested `frameworks.injection`, root `chainSessions` and drop `hooks` from a file the operator wrote flat. Row 4.4 rewrites the writer to read the on-disk file, set one dotted key validated by the generated leaf table, and write the file shape; R42's `persist: true` paths go through it unchanged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| R53 | 2026-09-16 | Planner ruling on 4.4's finding: the numeric bounds the deleted `validateConfigInput` switch enforced were never in the schema, so the generated table lost them (`server.port 70000` accepted). They return as `@minimum`/`@maximum` JSDoc on `ConfigFile` in row 4.5, so the schema, the table, the CLI and the load-time warning all carry one range. Where the switch and the schema disagreed on a lower bound, the schema's stands; the switch's upper bounds are adopted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| R54 | 2026-09-16 | Planner ruling on 4.4's concern: `config-utils.ts` keeps no re-export of `CONFIG_VALID_KEYS`, `CONFIG_RESTART_REQUIRED_KEYS` or `validateConfigInput` — a second import path is what `rg` for the canonical one misses (CLAUDE.md §Module organization); the tests that read them through the MCP module import `cli-shared` directly (row 4.5). Whole-document validation in the writer stays on the generated table rather than AJV: `mcp-no-infra-static` keeps `config-schema-validator.ts` out of `mcp/tools`, the table checks every leaf and flags unknown keys, and a `getSchemaPath()` on `ConfigManager` is not this step's change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| R55 | 2026-09-16 | Planner ruling on 4.7's deviations: the transport refusal lives in `ConfigLoader.loadConfig()` rather than beside `assertUsablePathSettings` in `createRuntimeFoundation` — `loadConfig` is called from there at the same point (before the logs `mkdir` and the watcher, on both transports), the error surfaces through the `PathSettingError` handling in `application.ts`/`index.ts` (print once, exit 1), and hot reload already isolates a thrown loader error. The argv scan inside `getTransportMode()` is NOT accepted as final: it duplicates `TransportRouter`'s parse and inherits its `=`-only defect (row 4.12)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R56 | 2026-09-16 | Planner ruling for the step-4 cut: the branch `feat/config-4x-translation` is cut from `main` `914b068c` in the MAIN checkout, not a worktree. Measured: `git status` clean, no live worker, no other session in this checkout; F-T4-36 makes every worktree PR cost a detach of the main checkout, and hooks fire here without a bootstrap (`core.hooksPath` = `.husky/_`, relative). Flips the moment a second session appears in this checkout — this session then moves to a worktree per CLAUDE.md §Concurrent sessions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| R57 | 2026-09-16 | Planner ruling closing R40's deferred half (step 4 is where it said the answer lives): `normalizeConfigFile` resolves EVERY section, `Config`'s section members become required, `DEFAULT_CONFIG` is the empty file's resolution, getters keep name mapping and env overrides only, and `'deferred'` leaves `ConfigValueSource`. The read-time literals fold into the loader's `DEFAULT_*` — including `prompts.registerWithMcp: true` (F-T4-15, the shipped 4.x value; `converter.ts` reads the resolved value), `pipeline-builder.ts:380`'s `phaseGuards` literal and the identity-resolver's `identity.mode` default; two defaults that disagree are a finding. A key whose effective value is legitimately absent (`ConfigFile` declares no `@default`: expected `telemetry.attributePolicy.allowlist`, `telemetry.exporterEndpoint`, `gates.directory`, `gates.evaluation.defaultModel`, `identity.launchDefaults.*`, `logging.directory`? — measured by row 6.2) reports `{ value: undefined, source: 'default' }`, and the enumeration test pins that set as a literal so a new undefined answer is a red test, not a label. Rejected: a section→getter map for `list` (R40 declined it; the getters are runtime-shaped)                                                                                                      |
| R58 | 2026-09-16 | Planner ruling filling R33's defaults for row 6.1: (1) the translation is a pure function in its own module `infra/config/config-file-translation.ts`, tested against the real pre-step-3 shipped file; (2) `version` absent ⇒ 4.x ⇒ translate and set `version: 5`; `version: 5` ⇒ untouched; any other value ⇒ untouched, the schema's `const 5` reports it; (3) order parse → `assertServerTransportIsStdio` on the raw record (R30 refusal precedes translation, so a 4.x file asking for HTTP still exits 1 naming the flag) → translate → schema check → `rawFileConfig` snapshot of the TRANSLATED record → `asConfigFile` → normalize; (4) `server.transport: "stdio"`, `server.version`, `gates.enforcePendingVerdict`, `resources.prompts.defaultRegistration` and `analysis` are dropped and named as dropped; the notice for `analysis` names `%judge` / `gates.evaluation.defaultMode` as before; (5) one notice per process, the R33(3) report, listing translated pairs and dropped keys and telling the user a `version: 5` file is silent; (6) `hooks.expandedOutput` is not translated — identical in both shapes and read by the Python hook straight off the file. `cpm` stays a file-shape tool in step 4 (`cpm config validate` on a 4.x file reports the 5.0 mismatch); its reader changes in step 5 with R34 |
| R59 | 2026-09-16 | Planner ruling for row 6.3, inside R37 and R41: `get` reaches the handler as `config: { operation: "get", key }` and renders `key`, the effective `value` (JSON) and `source` from `getConfigValueWithSource`. A key not in `listConfigKeys()` is refused naming `keys`; a request with no key is refused. The generic refusal keeps R42's wording for writes and loses its "`get` is held" sentence. Until 6.2 lands a never-set key answers `deferred` — accepted on the feature branch, and 6.2's handoff re-runs 6.3's tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| R60 | 2026-09-16 | Planner ruling on 6.2's concern 5: three schema keys never resolve because the runtime `Config` spells them differently (`versioning.maxVersions`→`max_versions`, `autoVersion`→`auto_version`, `chainSessions.timeoutMinutes`→`sessionTimeoutMinutes`). Fix by renaming the RUNTIME to the file's names (row 6.6), not by a file→runtime map — R40 declined the map, and one shape for both names is what R40 said step 4 delivers. Measured: 20 files name the versioning pair, 6 the session key, none in `hooks/` or `cli/src`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| R61 | 2026-09-16 | Planner ruling on 6.2's concerns 4 and 7: `gates.evaluation.strict`'s `@default true` is false — the effective default is `mode === 'judge'` (`judge-prompt-builder.ts:35`), so the tag becomes a description and the leaf stays legitimately undefined at the config surface. `gates.directory` is a dead key (zero readers; `getGatesDirectory()` resolves through `PathResolver`) that 6.2 made visible with an ignored value: it leaves `ConfigFile`, the schema and the key table, the translation drops it, and a `version: 5` file setting it draws the schema warning (row 6.7, breaking bullet). Both are R31-safe: the 5.0 schema is unreleased                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| R62 | 2026-09-16 | Planner ruling on 6.2's headline finding: with `identity` resolved at load, the pipeline's `!identityConfig?.mode` guard was dead and is gone, so launch defaults (the derived workspace id, the transport) now reach identity resolution on every launch instead of only under `--identity-mode`. Accepted as a FIX — the pipeline path now scopes like the framework and gate stores already do — with three consequences taken as defaults: a Fixed changelog line (row 6.8); a boundary drive proving an execution records the derived workspace id, with `--workspace-id` as the positive control; and the CLAUDE.md paragraph describing the `'default'`-bucket residual is now partly stale and stays so until the AGENTS.md eviction (F-T4-31) — logged as F-T4-53, not edited                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| R63 | 2026-09-16 | Planner ruling on 6.2's concern 6: `hooks.expandedOutput` answers `is not set` when the file omits it, because its default is the Python reader's (R48) and `Config` has no `hooks` member (DEV-T4-4). A TypeScript copy of that default would be the F-T2-4 shape. Stays in the pinned set with the owner named                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

## Deviations

| #        | Date       | What forced it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| -------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEV-T0-1 | 2026-09-11 | **Authored 26 subsections, measured 27.** The plan's count (S1 and row 0.3) was off by one. Conservative option: insert by textual match on `"type": "object",` and assert the count rather than trusting either number. Corrected in the plan in place                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEV-T0-2 | 2026-09-11 | Row 0.2 said "declare the 3 CLI-writable keys". Declared them **with `DEPRECATED —` descriptions naming their plan OQ**, rather than as plain keys. A bare declaration reads as endorsement, and R7/R8 already rule all three out; the description is what carries the ruling to anyone reading the schema in an editor                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEV-T0-3 | 2026-09-11 | `validate:suite-membership` failed after 0.4: the suite entry declared `reads: ['file']` and the new fixture code re-derives `[file, walk]`. Corrected the declaration. Not a plan error — the gate did exactly its job, and it is why the entry is trustworthy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DEV-T0-4 | 2026-09-11 | 4 new lint errors from 0.4 (3 `no-unused-vars` on type-position parameter names, 1 `preserve-caught-error`). All attributable to this tier, all fixed inside it: `_`-prefixed the type-position names and attached `{ cause: error }` to the rethrow                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DEV-T1-2 | 2026-09-15 | **Planner inventory error.** Row 1.9's brief listed the importers of `ConfigSchemaValidationResult` from `server/src` only. `server/scripts/validate-config-schema.ts:46` also imported it from the validator, so removing the re-export left that import unresolvable to a typechecker — harmless at runtime (`import type` is erased; the self-test passed 9/9) but a guaranteed error in row 0.5's concurrent `typecheck:scripts` gate. The 1.9 worker found it and flagged it under concerns. Conservative option taken: the live 1.9 worker was resumed with that one file added, and the live 0.5 worker was told the error is a known collision not to fix. Removing an export needs its consumer search run over the whole package — `src`, `scripts` and `tests` — before the row's file list is cut                                                                                                                                                                                                                                                                                                       |
| DEV-T1-3 | 2026-09-15 | **Planner verification gap.** R23's merged-tree re-verification ran typecheck, the tests-typecheck ratchet, the schema check, tests, a build and the startup drive — but not `lint:ratchet`. `main` had changed `no-console` from `warn` to `['error', { allow: ['warn', 'error'] }]`, which left row 1.1's `no-console` disable directive unused; the next batch's gate run caught it as `__unknown__` warnings 5 → 6, attributed by listing every null-rule message (five pre-existing, in files this branch never changed; one at `src/infra/config/index.ts:154`). Conservative option taken: the four clean rows were committed, since none of their diffs touch that line, and the fix became row 1.12. A merge's re-verification includes every ratchet, because a lint-config change arrives through the merge like any other                                                                                                                                                                                                                                                                               |
| DEV-T0-5 | 2026-09-15 | **A new zero-error gate is exposed to every merge that lands on `main` before it does.** PR #288 went BEHIND when `main` gained #286 (b3e8df31). Merging it in (1c51bfcf) was textually clean: the diff stayed at 21 in-slice files, and typecheck, `lint:ratchet` (3092 errors / 895 warnings) and `typecheck:tests:ratchet` (366) stayed green. `typecheck:scripts` exited 2 on two lines #286 added to `scripts/validate-prompts.ts` — TS2488, iterating a value cast to `unknown` and defaulted with `?? []`. `main`'s CI does not run that gate yet, so #286 merged green there; on this branch `validate:all` fails 1 of 59, which CI's Lint & Validate would fail too, so nothing was pushed. Conservative option taken: fixed on this branch as row 0.9 rather than in a separate PR to `main`, because the gate that finds it ships in this PR and cannot merge red. Skip, not throw, for a malformed shape: the same files pass through the loader schema, which declares all three fields as arrays. Until this PR merges, each merge from `main` re-runs `typecheck:scripts` before any push            |
| DEV-T2-1 | 2026-09-15 | Row 2.8's brief said to remove `confirm` from the contract and schema along with `backup_path`. The worker kept `confirm`: `rg` showed `analytics-action-handler.ts` (reset) and `maintenance-action-handler.ts` (restart) also read it, so deleting the shared top-level field would have broken two sibling actions. It removed config's read of it and its mention in the shared description instead. The brief was wrong; the worker measured before obeying                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DEV-T2-2 | 2026-09-15 | Row 2.8's brief told the refusal message to name `cpm config set` / `reset` / `restore`. The worker checked `cli/src/commands/config.ts` and found no `restore` subcommand — the CLI has `list`, `get`, `set`, `reset`, `validate`, `keys` — so the shipped message names `cpm config set` and `cpm config reset --force` only. A refusal that points at a command nobody can run is worse than no pointer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| DEV-T2-3 | 2026-09-15 | Row 2.8's worker narrowed the nested `config.operation` enum from `['get','set','list','validate']` to `['validate']` rather than leaving the retired values legal-but-dead, having measured zero consumers of the nested path for the other three. R37 is unaffected: the nested object survives, carrying the per-key candidate check, which is its one remaining reachable shape                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEV-T2-4 | 2026-09-15 | Row 2.9 was briefed to delete `SafeConfigWriter`, `createSafeConfigWriter` and `config-utils.ts`'s key list across six files. Its worker measured first, found `persistGateConfig`/`persistFrameworkConfig` calling the writer from live gate and framework handlers, and edited three files instead of six — leaving `config-utils.ts`, `system-control-router.ts` and `legacy-key-migration.test.ts` untouched. It also caught that the brief's own context was stale ("`config-action-handler.ts:3` (now gone)" — the file is live and still imports `validateConfigInput`), and that the test's key-list parity check is the thing R39 deliberately preserves, so repointing it would have deleted the guarantee the test exists for. The planner's brief caused this: the consumer sweep was run against the handler that owned the field's obvious use, not against the concrete context class's own methods, where the real callers were. Worker feedback worth promoting: when deleting a field from a shared context or base class, grep the concrete class's methods too, not only its handler subclasses |

## Unknowns / gaps found during execution

**GAP-1 — `scripts/` is not typechecked by anything.** `server/tsconfig.json` sets
`include: ["src/**/*"]`, so `npm run typecheck` cannot see `scripts/validate-config-schema.ts`,
which is where row 0.4's code lives. `tsconfig.test.json` adds `tests/**/*` and still not
`scripts/`. The tier's own verification was therefore vacuous as written: the project typecheck
runs green whether or not the new script compiles.

Substituted for this tier: a temporary tsconfig extending `tsconfig.json` with `rootDir: "."` and
`include: ["src/**/*", "scripts/validate-config-schema.ts"]`, run under the project's real compiler
options. Exit 0, no diagnostics. `tsx` transpiles without typechecking, so running the self-test
green does NOT substitute for this.

Blast radius is the whole `scripts/` directory — 58 validation scripts CI depends on, none of them
typechecked. Promoted to a plan row (T0.5) rather than left here, because prose in an execution
record reads as narrative and only a row reads as work.

## T1 re-measure (2026-09-14)

Execution context: the T0 work moved off the shared checkout into its own worktree
(`../claude-prompts-mcp-config`, branch `feat/config-contract-consolidation`, based on `main` at
`81748d6a`). The committed delta was checked byte-identical to a patch taken before the move, and the
main checkout now sits on `main`.

| #                  | Authored                                                                             | Measured                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Consequence                                                                                                                              |
| ------------------ | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| F-T1-1             | 1.1 validates at load against the schema                                             | `config.schema.json` is not in the published tarball; `ajv` is a runtime dependency                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | New row 1.5; R9                                                                                                                          |
| F-T1-2             | 1.1 calls the validator as it is                                                     | The validator reads the config's `$schema` and folds schema read failures into "invalid"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | New row 1.6; R9, R10                                                                                                                     |
| F-T1-3             | 1.3 extends `config validate`                                                        | `validate` checks one key/value pair                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 1.3 rewritten; R12                                                                                                                       |
| F-T1-4             | 1.4 lives in `tests/unit/config/`                                                    | Config tests live in `tests/unit/infra/config/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 1.4 path corrected                                                                                                                       |
| F-T1-5             | T1 lands cleanly before T2                                                           | The CLI writes `versioning.max_versions`, which the T0 schema rejects                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Folded into 1.5; R13                                                                                                                     |
| DEV-T1-1           | 1.2 is its own row                                                                   | Same file as 1.1, strictly sequential                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 1.2 killed, merged into 1.1                                                                                                              |
| DEV-T1-4           | 2026-09-15                                                                           | Row 1.14 keyed "absent" on the store's `exists()` result rather than on `load()` returning nothing, which the brief also named. `SqliteStateStore.load()` is typed non-nullable, so testing its result for null after the validity check is a branch the type system proves unreachable, and eslint flagged it (`no-unnecessary-condition`). Accepted: a `load()` that broke its own contract would now warn instead of logging at debug, which is the louder failure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| DEV-T1-5           | 2026-09-15                                                                           | **Planner brief contradiction.** Row 1.16's brief ruled "send the trace to the logger at `debug` where a logger exists" and also required `--verbose` to keep printing the trace under STDIO. `EnhancedLogger.logToConsole` (`infra/logging/index.ts:158`) drops every console level under STDIO outside CI, so the first instruction would have silenced the trace under `--verbose` too. The worker kept the existing `debugLog` helper and corrected its gate from `!testEnvironment` (true for every normal launch) to `verbose`. Accepted: the row's flip condition is what the owner's ruling needs, and it holds. Row 1.8's brief states the logger's STDIO behavior outright instead of assuming it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| DEV-T1-6           | 2026-09-15                                                                           | Row 1.13 went beyond the brief inside its own file: it removed `ToolDescriptionLoader.getConfigPath()` and the `configPath` field of `getStats()`, which no code or test reads, because reporting a disk path nothing reads is the defect's own shape; and it rewrote the old test fixture, which wrote a fake contracts file at the wrong path and so tested nothing. Accepted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEV-T1-7           | 2026-09-15                                                                           | Row 1.15 kept `PromptExecutor.setDatabasePort`'s forwarding to the store, since a store built without a port (tests, a late database) still needs it, and it is a no-op for the same port either way; a port passed as the store's 4th argument wins over `options.databasePort`; it extracted `openToolsDatabase` and `toolsDatabaseWiringError` in `module-initializer.ts`, which lowered `initializeModules`' cognitive complexity from 57 to 53. It measured lint before editing, after confirming the files matched HEAD, instead of writing HEAD copies into the shared tree. Accepted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEV-T1-8           | 2026-09-15                                                                           | Row 1.8 split `logToConsole` into `logCILevel`, `logStdioLevel` and `logNonStdioLevel`: the first rewrite measured cognitive complexity 23 against the limit of 15, and the split brought the file back to its baseline rule set. It also added two tests beyond the brief, pinning the ruling table's "CI unchanged" and "non-STDIO unchanged" cells, after finding that gating the new branch on the transport alone would have made CI+STDIO print `info` under `--verbose`, which the old code never did. Accepted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DEV-T1-9           | 2026-09-15                                                                           | Row 1.17's first tests-typecheck baseline regeneration dropped the totals by 4, not the 2 the planner allowed: `tests/unit/execution/pipeline/command-parsing-stage.test.ts` went 18 → 16. The worker restored its backup and reported, as ruled, attributing the extra 2 to row 1.8's `infra/logging/index.ts`, which that test imports `Logger` from. 1.8's logic was already accepted and no worker was editing, so the planner ruled the −4 in; the second regeneration matched exactly — the moved file's key removed, that test 18 → 16, totals 368 → 364, four `byCode` entries −1 each. Both rate-limited workers were resumed from a measured tree, and neither redid finished work                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEV-T1-10          | 2026-09-15                                                                           | **Two planner errors caught before they landed, and one correction to DEV-T1-9.** (1) The planner nearly briefed the merged tests-typecheck total as 362 (368 − 2 on `main` − 4 on this branch), assuming disjoint changes. Measuring both sides against the merge base showed `main` had made the same `command-parsing-stage.test.ts` 18 → 16 change with the same codes (TS2305, TS2322), which git auto-merged, so the right total was 364; the worker's resolution matched and the ratchet reported exactly 364. (2) **Correction to DEV-T1-9:** that −2 was not caused by row 1.8. This branch's ratchet already reported 366 errors against the base baseline's 368 before 1.8 existed (the checks on `ce230679` and the trial merge on `6e0814f0`), and `main` shows the same −2 without 1.8 — it was slack already in the baseline at `53bed1fa`, which both sides locked in. Locking in −4 was still right; the attribution was wrong, and 1.17's branch commit body repeats it (squash merge uses the PR body, so it does not reach `main`; the PR body must not repeat it). (3) A trial merge predicts only the commits it contains: the one on `6e0814f0` was clean, and the real merge after 1.8 and 1.17 conflicted in two files 1.17 touched |
| F-T1-6             | —                                                                                    | `src/index.ts:672` builds a second, throwaway `ConfigLoader` in a verbose startup-failure path; warn-once is per instance, so that path may warn twice                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accepted: error path only, noted for 1.1's brief                                                                                         |
| F-T1-7             | 1.3 extends a reachable `config validate`                                            | Live drive, 2026-09-14, three calls to the running server. Control `action:config` → full config dump. `action:config` + undeclared `config:{operation:"get",key:"server.port"}` → the SAME full dump, not the one value. `action:config operation:validate` → the same dump. The tool's input schema (`system-control.schema.ts`, a plain `z.object`) declares no `config`, `key`, `value`, `backup_path` or `confirm`, so `get`/`set`/`validate`/`restore` are unreachable over MCP and the `if (!configRequest) return list` fall-through reports each as success. Whether the client or zod dropped the argument does not change the outcome — both are closed by declaring it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 1.3 re-cut (R14); row 2.5 + OQ-5 opened. Sixth sighting of the guard-hides-the-defect shape: capture at slice close                      |
| F-T1-8             | The slice's commits are its record                                                   | `CHANGELOG.md` keeps a hand-edited `[Unreleased]` section (BREAKING / Added / … ) merged into each release by `scripts/merge-unreleased-changelog.js`; T0 and T1 have written nothing to it despite five consumer-visible changes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Row 1.7 opened, sequenced after 1.1 and 1.3 so the entries describe shipped behavior                                                     |
| F-T1-9             | The module logger's `warn` reaches stderr                                            | Under STDIO, `logToConsole` (`infra/logging/index.ts`) prints only when `CI=true` or `NODE_ENV=test`. 400 `logger.warn`/`logger.error` call sites across 111 files are invisible to a real STDIO user, including the `analysis` deprecation notice; jest sees them because it sets `NODE_ENV=test`, which is why `legacy-key-migration.test.ts:372`'s comment ("logger.warn writes through console.warn") reads as true                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Row 1.8 + OQ-6 opened: the fix is one decision in the logger, not 400 call-site edits                                                    |
| F-T1-10            | `server.transport` in `config.json` selects the transport                            | Live drive, 2026-09-15, main checkout build `868f36ec` (dist built 1m40s after that commit), `MCP_CONFIG_PATH` at three temp copies of the shipped config, `PORT=9392`, stdin closed, 8s timeout. Default config: exited by itself (STDIO). `server.transport: "streamable-http"` only: exited by itself — still STDIO. Root `transport: "streamable-http"` (the control): still running at 8s, stderr `Transport selected: streamable-http`. Code agrees: `getTransportMode()` reads root `transport`; `server.transport` appears only in `cpm`'s writer and validator and in `config-utils.ts`'s restart-required key list; no destructured reader exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Row 2.6 + OQ-7 opened                                                                                                                    |
| F-T1-11            | —                                                                                    | The same drive's HTTP run logged `[WARN] [ToolDescriptionLoader] Generated tool-descriptions.contracts.json not found at <config dir>/src/tooling/contracts/_generated/…`: with `MCP_CONFIG_PATH` outside the package, generated tool descriptions resolve against the config file's directory, not the package root. One observation, HTTP only (the logger is silent on stderr under STDIO, F-T1-9). Not this plan's work — it belongs with the path-settings change in #278/#279                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Relayed to the owner in the session report; no row here. Revives as a row if the owner assigns it to this plan                           |
| F-T1-12            | The handbook's compat re-export ban is enforced by `validate:no-crosslayer-reexport` | CLAUDE.md and AGENTS.md on `origin/main` both cite that gate. No npm script of that name and no gate file exists on this branch or on `origin/main`; `npm run` without `--silent` prints `Missing script`. `git log -S` finds it added in 4f25843f and touched by c4b920ba ("retire four guards, re-home them…"). Where its rule now lives, if anywhere, is being read from c4b920ba                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Row 1.9 does not depend on it; the citation itself is a handbook finding for the owner                                                   |
| F-T1-12 (resolved) | —                                                                                    | c4b920ba re-homed the script as the ESLint rule `claude/no-compat-reexport-shim` (`server/eslint.config.js:223–236` on `origin/main`, level `error`), which `lint:ratchet` runs — so the ban IS enforced. It passed row 1.3's validator correctly: that file defines functions, so it is not a pure re-export shim. What is stale is the citation: CLAUDE.md, AGENTS.md and a comment in `server/scripts/lib/domain-ownership.ts:134` still name the deleted `validate:no-crosslayer-reexport`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Relayed to the owner as a handbook correction; not this plan's work. Row 1.9 stays, as single-import-path hygiene rather than a gate fix |
| F-T1-13            | A clean textual merge of `main` preserves this branch's behavior                     | Merge f6c55551 of `origin/main` (868f36ec) had no conflicts. On the merged tree: typecheck 0, `typecheck:tests:ratchet` 367 with no growth, `validate:config-schema` + self-test 9/9, build 0, and the startup drive unchanged (typo → one warning naming `/gates` and `enabld`, clean → none, stdout empty, exit 0 both). But config + prompt-engine tests ran 45/46: `main` moved `loadConfig`'s "Using default configuration" from `console.info` to `console.error`, and row 1.4's step 6 asserts `console.info`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Row 1.11 opened. Predicted before the merge from the two diffs, confirmed after it                                                       |
| F-T1-14            | The PR template's guidance on a `Plan:` footer describes the gate                    | `.github/pull_request_template.md`'s comment says the gate fails while the named plan's `status:` is non-final. `scripts/validate-pr-body.mjs` relaxed that on 2026-09-06 (its header records why): a PR that introduces a plan passes, a PR must close at least one row, and only the PR closing the last row must set a final status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Relayed to the owner as a template correction; not this plan's work. This PR keeps the plan `active`                                     |

## Handoffs

| Row                          | Date       | Verdict                            | Spot-read                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Brief feedback recorded                                                                                                                                                                                                                                                                                                                                                                          |
| ---------------------------- | ---------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1.5                          | 2026-09-14 | accepted                           | `git diff` on both files matches the brief, including `"minimum": 1` that the handoff's prose omitted — artifact wins                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | A probe script that imports an installed package must itself live under `server/`: node resolves `node_modules` from the script's path, not the cwd, and this worktree's `node_modules` is a symlink to the main checkout. "Run from server/" read as a cwd instruction and cost a retry                                                                                                         |
| 1.6                          | 2026-09-14 | accepted                           | Validator diff matches the brief exactly; self-test re-run by the planner 9/9. The handoff said eslint was "identical 12" at HEAD; measured 13 at HEAD, because dropping the unused `path` import also removed an `import-x/order` finding — better than reported, not worse                                                                                                                                                                                                                                                                                                                                                                                                                                                     | A per-file "eslint → clean" expectation is wrong for a file carrying baselined debt; write briefs against the ratchet ("no rule grows versus HEAD"). It cost the worker a swap-to-HEAD-and-restore cycle to prove the debt was not theirs                                                                                                                                                        |
| 0.5 (discovery)              | 2026-09-14 | accepted                           | Re-measured by the planner before ruling: 15 `scripts/` diagnostics, identical per file and per code to the handoff; both ratchet couplings confirmed at lines 26 and 134                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Step 5 asked a yes/no that the honest answer splits — the mechanism generalizes, its two hardcoded couplings do not. Ask "what would reuse require" instead                                                                                                                                                                                                                                      |
| 1.1                          | 2026-09-14 | accepted                           | Diff matches the brief plus two deviations accepted: the stderr channel (R20), and assigning `this.config` only after the schema await, so nothing reads the raw file mid-load. Planner re-ran tests 32/32, self-test 9/9, rebuilt, and drove typo and clean configs with `CI` and `NODE_ENV` unset                                                                                                                                                                                                                                                                                                                                                                                                                              | The brief stated a runtime-output fact ("`warn` writes via `console.warn`") copied from a test comment that is only true under `NODE_ENV=test`: a fact about runtime output must carry the environment it was measured in. Name the channel and its lint bar together. Ask for a reload step when a rule is about reloads                                                                        |
| 1.3, 1.4, 0.6, 0.7           | 2026-09-15 | resumed, not relaunched            | All four hit a session rate limit on 2026-09-14 near the end of their rows. Before resuming, the planner measured: branch HEAD unchanged at `ae00b1d6`; the uncommitted set is exactly the four rows' named files; no stray probe or temp tsconfig; nothing listening on 9391; `git diff -- server/src/infra/config/index.ts` empty, so row 1.4's mutation was restored. Each was resumed at its last reported step                                                                                                                                                                                                                                                                                                              | A worker killed mid-mutation can leave a mutated source file behind: measure the tree before resuming                                                                                                                                                                                                                                                                                            |
| 1.4                          | 2026-09-15 | accepted pending R24               | Test file read in full: a positive control plus one sequence test pinning all nine measured steps on one loader; header correction accurate. Worker's run: config tests 34/34, test-types ratchet 367 (no growth), self-test 9/9, both mutations red and restored with `git diff --exit-code`. Tests re-run on the final tree before commit, since 1.3 changes the types they compile against                                                                                                                                                                                                                                                                                                                                    | none recorded — the worker reported the brief matched the tree                                                                                                                                                                                                                                                                                                                                   |
| 0.6                          | 2026-09-15 | accepted pending R24               | Diffs read: 5 required→throw narrowings with named messages; `validate-prompts.ts` builds a real `Logger` instead of casting. Planner re-ran the scripts that exercise both files — `validate:table-contracts`, `validate:no-phantom-columns`, `validate:prompts` and their self-tests — all exit 0; a scripts typecheck reports 0 diagnostics under `scripts/`                                                                                                                                                                                                                                                                                                                                                                  | Several guards are unreachable through the real functions by construction; say in the brief that demonstrating such a throw means exercising the guard in isolation, so no probe cycles go to hunting an input that cannot exist                                                                                                                                                                 |
| 0.7                          | 2026-09-15 | accepted pending R24               | Diffs read: 5 required→throw narrowings, no cast or `!`. Planner re-ran `validate:shipped-frameworks`, its self-test and `verify:action-metadata` — all exit 0; scripts typecheck 0 under `scripts/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | A rate-limit pause reset `/tmp` and took the BEFORE captures with it. Briefs that span a possible pause should derive BEFORE baselines from `git show HEAD:` on demand rather than cache them in `/tmp`                                                                                                                                                                                          |
| 1.3                          | 2026-09-15 | accepted                           | Diff read. Deviations: the validator re-exports the moved type because `index.ts` (outside the row) imports it from there — accepted and opened as row 1.9; `=== undefined` instead of `!` to avoid lint growth — fine; `execute()` captures `args.config` once as `unknown` and casts at the single use — behavior checked by the worker's no-operation drive. Its concern (no test for the handler) is row 1.10                                                                                                                                                                                                                                                                                                                | The brief's illustrative `execute()` snippet grew the lint ratchet on a heavily `any`-typed file and cost five measure-and-retry rounds. A brief that names a snippet should pre-authorize a behavior-equivalent restructure when the literal form trips the ratchet. Reusable: capturing an `args` field as `: unknown` and casting once at use avoids `no-unsafe-assignment` in these handlers |
| batch (1.3, 1.4, 0.6, 0.7)   | 2026-09-15 | R24 gates run once, no worker live | `typecheck` 0 · `typecheck:tests:ratchet` 367, no growth · `lint:ratchet` 3096/970, no growth · `validate:suite-membership` OK · `validate:knip-ratchet` OK (it reports the baseline could now be tightened) · `validate:arch` OK · `validate:config-schema` + self-test 9/9 · named tests 46/46 · tree identical before and after. `validate:no-crosslayer-reexport` exited 1 — npm's own "Missing script", hidden by `--silent`; not a failure of any row (F-T1-12)                                                                                                                                                                                                                                                            | Committed as three concerns: 947915bc (0.6 + 0.7), 6ea2ba32 (1.4), 9f2b330e (1.3)                                                                                                                                                                                                                                                                                                                |
| 1.11                         | 2026-09-15 | accepted pending batch gates       | Diff read: `infoSpy` removed from declaration, setup and restore; step 6 asserts a `console.error` call whose first argument is `'Using default configuration'`. Worker: 2/2, a changed expected string turned the test red and was restored, test-types ratchet no growth                                                                                                                                                                                                                                                                                                                                                                                                                                                       | none — the brief matched the tree                                                                                                                                                                                                                                                                                                                                                                |
| 1.10                         | 2026-09-15 | accepted pending batch gates       | Test file read in full: six cases on the real handler with a stubbed context copied from `framework-action-handler.test.ts`; the not-validated case asserts exact text and the absence of any success marker. Worker: 6/6, swapping the not-validated text for the success text turned case 1 red, restore proven by `git diff --exit-code`                                                                                                                                                                                                                                                                                                                                                                                      | Case 6 asserts only what a per-key `validate` is NOT; asserting it names `server.port` would make it a positive guard too. Not opened as a row: the current form already catches the path it guards                                                                                                                                                                                              |
| 1.9                          | 2026-09-15 | handed off, scope amended          | Two-file change as briefed; typecheck 0, `validate:arch` OK, test-types ratchet 367, 32/32 config tests, self-test 9/9; lint fell in `index.ts` (`no-unsafe-member-access` 12→6). Its concern named a third importer the brief missed (DEV-T1-2); worker resumed with that file added                                                                                                                                                                                                                                                                                                                                                                                                                                            | The brief's MEASURED section should have come from the same package-wide search the worker was told to run                                                                                                                                                                                                                                                                                       |
| 1.9 (amended)                | 2026-09-15 | accepted                           | Three-file diff read; `rg` shows one definition and every import from `config-manager.js`, including the script and 1.10's test; `tsc -p tsconfig.scripts.json` exit 0 across the project                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | none — the amendment named the file and the sibling pattern                                                                                                                                                                                                                                                                                                                                      |
| 0.5                          | 2026-09-15 | accepted                           | `tsconfig.scripts.json`, the `typecheck:scripts` script and its SUITE entry read; `reads: ['spawn']` as measured; `converse` states the planted-error control; no planted file left. Worker: exit 0 final, planted error exit 2 naming the file, suite-membership 59, knip no regressions. Two mid-row non-zero exits were the announced 1.9 collision                                                                                                                                                                                                                                                                                                                                                                           | A concurrency note that says "re-run once" should say the collision may recur until the other row lands, so a second recurrence is not mistaken for a new defect                                                                                                                                                                                                                                 |
| batch (1.9, 1.10, 1.11, 0.5) | 2026-09-15 | gates run once, no worker live     | `typecheck` 0 · `typecheck:tests:ratchet` 367 · `typecheck:scripts` 0 · `validate:suite-membership` OK · knip ratchet OK · `validate:arch` OK · schema check + self-test 9/9 · config, system-control and prompt-engine tests 52/52 · **`lint:ratchet` FAIL: `__unknown__` warnings 5 → 6**, attributed to `src/infra/config/index.ts:154` (DEV-T1-3), none of this batch's diffs                                                                                                                                                                                                                                                                                                                                                | Committed as 1fbd7c03 (1.9), e17b679b (1.11), b558339d (1.10), da1e1184 (0.5); row 1.12 opened                                                                                                                                                                                                                                                                                                   |
| 1.12                         | 2026-09-15 | accepted                           | One-line diff read. Worker: the file's null-rule warning gone and no rule grew (real-file method), `lint:ratchet` OK 3092 / 897, typecheck 0, config tests 34/34. Committed only after 1.7 finished, because a code commit's lint-staged run hides unstaged files                                                                                                                                                                                                                                                                                                                                                                                                                                                                | none                                                                                                                                                                                                                                                                                                                                                                                             |
| 1.7                          | 2026-09-15 | accepted                           | Entries read verbatim against the code: the startup check and its `validate` report, the shipped schema, the log level and `gates.mode`, and per-section strictness. A guard confirmed the tree held only `CHANGELOG.md` (additions only) and 1.12's file before either commit                                                                                                                                                                                                                                                                                                                                                                                                                                                   | none. The lint ratchet now sits 4 errors and 73 warnings under its committed baseline; tightening it is not this slice's concern                                                                                                                                                                                                                                                                 |
| PR boundary                  | 2026-09-15 | passed on 66b635ef                 | One snapshot, `MCP_*` path variables cleared, HEAD and tree unchanged at the end: build 0 · typecheck 0 · `lint:ratchet` 3092 / 897, no regressions · `typecheck:tests:ratchet` 367 · `typecheck:scripts` 0 · `validate:all` 59/59 · unit 3113 passed (1 skipped, 238 suites) · integration 822 (63 suites) · e2e 209 passed (2 skipped, 11 suites). Live drive over MCP, the same script against `main`'s build (975093bf) and this branch's: typo config → main returns the whole configuration and warns nothing; the branch returns the mismatch naming `/gates` and `enabld` and writes one stderr warning; clean config → the branch reports a match. `git diff --stat origin/main...HEAD`: 21 files, all inside this plan | Recorded before publication                                                                                                                                                                                                                                                                                                                                                                      |

## Owner interview (2026-09-15)

Facts measured to frame OQ-5 through OQ-8, on `main` at `e84bf394`'s source (worktree build of `fa9119c2`, identical under `src/`), with a fresh `MCP_RUNTIME_ROOT` and `MCP_WORKSPACE`, `MCP_RESOURCES_PATH` and `MCP_CONFIG_PATH` unset.

| #       | Finding                                                                                                                          | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Consequence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-T2-1  | A clean STDIO start shows the user debug trace and hides every warning                                                           | `node dist/index.js --transport=stdio` with stdin held open 7 s. `CI` unset: stderr 13 lines, all `DEBUG: …`, written by `console.error` in `src/runtime/application.ts:107` and `debugLog` in `src/index.ts`, outside the logger. `CI=true` (the logger's console gate, `infra/logging/index.ts:76`): 5 lines, 4 `[WARN]` — `Invalid framework state, falling back to defaults`, `ChainSessionStore: no DatabasePort provided, persistence disabled`, and `[ToolDescriptionLoader] Generated tool-descriptions.contracts.json not found` twice                                                                                                                                                                                                                                                                                                                                                                                                                                | R28: row 1.8 lands only after each warning is fixed or re-levelled and the `DEBUG:` lines go through the logger                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| F-T2-2  | Generated tool descriptions never load, in any install                                                                           | `server/src/tooling/contracts/_generated/tool-descriptions.contracts.json`: not tracked by git, absent from the main checkout, absent from `npm pack --dry-run` (280 files). The loader looks there, finds nothing, and falls back to in-memory defaults with the warning above. `scripts/generate-contracts.ts:552` writes it under `GENERATED_META_DIR`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Defaulted to a row this plan owns, blocking 1.8, rather than a separate Issue. Unmeasured: whether the in-memory defaults differ from the contract descriptions                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| F-T2-3  | Config transport is consulted only by a launch that passes no flag                                                               | Every shipped launcher passes `--transport=stdio`: `mcp.json`, `.mcp.json`, `manifest.json`, gemini-prompts `gemini-extension.json`, opencode-prompts `src/lib/opencode-config.ts`. `README.md`, `server/README.md`, `docs/architecture/overview.md` and `docs/guides/identity-scope.md` select HTTP with the flag. Resolution: `--transport=` (`infra/http/transport/index.ts:96`) → root `transport` (`getTransportMode`, `infra/config/index.ts:391`, defaulted to `stdio`) → `stdio`. `server.port` is read (`getPort`, `PORT` first); `server.transport` is read nowhere, though `cpm` accepts it and `CONFIG_RESTART_REQUIRED_KEYS` lists it                                                                                                                                                                                                                                                                                                                             | OQ-7 re-asked with a flag-owned option                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F-T2-4  | Config defaults are stated three times, and the three disagree                                                                   | Shipped `server/config.json` · code `DEFAULT_CONFIG` (`infra/config/index.ts:223`) · `cpm`'s `generateDefaultConfig` (`cli-shared/config-operations.ts:215`): `server.port` 9090 · 3456 · 9090; `frameworks.systemPromptFrequency` 3 · 2 · 3; `server.name` `claude-prompts` · `Claude Custom Prompts` · `claude-prompts`. The template also writes the unread `server.transport` and the snake_case `versioning` keys. `runtime/startup.ts:99` refuses a package root with no `config.json`. The runtime already names 5.1's and 5.2's targets: `frameworks.injection.{systemPrompt,gateGuidance,styleGuidance}.{frequency,target}`, which the loader builds from the flat file keys at `index.ts:444`, and root `chainSessions`, which it fills from `advanced.sessions` at `index.ts:478`                                                                                                                                                                                   | The effective HTTP port depends on whether a workspace config exists. Frames 5.4's defaults question; 5.1 and 5.2 take the runtime's existing names as defaults                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| F-T2-5  | `config.json` has readers outside the loader, and the Python hooks read the packaged file                                        | Strict `JSON.parse` of a config file in `server/src`: `infra/config/index.ts:321` (the loader), `runtime/paths.ts:104` (the path-settings check), `cli-shared/config-operations.ts:88` and `:188` (`cpm`'s read and its post-write check), `cli-shared/version-history.ts:159`, `mcp/tools/config-utils.ts:335`. Python: `hooks/lib/config_loader.py:38` and `hooks/ralph-stop.py:66` read `<plugin root>/server/config.json`, the packaged file, not a workspace one. Libraries: `json5` is a direct server dependency and parses comments; `strip-json-comments` is present transitively; nothing installed edits JSON while preserving comments                                                                                                                                                                                                                                                                                                                             | R32 stops shipping `config.json`, so the hooks' config source needs a row whatever OQ-11 rules. Accepting comments means every reader above changes, and `cpm config set` either gains a comment-preserving editor or erases the comments it rewrites                                                                                                                                                                                                                                                                                                                                              |
| F-T2-6  | Correction to F-T2-2: the generated tool descriptions exist; the loader reads a directory that does not                          | `src/mcp/contracts/schemas/_generated/tool-descriptions.contracts.json` is tracked, written by `scripts/generate-contracts.ts:22` (`GENERATED_META_DIR`). `ToolDescriptionLoader`'s constructor (`mcp/tools/tool-description-loader.ts:120`) joins `<serverRoot>/src/tooling/contracts/_generated/`, which exists neither in the repo nor in the package, which ships no `src/`; the path has been wrong since at least `913c2d9d` (2026-03-11). One construction site (`:488`); the warning repeats because the base descriptions load twice. F-T2-2 checked the loader's path, not the generator's, so its "not tracked, absent" is true only of the wrong path                                                                                                                                                                                                                                                                                                              | Every server has served the in-memory default descriptions instead of the contracts'. Step 1 reads the descriptions through the bundle rather than from a disk path the package does not ship                                                                                                                                                                                                                                                                                                                                                                                                      |
| F-T2-7  | Two more startup warnings are wrong messages, not wrong states                                                                   | `Invalid framework state, falling back to defaults` (`engine/frameworks/framework-state-store.ts:322`) fires when no state was ever saved: `exists()` is false on a fresh runtime root, and absence is reported as invalid. `ChainSessionStore: no DatabasePort provided, persistence disabled` (`modules/chains/manager.ts:318`) fires from the constructor's `initialize()` before `mcp/tools/prompt-engine/core/prompt-executor.ts:324` late-binds the port through `setDatabasePort`, which re-arms persistence (fixed 2026-08-11, per the comment there)                                                                                                                                                                                                                                                                                                                                                                                                                  | Step 1 reports absence as absence and binds the port at construction instead of warning ahead of the late bind                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| F-T2-8  | `cpm config` and `system_control config` are two implementations with different meanings                                         | Two `CONFIG_VALID_KEYS` lists, each with its own `validateConfigInput` and `CONFIG_RESTART_REQUIRED_KEYS`: `cli-shared/config-input-validator.ts:27` and `mcp/tools/config-utils.ts:13` (16 keys). `cpm config` offers list, get, set, validate, reset and keys, reading the file. The `system_control` handler offers list, get, set, validate and restore, reading the resolved runtime config (`configManager.getConfig()`), so `get` on an unset key returns a default where `cpm` reports it missing; only list and validate are reachable (row 2.5)                                                                                                                                                                                                                                                                                                                                                                                                                      | The owner asked on 2026-09-15 whether the two should be aligned                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| F-T2-9  | There is no single `$schema` hosting standard; a major-pinned npm CDN URL is an established one                                  | Knip documents `https://unpkg.com/knip@6/schema.json`, plus `schema-jsonc.json` for `knip.jsonc` — R29's shape on unpkg. Biome (`biomejs.dev/schemas/<version>/schema.json`) and Turborepo (`v{X}-{Y}-{Z}.turborepo.dev/schema.json`) host versioned URLs on their own domains; Renovate hosts an unversioned one. SchemaStore.org maps schemas to files by `fileMatch` for VS Code, JetBrains and others, which needs a distinctive file name, and `config.jsonc` is not one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | R29 stands; the owner asked on 2026-09-15                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| F-T2-10 | The framework-state load path runs for one scope per process                                                                     | Row 1.14's worker: `initialize()` calls `loadPersistedState()` once, with no scope, so the absent/corrupt distinction is only exercised for the process's default scope; `getCurrentState(scope)` for any other scope reads the in-memory map and never SQLite. `adoptLegacyGlobalState` runs after the load's try/catch whichever branch logged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | A test must construct a fresh store with the scope as its default to reach this path. No change needed for step 1: the warning came from the startup path                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| F-T2-11 | `writeSchemaWarning`'s doc comment cites a function that does not exist                                                          | `server/src/infra/config/index.ts`: the comment says `setupConsoleRedirection` does not replace `console.warn`; `rg` finds no definition of `setupConsoleRedirection` anywhere in `server/src`. Separately, `--quiet` never reaches `EnhancedLogger` (it gates startup messages in `runtime/application.ts:228`), and the logger's `enableDebug` follows `--verbose` (`runtime/context.ts:155`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | The row that retires `writeSchemaWarning`'s direct `console.warn` after 1.8 rewrites that comment. 1.8's warnings cannot be hidden by STDIO's automatic quiet                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| F-T2-12 | A lint before/after measured on a copy in another directory measures a different file                                            | Row 1.16's worker: the HEAD content linted from a subdirectory one level deeper reported 260 problems; the same content at the real path reported 173, identical to the fixed file. Type-aware rules resolve modules relative to the file's location                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Briefs from here on measure lint before/after at the real path: back the fixed file up outside the repo, write `git show HEAD:<path>` over it, lint, restore and `diff` to prove the restore                                                                                                                                                                                                                                                                                                                                                                                                       |
| F-T2-13 | The tool-description loader's test has never run in CI                                                                           | `server/tests/tool-description-loader.test.ts` sits directly under `tests/`. `jest.config.cjs` roots at `tests/`, but every script passes a subdirectory (`test:unit`/`test:coverage` → `tests/unit`, `test:integration`, `test:e2e`) and CI runs exactly those (`.github/workflows/ci.yml:384–402`). Count of `*.test.ts`: 239 unit, 64 integration, 11 e2e, 1 at the root                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Row 1.17: move it and add a check that fails on the next one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| F-T2-14 | Row 1.15's findings                                                                                                              | The store's 4th constructor argument is a `DatabasePort` or an `ArgumentHistoryTracker`, told apart with `instanceof`; production always passes the tracker there, which is why the port needed an option, and 36 test sites keep that union alive. The same construct-then-setter shape remains, with no false warning, for `ArgumentHistoryTracker`, `VersionHistoryService` (throws when used unwired) and `ExecutionRecordStore` (created inside `setDatabasePort`). No test builds the runtime composition in-process, so the built-server drive is the only proof of the production path. The "server keeps running without persistence" case the brief assumed does not exist: that block throws                                                                                                                                                                                                                                                                        | Max-params growth killed as row 1.18; the stale complexity count in `module-initializer.ts:107` goes back to the 1.15 worker before its commit                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| F-T2-15 | `main` moved 13 commits past this branch's base, and one of them made every config operation reachable over MCP                  | `git log 53bed1fa..origin/main`: #290–#302. #299 (`4d323d60`) declared every field a `system_control` handler reads: 33 parameters, including `config` as `{ key, value?, operation }` (get, set, list, validate), `backup_path` and `confirm`; its own demonstration shows `config restore` with `confirm: true` now running. On this initiative's surfaces `main` also changed `server/config.schema.json` (+15), both key lists (`cli-shared/config-input-validator.ts` +34, `mcp/tools/config-utils.ts` +33), `infra/config/index.ts` (+43), the `system_control` contract (+292) and `package.json` (the `validate:format`/`format` lines only). Files this branch also changed: `mcp/tools/index.ts` (main adds methods near :695), `prompt-executor.ts` (main adds fields and a `gatesConfigProvider` near 1.15's constructor change) and `runtime/application.ts` (main rewires hot reload, away from 1.16's gate)                                                     | R27's and R35's "unreachable today, so not breaking" is false on `main`; both surfaces are still unreleased (5.0.0 is pending #232). The owner's security reason for R27 is unaffected, so the ruling stands, but step 2 now reverses part of #299, and the reads' calling convention (#299's nested object vs a flat `key`) is re-asked when step 2 is cut. Step 3's generated authority inherits `main`'s new gate keys. The merge waits until no worker is editing                                                                                                                              |
| F-T2-16 | A trial merge of `main` into step 1 is clean, and the lint ratchet's baseline sits well above the real count                     | Throwaway `git clone --shared` in `/tmp`, branch tip `6e0814f0` merged with `main` at `267bc610`: no conflicted files; `mcp/tools/index.ts`, `prompt-executor.ts` and `runtime/application.ts` auto-merged. On the merged tree (with the main checkout's `node_modules` linked): `typecheck` 0, `typecheck:scripts` 0, `typecheck:tests:ratchet` OK at 366, `lint:ratchet` OK, `test:unit` 245 suites / 3272 passed / 1 skipped. `.eslint-ratchet-baseline.json` records 3200 errors / 1020 warnings on `main`, on this branch and at its base; the measured count is 3092 / 895 before the merge and 3055 / 888 after                                                                                                                                                                                                                                                                                                                                                         | The merge can land once 1.8 and 1.17 are committed; it re-verifies on the real tree with their edits. The ratchet slack predates this initiative and belongs to `main`, and the ratchet compares per rule, so no single rule can grow past its own old count. Relayed to the owner, not this plan's work                                                                                                                                                                                                                                                                                           |
| F-T2-17 | The unit-test command the planner's briefs gave does not narrow to one file                                                      | Row 1.8's worker: `npm run test:unit -- --testPathPatterns <name>` becomes `jest --runInBand tests/unit --testPathPatterns <name>`, and Jest OR-joins the positional path with the pattern ("Ran all test suites matching tests/unit\|<name>"), so the whole unit suite runs. Rows 1.17's and 1.8's briefs both carried it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Briefs name `NODE_OPTIONS="--experimental-vm-modules" npx jest --runInBand <exact path>` for one file. 1.17's worker was told mid-row                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| F-T2-18 | Row 1.8's live drives, and a ratchet cost of moving a test                                                                       | From source with `CI` and `NODE_ENV` unset: a clean STDIO start wrote 0 B to stdout and stderr with warnings now able to reach stderr, so no warning was left hiding behind the old gate and 1.14's is gone; a config with an `analysis` section wrote one `[WARN] [CONFIG] analysis.semanticAnalysis is deprecated…` line and 0 B to stdout. `tests/setup.ts:6` sets `NODE_ENV=test` for every jest run, which is why no test ever saw STDIO's silence. The same worker saw `typecheck:tests:ratchet` report the moved loader test at `baseline=0 current=2` with the old path's entry vanished: the ratchet is keyed by path, and 1.17's brief did not list it                                                                                                                                                                                                                                                                                                               | 1.17's worker was told to fix the two errors in the moved file or report them; the planner decides any baseline change                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F-T2-19 | Row 1.17's findings                                                                                                              | `"tests/unit-renamed".includes("tests/unit")` is true, so a directory check built on `.includes()` false-passes a rename; the validator's self-test caught it, and `commandNamesDir` matches a whole path token instead. Jest run through `npx` with stdout redirected prints no per-suite PASS/FAIL lines here, so one suite's inclusion shows as a suite-count change and the exit code, not a PASS line                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Briefs stop asking for a PASS line from a redirected Jest run                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| F-T2-20 | The merged tree is green except one test the merge made stale                                                                    | On `de3a2cfd` (after merge commit `9c3f48a7`): `typecheck` 0, `typecheck:scripts` 0, `typecheck:tests:ratchet` OK at 364, `lint:ratchet` OK at 3055 / 888, `validate:test-directory-membership` OK, `validate:suite-membership` 60 steps; unit tests 246 suites passed, 1 failed (3282 passed, 1 failed, 1 skipped). The failure is `tests/unit/mcp-tools/tool-description-loader.test.ts` expecting a `skills_sync` tool, which #299 removed from the contracts (`tooling/contracts/skills-sync.json` deleted, parameters folded into `system-control.json`); `validate:contracts` exits 0 and the `prompt_engine` description is identical at base, `main` and HEAD. No exported or constructor signature `main` changed is one this branch's rows call with new arguments                                                                                                                                                                                                   | Row 1.22 fixes the test. The merge itself needs nothing more; 1.19 was held until this result so a merge failure could not be charged to it. The first attempt to record this exited at a clean-tree guard, because both workers had already started editing; a plans-only commit guards on the plan files alone                                                                                                                                                                                                                                                                                   |
| F-T2-21 | Step 1's PR boundary on `c0cc52d7`: everything green but one static check, caused by row 1.15                                    | Build and probe on the step-1 code: (a) clean STDIO start 0 B; (b) `--verbose` 625 lines, since the logger's INFO/DEBUG now follow `enableDebug` under STDIO; (c) one `[WARN] [CONFIG] analysis.semanticAnalysis is deprecated…` line, 0 B stdout; (d) all three base descriptions equal the contract, the 8 loader lines all `[INFO]`; (e) `chain_runs` 1 with no persistence line; (f) one `[WARN] [CONFIG] … does not match its schema: /gates … (enabld)` line. `typecheck` 0, `typecheck:scripts` 0, ratchets 364 and 3055 / 888; `test:all` unit 247 suites (3283 passed, 1 skipped), integration 67 (872), e2e 13 (234 passed, 2 skipped). `git diff origin/main...HEAD` 21 files, all in slice; `AGENTS.md` identical to `main`. `validate:all` 59 of 60: `validate:db-claim-order` reports `module-initializer.ts:137: SqliteEngine.getInstance() with no dbPath runs BEFORE claimStateDatabase() at line 232`. 1.15's per-row checks did not include this suite step | Row 1.23. The push waits on 60/60. A composition-root edit that adds or moves a `getInstance` call belongs with `validate:db-claim-order` in the row's own checks                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F-T2-22 | Step 1's PR boundary passes on `6d8b0ddf`, and `main` moved while it ran                                                         | Build and probe: (a) clean STDIO start 0 B; (b) `--verbose` 625 lines; (c) one `[WARN] [CONFIG] analysis.semanticAnalysis is deprecated…` line, 0 B stdout; (d) all three base descriptions equal the contract, loader lines `[INFO]` only; (e) `chain_runs` 1, no persistence line; (f) one `[WARN]` schema line naming `/gates` and `enabld`. `typecheck` 0, `typecheck:scripts` 0, ratchets 364 and 3055 / 888; `test:all` unit 247 suites (3283 passed, 1 skipped), integration 67 (872), e2e 13 (234 passed, 2 skipped); `validate:all` 60 of 60; `git diff origin/main...HEAD` 21 files, all in slice. During the run `origin/main` moved from `267bc610` to `d59bffaf`, which this branch does not contain; required checks are strict                                                                                                                                                                                                                                  | Merge `main` before the push, so the PR does not open behind it and CI does not run twice                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| F-T2-23 | `main`'s #303 merged cleanly, and nothing it changed is something this branch extended                                           | `origin/main` moved to `d59bffaf` (#303, skills sync over MCP follows the server's workspace); merge commit `ddb2282d`, no conflicts. It overlapped this branch in three files: `runtime/module-initializer.ts` (+7: a `setSkillsSyncPathsProvider(() => resolveSkillsSyncPaths(pathResolver))` call after `updateData` — no `SqliteEngine.getInstance`, and no change to the `openToolsDatabase` or `createMcpToolsManager` calls rows 1.15 and 1.23 extended), `mcp/tools/index.ts` (+12: a new `setSkillsSyncPathsProvider` method; `initialize` and `createMcpToolRouter`, where 1.15 added `databasePort`, keep their signatures) and `CHANGELOG.md` (+1 under `### Fixed`, below this branch's three entries). Its only other signature change is an optional `skillsSyncPaths` field in `system-control/core/types.ts`                                                                                                                                                  | Checked per the clean-merge rule: no signature this branch passes new arguments to changed. The full boundary reruns on the merged head before the push, since #303 adds an e2e test and touches the composition root                                                                                                                                                                                                                                                                                                                                                                              |
| F-T2-24 | A third boundary pass, a third `main` move, and a change in how integration with `main` is verified                              | Boundary on the merged head `ddb2282d` (plans commit `d7333260` on top): probe (a)–(f) unchanged; `typecheck` 0, `typecheck:scripts` 0, ratchets 364 and 3055 / 888; `test:all` unit 247 suites (3283 passed, 1 skipped), integration 67 (872), e2e 14 (236 passed, 2 skipped, #303's new test included); `validate:all` 60 of 60. During the run `main` moved to `6fc40cbe` (#305), the third move in about an hour. Its overlap: `CHANGELOG.md` (conflict, row 1.24) and `prompt-executor.ts` (auto-merged: #305 replaces the parser's framework-id set with a lookup, away from 1.15's constructor parameter; typecheck 0, tests typecheck 364, 24 unit tests). Merge commit `55cda489`                                                                                                                                                                                                                                                                                     | Ruling: a full local rerun after every merge from `main` loses the race with a busy `main`. The local boundary has passed three times on this branch's own code, so from here CI verifies the merged head, and a new `main` commit that touches a file this branch changed gets targeted local checks (typecheck, the tests typecheck ratchet, the nearest unit tests) before the push                                                                                                                                                                                                             |
| F-T2-25 | Step 1 shipped; `main` moved a fourth time during its PR, and the merge guard needs a full SHA                                   | PR #307 opened on `b8e65532`: all 11 checks passed, but `mergeStateStatus` was BEHIND because #306 (`4f28604b`) landed mid-run. #306 overlapped this branch in one file, `infra/config/index.ts` (`getGatesConfig(): GatesConfig` → `GateSystemSettings`, plus an `executeInlineGateDefinitions` key); merge commit `0ce43e85`, no conflicts, and the clean-merge check found only untyped `getGatesConfig()` callers among this branch's files. Targeted checks on the merged head: `typecheck` 0, `typecheck:tests:ratchet` 364, the two config unit files 35 passed. CI green again on `0ce43e85`, merge state CLEAN, squash-merged as `e50095ac` at 22:35 UTC. `lint:ratchet` measured 3044 / 883 against the 3200 / 1020 baseline                                                                                                                                                                                                                                         | F-T2-24's ruling held: CI verified integration and only the overlapping file was checked locally. `gh pr merge --match-head-commit` takes the full 40-character SHA and refuses a short prefix before merging anything, so the guard costs a `gh pr view headRefOid` first. The ratchet slack widened again with #306 and stays `main`'s to regenerate, not this plan's work                                                                                                                                                                                                                       |
| F-T2-26 | Row 2.7's findings: dot-path reading is now the third implementation of the same walk, and the schema is read twice              | The worker's pre-flight `defined` probe found two generic dot-path readers already: `cli-shared/config-operations.ts`'s `getConfigValue(config, key)` and an inline reduce-based walker at `system-control/handlers/config-action-handler.ts:124`. Neither carries provenance or the env overrides. `.dependency-cruiser.cjs` declares no rule either way for `infra/` → `cli-shared/`, which sits outside the five declared layers, so the worker reported instead of restructuring. Its positive control (real `ConfigLoader`, temp config setting only `gates.enabled`, `PORT=9999`): `gates.enabled` → `file`, `frameworks.defaultFramework` → `default`, `server.port` → `environment` with value 9999 — three distinct labels in one run. `listConfigKeys()` returns 78 dot-paths from the packaged schema, excludes `$schema`, and throws when no schema path was injected rather than returning `[]`                                                                   | Row 2.8 deletes the handler's inline walker, leaving two: `cli-shared`'s reads a file, `infra`'s reads merged runtime state plus env, so they answer different questions and are not consolidated. The double schema read is row 2.12. Planner correction sent in the same round: the returned VALUE must come from the merged config with the raw file deciding only the source label, because `validateAndSetDefaults` replaces a malformed section wholesale and a file-first read would report a value the process is not using                                                                |
| F-T2-27 | `PORT` is parsed with no NaN guard, and every reader inherits it                                                                 | `getPort()` (`infra/config/index.ts:619`) does `parseInt(process.env['PORT'], 10)` with no validity check, so a non-numeric `PORT` resolves to `NaN`; 2.7's `getConfigValueWithSource('server.port')` faithfully reports `{ value: NaN, source: 'environment' }`, which matches what the server does rather than hiding it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Pre-existing, outside this initiative's slice and not introduced by any row here. Relayed to the owner as a defect row for whoever owns transport/runtime startup, alongside the ESLint ratchet slack                                                                                                                                                                                                                                                                                                                                                                                              |
| F-T2-28 | The read primitive ships with no production caller, by the owner's ruling, and must not read as live wiring                      | Rows 2.7 (`ba5de798`) and 2.13 (`729ca53a`) added `getConfigValueWithSource` and `listConfigKeys` to `ConfigManager`, and R41 then held `get` over MCP until step 4. `rg` for callers after row 2.8 lands: the unit tests (`config-value-source.test.ts`, 5 cases) and the literal-typed mock, plus `listConfigKeys` used by the handler's `keys` operation — `getConfigValueWithSource` itself has NO production consumer until step 4's row consumes it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Deliberate, not an oversight: the primitive is what step 4's generated shape reads, it carries its own enumeration gate, and re-deriving it there would repeat a measured round including the malformed-section correction. Stated in step 2's PR body so a reviewer is not left to infer it. Flips when a step 4 row calls `getConfigValueWithSource` in production, or when step 4 lands without one — at which point the member is dead and gets deleted with it                                                                                                                                |
| F-T2-29 | Spying on a Node builtin does not work in this repo's ESM jest suites, and one existing test reads as if it does                 | Row 2.12's worker, proving the shared schema cache by counting `readFile` calls: under `ts-jest/presets/default-esm` with `--experimental-vm-modules`, `jest.spyOn(nsObject, 'readFile')` throws `Cannot assign to read only property`, and a hoisted `jest.mock('node:fs/promises', factory)` silently leaves the real function in place — confirmed by `typeof nodeFsPromises.readFile.mockClear === 'undefined'` after an apparently-passing suite. The only mechanism that intercepts is `jest.unstable_mockModule(specifier, factory)` plus a dynamic `import()` of the module under test AFTER registering the mock, scoped with `jest.resetModules()`. The worker also flagged `tests/integration/scripts/script-tools-workflow.test.ts`, which uses the hoisted `jest.mock('fs', …)` form and therefore may intercept nothing — its mocked functions may simply never be exercised                                                                                     | Two consequences. For this plan: row 2.12's read-count test uses the working mechanism, and its mutation proof (1 read expected, 2 observed when the cache is bypassed) is what shows it observes anything at all. Relayed, not this plan's work: that integration test's mock is a candidate false-positive of exactly the shape this repo has recorded before — a check that cannot fail reads as coverage. Worth a project-level testing note so the next "spy on fs" attempt does not rediscover the dead end                                                                                  |
| F-T2-30 | A committed row left open cost a worker a dependency check, which is this repo's own satisfied-marker rule turned on the planner | Row 2.13 was committed as `729ca53a` and its marker was not flipped for four commits. Row 2.8's worker read `Depends: 2.7, 2.13` against an open 2.13, could not tell whether it was blocked, and spent part of its round reconciling the table with `rg` before proceeding — it raised this under `concerns` rather than guessing silently                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | The lapse is the planner's, not the brief's: a `✓` is written when the commit lands, in the same turn, or the plan stops being the state machine the workers read. Second half, from the same handoff: when a row is RE-SENT after an interruption, the message states whether its listed dependencies are satisfied, because a resumed worker re-reads the table from scratch                                                                                                                                                                                                                     |
| F-T2-31 | The wire-level input-schema snapshot cannot be refreshed by any row under shared-tree rules                                      | `scripts/capture-tool-schemas.mjs` (run by `capture:tool-schemas` / `validate:tool-schemas`) requires `dist/index.js` and exits early with "run `npm run build` first"; shared-tree workers are forbidden to build `dist/`. Row 2.8 therefore regenerated `_generated/` (source-level, `validate:contracts` exit 0) but left `tests/snapshots/mcp-input-schemas.json` stale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Consistent with this repo's own CI routing, which puts post-build schema gates at the PR boundary rather than per row. The planner owns it: build once on the green snapshot, run the capture, and commit the refreshed snapshot with the boundary. Until then the snapshot shows drift, and a row that stops at `validate:contracts` has not verified the wire shape                                                                                                                                                                                                                              |
| F-T2-32 | Step 2's boundary drive: the surface refuses at TWO layers, and only a `tools/call` drive shows which                            | Built server over STDIO, fresh `MCP_RUNTIME_ROOT`, `MCP_RESOURCES_PATH`/`MCP_WORKSPACE`/`MCP_CONFIG_PATH` unset, `CI` and `NODE_ENV` unset, driven through `tools/call` (`/tmp/step2-probe.mjs`). Served: `list`, `keys` (78), `validate`. Refused by the HANDLER, naming the operation: `get`, `set`, `restore`, and `{action:'config'}` with none — which reports `(missing)` instead of the old whole-config dump. Refused one layer EARLIER by the registered schema, before the handler runs: the same call carrying the nested `config: { operation: 'get', … }`, because DEV-T2-3 narrowed that enum to `validate`. stderr 0 bytes, so step 1's clean-start guarantee survives. `verify:mcp` 18/18 on the rebuilt `dist/`                                                                                                                                                                                                                                               | The two layers are both closed, but they answer with DIFFERENT text, and the conformance scenarios assert the handler's wording — verified that they send the top-level-only shape, which reaches it. A direct handler drive (what the rows ran) cannot see this split at all: the registered Zod schema strips undeclared keys before a handler is reached, so `tools/call` is the only drive that shows what a client gets. `verify:mcp` refused to run first because `dist/` was 10 minutes older than `src` — a green result against a stale build would have been a lie, and the gate said so |

## Proposed delivery order (2026-09-15, pending owner confirmation)

One PR per step, all before release PR #232 merges (R31). The generated authority comes before any hand reconciliation, because R31 puts the 5.0 reshapes in the same release: rows 2.1–2.4 would hand-edit a CLI key list that row 4.4 then generates.

| Step | Outcome                                                                       | Rulings              | Plan rows                                                         | Breaking                                                  |
| ---- | ----------------------------------------------------------------------------- | -------------------- | ----------------------------------------------------------------- | --------------------------------------------------------- |
| 1    | A clean STDIO start emits no false warnings, then `warn`/`error` reach stderr | R28                  | new rows per F-T2-1 warning, F-T2-2, the `DEBUG:` lines; then 1.8 | no                                                        |
| 2    | `system_control` config reads over MCP; writes stay in `cpm`                  | R27                  | 2.5                                                               | no — both removed operations are unreachable today        |
| 3    | One authority describes the 5.0 on-disk shape                                 | R30, R32, T5.1, T5.2 | 4.1–4.5, 5.1, 5.2, 5.4; absorbs 2.1–2.4; replaces 2.6             | yes                                                       |
| 4    | A 4.x config keeps working in 5.0                                             | R33, T5.3            | 5.3                                                               | no — it is what keeps step 3 from breaking existing files |
| 5    | The user's config is `config.jsonc` with generated examples                   | R34                  | new rows                                                          | yes — file name                                           |
| 6    | Editors validate a config anywhere                                            | R29                  | 3.1–3.3                                                           | no                                                        |

## Delivery step 1 — baseline before rows 1.13–1.16 (2026-09-15)

Measured with a throwaway probe against the worktree's `dist/`, built before any step-1 edit (workers do not build). Environment: `MCP_*` unset except a fresh `MCP_RUNTIME_ROOT`; `CI` and `NODE_ENV` unset. STDIO runs hold stdin open 7 s, then close it. The same probe re-runs on one build at step 1's PR boundary.

| Probe                                                                                | Measures                                                   | Before                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------ | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (a) clean STDIO start                                                                | bytes and lines on stderr                                  | 550 B, 13 lines, all `DEBUG:` (row 1.16)                                                                                                                                                                                                                                                                                                |
| (b) STDIO with `--verbose`                                                           | the startup trace stays reachable                          | 2431 B, 46 lines, 21 of them `DEBUG:`                                                                                                                                                                                                                                                                                                   |
| (c) STDIO with an `analysis` section in the config                                   | a `logger.warn` reaching stderr (row 1.8's control)        | 0 deprecation notices; stderr identical to (a)                                                                                                                                                                                                                                                                                          |
| (d) HTTP `tools/list` with `frameworks.dynamicToolDescriptions: false`               | whether the base descriptions come from the contracts file | all three are the in-memory defaults (`prompt_engine` opens "Executes prompts/chains with % modifiers"; the contract opens "Execute prompts with framework guidance"). Two `ToolDescriptionLoader` warnings name a path under the config file's own directory, so under `MCP_CONFIG_PATH` the disk path could never be right (row 1.13) |
| (e) HTTP chain step (`>>scaffold_project`), rows read back from the run's `state.db` | chain persistence and the no-port warning                  | `chain_runs` 1, `chain_run_nodes` 3, `chain_sessions` 1 — persistence works — beside one false `ChainSessionStore: no DatabasePort provided, persistence disabled` (row 1.15)                                                                                                                                                           |

Expected after step 1: (a) 0 B; (b) trace still present; (c) one deprecation notice once 1.8 lands; (d) all three from the contracts file, no loader warning; (e) the same rows, no persistence warning.

### After rows 1.13–1.16 (build of `bb732e31`)

Same probe and environment as the baseline above.

| Probe                      | Before                                                   | After                                                                               |
| -------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| (a) clean STDIO start      | 550 B, 13 `DEBUG:` lines                                 | 0 B                                                                                 |
| (b) `--verbose`            | 2431 B, 46 lines                                         | 2431 B, 46 lines — trace intact                                                     |
| (c) `analysis` section     | 0 deprecation notices                                    | 0 — expected until 1.8, and the negative half of its control                        |
| (d) base tool descriptions | all three in-memory defaults; two loader warnings        | all three equal the contract text; no loader warning                                |
| (e) chain step             | rows persisted, one false "persistence disabled" warning | the same rows (`chain_runs` 1, `chain_run_nodes` 3, `chain_sessions` 1); no warning |

1.14's warning is not observable in either run: the STDIO logger dropped it before 1.8, and the HTTP drives filter other lines. After 1.8, (a) staying at 0 B is its check.

## Delivery step 3 — cut (2026-09-16)

| #        | Deviation                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEV-T4-1 | Row 4.3 adds `ts-json-schema-generator` (2.9.0, Node ≥22) as a devDependency inside the worktree. The worktree's `server/node_modules` is a symlink into the main checkout, so the install lands there while the main checkout's lockfile does not carry it: `validate:lockfile-sync` on `main` reads red until this branch merges. Known cost, accepted over a per-worktree install                                                     |
| DEV-T4-2 | The slice spans six modules, over the 3-module subplanner trigger in `/claude-code` §Session-per-planner topology. Not opened as a subplanner: the rows form one dependency chain on `ConfigFile` and share files, so fan-out never exceeds two, and a subplanner would need this repo's own worktree bootstrap (CLAUDE.md forbids `--worktree` here). Recorded so the trigger's wording can distinguish depth from width if this recurs |

Worker tier declarations for the step-3 rows (surface: `Agent` tool, which binds `model` only; effort is this session's):

| Row | Tier   | Effort | Failure shape                                                                                                                                                  |
| --- | ------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4.1 | opus   | high   | wrong approach — the file shape is derived from two files the row does not quote, and a file/runtime boundary drawn wrong re-creates the root cause            |
| 4.3 | sonnet | high   | wrong output — the generator's options are decided (draft-07, per-object `additionalProperties:false`, JSDoc tags); the risk is a drift check that cannot fail |
| 4.2 | opus   | high   | wrong approach — `normalize` is the mapping the whole plan exists to make checkable; a cast that survives in a new spelling is the failure                     |
| 4.4 | sonnet | high   | wrong output — R44 decides the form; the risk is a table that omits a constraint the switch enforced                                                           |
| 4.6 | sonnet | high   | wrong output — values are named by F-T2-4; the risk is a getter default left disagreeing                                                                       |
| 4.7 | sonnet | high   | wrong output — the enumeration is an `rg`; the risk is a consumer left reading a removed field through `?.`                                                    |
| 4.5 | sonnet | medium | wrong output — the target file is two keys; the risk is a fixture that copied the old shape                                                                    |
| 4.8 | sonnet | medium | wrong output — prose row with named keys                                                                                                                       |

### Findings and feedback from row 4.1 (2026-09-16)

| #        | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-T4-1   | `DEFAULT_CONFIG.server` is the stale outlier: `name` `'Claude Custom Prompts'` and `port` 3456 (`infra/config/index.ts:259,261`) disagree with BOTH the shipped file and the hand-written schema (`claude-prompts`, 9090). `frameworks.injection.systemPrompt.frequency` is the reverse: schema and `DEFAULT_FRAMEWORKS_CONFIG` say 2, the shipped file 3, and per R32 the shipped value wins. Row 4.6 aligns all three                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| F-T4-2   | Two keys the 4.1 brief listed for removal do not exist: `gates.mode` (row 0.1 already deleted it) and root `transport` (runtime-only, fed from `server.transport` and the flag). Each cost the worker a full pass to confirm absence. A removal list built by the same leaf enumeration the row demands would not carry them — planner error, captured for the next brief                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| F-T4-3   | 13 leaves are `integer` in the hand-written schema; a TypeScript `number` regenerates as `number`. Row 4.3 must use the generator's integer mechanism or report the widening. The worker declined to ship an unverified tag because the generator was not installed in the worktree — correct, and the reason 4.3 installs it first                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| F-T4-4   | `no-orphans` (`.dependency-cruiser.cjs:309`) is a standing constraint on `config-file.ts`: with zero consumers, its one import is what keeps `validate:arch` green until row 4.2 imports it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F-T4-5   | Supersedes DEV-T4-1's prediction: `npm install` of a new devDependency in the worktree did not write through the `server/node_modules` symlink — npm replaced the symlink with a real directory, leaving the main checkout untouched. `validate:lockfile-sync` on `main` is NOT affected. The worktree now has its own `server/node_modules`; a later `npm ci` there is safe                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| F-T4-6   | `ts-json-schema-generator` 2.9.0: `sortProps` is read only by the CLI entry, never by `createSchema()` (doc-vs-behaviour gap in the library's `Config.ts`); the default `expose: 'export'` emits `$ref` + `definitions`, which `validate-config-schema.ts`'s self-test walker does not resolve — the generator uses `expose: 'none'`, `topRef: false` to keep the shipped schema's inlined shape                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| F-T4-7   | `config-file.ts` exports 25 section interfaces with no importer outside the file, and the knip ratchet reads `types +21` against the `f2e07a45` baseline. Row 4.6 trims the exports once 4.2 shows which ones the loader imports                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| F-T4-8   | 4.2 reported `listConfigKeys` under-reporting because the generated schema used `$ref`; measured on the committed schema (a4cf7a5b): zero `$ref`, 69 leaves, `collectSchemaKeys` walks `properties` and sees all of them. The worker measured an interim regeneration from before 4.3 settled on `expose: 'none'`. No row                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| F-T4-9   | The `gates.directory` → `definitionsDirectory` rename stays in `getGatesConfig()`, not in `normalize`: moving it changes the key `system_control config list` prints and the key the writer would persist. `config-file.ts:8` describes the rename as the loader's; that sentence stays true at the getter                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| F-T4-10  | Three brief defects from 4.2's feedback, each a pattern for the next brief: (1) point 2 was unsatisfiable while `core-config.ts` was off-limits — a brief that forbids a file must say what a worker may add locally instead; (2) a shared-tree row's test baseline moves under it — measure the baseline with the mutation probe BEFORE the first edit, by swapping `git show HEAD:<file>` into place; (3) `npx jest` on this repo's unit tests needs `NODE_OPTIONS="--experimental-vm-modules"`, and `typecheck:tests:ratchet` belongs in the verification list of any row that adds a test file — it caught 4.2's one real type gap                                                                                                                                                                                                                                                  |
| F-T4-11  | `ts-json-schema-generator` 2.9.0 in `jsDoc: 'extended'` mode honours a NATIVE `@asType <text>` tag (`ExtendedAnnotationsReader.getTypeAnnotation`) that overwrites the emitted `type`, independent of `extraTags` — the brief's post-pass design was unbuildable because the rewrite it planned had already happened, and its `if (type !== 'number') throw` fired on every tagged node. Registering `extraTags: ['asType']` alongside would leak a redundant `asType` key into the shipped schema. The row's AST guard checks the tag against the TypeScript member type instead, the only place that information survives. Worth a `/docs` note for the next generator in this repo                                                                                                                                                                                                   |
| F-T4-12  | The drift check sits inside `validate:config-schema` rather than as `--check` on the generator, which is the shape `generate-contracts`, `generate-gate-index` and `generate-framework-schemas` use. Accepted as-is: the validator step is already the SUITE member CI runs, and the temp-file compare exercises the real write path. A consolidation to the sibling shape is not a row until a fourth generator appears                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| F-T4-13  | context7 (`/docs`) was not reachable from the worker's session; it verified against the installed package's compiled source instead. Briefs that require `/docs` should name that fallback explicitly                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| F-T4-14  | `GatesConfig.definitionsDirectory` is dead at runtime: `rg -n "\.definitionsDirectory\b" src` finds only its own assignment in `getGatesConfig()`; the gates directory is resolved by `getGatesDirectory()` via `resourcePaths.getGatesPath()`. 4.6 aligned its default to `resources/gates`; row 4.5 deletes it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| F-T4-15  | `prompts.registerWithMcp` has no `ConfigManager`-level default: the shipped `true` is a fallback three layers away in `modules/prompts/converter.ts:44` (`resolveRegisterWithMcp`), so the getter-surface equality test excludes it. Folding it into `ConfigManager` flips its `getConfigValueWithSource` label from `deferred` to `default`. Not a row in this step; noted for step 4's translation, which reads every key's source                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| F-T4-16  | 4.6's brief listed the row's own trip-wire test (`config-file-normalize.test.ts`, whose comment named row 4.6 as its trigger) nowhere, and named a hook test that did not exist. Before finalizing a defaults row's file list, `rg` the shipped literals across `tests/` and `hooks/tests/`, not only `src/`; and a brief that names a test should first confirm the file exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| F-T4-17  | The raw `Config` two loaders produce is never deep-equal even when defaults align: `normalizeConfigFile` carries `gates`, `resources`, `logging`, `phaseGuards`, `verification`, `identity` only when the file sets them (`undefined` otherwise), which is the documented `deferred`/`default` distinction in `config-manager.ts`. "The shipped file holds no override" is true and testable at the `ConfigManager` getter surface, which is where 4.6's test measures it                                                                                                                                                                                                                                                                                                                                                                                                               |
| F-T4-18  | The 5.0 settable surface is a different SET, not a rename: gone `server.transport`, `gates.enforcePendingVerdict`, `resources.prompts.defaultRegistration`, snake_case versioning, `advanced.sessions.*`, the flat injection keys, `identity.mode: 'strict'`; new `frameworks.defaultFramework`, `frameworks.injection.*` (9), `gates.evaluation.*` (3), `telemetry.attributePolicy.allowlist`, `chainSessions.*` (3). Row 4.8 carries this into `docs/` and the CHANGELOG                                                                                                                                                                                                                                                                                                                                                                                                              |
| F-T4-19  | `getKeyTypeInfo` in `config-operations.ts` was a THIRD hand-kept key-metadata list, stale both ways (`telemetry.mode` described as on/off/auto against the real off/traces/full; a retired `identity.mode: 'strict'`; suffix rules for keys that no longer exist). 4.4 replaced it with a table-derived `describeLeaf`. Three lists for one question was the shape; the generator closes the class                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| F-T4-20  | Enum matching is now case-insensitive across every enum key (`logging.level` accepts `INFO`; the old switch was case-sensitive there). Row 4.5 verifies the STORED value is the canonical enum member                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| F-T4-21  | The standalone CLI suite leaves `cli/tests/fixtures/.tmp-<epoch>/` behind on every run, untracked, so no gate sees it — the class commit 7a246763 closed for `server/`, not for `cli/`. Relayed to the hermetic-test-env plan (`fix/hermetic-test-env`), not a row here. Also: `cli`'s 68 integration tests need `npm run build` first                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F-T4-22  | A generator can emit prettier-clean TypeScript (newline after every `{`, string arrays inline only under 100 columns), so `_generated/config-keys.ts` needs no `.prettierignore` entry. `knip.json` does not yet list `src/cli-shared/_generated/**`; nothing is flagged today, so no row                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| F-T4-23  | The pre-commit guard (`Direct edits to generated files detected!`) refuses any staged `_generated/` path and names `generate:contracts` as the only generator — a location-keyed gate that predates a second generator (the pattern of `feedback_gate_scope_vs_new_construct`). Row 4.11 turns it into a registry keyed by generator                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| F-T4-24  | A registry row packed as one `"script                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | regex | paths"`string and split on` | `truncates a source regex that is itself an alternation — the worker hit`grep: Unmatched (`and moved to a`sh` function setting three variables. A delimiter that can appear inside a field is not a delimiter; worth remembering for the next shell registry |
| F-T4-25  | Commit order at the end of step 3 is forced by the hook: `.husky/pre-commit` itself, then 4.4's artifacts (now accepted by the registry), then 4.5, then 4.7 — and every one of them runs `typecheck`, so none can land until 4.7's `Config.transport` removal compiles. Rows 4.4 and 4.11 therefore sit uncommitted in the shared tree longer than their handoffs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| F-T4-26  | R46 has a consumer class the row text did not name: anything that builds a fixture by MUTATING the shipped `config.json` as if it were a full example. Two instances — the `validate:config-schema` self-test (4 of 5 `mutate(shipped, …)` sites) and `config-schema-warning.test.ts` — threw `Cannot set properties of undefined` once the file held two keys. Both now initialise the section they mutate. Enumeration for the class: `rg -n "config\.json" server/tests server/scripts` read for mutation, not mention; no third instance found                                                                                                                                                                                                                                                                                                                                      |
| F-T4-27  | Prettier pads table row ids to the widest id in the table, so once `4.11` existed every `                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 4.5   | `anchor became`             | 4.5                                                                                                                                                                                                                                                          | `and a fixed-string row lookup failed silently-then-loudly. Row anchors in writeback scripts match`^\| 4\.5\s+\|` from here on |
| F-T4-28  | `--transport` has two parsers: `parseServerCliArgs` handles `--transport value` and `--transport=value`; `TransportRouter.determineTransport` (and 4.7's `getTransportMode()`) scan argv for the `=` form only. The space form therefore quiets the logger and still serves STDIO — a latent defect older than this branch, in the exact claim R30 makes. Row 4.12                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| F-T4-29  | `src/index.ts` `main()` cognitive complexity 34 → 36 with the added `TransportConfigError` arm mirroring `PathSettingError`; over the ≤15 limit before this row. Not decomposed here; an extraction of the entry point's error-classification arms would close both. Not a row in this step                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F-T4-30  | Five stale runtime comments still described pre-R30 behaviour ("CLI args > config.transport > default") after the schema/type/CLI side had moved — a partial cleanup lands the artifact and leaves its prose. 4.7 rewrote them; the pattern is the `cleanup-standards` rewrite-current-state rule applied to comments                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| F-T4-31  | The AGENTS.md projection is at 32767 of 32768 bytes after 4.8's one handbook sentence. The Command Reference rows for `generate:config-schema` and `validate:config-schema` do not fit, and the next projected-section edit of any size fails `guidance:check`. Eviction from `PROJECTED_HANDBOOK_SECTIONS` is the owner call already recorded at #317; until it is made, handbook facts from this arc land in `CONTRIBUTING.md` or non-projected sections. A planner cutting rows that touch CLAUDE.md budget-checks the projection first                                                                                                                                                                                                                                                                                                                                              |
| F-T4-32  | A worker restored a mutation probe with `git checkout -- <file>` and wiped its own uncommitted edits to that file (caught and redone). Brief pattern: when the file carries uncommitted work from the same row, a probe is removed by editing out the inserted text, never by `git checkout`/`git stash`/`git restore`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F-T4-33  | 4.12's brief named the design ("the router receives the option") but not the router's callers (`runtime/context.ts`, `runtime/startup-server.ts`), which were therefore out of the file grant; and `infra/` cannot import `runtime/cli.ts` (`no-imports-into-runtime`), so the router cannot fetch the parsed value itself. The bug is fixed and the loader's scan is gone; the router's scan is row 4.13 — the third brief in this step whose file list omitted a caller the design needed (`feedback_worker_brief_facts`: trace callers before cutting the file list)                                                                                                                                                                                                                                                                                                                 |
| DEV-T4-3 | Row 4.12's worker ran `git stash` / `git stash pop` once against the brief's prohibition, in a tree carrying row 4.8's uncommitted docs edits; it verified the stash list empty and every file intact afterwards, and 4.8's edits later committed unchanged (31cc3380). No loss; recorded because the prohibition exists for exactly this state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| F-T4-34  | R46's verification sentence ("answers `list` identically") is false at the `list` level and true at the getter surface. Measured on a built server over HTTP, hermetic env: the two-key file and the full-value 5.0 file (45e52509) agree on every key both list (server, prompts.directory, frameworks incl. nested injection, chainSessions, versioning, telemetry, execution, the deprecated analysis defaults); the two-key answer omits `gates`, `phaseGuards`, `logging`, `verification`, `prompts.registerWithMcp` because `normalizeConfigFile` carries those only when the file sets them — the `deferred` source label (F-T4-17). A 4.x file draws the schema warnings R43 predicted; a file setting `server.transport: streamable-http` exits 1 naming `--transport=streamable-http`. `list` resolving every default is step 4's decision with `get` (R41), not a step-3 row |
| F-T4-35  | Two boundary-only failures on the merged tree, both test surfaces: `tests/e2e/conformance/tool-surface.yaml` pins the OLD validator message for `logging.level` (the table-driven validator says `must be one of: error, warn, info, debug`); `tests/e2e/framework-selection-lifecycle.e2e.test.ts:204` copies the shipped `config.json` and sets `config.frameworks.defaultFramework` — the THIRD instance of the F-T4-26 class, which the earlier enumeration missed because it searched for schema validation, not for copies of the shipped file. New enumeration: every `config.json` read under `server/tests/e2e` and `server/tests/integration` followed by a property set. Also `validate:module-catalog` drifted (mcp-tools → cli-shared edge); regenerated and committed                                                                                                     |
| F-T4-36  | `~/.claude/hooks/validation/pr-body-guard.py` runs `pr:check` in the SESSION cwd (the main checkout), not in the worktree the PR is cut from. There `merge-base(origin/HEAD, HEAD) == HEAD` whenever the checkout sits on or behind `origin/main`, which the validator reports as "vacuous" (its shallow-clone message) — a false negative for every worktree PR. #317 passed only because that checkout predated `scripts/pr-check.mjs`. Worked around by detaching the main checkout at the branch tip for the create; the validator should distinguish "HEAD is an ancestor of the base" from "shallow", and the hook should run in the repo of the branch named by the command's `cd`                                                                                                                                                                                               |

## Delivery step 4 — cut (2026-09-16)

Branch `feat/config-4x-translation` from `main` `914b068c`, main checkout (R56). Rows 6.1–6.5; T5 row 5.3 executed by 6.1 + 6.4. Re-measured before cutting: 4.x leaf list from `git show 83196fcd:server/config.schema.json` (78 leaves + `$schema`), 5.0 leaf list from `server/config.schema.json` (67 leaves + `version`); `adoptInertSpellings` table 13 entries at `index.ts:74-104`; `normalizeConfigFile` returns `undefined` for `gates`, `phaseGuards`, `logging`, `resources` when the file omits them and passes `verification`/`identity` raw (`:416-419, :536-539`); read-time `??` defaults at 19 sites in the getters plus three literals outside the loader (`pipeline-builder.ts:380`, `converter.ts:44`, identity resolver); `.analysis` has zero readers in `src` outside the loader; `hooks/lib/config_loader.py` reads only `hooks.expandedOutput`.

Worker tier declarations (surface: `Agent`, binds `model` only; `branch_mode: shared-tree`):

| Row | Tier   | Effort | Failure shape                                                                                                                                                                                    |
| --- | ------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 6.1 | opus   | high   | wrong approach — the translation table is derived from two schema leaf lists the brief quotes, but the ordering against the schema check and the snapshot, and what the notice names, are design |
| 6.3 | sonnet | high   | wrong output — shape and refusals ruled (R37, R59); the risk is contract, Zod schema, generated metadata, snapshot and conformance drifting apart, which this repo gates                         |
| 6.2 | opus   | high   | wrong approach — which layer owns each default across eight getters and three out-of-loader literals, and `Config`'s optionality is a contract over 20+ readers                                  |
| 6.4 | sonnet | medium | wrong output — a named construct deleted across a measured list; the risk is touching the homonymous `semanticAnalysis` result field                                                             |
| 6.5 | sonnet | medium | wrong output — the entries are enumerated in the row; the risk is session vocabulary leaking into release notes                                                                                  |

### Findings from row 6.3 (2026-09-16)

| #       | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F-T4-37 | `get` on a `deferred` key renders `= undefined`, because `JSON.stringify(undefined)` is the JS value `undefined` and the template coerces it to the word. Honest but not JSON. Row 6.2 retires `deferred`; the keys R57 leaves legitimately undefined (`gates.evaluation.defaultModel`, `telemetry.attributePolicy.allowlist`, `identity.launchDefaults.*`) still hit this line — 6.2 renders them as `null`-free prose ("not set") rather than a non-JSON token, or reports why not |
| F-T4-38 | `lint:ratchet` read `import-x/order` 6 → 7 during 6.3, traced to row 6.1's in-flight import of `./config-file-translation.js` in `index.ts` — a shared-tree cross-talk the worker attributed correctly instead of fixing a file it was barred from. Re-checked at 6.1's acceptance                                                                                                                                                                                                   |
| F-T4-39 | Committing 6.3 while 6.1 was mid-row succeeded only because 6.1's in-progress `index.ts` typechecked at that moment (pre-commit typechecks the whole tree, F-T4-25). Planner luck, not design: a shared tree should commit a finished row while the other row is at a typecheck-clean point, or wait                                                                                                                                                                                 |

### Findings and deviations from row 6.1 (2026-09-16)

| #        | Finding / deviation                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| DEV-T4-4 | The brief said to delete the `getConfigValueWithSource` raw-file fallback as dead once the snapshot is post-translation. The worker measured a second consumer the comment's NAME hid: `hooks.expandedOutput`, a file key the runtime `Config` has no member for — deleting the fallback would report a live hook setting as `undefined`/`file`. Kept, comment rewritten to name that consumer. Accepted: the brief's reason was right and its scope wrong (feedback captured below) |
| F-T4-40  | The 4.x SCHEMA spelled versioning `max_versions`/`auto_version`; the shipped 4.x `config.json` spelled it camelCase, disagreeing with its own schema. So the shipped-file fixture translates nothing under `versioning`, and the snake_case move is exercised only by hand-built fixtures. The brief's prose had the direction backwards while its table was right; the worker ran the schema diff to decide                                                                         |
| F-T4-41  | The shipped 4.x file's `dropped` list is exactly `['server.transport']`: it carries no `analysis`, `server.version`, `enforcePendingVerdict` or `defaultRegistration`, so the analysis-notice branch is covered by hand-built fixtures only                                                                                                                                                                                                                                          |
| F-T4-42  | A `version: 5` file carrying 4.x flat keys draws TWO schema warnings (AJV reports `/advanced` and `/frameworks` per instance path), not the one the brief predicted. A brief should name expected substrings and let the worker measure the count                                                                                                                                                                                                                                    |
| F-T4-43  | The once-per-process notice is once-per-`ConfigLoader` — the same shape as the flag it replaces, and the server constructs one loader, so behaviour is as ruled; the doc comment says "process". Not a row                                                                                                                                                                                                                                                                           |
| F-T4-44  | The knip ratchet baseline is 4 findings loose after 6.1 (exports −2, types −2); the PR boundary regenerates it on the final tree, per `feedback_ratchet_ceiling_slack` (re-measure on the merged tree, not per row)                                                                                                                                                                                                                                                                  |

### Findings from row 6.4 (2026-09-16)

| #        | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-T4-45  | The row's flip condition ("the `rg` is empty under `server/tests`") was unsatisfiable as written: the two config tests keep `analysis.semanticAnalysis…` as 4.x fixture literals because they test the drop itself. An enumeration predicate for a deletion must exclude the fixtures that exercise the deletion, or say "type surface" — the worker reconciled it correctly                                                                                                                                                          |
| F-T4-46  | Three tests outside the row (`content-analyzer.test.ts`, `consolidated-tools.test.ts`, `prompt-engine-validation.test.ts`) still mock a `getConfig`/`llmIntegration` shape `ContentAnalyzer` no longer exposes — untyped or `as any`, so no gate sees them. Not a row in this step; candidate for the hermetic/fixture cleanup arc                                                                                                                                                                                                    |
| DEV-T4-5 | Planner: row 6.5 was dispatched while 6.2 was still running, against 6.5's own `Depends: 6.1, 6.2, 6.3, 6.4`. Reason: 6.5's entries derive from rulings R57/R59, not from 6.2's diff, and the file sets are disjoint (`CHANGELOG.md`, `docs/` vs `server/`). If 6.2 comes back with a user-visible deviation, 6.5's `list` sentence is re-checked at acceptance                                                                                                                                                                       |
| F-T4-47  | `>>documentation_change` invoked without `doc_type`/`target`/`audience` loops on "Unknown Document Type" across both gate rounds and never yields content guidance; the 6.5 worker did the file work from source and closed the chain. Either the prompt requires those arguments up front, or doc rows go to `strategic_worker` when the call site cannot supply them. Also: the chain's pending gate blocked THIS session's stop hook while the worker held it — a shared-server chain is visible to every session in the workspace |

### Findings and deviations from row 6.2 (2026-09-16)

| #        | Finding / deviation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEV-T4-6 | `PromptConverter`'s third constructor parameter stays optional, defaulting to `DEFAULT_PROMPTS_CONFIG.registerWithMcp` (one declared home, referenced not restated), because making it required breaks 6 integration-test call sites outside the row. The module-private `resolveRegisterWithMcp` parameter IS required. Accepted                                                                                                                                                                                                                                   |
| F-T4-48  | The pinned undefined set measured 13, not the 8 predicted from `@default` tags: +3 rename orphans (R60), +1 `hooks.expandedOutput` (R63), +1 `gates.evaluation.strict` (R61 — its tag was false). "Measure, and a disagreement is a finding" caught all five; the brief's prediction from tags alone was the wrong probe for the property                                                                                                                                                                                                                           |
| F-T4-49  | The identity stage silently discarded launch defaults on every ordinary launch: `applyRuntimeIdentityOverrides` set `identity.mode` only under `--identity-mode`, so `pipeline-builder`'s guard returned `null` and `03-identity-resolution-stage` resolved with no launch defaults and no transport — continuity scope fell to `'default'`. Fixed as a consequence of 6.2 (R62)                                                                                                                                                                                    |
| F-T4-50  | Required `Config` sections have a mechanical blast radius outside the row: 16 now-dead `?.` in 5 runtime files, 8 tests-typecheck errors in 3 fixtures, 1 falsified assertion, 2 dead literals in pipeline stages (`19-phase-guard-verification-stage.ts:67`, `03-identity-resolution-stage.ts:88-91`). A brief that mandates required members should grant "any file whose only change is a dead `?` or a completed fixture", or declare the ratchet regression the expected handoff shape — the worker had to choose between the constraint and the gate. Row 6.8 |
| F-T4-51  | `config-defaults.test.ts` proves "the shipped file holds no override" (two loads of the same code) and is structurally blind to a changed default; only `config-file-normalize.test.ts`'s hand-written `DEFAULTS` literal pins the values. The brief named the wrong observer for mutation 2                                                                                                                                                                                                                                                                        |
| F-T4-52  | The brief located the identity fallbacks at a caller's line (`pipeline-builder.ts:126-131`) three layers from the symbol (`toIdentityContext`); the resolver's options type already required both fields, so the question had no yes/no answer. Name the symbol, not a caller's line                                                                                                                                                                                                                                                                                |
| F-T4-53  | CLAUDE.md §Runtime State's `'default'`-bucket residual paragraph is partly stale after R62 (the pipeline path no longer falls to `'default'` when a workspace derives). Projected section, 1 byte of headroom (F-T4-31): not edited; the owner's eviction call unblocks it                                                                                                                                                                                                                                                                                          |

### Findings from row 6.8, first half (2026-09-16)

| #       | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-T4-54 | A single `tsc` pass reports one diagnostic per object literal (the first missing member), so an error count taken from one pass undercounts a fixture's distance from a tightened interface: `context.identity-overrides.test.ts` was briefed as 4 errors and needed all 13 `Config` sections. Brief-authoring rule: re-check after the first fix before quoting a count, or brief the shape ("complete the fixture to a full `Config`") instead of the count                                                 |
| F-T4-55 | Mocks cast `as any` / `as ConfigManager` bypass both `typecheck` and the tests-typecheck ratchet, so a required member surfaces there only at RUN time: 16 unit failures in `consolidated-tools.test.ts` and `prompt-engine-validation.test.ts` once `prompt-executor.ts:203` stopped guarding `identity`. The lint gate caught the real call sites, the test gate caught the doubles; neither static gate could. `npm run test:unit` belongs in the verification list of any row that tightens a shared type |

### Findings from row 6.7 (2026-09-16)

| #       | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-T4-56 | `GatesConfig.directory` on the runtime type (`core-config.ts`) is now written from a placeholder constant and read by nothing; row 6.6 deletes it with the placeholder, and rewrites the one normalize test that still sets it. The brief said "leave it optional and unset" about a required member on a file outside the grant — an instruction that could not be followed without failing `typecheck`; say "report it" when the member is out of grant |
| F-T4-57 | `config-file.ts`'s header cited a `gates.directory` → `definitionsDirectory` rename as its example of a loader rename; no such rename ever existed in code. Doc-comment examples in that file deserve a sweep against the code they illustrate (not a row in this step)                                                                                                                                                                                   |
