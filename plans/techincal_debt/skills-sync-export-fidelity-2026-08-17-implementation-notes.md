---
title: "Implementation notes — Skills Sync export fidelity"
date: 2026-08-17
status: backlog
tags: []
---

# Implementation notes — Skills Sync export fidelity

Deviation log for `skills-sync-export-fidelity-2026-08-17.md`. Created before the first edit, per
the standing rule that this artifact has no gate and is otherwise written at the end, when it is
worth least.

Workers: log under `## Deviations` as they happen, not at the end. Take the conservative option and
keep going; the log is the record, not a request for permission.

## Rulings

Record `>>diagnose` rulings on the plan's Open Questions here, with the evidence that settled them.
Flip the plan row to RULED in the same edit.

| Id  | Ruling                                                                                                                                                                                                                                                                                                                                 | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Date       |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| Q1  | **RULED — the RUNTIME is authoritative.** Export and runtime diverge: 14 gates (runtime) vs 9 (export). Export must delegate to the canonical activation utility, not keep its own encoding.                                                                                                                                           | ADR 0001 (`docs/adr/0001-gate-resolution-precedence.md`) names "adding a fourth encoding" as the rejected alternative; `gate-set-resolver.ts:1` is marked `@lifecycle canonical`; `gate-activation.ts:5-7` states all activation checks should delegate to it. `resolveActiveGateRefs` is that fourth encoding. Export set of 9 confirmed by live export.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 2026-08-17 |
| Q1b | **Root cause is one missing case, not three.** Five gates ship with **no `activation:` block**. Runtime treats absent activation as always-active (`gate-activation.ts:43-45`); export does `activation?.['prompt_categories']` then `cats?.includes(...)`, which short-circuits to `undefined` and skips the gate.                    | Verified independently: `information-placement`, `intent-quality`, `product-positioning-fidelity`, `prose-hygiene`, `semantic-discoverability` have no `activation:` key. Export emitted exactly 9 gates, none of them these five.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 2026-08-17 |
| Q1c | Two further latent divergences carried by the exporter: it never applies the `gate_type: 'framework'` AND-logic branch, and it compares categories case-sensitively where the runtime lowercases both sides.                                                                                                                           | `gate-activation.ts:53-55, 65-104, 82-84`; `gate-set-resolver.ts:230`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 2026-08-17 |
| F2  | **RULED — not a phantom field. A live RUNTIME bug, wider than this plan.** `framework_gates: false` IS read and vetoed at `gate-set-resolver.ts:352-355`, then silently undone by `ensureDefaultFrameworkGate` (`gate-enhancement-service.ts:842-856`), which consults only the operator switch and re-appends `framework-compliance`. | Verified by reading both sites. Diagnosing agent proved it live: plain `>>strategicImplement` enforced `framework-compliance` and rendered its CAGEERF body despite `framework_gates: false`; `%lean` control returned the same set minus that gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 2026-08-17 |
| F2b | **`CHANGELOG.md:470` is false as shipped** — it claims the opt-out "now withholds methodology gates across every tier, including registry-activated ones." Also contradicts ADR 0001:117 and `docs/reference/prompt-yaml-schema.md:95`.                                                                                                | The adjacent CHANGELOG line fixes the **identical bug shape** for `gateConfiguration.exclude` ("honoured during planning and then silently undone when re-added by category activation"). The same fix was not applied to `framework_gates`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-08-17 |
| Q2  | **RULED — WARN and continue. Never refuse the command, never skip the resource.** Export emits every registered resource and reports, per resource, exactly which constructs did not translate. The plan's stated default ("refuse or warn") is narrowed to warn-only.                                                                 | Three independent reasons, in order of weight. (1) **The pull precedent does not transfer.** Pull skips (`service.ts:3195-3203`, comment "skip to prevent destruction") because writing would overwrite canonical source — irreversible. Export writes a derived, regenerable artifact; nothing is destroyed, so the same severity does not follow. The asymmetry in the fix mirrors an asymmetry in consequence. (2) **A skip policy drops skills in live use.** Measured: `dev-workflow` and `strategicImplement` each carry arguments, so an F4 skip would stop exporting the two skills actually installed and used. (3) **Silent absence is worse than visible degradation** — F10 already documents this project paying for artifacts nobody can see. A stale-but-present skill with a loud CLI warning is recoverable; a skill that quietly stopped exporting is not.                                                                                                                                                                                                                                                                                          | 2026-08-17 |
| Q4  | **RULED 2026-08-17 by the owner — export DOES pre-approve the emitted scripts, scoped by command.** Claude Code adapter emits `allowed-tools: [Bash(<runner> tools/<id>/script<ext>:*)]`, one entry per script tool. Not a bare `Bash`, and not emitted for any other client.                                                          | Owner's call, with the reasoning that this server is used through an agent, so auto-accept is the normal case. Refined on two points of evidence. (1) **The agent/human discriminator the owner floated does not exist at this layer and is not needed**: a SKILL.md is only ever read by a model, so every consumer of this frontmatter is already the agent case — there is no human branch to detect. (2) A bare `Bash` grant would hand every exported skill arbitrary shell, which the prefix specifier avoids while still auto-accepting the one intended command. `Bash(cmd:*)` matches by command prefix, so the documented command and the granted command are generated by ONE function (`scriptToolCommand`) — drift between them would silently degrade to an unexplained permission prompt, and is guarded by its own test. Live-verified on `reference_demo`: grant, documented command, and emitted file path all agree. **Residual**: the grant is a relative path, so it will not match if the model invokes the script by absolute path. That fails safe (a prompt, i.e. today's behaviour) and there is no skill-root variable to resolve it with. | 2026-08-17 |
| Q2b | **Scope narrowed deliberately**: the warning is CLI-only. Annotating the SKILL.md body itself (telling the reading model that `{name}` placeholders are never substituted) is defensible and was considered, but it edits the artifact to describe its own defect and belongs with the F4 fix proper, not its detector.                | Considered and rejected during Wave 2 to keep the guard separable from any change to emitted content.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 2026-08-17 |

## Discovered unknowns

| Id       | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Bearing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEV-W2-1 | **F3 and F4 interact, and the compile keeps the wrong branch.** `dev-workflow/user-message.md:8-9` reads `{% if work_type %}{{ work_type }}{% else %}[bug_fix \| feature \| refactor \| explore \| optimize]{% endif %}`. `compileTemplate` keeps the if-branch and drops the else, so the exported skill renders `{work_type}` — a placeholder that never binds (F4) — while discarding the enumeration that would have told the reader what belongs there. For a skill, **the else-branch is the more useful one**: arguments never substitute, so the fallback text is the only content that ever reaches the reader.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | The eventual F3/F4 repair should prefer the ELSE branch when compiling for export, inverting `compileTemplate`'s current choice. Recorded, not acted on — Wave 2's scope is the detector, and changing which branch is emitted alters artifact content, which Q2b deliberately deferred.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| DEV-W3-2 | **F6's stated root cause is wrong, and the fix it prescribes would not work.** The plan says "fix the discarded `SyncResult` first — the export symptom is downstream of it." Verified directly: a script tool that FAILS VALIDATION never increments `errors`. `ScriptToolDefinitionLoader.loadTool` **returns `undefined`** rather than throwing (`script-definition-loader.ts:289-299`, writes stderr, bumps a private `stats.loadErrors`); `loadToolsForPrompt:169-182` skips falsy results silently unless `debug`; so `resource-indexer.ts:840`'s catch never fires, the tool is absent from `seen`, and `syncTools` **DELETES it and counts `result.removed++`** (`resource-indexer.ts:845-850`). A validation-failed tool is therefore **indistinguishable from a tool deleted off disk** — both read as `removed`, `errors` stays 0. Inspecting the discarded return at the two call sites cannot detect the failure F6 exists to detect. Compounding: `SyncResult` is five bare counters (`resource-indexer.ts:71-77`) with no ids or reasons, so even a non-zero `errors` yields a non-actionable message; and `syncTools`' own summary log (`:853`) omits `errors` from the string, unlike `syncAll`'s (`:426-430`). | The real fix is upstream and F6 must be re-scoped to it: make validation drop-out **distinguishable** from disk-deletion before touching either call site. Cheapest route is exposing the loader's existing private `stats.loadErrors` through the `toolLoader` boundary, or widening `SyncResult` to carry `{id, type, reason}[]` beside the counts. Fixing only the discarded return would close the row while leaving the defect live — the exact "green test that observes nothing" failure this plan was written against.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| DEV-W3-3 | **Partial-cold is silent — a worse variant the plan does not describe.** `loadToolsCache` (`service.ts:598`) returns early whenever the map is non-empty, so its warning at `:604` fires only when the cache is entirely empty. If most tools indexed but one dropped out (exactly the DEV-W3-2 case), that tool falls into the per-tool fallback at `service.ts:925` **with no warning at all** and exports without `schema.json`/`tool.json`. Also the emitted filename is not always `script.py`: `service.ts:2117` derives the extension from `tool.runtime`, and the cold fallback sets `runtime: toolData.runtime ?? 'unknown'`, which yields an empty extension — a file literally named `script`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Warn per-tool at the `:925` fallback, not only on a wholly-empty cache. This is the half of F6 that is genuinely fixable in `service.ts` and is independent of the upstream re-scope above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| DEV-W3-5 | **F1's delegation target is `isGateActiveForContext`, NOT `GateSetResolver` — and the arch boundary is not the obstacle.** `modules/ → engine/` is explicitly allowed (`.dependency-cruiser.cjs:7` layer comment; no `modules-no-engine` rule; 13 `#engine/` imports already exist under `src/modules/`, and `modules/formatting/style-manager.ts:24` already imports this exact symbol). The binding constraint is **form**: `no-crosslayer-relative` forces the `#engine/…` subpath specifier, never a relative path. But `GateSetResolver` is **unreachable** from export: it requires `frameworkInjected: boolean` and a `ConvertedPrompt` (export holds a raw `PromptYaml`), and no `GateManager` is injected anywhere on the CLI path — without one, `registryGateIds()` returns `[]` and Stage 1.5 no-ops, so delegating there would resolve **fewer** gates, not more.                                                                                                                                                                                                                                                                                                                                                   | Delegate the ACTIVATION DECISION only, to `isGateActiveForContext(activation, context, gateType)`. Confirmed the single inversion (export treats absent `activation:` as inactive; canonical returns `true` at `gate-activation.ts:42-45`) accounts for exactly the 5 missing gates. Three sub-blockers to rule on first: (a) `gate_type` is absent from the local `GateYaml` so arg 3 cannot be supplied without widening it, and passing `undefined` silently downgrades `framework-compliance` from AND to blocking logic; (b) there is no framework id at export time, so `framework_context` gates resolve differently than any real run — needs a documented posture or a new option; (c) `framework_gates: false` and `enabled: false` live in `GateSetResolver`/`GateManager` and would remain hand-rolled regardless. Raw `gate.yaml` text reads must stay either way — no canonical loader carries the payload the drift hash needs. **The runtime figure of 14 is still unverified**; only the 9-gate export set was measured. |
| DEV-W3-4 | **A one-adapter test cannot close a two-adapter defect, and the suite proved it.** F8's serialization bug existed as byte-identical copies in the claude-code exporter and the generic one. With the claude-code copy fixed and tested, reverting ONLY the generic copy (mutation M-K) left **74/74 green**. The three tests written for F8 were all blind to half the defect they were written for.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Fixed by extracting `buildPassCriteriaSection` so one helper serves both, plus a test that exports the same gate through the `codex` client. General bearing for the remaining rows: `service.ts` has **two parallel exporters** (claude-code at ~2050, capability-aware at ~2215) and any finding phrased as "the exporter does X" is really about two call sites. F5 below is the same shape — it has two emission sites, and the plan named only one. **It then happened a third time**: F5's first allowlist test asserted against claude-code only, and mutation M-M restored the generic adapter's `allowed-tools` with all 79 tests green. Both F5 assertions are now `it.each` over both clients. Treat single-adapter assertions in this file as a defect by default.                                                                                                                                                                                                                                                            |
| DEV-W2-2 | The detector found a dropped `{% else %}` in `dev-workflow`, which the plan's F3 description did not list (it named only `{{ref:}}`, `{{script:}}`, filters, dotted access, and `for`/`set`/`macro`). A daily-use skill was silently losing content that nobody had enumerated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Confirms the plan's own premise — enumerate surfaces before trusting a finding list. F3's description is now known to have been incomplete when written.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

## Deviations

| Id       | Row      | Deviation                                                                                                                                                                                                         | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Date       |
| -------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| DEV-W3-1 | F1,F5–F8 | **Rejected the plan's own `isolation: "worktree"` option for parallel workers.** Dispatch shape is instead: read-only diagnosis subagents in parallel, every edit serialized by the main thread in this worktree. | A worktree is cut from HEAD. Probed: `service.ts` carries **384 uncommitted insertions** from Waves 1–2, and all three skills-sync test files are **untracked** — so a worker's worktree would contain neither the code under test nor the tests, would rediscover Wave 1–2 from scratch, and would conflict on every merge. 84 files are dirty repo-wide (a concurrent session). The plan's serialization constraint was written before that uncommitted state existed. Matches the global rule that evidence production delegates and judgment does not. | 2026-08-17 |

| DEV-W3-6 | F2 | **The `%lean` case survives M-F2-1 and is labelled as such.** It was written expecting the veto guard to be what withholds the gate; the probe showed `FrameworkDecisionAuthority` (`framework-decision-authority.ts:115-130`) already returns no framework for `%clean`/`%lean`, so `activeFrameworkId` is undefined and the pre-existing FIRST guard blocks the append. Kept as a boundary marker with the measurement in its docblock rather than deleted or left reading as coverage it does not provide. This also means ADR 0001's F4 (`%lean` keeps framework-dependent gates) is not closed by this work at the default-append site — it would have to be about the rank-40 source, which is untested here. |

| DEV-W4-1 | F1 | **The plan's own count was stale — 14 vs 9 was actually 11 vs 7.** Re-measured against the real gate set for category `development` before writing anything, via a throwaway probe calling `isGateActiveForContext` directly. The membership differs three ways, not just the total: five `custom` gates missing from export, `framework-compliance` present in export but inactive without a framework, `creed-fidelity` the reverse. Acting on the plan's numbers would have made the fix look wrong when it was right. Same untrusted-inventory shape as DEV-W2/W3 rows. |
| DEV-W4-2 | F1,F16 | **Delegating to the engine was necessary and NOT sufficient.** `checkRegularGateActivation` treats an ABSENT framework as unconstrained rather than as a mismatch, so a naive delegation activates `creed-fidelity` — `gate_type: category`, `framework_context: [RADIANT]` — into a skill that has no RADIANT anywhere. Reading `gate_type` alone misses it too. `dependsOnFramework` therefore checks both declarations, and is a rule ABOUT export, layered on top of the shared activation function rather than pushed into it: the engine's semantics are right for a live execution, where absence means unconstrained. |
| DEV-W4-3 | F1 | **A mutation caught a self-satisfying assertion.** The case-insensitivity test asserted `toContain('cased')` on a skill built from a prompt named `cased_prompt`, so the gate id matched the prompt id echoed in the header and the test passed with the fix reverted. Renamed the gate to `upper-cat` and the prompt to `mixed_capitals`. This is the third time a mutation, not a review, found the weak test — it is what the falsification rule buys. |
| DEV-W4-4 | F1 | **Two existing fixtures had to be scoped.** `writeGate` wrote gates with no `activation`, which under correct semantics are ALWAYS active, so they began auto-activating into prompts that were meant to have none — the `emits no hook at all for a prompt with no gates` negative case in particular. `writeGate` now takes an `extra` bag and the hook-emission fixture declares a category none of its prompts use, reaching those skills only through the explicit `include` each test already had. The fixtures were wrong, not the assertions. |

## Falsification record

A row closes only with an entry here. "Test added" is not a closure.

| Row | Mutation applied                                                      | Test that failed                                                                              | Date       |
| --- | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ---------- |
| F1  | Hand-rolled category check restored (M-F1-1)                          | 2 tests: activates an unrestricted gate / matches the category case-insensitively             | 2026-08-18 |
| F1  | `enabled === false` skip removed (M-F1-2)                             | `export-command.test.ts` › skips a disabled gate, as selectGates does                         | 2026-08-18 |
| F16 | `dependsOnFramework` reads `gate_type` only (M-F1-3)                  | `export-command.test.ts` › withholds a category gate that names a framework_context           | 2026-08-18 |
| F16 | `dependsOnFramework` always false (M-F1-4)                            | 3 tests: both withholding cases + the framework-free control                                  | 2026-08-18 |
| F2  | Veto guard removed from `ensureDefaultFrameworkGate` (M-F2-1)         | 2 tests: `framework_gates: false` withholds it / `system-prompt.enabled: false` withholds it  | 2026-08-18 |
| F2  | `framework-gates-opt-out` dropped from the shared helper (M-F2-2)     | `framework-gate-opt-out.test.ts` › `framework_gates: false` withholds it — the shipped defect | 2026-08-18 |
| F2  | Default append neutered entirely (M-F2-3)                             | 2 tests: control (active framework still gets it) / `framework_gates: true` is not an opt-out | 2026-08-18 |
| F3  | `BARE_VARIABLE_BODY` matches everything (expression detection dead)   | 4 tests: filter expression / `{{ref:}}`+`{{script:}}` / dotted access / exports-anyway        | 2026-08-17 |
| F3  | `{% else %}` detection never fires (`if (false)`)                     | `export-command.test.ts` › reports a dropped `{% else %}` branch                              | 2026-08-17 |
| F3  | Control-flow regex never matches (`for`/`set`/`macro`)                | `export-command.test.ts` › reports control-flow blocks other than `{% if %}`                  | 2026-08-17 |
| F4  | Literal-argument reporting removed (`if (false)`)                     | `export-command.test.ts` › reports argument placeholders that stay literal                    | 2026-08-17 |
| Q2  | Silence guard removed — warn even with zero gaps                      | 2 tests: bare-variable-only stays silent / declared-but-uninterpolated stays silent           | 2026-08-17 |
| Q2  | **The rejected branch**: `continue` on any gap, skipping the resource | `export-command.test.ts` › exports the resource anyway — warnings never skip or refuse        | 2026-08-17 |
| F5  | Blanket `Bash` instead of a scoped specifier (M-O)                    | 2 tests: pre-approves the exact command, not blanket Bash / cannot drift                      | 2026-08-17 |
| F5  | Granted command drifts from the documented one (M-P)                  | 2 tests: same pair — the drift guard fires independently of the shape guard                   | 2026-08-17 |
| F5  | Leak `Bash(...)` permission syntax into the generic adapter (M-Q)     | `export-command.test.ts` › does not emit Claude Code permission syntax for other clients      | 2026-08-17 |
| F5  | Re-emit the invalid `tools:` frontmatter key (M-L)                    | 2 tests: never emits a `tools:` key / never claims an allowlist (claude-code)                 | 2026-08-17 |
| F5  | Re-emit `allowed-tools` from script-tool ids, generic adapter (M-M)   | `export-command.test.ts` › never claims an allowlist … (codex) — **see DEV-W3-4**             | 2026-08-17 |
| F5  | Drop the Script Tools section from the generic adapter only (M-N)     | `export-command.test.ts` › documents how to run the script (codex)                            | 2026-08-17 |
| F7  | Flow line reverted to `promptId` (M-G)                                | 2 tests: renders step names / falls back to the invocable id                                  | 2026-08-17 |
| F7  | `stepName !== undefined` instead of truthiness — the `''` trap (M-H)  | `export-command.test.ts` › falls back to the invocable id when a step declares no name        | 2026-08-17 |
| F8  | Serialization defect restored in the shared helper (M-I)              | 3 tests: prose / shell_verify summary / unrecognized-by-keys                                  | 2026-08-17 |
| F8  | Unrecognized-criterion fallback dumps values instead of keys (M-J)    | `export-command.test.ts` › names an unrecognized criterion by its keys                        | 2026-08-17 |
| F8  | **Generic adapter only** reverted, claude-code left fixed (M-K)       | `export-command.test.ts` › renders prose for the generic adapter too                          | 2026-08-17 |
| —   | Drop chain-step `inlineGateIds`                                       | `export-command.test.ts` › includes a gate that only a chain step declares                    | 2026-08-16 |
| —   | `saveManifestBatch` returns true when no db                           | `export-command.test.ts` › reports the manifest as NOT saved                                  | 2026-08-16 |
| —   | Remove `hooks` from claude-code frontmatter                           | `export-command.test.ts` › emits both the frontmatter hook and the script                     | 2026-08-16 |
| —   | Gate section claims enforcement unconditionally                       | `export-command.test.ts` › says it is NOT enforced on a client with no support                | 2026-08-16 |
| —   | Deregistration reader reads dead `exports` key                        | `deregistration.test.ts` › 5 tests                                                            | 2026-08-16 |
| —   | Emit bare id instead of `category/id`                                 | `deregistration.test.ts` › 5 tests                                                            | 2026-08-16 |
| —   | Drop the `'all'` expansion                                            | `deregistration.test.ts` › deregisters every prompt when a client selects `all`               | 2026-08-16 |
