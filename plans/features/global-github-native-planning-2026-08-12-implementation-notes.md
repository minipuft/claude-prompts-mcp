---
title: Global GitHub-Native Planning and Task Management — Implementation Notes
date: 2026-08-13
status: active
tags:
  - planning
  - github
  - global-config
---

# Global GitHub-Native Planning and Task Management — Implementation Notes

Companion: [`global-github-native-planning-2026-08-12.md`](global-github-native-planning-2026-08-12.md)

## Open-question rulings

| ID   | Ruling                                                                                                                                                 | Rationale                                                                                                                                                                                                                                                                                                                                                             |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OQ-1 | Request `read:project` and `project` only at explicit apply after dry-run review.                                                                      | Local implementation and read-only detection do not justify durable mutation permission.                                                                                                                                                                                                                                                                              |
| OQ-2 | Create the pilot Project as private.                                                                                                                   | Visibility changes remain reversible after the live drive.                                                                                                                                                                                                                                                                                                            |
| OQ-3 | ~~Use this initiative for the retained lifecycle drive.~~ **Revised 2026-08-15**: use the Windows hook-interpreter RFC — Discussion #225 → Issue #224. | Original ruling avoided synthetic tracker noise but left the drive self-referential: the plan would be both instrument and subject, so its Discussion→Issue conversion would be staged rather than observed. The hook-interpreter question is genuinely unresolved — four candidate options and two unestablished facts — so the conversion is real. Operator ruling. |
| OQ-4 | **Closed 2026-08-23**: pilot `claude-code-config` and `repository-standards`; retain advisory enforcement.                                             | Three repository roles were measured. The two additional repositories both support Issues and Projects but intentionally have Discussions disabled and no Roadmap configured. Blocking the current aggregate validator would therefore convert optional setup state into a planning failure.                                                                          |

## Deviations

| ID       | Tier | Plan assumption                                                                                | Evidence and ruling                                                                                                                                                                                                                                                                                              | Consequence                                                                                                                                                                                                                               |
| -------- | ---: | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEV-T1-1 |    1 | The planning document would remain in `~/.claude/plans`.                                       | The operator requested the durable plan under this repository's `plans/` tree.                                                                                                                                                                                                                                   | This file is canonical; the untracked global copy is removed after validation.                                                                                                                                                            |
| DEV-T1-2 |  1–4 | Global edits would run in an isolated clone.                                                   | The operator authorized direct edits on the live `~/.claude` main branch and canonical Codex synchronization.                                                                                                                                                                                                    | Preserve pre-existing dirty changes and stage/commit only initiative-owned paths.                                                                                                                                                         |
| DEV-T5-1 |    5 | Local and remote pilot setup could be one tier.                                                | GitHub Project writes are durable and cannot be isolated through git.                                                                                                                                                                                                                                            | Implement and validate dry-run behavior now; defer `--apply` until review.                                                                                                                                                                |
| DEV-T5-2 |    5 | The planned `planning` commit scope was valid.                                                 | Repository commitlint rejected it because the scope enum has no `planning` value.                                                                                                                                                                                                                                | Use the existing `docs` scope for plan and intake-surface changes.                                                                                                                                                                        |
| DEV-T4-1 |    4 | The existing Codex sync was a safe canonical-copy operation.                                   | The first post-change backup proved that `rsync --delete` replaced 108 destination skills with 54 Claude skills: 105 Codex-only entries were removed and three shared names overwritten.                                                                                                                         | Tier 4 acceptance was invalidated, then remediated with manifest-owned, conflict-aborting synchronization; the stale full export remains inactive in a forensic archive.                                                                  |
| DEV-T5-3 |    5 | Effort, target-release, start-date, and target-date fields formed a reusable standard.         | The operator challenged the template, and the pilot workflow does not demonstrate a need for those fields. GitHub's built-in Milestone already owns release grouping.                                                                                                                                            | Replace the universal template with a minimal base profile plus explicit repository profiles; add date or effort fields only when observed scheduling work requires them.                                                                 |
| DEV-T5-4 |    5 | A generic Area field could be copied between repositories.                                     | Classification consumers differ by repository. For this pilot, the durable work surfaces are prompt engine, resource manager, system control, chains, gates, frameworks, CLI/installation, and documentation.                                                                                                    | Define the pilot's Area options in its repository profile instead of global guidance.                                                                                                                                                     |
| DEV-T5-5 |    5 | Project view mutations might require guided manual setup.                                      | Runtime GraphQL schema inspection exposed supported create/update operations, and the live apply created or adopted all six views without manual steps.                                                                                                                                                          | Keep the automated path covered by tests and retain guided recovery only for API drift or partial failure.                                                                                                                                |
| DEV-T6-1 |    6 | Numeric Priority options would remain distinct from plan structure.                            | The plan corpus uses `P0`–`P7` phase and finding identifiers, so `P0`–`P3` Project values create a second meaning for the same symbols. The first real item also exposed a missing repository-automation area.                                                                                                   | Rename Priority to Urgent/High/Normal/Low, add Repository Automation, and reconcile existing options by discovered IDs without deleting fields.                                                                                           |
| DEV-T6-2 |    6 | `gh pr edit` could add the non-closing Issue reference.                                        | The installed CLI queried deprecated Projects Classic `projectCards` and rejected the edit before mutation.                                                                                                                                                                                                      | Preserve the complete body and apply the same change through the pull-request REST endpoint; read back the body and Issue timeline afterward.                                                                                             |
| DEV-T6-3 |    6 | The retained live drive would use this initiative as its own subject (OQ-3).                   | Zero Discussions existed in the repository, so the Discussion leg had never been exercised; and a self-referential drive would stage the Discussion→Issue conversion rather than observe one. The Windows hook-interpreter question surfaced during #139/#148 verification with a genuinely open approach space. | OQ-3 revised: Discussion #225 → Issue #224 → Project #1 → PR is the Tier 6.1 subject. Issue #224 was filed before the Discussion, so its conversion is recorded by cross-reference comment rather than by GitHub's native convert action. |
| DEV-T5-6 |    5 | The current `gh` authentication would still carry the previously granted `read:project` scope. | Revalidation on 2026-08-23 found scopes `gist`, `read:org`, `repo`, and `workflow`. The read-only setup command therefore reports Project state as unknown. A device-flow refresh was started and cancelled because the collaborative browser was not signed in; no remote mutation occurred.                    | Preserve the earlier successful apply evidence, complete plan-to-Issue binding through the accessible Issue API, and require renewed `read:project` authorization before Tier 6 Project-state acceptance.                                 |
| DEV-T6-4 |    6 | The Discussion would select one of four mechanisms without implementation evidence.            | The plugin already requires Node.js, while its Python hooks only need a portable interpreter boundary. A shipped Node launcher can use argv arrays without shell parsing, prefer `py -3` on Windows, and preserve a launched hook's exit status.                                                                 | Route all seven Claude Code hook registrations through `python-hook-runner.cjs`; fall back only on `ENOENT`, retain direct Python commands as contributor-local tests, and leave shared Python hook behavior unchanged for other clients. |
| DEV-T6-5 |    6 | Validation and implementation-note acceptance would wait for the live PR closure in 6.1.       | PR creation is a commit-boundary operation, while pre-PR review requires a fully validated implementation and current evidence. The Project read-scope and two-pilot requirements also remain independent external conditions.                                                                                   | Complete and record rows 6.2–6.3 before 6.1 closes; evaluate 6.4 independently from its three-repository evidence, and keep only 6.1 open for the retained PR trace.                                                                      |
| DEV-T6-6 |    6 | The third pilot would be another repository with a decision-bearing plan.                      | The operator selected `repository-standards`. It has no plan corpus, while `claude-code-config` has two active plans and no Issues. This gives one positive migration case and one negative/no-publication control instead of two favorable positive cases.                                                      | Count both as independent measurements, not completed setup rollouts. The negative control is decision-bearing evidence: blocking must not create a Roadmap or require Discussions merely because a GitHub repository exists.             |

## Validation ledger

| Surface               | Command                                                                                              | Result                                                                                                                                                     |
| --------------------- | ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Global rule budgets   | `~/.claude/scripts/check-rules.sh`                                                                   | PASS; unrelated pre-existing soft warnings only                                                                                                            |
| Skill/rule catalog    | `validate-indexes.py`                                                                                | INITIATIVE PASS; reports unrelated untracked `codex-plugins` and `diffusion-lora-bake` work from another session                                           |
| Global hook suite     | `python3 -m unittest discover ~/.claude/hooks/tests`                                                 | PASS: 66 tests after profile reconciliation                                                                                                                |
| Provider unit suite   | `test_github_planning.py`                                                                            | PASS: 21 tests, including repository profiles, option drift/reconciliation, view setup, and null-list normalization                                        |
| Local plan lint       | `lint-plans.py` on this plan and notes                                                               | PASS                                                                                                                                                       |
| Capability detection  | `github_planning.py detect --no-cache`                                                               | PASS: GitHub, Issues/Projects/Discussions enabled; `project` scope acquired only at the explicit-apply boundary                                            |
| Setup dry run         | `github_planning.py plan-setup --no-cache`                                                           | PASS: reviewed 12-change profile for two labels, private Project, Priority/Area, repository link, and six views; no mutation                               |
| Apply guards          | missing `--apply`; missing `project` scope                                                           | PASS: both exit 2 before mutation                                                                                                                          |
| Explicit remote apply | `github_planning.py apply-setup --no-cache --apply`                                                  | PASS: created private user Project 1, labels, fields, repository link, and six views; no manual steps                                                      |
| Live Project state    | `github_planning.py validate --no-cache` and `gh project field-list`                                 | PASS: `valid: true`, no problems; declared Priority and Area options match                                                                                 |
| Profile correction    | reviewed dry run, explicit apply, second dry run                                                     | PASS: Priority is Urgent/High/Normal/Low; Repository Automation was added; `changes: []`, `manual: []` after apply                                         |
| Accepted-work drive   | Issue #210 → Project 1 → retirement plan → PR #209                                                   | PASS: one Project item; In Progress, Normal, Repository Automation, no milestone; plan tracking URL present; PR uses non-closing `Refs #210`               |
| Pilot forms           | Prettier, YAML parse, repository-link scan, live labels/categories                                   | PASS                                                                                                                                                       |
| Codex synchronization | six focused ownership tests, live dry run, archive inventory, and full non-system SHA-256 comparison | PASS: 286 Claude-managed files match; destination-only files are preserved by contract; 79 prompt/19 gate/6 methodology/4 style exports archived inactive  |
| Pilot typecheck       | `npm run typecheck`                                                                                  | PASS                                                                                                                                                       |
| Pilot lint            | `npm run lint:ratchet`                                                                               | PASS: no regression                                                                                                                                        |
| Pilot tests           | `npm run test:ci`                                                                                    | PASS: 190 suites / 2480 tests                                                                                                                              |
| Pilot build           | `npm run build`                                                                                      | PASS                                                                                                                                                       |
| Changelog             | `[Unreleased]` Added entry                                                                           | PASS                                                                                                                                                       |
| Plan tracking         | local plan lint plus `gh issue view 224`                                                             | PASS: plan remains active and now tracks open accepted-work Issue #224                                                                                     |
| Project revalidation  | `github_planning.py validate --no-cache`                                                             | PASS after `read:project` authorization propagated: `valid: true`, `changes: []`, `manual: []`                                                             |
| Hook runner focused   | runner self-test, focused pytest, registration validator, harness self-test, plugin E2E              | PASS: 7 launcher checks, 2 focused tests, 7/7 registrations, harness self-test, and 11 plugin E2E tests                                                    |
| Current global suites | rule checker, index validator, global hook unittest discovery                                        | PASS: 15 rule files, index validation, and 118 hook tests; existing rule-size warnings remain advisory                                                     |
| Current server suites | typecheck, lint ratchet, tests typecheck ratchet, unit CI, integration, build, `validate:all`        | PASS after classifying grep/git documentation flags as third-party CLI options; 210 unit suites, 57 integration suites, build, and all 48 validation steps |
| Config-repo pilot     | local-root detect, plan lint, `plan-setup --no-cache`, live Issue inspection                         | PASS as an advisory measurement: two active plans lint clean, zero Issues, 11 opt-in setup changes, and disabled Discussions reported without mutation     |
| Standards-repo pilot  | local-root detect, corpus inspection, `plan-setup --no-cache`, live Issue inspection                 | PASS as a negative control: no plan corpus, one open Issue, 11 opt-in setup changes, and disabled Discussions reported without mutation                    |
| Enforcement decision  | three-repository evidence review                                                                     | PASS: retain advisory behavior; do not make aggregate setup validation a publication blocker                                                               |
| Closing PR            | [#241](https://github.com/minipuft/claude-prompts-mcp/pull/241)                                      | OPEN: targets `main` with `Closes #224`; merge will complete the retained Tier 6.1 trace                                                                   |

## Current boundary

Tiers 1–5 are complete. This active plan now tracks accepted-work Issue
[#224](https://github.com/minipuft/claude-prompts-mcp/issues/224). The private
[`claude-prompts-mcp Roadmap`](https://github.com/users/minipuft/projects/1) is linked to the
repository with the minimal profile, six configured views, and a clean second dry run.

The first real accepted-work path is also live: retirement federation Issue #210 is the sole Project
item, the retirement plan tracks it, and draft PR #209 cross-references it. Automatic closure remains
deferred to a release PR targeting `main`; GitHub ignores closing keywords on PRs targeting other branches.

The Tier 6.1 live drive began 2026-08-15 on a revised subject (DEV-T6-3). Discussion #225 (Ideas)
now records the Node-launcher decision and Issue #224 is its accepted-work output. The local
implementation routes every installed Claude Code hook through the launcher and has focused plus
repository-wide validation evidence. This is the first Discussion in the repository, so
`.github/DISCUSSION_TEMPLATE/idea.yml` (5.4) is exercised rather than merely present. PR
[#241](https://github.com/minipuft/claude-prompts-mcp/pull/241) now carries `Closes #224` and targets
`main`; merge supplies the retained Discussion → Issue → Project → plan → PR closure trace.

Two facts about the drive are worth keeping: the Issue preceded the Discussion, so the conversion is
recorded by cross-reference rather than GitHub's native convert action; and the `triage` label places
#224 in the Inbox view and correctly excludes it from Backlog, which is the first observation of the
view filters discriminating between two live items.

Tier 4's Codex synchronization acceptance was re-opened after the operator noticed the skill catalog
had contracted. Remediation is complete: the pre-sync full export is inactive under
`~/.codex/skills-archive/claude-prompts-full-export-20260227`, `/mcp-prompt-router` routes on demand
to canonical MCP resources, and synchronization now updates only manifest-owned files while refusing
unowned collisions. The separate dirty `codex-prompts` checkout was not edited.

The three-pilot review now covers `claude-prompts-mcp`, `claude-code-config`, and
`repository-standards`. The product repository proves the complete configured lifecycle. The global
configuration repository exposes a real migration need: two active plans, no accepted-work Issues,
and no Roadmap. The standards repository is the negative control: it has no decision-bearing plan,
so repository capability alone must not trigger setup or fail work. Both additional repositories
also keep Discussions disabled. The current aggregate `validate` result therefore measures opt-in
setup state, not plan validity. Publication enforcement remains advisory; no pilot repository was
mutated.

## Remaining closeout

- Merge PR #241 to close Issue #224 and complete the retained Tier 6.1 trace.
- Decide whether the private Project should become public after the live drive.
