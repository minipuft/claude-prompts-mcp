---
title: "Contract layer — rulings ledger"
date: 2026-09-06
status: active
tags: [contracts, gates, module-descriptors, chains, memory]
---

# Contract Layer — Rulings Ledger

Running state for the 2026-09-06 discussion. Each turn edits this file; the message reports the
delta. An open row's **Default** is what happens if nobody objects.

## Rulings

| ID   | Date       | Ruling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1   | 2026-09-06 | **Chain integrity: one resolver, three boundaries.** Reuse the workflow-IR `unknown-prompt` resolver at resource write, resource load/reload, and `validate:prompts`. Write REFUSES (delete the warning path and the `/` skip). Load marks the chain unexecutable with a diagnostic. The four runtime throws stay as last-line asserts. Positive control: one chain with a missing step must trip all three.                                                                                                                                                                                                                                                                                             |
| D1a  | 2026-09-06 | CI resolves against the bundled tree only; overlay chains are validated at load, where the overlay is visible.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D1b  | 2026-09-06 | An e2e flow that creates a parent chain before its steps changes to scaffold steps first. The test moves, not the rule.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D1c  | 2026-09-06 | Re-measure gap 3 (sub-prompt scaffolding) before writing rows — commit `47a2c9ef` (2026-04-13) is titled as shipping it while the memory lists it open.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D2   | 2026-09-06 | **Memory falsifiers run at the edit boundary, not session start.** A PostToolUse(Edit\|Write) hook runs only the `holds_while` probes whose path matches the edited file. Session-start sweeps are rejected: they run unconditionally and land before the session has a goal.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D2a  | 2026-09-06 | One falsifier per claim. memory-shape refuses a memory whose body carries more claims than probes (the chain-tooling memory guards gap 1 with a probe that cannot see gap 4).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D3   | 2026-09-06 | **Placement rule.** A falsifiable claim about the repo whose failure costs a session is a repo validator. Memory holds operator, preference, and cross-repo claims. No rewrite: `table-contracts.ts` + validators is already the pattern.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D4   | 2026-09-06 | **Gates are the home for script-shaped validators, not for harness-event hooks.** Add `activation.always` (additive). One script, two consumers: `validate:*` in CI and `shell_verify` in the prompt run. Do NOT adopt the Claude Code hook schema (event/matcher semantics the server cannot honor). Chain fix (D1) ships first.                                                                                                                                                                                                                                                                                                                                                                        |
| D5   | 2026-09-06 | **Findings schema, once, at the verdict contract.** Optional `findings[]` per `per_gate` entry: `id, file?, line?, summary, failure_scenario, category, verdict: confirmed\|plausible, probe`. Persisted in `gate_verdicts_json`. Structured verdict only; legacy string branch keeps retiring.                                                                                                                                                                                                                                                                                                                                                                                                          |
| D6   | 2026-09-06 | **Extend `module.yaml`, do not add file tags.** Order: strip `@lifecycle canonical` headers (445 files; drop the filesize exemption) → `owns` for the 14 Domain Ownership Matrix rows + validator → `invariants: [{id, rule, verifiedBy}]` with test-exists check → catalog columns with ✓/✗ from validators.                                                                                                                                                                                                                                                                                                                                                                                            |
| D7   | 2026-09-06 | **Cross-project baseline goes through `repository-standards`, not per-project requests.** Prove the descriptor schema + validator + catalog here first; promote to a standards profile on the second consuming repo (the fleet drift audit then enforces it). Never re-add a local fleet check here.                                                                                                                                                                                                                                                                                                                                                                                                     |
| D8   | 2026-09-06 | **Ids are author-minted slugs, namespaced by module id.** Invariants: `<module-id>.<slug>`. Findings: `<gate-id>.<slug>`; the positional `index` on `per_gate` is retired once findings land. Findings schema lives in `tooling/contracts` (generated surface); the `module.yaml` Zod schema is published as a `$schema` for editors.                                                                                                                                                                                                                                                                                                                                                                    |
| D9   | 2026-09-06 | **dependency-cruiser stays as the engine; policy moves to `module.yaml`.** Generate the cruiser rule section from descriptors with a generated-equals-committed check (same pattern as `generate:contracts`). Replacing the import parser is a rewrite that buys nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D10  | 2026-09-06 | ✗ KILLED — reconstruction-cost framing as a memory feature (`holds_while` already forces the question; revives if the persistence-tier decision gets re-derived twice).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D11  | 2026-09-06 | **Interview format.** State lives here. Per turn: delta first, ≤1 question with a default, evidence in this file. Memory: `feedback_interview_decision_fatigue`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D12  | 2026-09-06 | **Skill integration, not a one-off.** `/unknowns` edited: calibration selects the move (exploring → react to candidate rulings, converging → frontier rounds, committed → no questions); the design tree lives in a ledger file; width priced by question class (P1–2 explicit, P3–4 default-and-proceed). `dev-workflow.md` Phase 1b row untouched until a second sighting.                                                                                                                                                                                                                                                                                                                             |
| D13  | 2026-09-06 | **Quadrant check on this work.** D1: known unknowns only, defaults set, area shipped-in before → no blind-spot pass. D5/D6/D8: unknown knowns (shape recognized on sight) → prototype three divergent samples before any schema is written. D7: not a decision, an open row that flips when a second consumer exists.                                                                                                                                                                                                                                                                                                                                                                                    |
| D14  | 2026-09-06 | **Context-compiler discussion does not enter this plan.** It is a separate initiative in the unknown-unknowns quadrant (open approach space; landscape claims MCE/ACE/ACDL unverified here). It DEPENDS on this plan's outputs — descriptors, invariants, findings with provenance are its substrate — so this work proceeds unchanged. Blind-spot pass + `>>tech_recommendation` before any commitment to it.                                                                                                                                                                                                                                                                                           |
| D15  | 2026-09-06 | **O4 gains a fourth sample.** The one objection from that discussion that touches current work is claim-vs-primitive: sample (d) expresses finding (a) as substrate primitives (constraint + observation + delta) so the owner can react to whether a finding is a rendered claim with provenance or a graph fragment.                                                                                                                                                                                                                                                                                                                                                                                   |
| D16  | 2026-09-06 | **Prototype graded (proposed, awaiting owner reaction).** Shape = B (claim with provenance) with two borrowings: from C, a finding names the rule it violates (`violates: <invariant-id>`), which links gate findings to descriptor invariants; from A, reuse `rationale` as the summary. Omit `location` when n/a. Invariants: `{id, rule, verifiedBy}` (A and B converged). C's relations rejected: 30 leaf fields for traversal nothing asked for.                                                                                                                                                                                                                                                    |
| D17  | 2026-09-06 | **Sighting for O2.** All three agents independently excluded `resolveEnforcementMode` from `engine-gates.owns` because it lives under `engine/execution/pipeline/decisions/gates/`. The Domain Ownership Matrix and the file tree disagree on one row today.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D17a | 2026-09-07 | Correction: no matrix row disagrees with the tree on enforcement mode; the matrix is grouped by capability, descriptors by module. What the probe DID find: `CommandParser` has no export (actual: `UnifiedCommandParser`, `execution/parsers/command-parser.ts`); StyleManager path is `modules/formatting/style-manager.ts`, not `styles/`. Two of fourteen rows drifted.                                                                                                                                                                                                                                                                                                                              |
| D18  | 2026-09-07 | **O5 flipped: matrix stays hand-written; validator enforces equality both ways.** AGENTS.md is already generated FROM CLAUDE.md (`sync-project-guidance`), so a generated block inside CLAUDE.md would put two generators on one file. Bidirectional check: every matrix row ↔ one `owns` entry; owner-cell path fragments must be substrings of the real definition path.                                                                                                                                                                                                                                                                                                                               |
| D19  | 2026-09-07 | **Evidence vocabulary (default for the findings step, not today).** One `evidence` object replaces `probe` / `verifiedBy` / `holds_while`: `{kind: shell\|test\|grep\|judge, reproduce, exit?, line?, output?}`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D20  | 2026-09-07 | **Pre-flight (validate-domain-ownership).** failures 0 · compound none. domain: descriptor lib (3 consumers). service: descriptor validator does SHAPE; ownership is cross-source (src + CLAUDE.md) → new script reusing the lib; rejected extending the validator (mixes concerns; lib is an import-free loader). defined: no symbol-definition scanner exists in scripts (knip-ratchet delegates to knip). reached: positive control = self-test fixture with a wrong path fragment and a missing symbol must FAIL. Worktree `claude-prompts-mcp-own` on `feat/ownership-descriptors` from origin/main a0af3278 (local main had diverged: 2 unpushed doc commits vs 3 unpulled).                       |
| D21  | 2026-09-07 | ✓ **D6 step 2 landed** — `08a5e1ae` on `feat/ownership-descriptors` (worktree `claude-prompts-mcp-own`, base origin/main a0af3278). 15 files, +898. Verified by me, not the agent: validator OK 14/5, self-test 8/8, positive control (misspelled symbol) FAILS with two addressed lines, all 55 suite steps pass, 3020 unit tests pass, no binary hunks. Agent deviation accepted: SUITE `reads: ['declared']` because the substrate audit only follows `.js` imports. Owner said proceed 2026-09-07: local main pull-rebased and pushed (6d99b7cf), branch rebased onto it (9c0bec06), pushed, PR opened with `ci(scripts)` title (hidden from consumer changelog — nothing consumer-visible changed). |
| D22  | 2026-09-07 | ✓ **#266 merged** (squash d88b35e1) after #267 (c951834e) fixed main's own red: the adoption implementation notes lacked frontmatter, so `plans:retire:check` failed on main and on every PR rebased onto it. Worktrees `-own` and `-fm` removed, branches deleted. D1 plan written: `plans/chain-integrity-2026-09-07.md` (5 tiers, 18 rows, plan-row-tracking clean), awaiting owner review before dispatch.                                                                                                                                                                                                                                                                                           |

## Open decisions

| ID  | Question                                                                                     | Default                                                                                                                                                    | Flips when                                  |
| --- | -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| O1  | Which item becomes the first strategicImplement plan?                                        | D1 (chain integrity) — smallest, produces the script D4 later reuses                                                                                       | Owner names another                         |
| O2  | Does the `owns` validator check "sole definer" or "sole definer + sole importer for stages"? | Sole definer first; importer check when a cross-layer sighting lands                                                                                       | A stage found calling a non-owner           |
| O3  | D2 hook on a failed probe at edit time: block the edit or advise?                            | Advise (additionalContext naming the stale memory); a stale memory must never block a code edit                                                            | A stale memory causes a wrong edit twice    |
| O4  | D5/D6/D8 shape: which three samples to prototype?                                            | (a) shell_verify finding from the chain validator, (b) LLM-judged finding from `code-quality`, (c) `engine-gates/module.yaml` with `owns` + two invariants | Owner reacts; the reaction fixes the schema |
| O5  | ✓ 2026-09-07 → D18 (hand-written + bidirectional validator)                                  |                                                                                                                                                            |                                             |

## Sequence

0. O4 prototype (one file, three samples, nothing wired) — before any schema lands
1. D1 plan file ✓ written 2026-09-07 → owner review → opus subagent via strategicImplement
2. D6 step 1 (header strip) — mechanical, can ride any commit · step 2 (`owns` + validator) ✓ merged d88b35e1 (#266)
3. D2 hook + D2a memory-shape rule — personal framework, separate from repo work
4. D5 findings → D4 `activation.always` → D6 steps 2–4 → D9 → D7

## Evidence (probes run 2026-09-06)

- `validateChainStepReferences` (`server/src/mcp/tools/resource-manager/prompt/utils/validation.ts:479`): warning-only, skips ids containing `/`.
- Runtime throw sites: `execution-planner.ts:203`, `symbolic-command-builder.ts:165,259`, `04-parsing-stage.ts:292`; `chain-operator-executor.ts:397` warns.
- IR resolver: `server/src/modules/workflow-ir/validator.ts:35` (`unknown-prompt`).
- Gate activation: category | framework | explicit only (`gate-guide-types.ts:39`). Enforcement: blocking | advisory | informational.
- Verdict: `per_gate: [{index, passed, rationale}]`; persisted `GateVerdictSummary {gateId, verdict, rationale}`.
- `module.yaml`: 36 descriptors, all `canonical`; catalog at `docs/reference/module-catalog.md` says policy remains in cruiser config.
- `@lifecycle canonical`: 445 files; sole reader `scripts/validate-filesize.js:66` (exemption).
- Memory probes: 34/37 stamped; runner `memory-search.py --stale` is manual; memory-shape hook fires only on memory-dir writes.
