---
title: "Hermetic validation routes — checks read what CI and a fresh install read"
date: 2026-09-14
status: active
tags: [ci, scripts, config, validation]
---

# Hermetic Validation Routes

**Status**: ACTIVE — #278 merged (`c141a048`) with every row but 1.9, 1.11 and 1.12; the owner ruled both open rows 2026-09-14, and Tier 2 closes them on `fix/refusals-and-gate-persistence`
**Owner**: minipuft
**Created**: 2026-09-14
**Predecessor**: `plans/readme-install-path-2026-09-13.md` rows 3.7, 3.8 and 3.9, superseded here.

## Now (2026-09-14)

**Goal**: close rows 1.9, 1.11 and 1.12 in claude-prompts, and keep both downstream extensions working through the
release that carries R6 (R10). **Current slice**: Tier 2 on `fix/refusals-and-gate-persistence`. Workers C (rows 2.1,
2.5, 2.2, 2.3) and D (rows 2.8–2.10) stalled after a session restart with uncommitted edits and no handoff; the
planner is reading why before resuming or relaunching them. Rows 2.11 and 2.12 get downstream workers once both
repositories' state is measured. **Next decision**: resume versus relaunch for C and D. **Constraint in force**: nothing
is pushed to `gemini-prompts` or `opencode-prompts`; the claude-prompts release waits on both (R10).

## Why this exists

The README install-path work found three places where a check or a startup path answers with the
operator's environment instead of the state CI or an installed user has:

- CI runs the CONTRIBUTING-commands and plan-row-tracking checks only when the scope is exactly `docs`.
  A documentation edit that rides with a `hooks/**` change classifies `hooks` and skips both.
- Four scripts spawn the built server spreading `process.env` outside `validate:hermetic-child-env`,
  which walks `server/tests/e2e` only. One of them, `capture-tool-schemas.mjs`, writes the committed
  `server/tests/snapshots/mcp-input-schemas.json` while inheriting `MCP_CONFIG_PATH`.
- A set `MCP_CONFIG_PATH` naming a missing file produced no error and the same catalog in the one
  indirect observation made so far (2026-09-13, through `verify:mcp`, server stderr not captured).

## Rulings (planner, 2026-09-14 — carried from the owner's instruction of 2026-09-13)

| #   | Ruling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | **Routing, not new checks.** Every step that exists for documentation-only changes runs on both lightweight routes (`!= 'full'`), mirroring the projection and README-charter steps. CLAUDE.md's scope table says what each route runs; `AGENTS.md` is re-rendered from it                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R2  | **One scrub list.** The e2e helper and every server-spawning script consume one list. A shared import is preferred; a gate failing on two differing lists is acceptable where TypeScript/ESM boundaries forbid the import, recorded with the reason. `validate:hermetic-child-env` widens to `server/scripts`                                                                                                                                                                                                                                                                                                                                                                                     |
| R3  | **A set config path that cannot be used refuses startup** on STDIO and Streamable HTTP, before serving, non-zero exit, stderr only. The message names the variable or flag, the value, the resolved absolute path, what is wrong, and what is expected (a readable JSON config file, or unset it to use the packaged default at its path). Scope is every explicit config-path source: `MCP_CONFIG_PATH` and `--config`                                                                                                                                                                                                                                                                           |
| R4  | **Listed under ⚠ BREAKING CHANGES**, following `[Unreleased]`'s precedent "a gate criterion that cannot be enforced now fails to load instead of auto-passing"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| R5  | **Opus / high for both workers, not Fable.** The problems are specified; what remains open is approach (sharing a list across TS tests and `.mjs` scripts; a refusal that holds on both transports). Fable is the tier for a wrong-problem read and degrades on step-shaped briefs                                                                                                                                                                                                                                                                                                                                                                                                                |
| R6  | **Owner, 2026-09-14: every unusable operator path setting refuses startup (row 1.9).** `MCP_WORKSPACE` or `MCP_RESOURCES_PATH` naming a path that does not exist or is not a directory, and a workspace `config.json` that exists but is unreadable, a directory, malformed JSON or not a JSON object, each refuse on STDIO and Streamable HTTP in R3's form: before serving, non-zero exit, stderr only, naming the setting, the value, the resolved path, what is wrong and what was expected. A workspace with no `config.json` still falls back to the packaged config; `MCP_RUNTIME_ROOT` is still created on demand; an empty value counts as unset. Listed under ⚠ BREAKING CHANGES per R4 |
| R7  | **Owner, 2026-09-14: a gates toggle survives a restart (row 1.12).** `system_control` disables gates to save tokens, so a freshly started server serving the same workspace and runtime root advertises the `prompt_engine` schema that matches the persisted toggle, on both transports. The scope a toggle writes and the scope the advertised schema reads are one key                                                                                                                                                                                                                                                                                                                         |
| R8  | **Row 1.11 revives with 1.12, in the same PR.** Once persisted state reaches a fresh server, a developer's toggle could reach the committed schema snapshot and `verify:mcp`, so the capture and verify scripts run on a temp `MCP_RUNTIME_ROOT`. The committed snapshot stays the gates-enabled union shape                                                                                                                                                                                                                                                                                                                                                                                      |
| R9  | **One PR closes the plan.** Rows 1.9, 1.11 and 1.12 are its last open rows, so the PR that lands them sets a final `status:`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Superseded by R10 (2026-09-14)** |
| R10 | **Owner, 2026-09-14: fix downstream first.** R6 would break the gemini and opencode extensions on the next claude-prompts release (rows 2.11, 2.12). The planner prepares both fixes in `gemini-prompts` and `opencode-prompts` on local branches, pushing nothing; the owner reviews and pushes them, and the claude-prompts release waits until both downstream releases are out. The claude-prompts PR still lands Tier 2, but the plan stays `active` until rows 2.11 and 2.12 are terminal, so its PR carries a progress footer rather than closing the plan                                                                                                                                 |

## Dispatch

| Worker | Rows               | Tier / effort | Failure shape it guards                                                                                                                                                               | Branch                                                                                                    | File bound                 |
| ------ | ------------------ | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | -------------------------- |
| A      | 1.1–1.4            | opus / high   | wrong approach — one list across TS and ESM, a gate that must keep its e2e coverage                                                                                                   | `fix/hermetic-validation-routes--env`                                                                     | ≤ 14 files                 |
| A      | 1.11               | opus / high   | wrong approach — whether the runtime-root pin closes the class at the shared builder or per script                                                                                    | `fix/hermetic-validation-routes--env`, rebased onto `3e56bb8a`                                            | ≤ 6 files                  |
| A      | 1.13               | opus / high   | wrong problem at the declaration file — a knip blind spot on a file tsc needs, versus a file nothing needs; reuses the live session, since the un-export alone would be a sonnet task | `fix/hermetic-validation-routes--env`, rebased onto the initiative branch                                 | ≤ 5 files                  |
| A      | 1.14               | opus / high   | wrong problem — a ceiling the merge loosened, which neither branch alone can see; reuses the live session                                                                             | `fix/hermetic-validation-routes--env`, rebased onto the initiative branch                                 | ≤ 4 files                  |
| A      | 1.15               | opus / high   | wrong output on a bounded task — two declarations; reuses the live session, which already knows both validators                                                                       | `fix/hermetic-validation-routes--env`, rebased onto the initiative branch                                 | ≤ 2 files                  |
| B      | 1.5–1.8            | opus / high   | wrong approach — startup refusal with transport parity and a breaking-change entry                                                                                                    | `fix/hermetic-validation-routes--config`                                                                  | ≤ 14 files                 |
| B      | 1.10               | opus / high   | wrong approach — closing a lint class without widening ratchet ceilings, across file-wide exemptions the first count could not see                                                    | `fix/hermetic-validation-routes--config`                                                                  | ≤ 14 files across the row  |
| C      | 2.1, 2.5, 2.2, 2.3 | opus / high   | wrong approach — one refusal across three settings placed before the transport and the logs `mkdir`, with a release job and a CLI that silently depend on the fallback                | `fix/refusals-and-gate-persistence--paths` (worktree `claude-prompts-mcp-hvr2-paths`), background session | ≤ 18 files across the rows |
| D      | 2.8, 2.9, 2.10     | opus / high   | wrong approach — which scopes a fresh store loads, a synchronous check that cannot lazy-load, and a legacy-row adoption that must not bump the schema                                 | `fix/refusals-and-gate-persistence--gates` (worktree `claude-prompts-mcp-hvr2-gates`), background session | ≤ 14 files across the rows |

Neither worker spawns subagents. Neither pushes, opens a PR, or edits this plan; each returns the five-heading
handoff and the planner writes back.

---

## Tier 1

| #    | St                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Worker  | Verification                                                                                                                                                                                              |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1  | ✓ (verified 2026-09-14 · worker A `e35b2c66`, cherry-picked; planner re-parse: the CONTRIBUTING-commands, plan-row-tracking, README-charter and projection steps all run on `!= 'full'`; `hooks/gate-enforce.py` + `CONTRIBUTING.md` classifies `hooks`; the projection check matches)                                                                                                                                                                                                                                                                                                    | CI runs the CONTRIBUTING-commands and plan-row-tracking steps on every lightweight route; CLAUDE.md's scope table and the rendered `AGENTS.md` say so                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | A       | parsed step conditions; the classifier's scope for `hooks/gate-enforce.py` + `CONTRIBUTING.md`; `node scripts/sync-project-guidance.js --check`                                                           |
| 1.2  | ✓ (verified 2026-09-14 · worker A `83df3078`, cherry-picked; `validate:hermetic-child-env` reports 11 e2e files spreading no environment and 5 server-spawning scripts using `buildServerEnv`; the two further matches of the planner's wider enumeration start no server: `prepare-release-artifacts.js` runs `cpm --version` and `tar`, `validate-extension-artifact.js` runs `stage-server-runtime.sh`)                                                                                                                                                                                | One scrub list, consumed by `server/tests/e2e/helpers/child-env.ts` and every server-spawning script, each keeping its deliberate overrides                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | A       | an enumeration of spawn sites with the list each uses                                                                                                                                                     |
| 1.3  | ✓ (verified 2026-09-14 · planner mutant: replacing `buildServerEnv(` in `capture-tool-schemas.mjs` with a `process.env` spread makes the gate exit 1 naming that file, restored byte-identical; the self-test passes its 10 cases)                                                                                                                                                                                                                                                                                                                                                        | `validate:hermetic-child-env` covers server-spawning scripts and keeps its e2e coverage                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | A       | the named positive control, and the gate's existing self-test                                                                                                                                             |
| 1.4  | ✓ (verified 2026-09-14 · worker A: the pre-change capture opened an exported alternate config and resources copy and created an exported runtime root, yet under three alternate configs the capture equalled the committed snapshot, because the advertised gate parameters follow persisted gate state rather than config; that state is row 1.11)                                                                                                                                                                                                                                      | Prove whether the committed schema snapshot was captured under an operator environment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | A       | a capture with `MCP_CONFIG_PATH` and `MCP_RESOURCES_PATH` exported in the calling shell, diffed against `server/tests/snapshots/mcp-input-schemas.json`                                                   |
| 1.5  | ✓ (verified 2026-09-14 · worker B `836bb3af`, cherry-picked; planner re-ran it on a fresh build: a missing, directory or invalid-JSON `MCP_CONFIG_PATH` and a missing `--config` each exit 1 with the ruled message on stderr and nothing on stdout, on STDIO and HTTP; a valid path and no variable both boot healthy; unit 11/11, e2e 3/3; the worker's mutation fails 6 unit and 2 e2e cases)                                                                                                                                                                                          | Startup refusal for an unusable explicit config path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | B       | a spawned server per transport through `buildServerEnv` overrides; a mutation restoring the silent fallback fails the test                                                                                |
| 1.6  | ✓ (verified 2026-09-14 · worker B `e8fe5050`; documented mentions went from 9 with no refusal statement to 13 with 4 new statements; `AGENTS.md` re-rendered and the projection check passes)                                                                                                                                                                                                                                                                                                                                                                                             | `[Unreleased]` ⚠ BREAKING CHANGES entry; CLAUDE.md §Environment (paths), `docs/guides/custom-resources.md`, `docs/reference/mcp-tools.md`, `server/README.md`, and the `server/src/index.ts` help text                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | B       | an enumeration of mentions before and after                                                                                                                                                               |
| 1.7  | ✓ (verified 2026-09-14 · measured and left unchanged, table in the notes: `MCP_RESOURCES_PATH` and `MCP_WORKSPACE` naming missing paths boot on the bundle on both transports with no line naming the path; `MCP_RUNTIME_ROOT` is created on demand)                                                                                                                                                                                                                                                                                                                                      | Measure the other path variables; do not change them                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | B       | a spawned server per variable, stderr and exit captured                                                                                                                                                   |
| 1.8  | ✓ (verified 2026-09-14 · worker B `d9e3a7a4`, cherry-picked; planner probe over STDIO with a malformed workspace `config.json`: the branch without it writes `Using default configuration` to stdout before answering initialize, and with it stdout carries only protocol frames while initialize still answers)                                                                                                                                                                                                                                                                         | **The default-config fallback writes to stdout, the STDIO protocol channel.** Found by worker B: with no explicit path and an unusable default config (for example a malformed `config.json` under `MCP_WORKSPACE`), the loader prints `Using default configuration` to stdout and keeps serving. The line moves to stderr; whether that fallback should refuse at all is row 1.9                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | B       | a STDIO spawn with a malformed workspace `config.json` shows stdout carrying only protocol frames, and restoring the stdout write fails the test                                                          |
| 1.9  | ☐ (as of 2026-09-14 · flips when a missing `MCP_WORKSPACE` directory, a missing `MCP_RESOURCES_PATH` directory and a malformed workspace `config.json` each exit non-zero on STDIO and Streamable HTTP with the R3-form message and nothing on stdout, while a workspace with no `config.json` still boots)                                                                                                                                                                                                                                                                               | **Three silent fallbacks remain, and they are a policy choice rather than a defect fix.** A malformed workspace `config.json`, and `MCP_RESOURCES_PATH` or `MCP_WORKSPACE` naming a missing directory, each boot on packaged defaults with no line naming the path (worker B, both transports). The explicit config path now refuses; these do not. **Ruled by the owner 2026-09-14 (R6): all three refuse to start**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | planner | a ruling recorded here, then the change it calls for                                                                                                                                                      |
| 1.10 | ✓ (verified 2026-09-14 · worker B `8ac0f676` and `f086189c`, cherry-picked as `952c5b11` and `08b2207c`; on the merged tree `lint:ratchet` is OK at 3092 errors and 898 warnings, and a planner mutant putting `console.log`, `process.stdout.write` and `import { stdout } from 'node:process'` in an ordinary module plus a `console.log` in `startup.ts` exits 1 with `no-console` +2, `no-restricted-imports` +1 and `no-restricted-properties` +1, while `console.error` and `console.warn` in the same file stay unreported; the lint baseline changes only its `no-console` entry) | **Nothing structural keeps server output off the STDIO protocol channel.** Row 1.8 fixed one `console.info` on the config fallback; worker B reported the class. `setupConsoleRedirection` (`server/src/infra/logging/index.ts`), written for exactly this, has had no caller since `8432e87f`, and ESLint's `no-console` only warns, for every console method alike, so a stdout write reads the same as a stderr one. Measured 2026-09-14: 2 stdout-bound calls remain in `server/src`, at `src/index.ts:563` and `src/modules/skills-sync/service.ts:521`. Ruling: delete the dead helper, since a runtime patch of `console` is the wrong layer when lint can refuse the write; make stdout-bound console methods an error for server source with `warn` and `error` allowed; classify both remaining sites and remove or exempt each with evidence **Amended 2026-09-14 on B's partial handoff** (`8ac0f676` deletes the dead helper and routes skills-sync's console writes through its CLI; `src/index.ts:563` is exempt as the `--init` early exit). The three files with `no-console` off file-wide (`src/index.ts`, `src/runtime/startup.ts`, `src/infra/logging/index.ts`) all run in the serving process, and a lint count under that override cannot see them, so they keep only per-line exemptions with a reason. The 22 `eslint-disable no-console` directives the rule makes unused are deleted. The lint baseline changes only where this rule's counts moved, since a full regeneration would drop every other rule's slack from branches this row does not own. `process.stdout` in server source is refused too, because `no-console` cannot see it: 0 code sites today, against 21 `process.stderr` code lines as the search's positive control | B       | a `console.log` added to a server module fails lint; each remaining site is removed, or exempted with the reason it cannot reach STDIO; the lint ratchet is compared per rule against a fresh measurement |
| 1.11 | ☐ (as of 2026-09-14 · flips when `capture-tool-schemas.mjs`, `verify-mcp-surface.mjs` and `verify-unknown-interrupt.mjs` each run on a temp `MCP_RUNTIME_ROOT`, and a disabled gates row persisted in the checkout's `state.db` narrows neither the capture nor `verify:mcp` on a tree where row 1.12 makes persisted state reach a fresh server)                                                                                                                                                                                                                                         | **Persisted local state can still reach a committed snapshot and a verification.** Worker A: `capture-tool-schemas.mjs`, `verify-mcp-surface.mjs` and `verify-unknown-interrupt.mjs` pin `MCP_WORKSPACE` to the repo or server root and set no runtime root, so they read and write `<checkout>/runtime-state/state.db`. Nothing changes the snapshot today, since a gates toggle writes a workspace-scoped row and the advertised schema reads the `default` row, but nothing prevents it. Ruling: pin a temp runtime root in each. **Revived 2026-09-14 (R8)**: the owner's 1.12 ruling builds exactly the condition its kill stamp named, `✗ KILLED (2026-09-14 · … · revives if a freshly started server's advertised schema narrows from a persisted row, which fixing row 1.12 would make true)`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | A       | the seeded-row positive control named in the status                                                                                                                                                       |
| 1.12 | ☐ (as of 2026-09-14 · flips when `system_control gates disable` on a running server, followed by a fresh process on the same workspace and runtime root, advertises `prompt_engine` without `gates`, `gate_verdict` and `gate_action` on STDIO and HTTP, and `enable` then restores all three after another restart)                                                                                                                                                                                                                                                                      | **A gates toggle does not reach the advertised tool schema after a restart.** Worker A measured once: `system_control gates disable` persists under the scope derived from the working directory (`server`), while a freshly started server builds `prompt_engine`'s advertised schema from the `default` row, so the toggled server narrows and a fresh process re-widens. Outside this plan's class; recorded so the measurement is not lost. Worker A's row 1.11 probe adds two facts: a seeded disabled `default` row, re-saved by a fresh capture, still did not narrow its schema; and `SqliteEngine.getInstance(serverRoot)` is a process singleton whose first caller's runtime root locates the file, so the gate store's `getServerRoot()` argument never does. A source read, unverified at runtime, suggests the cause: `GateStateStore.initialize()` loads unscoped once, and a scope key not yet in memory is created enabled without reading SQLite (`gate-state-store.ts:129–145, 265–267`). A fix here revives row 1.11, so it must pin a temp runtime root in the capture and verify scripts in the same change. **Ruled by the owner 2026-09-14 (R7): fix it — the toggle exists to save tokens**. **Mechanism corrected 2026-09-14 by a read-only trace**: the toggle and a fresh server's advertised schema already use one scope key, the launch workspace id (basename of `CLAUDE_PROJECT_DIR`, else of cwd). The defect is the store: it loads only key `default` at initialize (`gate-state-store.ts:145, 189-195`) and creates an enabled state for any other key without reading SQLite (`:129-137`). Rows 2.8–2.9                                                                                                                         | planner | the owner's ruling recorded here                                                                                                                                                                          |
| 1.13 | ✓ (verified 2026-09-14 · worker A `847cc3d2`, cherry-picked as `d51fad80`; the ratchet failed at `a9b924f0` with exports 493 over 492 and files 17 over 16, and on the merged tree reports OK at 1195 findings with knip naming none of the four; the declaration file stays, since removing it adds TS7016 in `child-env.ts`; knip's `entry` now credits hand-written declarations, which also took three siblings out of the files count)                                                                                                                                               | **The knip ratchet fails on this branch: exports +1, files +1.** The planner's knip run names the whole `hermetic-server-env.d.ts` and the `SCRUBBED_KEYS` export (row 1.2), and the `describeUnusableConfigFile` and `formatConfigPathRefusal` exports in `paths.ts` (row 1.5), each referenced only inside its own module. `validate:all` runs the ratchet, so CI would fail. Ruling: fix at the source, never regenerate the baseline upward; one worker owns the count, because it is one measurement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | A       | the ratchet output before and after                                                                                                                                                                       |
| 1.14 | ✓ (verified 2026-09-14 · worker A `a9f80614`, cherry-picked as `91bd03ef`; on the merged tree the knip ratchet is OK at 1194 findings, re-exporting `formatConfigPathRefusal` exits 1 at exports 490 over 489, a `scripts/zz-orphan.d.ts` exits 1 naming the file, and the self-test passes; `plugin-test-helpers.ts` had no importer and is deleted)                                                                                                                                                                                                                                     | **The knip ratchet cannot see one new unused export on the merged tree, or an orphaned declaration file.** B's `8ac0f676` deleted the exported `setupConsoleRedirection`, so the merged tree sits at 489 exports under a 490 baseline, and a planner mutant re-exporting `formatConfigPathRefusal` passed the ratchet. Neither branch alone could show this. Row 1.13's `entry` glob credits every hand-written declaration under `scripts/` and `eslint-rules/`, so one whose `.js` is deleted would never be reported. Knip also lists `tests/e2e/helpers/plugin-test-helpers.ts` as unused, which predates this branch. Ruling: refuse orphaned declarations in the ratchet script, delete the helper if nothing imports it, then tighten the baseline once                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | A       | both mutants' exit codes, and the ratchet's self-test                                                                                                                                                     |
| 1.15 | ✓ (verified 2026-09-14 · worker A `ad935606` and `23913bc5`, cherry-picked as `c53e5872` and `de427c55`; knip-ratchet declares its real `walk`; the hermetic gate declares its textual `spawn` in the `validate:contributing` precedent's form with its fixtures identical to `ffa50a2a`; removing that `spawn` makes `validate:suite-membership` exit 1 naming the step; `validate:all` passes 58 of 58 on the merged tree)                                                                                                                                                              | **The validation suite's substrate registry no longer matches two validators this branch changed.** The PR-boundary `validate:all` failed 1 of 58 steps, new this run: `validate:suite-membership` reports that `validate:knip-ratchet` declares `[file, spawn]` while its source also walks directories (row 1.14's orphan check), and `validate:hermetic-child-env` declares `[file, walk]` while its source also spawns (row 1.3's widening). Neither row's check ran that step. Ruling: each declaration states what its script does; an accepted exception only with evidence that the detector misreads the source. **Half accepted 2026-09-14**: A's `ad935606` (cherry-picked as `c53e5872`) declares knip-ratchet's real `walk`. Its other half composed `spawnSync` at runtime in the hermetic gate's fixtures so the textual detector would not see it. That is overruled: the `validate:contributing` entry already rules a textual match is "declared rather than worked around, because the detector is textual by design"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | A       | the suite-membership output before and after, and `validate:all` exit 0                                                                                                                                   |

## Tier 2

Implements the owner's rulings R6–R8 on rows 1.9, 1.11 and 1.12. Map measured by a read-only trace on `c141a048`.

| #    | St                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Worker  | Depends | Verification                                                                                                                                   |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 2.1  | ☐ (as of 2026-09-14 · flips when a missing `MCP_WORKSPACE` directory, a missing `MCP_RESOURCES_PATH` directory and a malformed workspace `config.json` each exit 1 on STDIO and HTTP with the R3-form message, nothing on stdout and no directory created, while a workspace without `config.json` and an empty value both boot)                                                                                                                                                                                                                                                                                                                                  | **Startup refuses the three unusable path settings (R6, parent 1.9).** The check runs once in `createRuntimeFoundation` beside `getConfigPath()` (`server/src/runtime/context.ts:111`), before the config watcher (`:128`), the transport choice (`:130`) and the logs `mkdir` (`:139`). Today a missing workspace is created as a side effect, because the runtime root defaults to it (`paths.ts:205`), and `MCP_RESOURCES_PATH` is first resolved after the transport (`context.ts:169`). One refusal error type, generalized from `ConfigPathError`; `describeUnusableConfigFile` reused for the workspace file; the check stays out of the path getters. The e2e fallback case (`config-path-refusal.e2e.test.ts:180-227`) uses a malformed workspace `config.json` and becomes a refusal case | C       | —       | unit and e2e files; a mutation disabling each refusal fails a named case; a live STDIO and HTTP drive per case                                 |
| 2.2  | ☐ (as of 2026-09-14 · flips when both smoke steps in `.github/workflows/extension-publish.yml` start the server on a workspace directory that exists when the step runs)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Two release-job smoke steps start the server on a workspace that never exists** (`extension-publish.yml:227, 648`). R6 turns their silent fallback into a failed release job                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | C       | 2.1     | each step's command run locally with its env; the workflow parses                                                                              |
| 2.3  | ☐ (as of 2026-09-14 · flips when every named doc mention of `MCP_WORKSPACE`, `MCP_RESOURCES_PATH` or the workspace `config.json` states the refusal where it applies, and the `[Unreleased]` ⚠ BREAKING entry covers every unusable path setting)                                                                                                                                                                                                                                                                                                                                                                                                                 | **Docs and CHANGELOG state the refusal.** `CLAUDE.md:291` (then `AGENTS.md` re-rendered), `docs/guides/custom-resources.md:15,25-26,64-66`, `docs/reference/mcp-tools.md:1650-1653,1679-1680,1689-1697`, `server/README.md:372-382,435,449`, `docs/guides/troubleshooting.md:7-40` (its line-34 claim that invalid JSON blocks startup becomes true), the `server/src/index.ts:325-356` help text, and the existing BREAKING entry at `CHANGELOG.md:24` extended rather than duplicated                                                                                                                                                                                                                                                                                                             | C       | 2.1     | mentions enumerated at `466661fd` and after; `sync-project-guidance.js --check`                                                                |
| 2.4  | ✓ (verified 2026-09-14 · planner read of the sibling repositories: `minipuft-plugins` sets no path variable; `gemini-prompts` sets `MCP_RESOURCES_PATH` to `${extensionPath}/node_modules/claude-prompts/resources` (`gemini-extension.json:15`), which `gemini extensions install` never creates, because it clones without installing dependencies and `node_modules/` is gitignored; `opencode-prompts` writes `npx claude-prompts` with `MCP_WORKSPACE=./node_modules/claude-prompts` (`src/lib/opencode-config.ts:94-95`, `src/cli/commands/install.ts:27`), resolved from the server's working directory; both would be refused by R6 → rows 2.11 and 2.12) | **Downstream plugins may set a path R6 now refuses.** `.mcp.json:10` relies on the client expanding `${CLAUDE_PLUGIN_ROOT}`; the opencode installer's relative `./node_modules/claude-prompts` and the gemini extension's settings were not verified                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | planner | —       | each config's setting and resolved path, read in the sibling repositories                                                                      |
| 2.5  | ☐ (as of 2026-09-14 · flips when a skills-sync CLI run with `MCP_RESOURCES_PATH` naming a missing directory exits non-zero before writing anything, naming the variable)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **The skills-sync CLI keeps its own silent fallback for `MCP_RESOURCES_PATH`** (`server/src/modules/skills-sync/service.ts:50-51`), so a typo exports the bundled set into client skill directories. Inside a running server that path is unreachable after 2.1; from the CLI it is not. Same check as 2.1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | C       | 2.1     | a CLI run against a temp `HOME` writes nothing; a mutation fails the new case; `validate:arch`                                                 |
| 2.6  | ✗ KILLED (2026-09-14 · a hook cannot refuse without breaking the client's session, and the server's startup refusal already surfaces the same misconfiguration · revives if a hook-only install without the server becomes supported)                                                                                                                                                                                                                                                                                                                                                                                                                             | `hooks/lib/workspace.py:29-33` silently skips a missing `MCP_WORKSPACE`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —       | —       | —                                                                                                                                              |
| 2.7  | ✗ KILLED (2026-09-14 · R6 rules the server's startup, and `cli/src/lib/workspace.ts:20-26` already refuses a bad workspace for the CLI's own commands · revives if the owner extends R6 to every `cpm` command)                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `cli/src/commands/enable-disable.ts:38` reads `MCP_WORKSPACE` without checking it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | —       | —       | —                                                                                                                                              |
| 2.8  | ☐ (as of 2026-09-14 · flips when a `system_control gates disable` in one process leaves a fresh process on the same workspace and runtime root advertising `prompt_engine` without `gates`, `gate_verdict` and `gate_action` on STDIO and HTTP, `enable` restores them after another restart, and removing the initialize load fails those cases)                                                                                                                                                                                                                                                                                                                 | **The gate store reads persisted toggles back (R7, parent 1.12).** Load every persisted `gates` row at initialize, one per scope, so the synchronous `isGateSystemEnabled` (`gate-state-store.ts:266`) needs no lazy load and HTTP identities beyond the launch scope are covered; if the store cannot enumerate scopes within the bound, load the launch scope the way `FrameworkStateStore` does (`framework-state-store.ts:143-160, 293-306`) and return the rest under concerns. Adopt a legacy `default` row into the launch scope, mirroring `:331-388`. No `SCHEMA_VERSION` change: `kv_state` is ephemeral and a bump drops the toggle (`table-contracts.ts:150-172`). Execution-time gate checks share the fix (`prompt-executor.ts:179-186`)                                              | D       | —       | a unit restart test and a legacy-adoption case; an e2e restart drive on both transports; the named mutation; `validate:tool-schemas` unchanged |
| 2.9  | ☐ (as of 2026-09-14 · flips when CLAUDE.md, `docs/guides/gates.md` and an `[Unreleased]` Fixed entry say a gates toggle survives a restart and a legacy toggle is adopted once)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Docs state the persistence.** CLAUDE.md's Runtime State paragraph says gate enablement re-derives from config after the 2026-08-27 isolation and costs a re-toggle; for gates that stops being true                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | D       | 2.8     | the paragraph and entry read against the 2.8 behaviour; `sync-project-guidance.js --check`                                                     |
| 2.10 | ☐ (as of 2026-09-14 · flips when a disabled toggle persisted in the checkout's `state.db` narrows each script's server without the pin but neither the capture nor `verify:mcp` with it, on a tree with 2.8, and `validate:suite-membership` and the knip ratchet pass)                                                                                                                                                                                                                                                                                                                                                                                           | **The capture and verify scripts run on a temp runtime root (R8, parent 1.11).** `capture-tool-schemas.mjs:75`, `verify-mcp-surface.mjs:297` and `verify-unknown-interrupt.mjs:87` call `buildServerEnv({PORT, MCP_WORKSPACE})` with no `MCP_RUNTIME_ROOT`, so they read the checkout's `state.db`. Each creates its own temp root and removes it at exit; `buildServerEnv` gains no default, because `verify-handoff.mjs` needs two spawns sharing one `state.db`                                                                                                                                                                                                                                                                                                                                  | D       | 2.8     | the seeded-toggle positive control with and without the pin; `validate:suite-membership`; knip ratchet; `validate:hermetic-child-env`          |
| 2.11 | ☐ (as of 2026-09-14 · flips when a fresh `gemini extensions install` of `gemini-prompts`, with no `node_modules`, starts the claude-prompts server that carries R6)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **The gemini extension would refuse to start once R6 is released.** Its config points `MCP_RESOURCES_PATH` into the extension's own `node_modules`, which install never creates, and `npx claude-prompts` fetches the newest server. Today that silently serves the package's bundled resources                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | owner   | —       | an extension started from a fresh clone against a build carrying R6                                                                            |
| 2.12 | ☐ (as of 2026-09-14 · flips when a config written by `opencode-prompts install` starts the claude-prompts server that carries R6 in a project with no local `node_modules/claude-prompts`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **The opencode installer writes a workspace R6 refuses.** It defaults `MCP_WORKSPACE` to the relative `./node_modules/claude-prompts` in both project and global configs; wherever the server's working directory holds no local install, that path does not exist                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | owner   | —       | an installed config started in a project with no local `claude-prompts`, against a build carrying R6                                           |

**Gate (PR boundary)**: inside `server/`, `npm run typecheck && npm run lint:ratchet && npm run typecheck:tests:ratchet && npm run test:all && npm run validate:all` green on the merged initiative branch, plus a live drive of the refusal on both transports.

## Open questions

| #    | Question                                                                                                               | Default                                                                                                                                          |
| ---- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| OQ-1 | If row 1.4 finds the committed snapshot differs under a scrubbed capture, does the corrected snapshot land in this PR? | **RULED — moot (2026-09-14).** Row 1.4 found the committed snapshot identical under a scrubbed capture, so no corrected snapshot existed to land |
