---
title: "A chain step that names an unregistered prompt is refused at write, reported at load, and failed in CI"
date: 2026-09-07
status: reference
tags: [chains, resources, validation, contracts]
---

# Chain Integrity — one resolver, three boundaries

## Defect

A chain step whose `promptId` resolves to nothing is accepted today and fails one run later, far
from the write that introduced it.

| Boundary              | Today (measured 2026-09-06/07)                                                                                                                                                                                                                       | Site                                                                                                                                                                                                |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Resource write        | `validateChainStepReferences` returns **warnings**; its docstring says non-blocking by design; it **skips any id containing `/`**, which `PROMPT_ID_PATTERN` (`/^[a-z][a-z0-9_]*(?:\/[a-z][a-z0-9_]*)*$/`) makes the canonical form for a sub-prompt | `server/src/mcp/tools/resource-manager/prompt/utils/validation.ts:475-497`, called at `prompt-lifecycle-processor.ts:401-407`, surfaced as warnings at `:198` and `:568`                            |
| Resource load         | `validatePromptYaml` rejects a missing **edge endpoint** or a cycle among a chain's own step ids; nothing checks that a step's `promptId` is registered                                                                                              | `server/src/modules/prompts/yaml-prompt-loader.ts:393-470`                                                                                                                                          |
| CI (bundled tree)     | `validate:prompts` checks id canonicality only                                                                                                                                                                                                       | `server/scripts/validate-prompts.ts`                                                                                                                                                                |
| Runtime               | four throw sites report "converted prompt data not found for chain step"; one warns                                                                                                                                                                  | `execution-planner.ts:203`, `symbolic-command-builder.ts:165,259`, `04-parsing-stage.ts:292`, `chain-operator-executor.ts:397`                                                                      |
| Workflow IR (per-run) | already answers the same question with reason `unknown-prompt` through an injected `lookupPrompt`                                                                                                                                                    | `server/src/modules/workflow-ir/validator.ts:35`; `lookupPrompt` is constructed separately at `remainder-processor.ts:192`, `symbolic-command-builder.ts:315`, `workflow-command-builder.ts:83,105` |

Two derivations of one question ("does this prompt id resolve?") exist and disagree about
nested ids. The memory `project_chain_management_tooling` lists this as gap 4 and lists sub-prompt
scaffolding (gap 3) as open; `scaffoldChainStepDirectories` exists at `file-operations.ts:350`,
so gap 3 must be re-measured before any row assumes ordering constraints.

## Rulings (owner, 2026-09-06 · ledger `plans/contract-layer-2026-09-06.md` D1, D1a–c)

- **R1** One resolver, three boundaries. One registry-backed lookup answers "does this id resolve", and write, load, and CI all call it. The workflow-IR validator keeps its injected `lookupPrompt` but receives the same function.
- **R2** Write **refuses**. Defect fix with a provably correct target: the warning path and the `/` skip are deleted in the same change (no parity gate).
- **R3** Load **reports, does not silently load**: one structured warning per broken chain naming chain, step index, and the unresolved id. The chain still loads; the existing runtime throws remain the refusal for in-process callers.
- **R4** CI resolves against the bundled tree only (`server/resources`); overlay chains are validated at load, where the overlay is visible.
- **R5** An e2e flow that creates a parent chain before its steps changes to scaffold steps first. The test moves, not the rule.
- **R6** The four runtime throw sites stay as last-line asserts.
- **R7** (from Tier 0, 2026-09-07) A step id of the form `<chainId>/<one-segment>` is satisfied by the same call's scaffolding and is exempt from resolution at write; every other id (bare, external, or nested two or more levels below the chain) must resolve in the registry or the write is refused. Deeper nesting is what `scaffoldChainStepDirectories` already skips, so the exemption and the scaffold agree by construction.

## Rows

Status vocabulary: `☐ (as of <date> · flips when <observation>)` · `✓ <date> · <receipt>` · `⊘ (verified <date> · <reason>)` · `✗ KILLED (<date> · <reason> · revives if <observation>)`.

### Tier 0 — re-measure before design lands

| Row | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | File(s)                                                           | Change                                                                                                                                                                                                    | Depends | Verify                                                                                                  |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------- |
| 0.1 | ✓ 2026-09-07 · registry id of a nested prompt is `parent/step`, derived from the path relative to the category root (`yaml-prompt-loader.ts:660-665`), and `chain_steps[].promptId` in bundled chains is written in that same form (`scaffold_project/scaffold_analyze`). The scaffold stub's own `id:` is the bare step name (`file-operations.ts:967`) and is overridden by the path-derived id at load. So the `/` skip in `validateChainStepReferences` exempts exactly the canonical form                   | `server/src/modules/prompts/registry.ts`, `yaml-prompt-loader.ts` | Record how a sub-prompt is keyed in the registry (`parent/step`? bare `step`?) and whether a step's `promptId` in `chain_steps` is stored canonical or bare                                               | —       | `node -e` against `dist/` or a unit probe: create a chain via the loader fixture, print registered ids  |
| 0.2 | ✓ 2026-09-07 · gap 3 is CLOSED: `createPrompt`/`updatePrompt` write the prompt (`prompt-lifecycle-processor.ts:606`) and `updatePromptImplementation` then calls `scaffoldChainStepDirectories` (`file-operations.ts:350`) for every step whose id starts with `<chainId>/`, one level deep, skipping existing dirs. The reference check at `:485` runs BEFORE that write, so a new chain's own steps are unresolvable at check time → R7. Memory `project_chain_management_tooling` gap 3 line corrected in 4.3 | memory `project_chain_management_tooling`                         | Re-measure sub-prompt scaffolding: does a `resource_manager create` with `chain_steps` create the step directories in the same call (`file-operations.ts:350`)? Update the memory's gap 3 line either way | —       | `tests/unit/mcp-tools/resource-manager/prompt/prompt-lifecycle-processor.test.ts` cases naming scaffold |
| 0.3 | ✓ 2026-09-07 · 7 files write chains: `tests/integration/{skills-sync/export-command, mcp-tools/p7-acceptance.integration, mcp-tools/prompt-patch-update, prompts/nested-prompt-discovery, chain/p6-workflow-ir.integration, pipeline/delegation-operator-flow, resources/resource-registration}.test.ts`; none under `tests/e2e`. Row 1.4 reads each and reorders any that writes an external (non-`<chainId>/`) step before it exists                                                                           | `tests/integration/**`, `tests/e2e/**`                            | Enumerate every test that creates a chain whose steps do not yet exist (`rg -l "chain_steps" tests/integration tests/e2e` then read each) — R5 input                                                      | —       | list in this row's receipt                                                                              |
| 0.4 | ✓ 2026-09-07 · all four are the same derivation, "find the converted prompt with this exact id": `remainder-processor.ts:192` linear `prompts.find`; `symbolic-command-builder.ts:315` map lookup with `findPrompt` fallback; `workflow-command-builder.ts:83,105` wrap the injected `PromptLookup` (`symbolic-command-builder.ts:59`). The IR sites additionally project to `WorkflowPromptInfo`; that projection stays, the lookup unifies (3.2)                                                               | the four `lookupPrompt` sites above                               | Classify each: a thin lambda over the converted-prompt map (migrate in 3.2) or a different derivation (leave, say why)                                                                                    | —       | table in receipt                                                                                        |

### Tier 1 — the resolver and the write boundary

| Row | Status                                                                                                                                                                                                                                                                                                      | File(s)                                                                                   | Change                                                                                                                                                                                                                                               | Depends  | Verify                                                                                                                     |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------- |
| 1.1 | ✓ 2026-09-07 · `modules/prompts/chain-step-resolution.ts` — `resolveChainStepPromptId` / `resolveChainSteps` / `describeUnresolvedChainStep`, pure. `test:match -- chain-step-resolution` → 13 passed, 13 total                                                                                             | `server/src/modules/prompts/` (new pure fn beside the registry, name it for the behavior) | One function `(promptId, chainId, registeredIds) → resolved \| scaffolded-by-this-write \| unresolved`: exact-id lookup (0.1) plus the R7 exemption. No I/O, no logger                                                                               | 0.1      | `test:match -- chain-step-resolve`: bare id, nested id, unknown id, malformed id                                           |
| 1.2 | ✓ 2026-09-07 · `rg -c "includes\('/'\)" src/mcp/tools/resource-manager/prompt/utils/validation.ts` → 0; the docstring now reads "BLOCKING"; returns `problems`, not `warnings`; nested-unknown-id case covered by `chain-step-resolution.test.ts` and `chain-step-reference-refusal.test.ts`                | `validation.ts:475-497`                                                                   | `validateChainStepReferences(steps, chainId, registeredIds)` uses 1.1, returns problems (not warnings); the `/` exemption is replaced by the R7 exemption (`<chainId>/<one-segment>` only); delete the docstring sentence that promised non-blocking | 1.1      | `rg -n "includes\('/'\)" validation.ts` → 0; unit test: nested unknown id is a problem                                     |
| 1.3 | ✓ 2026-09-07 · `test:match -- chain-step-reference-refusal` → 8 passed (5 of them FAILED against the pre-change code: the two nested cases, the bare case, the full-replacement update and `chain_step_operation:"add"`); `chainIntegrityWarnings` is gone from `prompt-lifecycle-processor.ts`             | `prompt-lifecycle-processor.ts:198,401-407,568`                                           | Refuse the write with one addressed line per problem (`step N references unknown promptId 'x'`), nothing written, nothing scaffolded; delete the `chainIntegrityWarnings` plumbing at `:198`, `:485`, `:568` (R2, R7)                                | 1.2, 0.2 | `prompt-lifecycle-processor.test.ts`: create + update + `chain_step_operation: add` each refuse; a valid chain still saves |
| 1.4 | ✓ 2026-09-07 · `test:match -- chain-step-refusal` → 5 passed against the real `FileOperations`: refusal leaves no directory on disk, the registered step saves, all-`<chainId>/` steps save AND scaffold. R5 no-op — the seven chain-writing tests from 0.3 all pass unchanged (`test:integration` 805/805) | `tests/integration/resource-manager/**`                                                   | Integration test: `resource_manager create` with one dangling step → refused with the step index; same call with the step scaffolded → succeeds. Any test found in 0.3 is reordered per R5                                                           | 1.3, 0.3 | `npm run test:integration`                                                                                                 |

### Tier 2 — load diagnostic and CI

| Row | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | File(s)                                                                                                 | Change                                                                                                                              | Depends | Verify                                                                                                                        |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------- |
| 2.1 | ✓ 2026-09-07 · `PromptRegistry.setLivePrompts` → `reportBrokenChains`. Fixture workspace with a dangling step, `MCP_WORKSPACE` set, built server: `grep -c "Chain 'broken_chain'"` → 1, line reads `[WARN] Chain 'broken_chain' references 1 unregistered prompt(s): step 2 references unknown promptId 'publish_release' {"chainId":"broken_chain","unresolvedSteps":[{"stepIndex":1,"promptId":"publish_release"}]}`; the chain's other step (bundled `readme_improver`) resolved, so the overlay merge provably ran first. `verify:mcp` → 18/18. Unit coverage `registry-broken-chain-report.test.ts` 7/7 | `registry.ts` load completion (after every prompt is registered)                                        | Run 1.1 over every chain; `logger.warn` once per chain with `{chainId, stepIndex, promptId}` (R3). No new state, no refusal at load | 1.1     | start the server with a fixture chain whose step is dangling → one warning line; `verify:mcp` still 18/18                     |
| 2.2 | ✓ 2026-09-07 · `npm run validate:prompts` → OK on the tree (39 prompts). Positive control: `mv resources/prompts/examples/deep_analysis/synthesis{,_renamed}` → exit 1, `CHAIN examples/deep_analysis/prompt.yaml — chain 'deep_analysis' step 3 references unknown promptId 'deep_analysis/synthesis'`; restored → exit 0                                                                                                                                                                                                                                                                                   | `server/scripts/validate-prompts.ts`                                                                    | After id canonicality, resolve every chain step against the bundled tree using 1.1 (R4); one addressed line per problem             | 1.1     | `npm run validate:prompts` on the tree → OK; positive control: temporarily rename a bundled step dir → FAIL naming it; revert |
| 2.3 | ✓ 2026-09-07 · `validate:prompts --self-test` gained `good/ok_chain` (must pass) and `bad/broken_chain` (must fail by position). Falsified by stubbing `findChainProblems` to return `[]` → exit 1, `a chain step naming no prompt was NOT reported by position`. Wired as `validate:prompts` already was (SUITE `converse` updated); `validate:suite-membership` → 55 in SUITE, all wired                                                                                                                                                                                                                   | `validate-prompts.ts` `--self-test` (add if absent, sibling: `validate-semantic-module-descriptors.ts`) | Fixture with one dangling step must FAIL by message; a valid fixture must pass                                                      | 2.2     | `tsx scripts/validate-prompts.ts --self-test`                                                                                 |

### Tier 3 — runtime sites and sibling derivations

| Row | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | File(s)                                                                                      | Change                                                                                                                           | Depends  | Verify                                                                                          |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------- |
| 3.1 | ⊘ (verified 2026-09-07 · R6: the four throw sites are the in-process assert and stay; no edit)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `execution-planner.ts:203`, `symbolic-command-builder.ts:165,259`, `04-parsing-stage.ts:292` | none                                                                                                                             | —        | —                                                                                               |
| 3.2 | ✓ 2026-09-07 · `engine/execution/workflow-prompt-lookup.ts` owns the projection + lookup; `rg -n "lookupPrompt" src` → `workflow-command-builder.ts:85` calls it, `:97` passes the shared `findPrompt` unwrapped, `remainder-processor.ts:196` calls it, `symbolic-command-builder.ts:318` carries the one-line comment (a memo over prompts THIS call already resolved; the `??` arm IS the shared lookup). Two verbatim copies of the `WorkflowPromptInfo` projection collapsed to one                                                                                                                                                                                                                                               | sites classified same-derivation in 0.4                                                      | Replace the lambda with 1.1 so there is one derivation; different-derivation sites get a one-line comment naming why             | 0.4, 1.1 | `rg -n "lookupPrompt" server/src` → each site either calls the shared fn or carries the comment |
| 3.3 | ✓ 2026-09-07 · THROWS. Reachable, not unreachable: `node-step-projection.synthesizeStep` builds a step with no `convertedPrompt`, so a run resumed against a catalog that no longer holds the prompt lands here. It rendered `Execute the prompt "<id>"` / `Complete this step manually` — a step that cannot run, counted as progress by the footer. Now `PromptError("Converted prompt data not found for chain step: <id>")`, the same message as its four siblings. `chain-operator-executor.test.ts` 'refuses a step whose prompt is missing, like the four sibling assert sites'. The sibling `find` at `:163` stays a soft miss with a comment: it quotes an ALREADY-EXECUTED step for review, whose own render already refused | `chain-operator-executor.ts:397`                                                             | Today it `warn`s where the other four throw. Decide: throw like its siblings, or ⊘ with the reason it cannot reach a dangling id | 3.2      | unit test or receipt                                                                            |

### Tier 4 — docs, changelog, memory, full validation

| Row | Status                                                                                                                                                                                                                                                                                                                                             | File(s)                                                            | Change                                                                                                                                                                  | Depends       | Verify                                            |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------- |
| 4.1 | ✓ 2026-09-07 · `docs/concepts/chains-lifecycle.md` §Step References Must Resolve (three-boundary table + the one-level `<chainId>/<step>` exemption, current behaviour only); `docs/reference/mcp-tools.md` `chain_steps` row and the paragraph under the prompt-parameter table. `rg -n "refus" docs/concepts/chains-lifecycle.md` → lines 59, 64 | `docs/concepts/chains-lifecycle.md`, `docs/reference/mcp-tools.md` | State current behavior only: a chain step must name a registered prompt; the write is refused otherwise; startup warns for bundled/overlay chains; CI checks the bundle | 1.3, 2.1, 2.2 | `rg -n "refus" docs/concepts/chains-lifecycle.md` |
| 4.2 | ✓ 2026-09-07 · `rg -n "unregistered prompt" CHANGELOG.md` → line 53, under `[Unreleased] → Fixed`                                                                                                                                                                                                                                                  | `CHANGELOG.md`                                                     | Consumer voice: "resource_manager refuses a chain whose step names an unregistered prompt; it previously saved it with a warning and the run failed at that step"       | 1.3           | `rg -n "unregistered prompt" CHANGELOG.md`        |
| 4.3 | ✓ 2026-09-07 · memory `project_chain_management_tooling` marks gaps 3 and 4 closed with commits; its `holds_while` now fails if the `/` skip returns (`! rg -q "includes('/')" validation.ts`)                                                                                                                                                     | memory `project_chain_management_tooling`                          | Gap 4 closed with the commit; gap 3 per 0.2; `holds_while` re-pointed at a probe that measures gap 4 (`rg -q "includes\('/'\)" validation.ts` must FAIL)                | 0.2, 1.3      | memory-search `--stale` clean                     |
| 4.4 | ✓ 2026-09-07 · `validate:all` → ✅ All 55 validation steps passed (75.4 s). `test:all` → unit 3048 passed/1 skipped in 232 suites · integration 810 passed in 64 suites · e2e 190 passed/2 skipped in 8 suites. `verify:mcp` → OK: 18/18 checks passed. `git diff origin/main --stat -- server/src/mcp/contracts tooling` → empty                  | —                                                                  | `npm run validate:all && npm run test:all && npm run verify:mcp` in the worktree                                                                                        | all           | command output in receipt                         |

## Execution dispatch

- Worktree from `origin/main` via `npm run worktree:create -- ../claude-prompts-mcp-chain fix/chain-integrity --from origin/main` after #266 and the frontmatter fix merge.
- Tier 0 is measurement; its receipts are the inputs to 1.1 and 1.3. Do not start Tier 1 without 0.1 and 0.2.
- One commit per tier is the floor; 1.2+1.3+1.4 may be one commit (behavior + its test + its refusal message cannot be reverted apart).
- PR type `fix(resources)`; title names the outcome ("a chain step that names an unregistered prompt is refused at write").

## Completion criteria

| Criterion                                                | Validation                                                                           |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| A dangling step is refused at write, nested ids included | 1.4 integration test                                                                 |
| A dangling bundled step fails CI                         | 2.2 positive control + 2.3 self-test                                                 |
| Startup names every broken chain once                    | 2.1 fixture run                                                                      |
| One derivation of "does this id resolve"                 | 3.2 grep receipt                                                                     |
| Tool surface, schema, tarball unchanged                  | `git diff origin/main -- server/src/mcp/contracts tooling` empty; `verify:mcp` 18/18 |
| Docs and changelog describe current behavior only        | 4.1, 4.2                                                                             |

## Risks

- **A client that authors parent-then-steps.** If 0.2 shows scaffolding does NOT create steps in the same call, R2 breaks that authoring order. Then the refusal must exempt steps the same call scaffolds, and this plan gains a row before 1.3. Decide on 0.2's evidence, not in advance.
- **Overlay chains referencing bundled prompts.** R4 keeps CI blind to them by design; 2.1 is the only check that sees the overlay. Verify 2.1 runs after overlays merge, not before.
- **Nested id form.** If 0.1 shows the registry keys sub-prompts differently from `chain_steps`, 1.1 must normalize both sides; a mismatch there would make the refusal fire on every valid chain.

## Sources

- Ledger: `plans/contract-layer-2026-09-06.md` (D1, D1a–c, D13, D14)
- Prototype samples (shape of a finding, not in scope here): `plans/prototypes/contract-layer-2026-09-06/`
- Memory: `project_chain_management_tooling` (gaps 3–4), `reference_chain_execution_internals`
