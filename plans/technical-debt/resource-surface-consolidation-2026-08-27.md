---
title: "resource_manager surface consolidation — where resources live and what is authorable"
date: 2026-08-27
status: active
tags: []
---

# resource_manager Surface Consolidation

Successor to `resource-manager-settability-matrix-2026-08-13.md`, which retires to `reference` with
every row terminal. That file remains the **audit and decision record** — the field-by-field
settability matrix (its §1–§8), owner rulings D1–D8 (its §10, §12, §14), the storage-model option
space (its §13), and the Arc 1 execution record (its §12.5–§14.9). Nothing here restates it; read it
for why, read this for what is left.

**Why a successor rather than a longer file.** The matrix began as a read-only audit and accreted
an execution arc, two decision sets and a design. At ~1,200 lines it held five documents with one
`status:`, so "is it done" had no answer — Arc 1 was complete while 29 rows were open. The audit is
finished and stable; the work it uncovered is not. Splitting on that seam gives each half a
lifecycle it can actually reach.

## What already landed (do not redo)

D8 Arc 1 — read and write now agree about where a resource lives. Eleven commits on
`feat/settability-parity`, `validate:all` 48/48.

| Landed                                                                               | Commit     |
| ------------------------------------------------------------------------------------ | ---------- |
| prompt writes resolve through `PathResolver`                                         | `f2abd9c5` |
| gate writes resolve through `PathResolver`                                           | `4fe5061f` |
| framework writes resolve; plus the loader-singleton and registry-loader read defects | `9e229e1e` |
| e2e coverage for the wiring, mutation-verified                                       | `d881dad2` |
| startup logs the resolved root and served count per resource type                    | `92cafa83` |

Styles were found to have **no** write path (`resource_type` is `z.enum(['prompt','gate','framework'])`),
so there was nothing to unify — P3.1 below is what would create one.

## Standing constraints

- **The server learns no "personal library" concept** (D8). The rule is: a write goes to the
  highest-precedence writable root, and the bundled tree always loads as fallback. A personal
  library is that mechanism configured, not a feature. No `target:` parameter.
  The second half of that rule was **aspirational until P1.0a** (2026-08-28): a workspace resource
  directory REPLACED the bundled tree rather than layering over it. It now holds for all four
  types.
- **Removing a union member is breaking** (CLAUDE.md §Public API Contract). P2 removes `dry_run`
  and `chain_step_operation:'replace'`; both need CHANGELOG breaking entries and ride the in-flight
  major.
- **No parity gates** (`cleanup-standards.md`). Replacements ship on and delete the old path in the
  same change.
- **A write goes to the highest-precedence writable root, and carries the resource's whole subtree
  with it** (D8 + P1.2, 2026-08-30). The first half was already true; the second was not, and its
  absence was silent — an update re-materialised the prompt from its fields, so anything on disk
  that is not a field was lost. Any new resource type inherits both halves.
- **Every join whose segments come from a tool payload resolves through `resolveContainedPath`**
  (P1.7). Enforced by `validate:contained-resource-writes`, not by review.
- **Probes over `resources/prompts/` need `rg --no-ignore`** until P1.6 lands — **84** of 123
  prompts are untracked (authored 83; re-measured 2026-08-28 in the MAIN checkout, `123 - 39`).
- **Prompt-count probes must name the checkout.** A worktree checks out tracked files only, so
  every "on disk vs tracked" ratio is 39/39 here and 123/39 in main. Any P1 probe that compares the
  two runs against `/home/minipuft/Applications/claude-prompts-mcp`, never a worktree.

---

## P1 — Storage model, Arc 2

Closes the matrix's T1-F1, T1-F2, T1-F3, T1-F5 and P7-F4. Arc 1 made the four write paths agree
with the read paths; Arc 2 makes a write prefer the overlay, so a personal store is reachable.

**Blocking question first.** P1.0 gates P1.6 only; P1.1–P1.5 may proceed without it.
**Ruled 2026-08-28 — its premise was false, and answering it uncovered P1.0a.** See the two rows
below; the reasoning is in the implementation notes.

| #     | St                                                                                                                                                                                                                                                                | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Depends | Verification                                                                                                                              |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| P1.0  | ⚠ ✓ RULED (verified 2026-08-28 · premise false: `MCP_WORKSPACE` does NOT drive all three)                                                                                                                                                                         | **No new env var.** `MCP_RUNTIME_ROOT` (`paths.ts:130`) already pins `runtime-state/` and relative `logs/` independently, so a personal library sets `MCP_WORKSPACE` and pins `MCP_RUNTIME_ROOT`. It must be `MCP_WORKSPACE`: overlay detection compares workspace to package root, so `MCP_RESOURCES_PATH` alone leaves overlays off                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | —       | ruling in the implementation notes; `MCP_RUNTIME_ROOT` added to `src/index.ts` help and CLAUDE.md §Environment, where it was undocumented |
| P1.0a | ✓ (verified 2026-08-28 · a workspace holding one framework boots and serves the bundled 8; one prompt serves 40, not 1; an empty styles dir serves 4)                                                                                                             | **A workspace resource dir REPLACED the bundled tree rather than overlaying it.** `resolveResourceSubdir` returns the first existing candidate and stops. Frameworks: `FATAL: Framework 'cageerf' not found`, process exits 1. Prompts/styles: silent subset. Fixed for all four types — bundled dir is now the lowest-precedence contributing root. Gates additionally had reads on `resolveGatesDir()` (package-only) while writes used `getGatesPath()`; both now resolve the same root                                                                                                                                                                                                                                                                                                                                               | —       | `tests/e2e/bundled-resource-fallback.e2e.test.ts` — 5 assertions, all 5 red against the reverted fix                                      |
| P1.1  | ✓ (verified 2026-08-30 · `inspect probe_only` reports the overlay root, `inspect implementation_plan` the package root, on one live server)                                                                                                                       | `PromptData.sourceRoot` / `ConvertedPrompt.sourceRoot`, stamped in `loadAndConvertPrompts` where the root IS the argument — one pass loads one root, and every root (bundled base, primary, each overlay) arrives there on its own call. Threading it through `loadFromDirectories` → `loadYamlPrompt` → `yamlToPromptData` would have touched three signatures to carry a value constant for the whole pass. Kept OUT of `canonicalPromptSnapshot`, so provenance never reaches a version snapshot or a diff. `inspect` exposes `source_root` plus `edit_copies_on_write`, stated rather than left to a comparison of two absolute paths. Authored anchor `yaml-prompt-loader.ts:518` re-measured and EXACT                                                                                                                             | —       | `resource-copy-on-write.e2e.test.ts` — mutation: un-stamping `sourceRoot` fails 5 of 11                                                   |
| P1.2  | ✓ (verified 2026-08-30 · updating `implementation_plan` leaves 12 of 13 files byte-identical, the 13th being the `prompt.yaml` the update was asked to change)                                                                                                    | ⚠ **The row's premise was understated.** An update ALREADY resolved the highest-precedence writable root and already copied the top-level files; what it did not carry was the on-disk SUBTREE. Measured: editing `description` alone replaced all 5 chain steps with 42–55 byte stubs (`discovery/user-message.md` 3852B → 50B, and the served step then returned the stub), and dropped all 4 files under `create_framework`'s `tools/`. Frameworks failed outright with `Files may be corrupted`. Fixed by copying the whole source subtree BEFORE the write, which turns the fresh-directory case back into the ordinary one — `suppliedKeys` and the scaffold-skip were already correct and were being handed an empty directory. Whole-subtree, not a list of known kinds. Per owner ruling R1 the receipt names the fork out loud | P1.1    | `resource-copy-on-write.e2e.test.ts` — 6 cases; mutations: no-copy-source fails 4, framework-writable-root-only fails 1                   |
| P1.3  | ✓ (verified 2026-08-30 · prompt, gate and framework each refuse naming where the resource lives; deleting your own copy says the bundled one is served again; a workspace-only delete says no such thing)                                                         | ⚠ **The authored falsifier already passed before any work** — a bundled-only resource is not found in the writable root, so the delete refused and touched nothing. A stale `☐`. The real defect was that all three refusals stated a FALSE reason: `Prompt not found` for a prompt the same server had just inspected, and `Gate/Framework directory not found: <path>` naming paths never meant to exist. Each now names the bundled root, the caller's own root, and update-as-the-alternative. Covers `focus`/`liquescent`/`radiant`/`verify`, which ship but are absent from P4.3's stale literal, so they refuse for the reason true of them. Per ruling R2, deleting your own copy succeeds and says the bundled one is served again                                                                                              | P1.1    | `resource-copy-on-write.e2e.test.ts` — 3 refusal cases + a re-exposure case + a no-fallback control                                       |
| P1.4  | ✓ (verified 2026-08-29 · `grep -rn "readCategoryShipStatus\|resolveCategoryShipStatus\|CategoryShipStatus\|category_ship_status"` over `src`, `tests`, `tooling`, `docs` returns zero; a real write's `affectedFiles` all sit under the root the receipt reports) | Receipt reports the write destination root; retired `category_ship_status`, `readCategoryShipStatus`, `resolveCategoryShipStatus` and the 60-line mini-`.gitignore` engine behind them (~200 lines, plus 164 lines of test). P1.6 deleted the `.gitignore` they parsed, so the answer became the constant `ships: true` — a warning that could no longer fire and two unit suites that could no longer run. `resource_root` already resolved through the same `getResolvedPromptsDirectory()` call `FileOperations` writes through; the row's second half was making that checkable rather than adding a field. **Breaking**: `category_ship_status` leaves the mutation receipt                                                                                                                                                         | P1.2    | `grep` returns zero; `file-operations.validation.test.ts` binds every written path to the receipt's root                                  |
| P1.5  | ✓ (verified 2026-08-29 · both consumers serve the personal store at `~/.claude/resources` merged over the bundled base)                                                                                                                                           | Personal store moved outside every checkout, reached by `MCP_RESOURCES_PATH` set machine-locally in `~/.claude/settings.json` (plugin) and `~/.config/claude-prompts-catalog.env` (catalog). Executed as tier P1.5-X. The originals under `server/resources/prompts` remain in place — deleting them is P1.6                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | P1.0    | done: see tier P1.5-X                                                                                                                     |
| P1.6  | ✓ (verified 2026-08-29 · main reports no ignored `prompt.yaml` under `server/resources/prompts`; 39 on disk, 39 tracked; served ID set identical before and after at 120)                                                                                         | Deleted the 84 migrated prompts (231 files) plus `.gitignore` and `.ignore`, whose only rationale was the sibling gitignore. Every deleted file was confirmed present and byte-identical in the personal store first. The git diff is ONE removed file, because the other 231 were untracked — which is precisely the invisibility this row existed to end                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | P1.5    | done: `git status --ignored` clean, set difference empty both directions                                                                  |

### P1.7–P1.8 — path containment (found while measuring P1.2, ruled in-arc by the owner)

| #    | St                                                                                                                                                                                                           | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Depends | Verification                                                                                                |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | ----------------------------------------------------------------------------------------------------------- |
| P1.7 | ✓ (verified 2026-08-30 · commit `233b2bf2`, which also carries P1.8's validator — see the receipt correction below · all four traversal payloads refuse and write nothing; four benign controls still write) | A caller-supplied segment could steer a resource write outside the resources root — prompt `category`, gate `id`, framework `id`, each measured writing outside and reporting success. `validateCategoryName` had **zero call sites**; the prompt `id` was guarded by a validator reached only from the draft service. Every payload-derived join now resolves through `resolveContainedPath`, including framework `phasesFile`/`judgePromptFile`, which are read back to the client | —       | `contained-path.test.ts` (18 cases, 2 mutations killed) + `resource-path-containment.e2e.test.ts` (8 cases) |
| P1.8 | ✓ (verified 2026-08-30 · commits `233b2bf2` + `b6e66d6f` · the enumeration found 3 sites no per-type pass reached; stripping the guard from one writer exits 1)                                              | The class-closer for P1.7. `validate:contained-resource-writes` selects every file that resolves a resources root and writes, and fails one lacking the guard. It found the HTTP `create_category` handler, then — once widened to catch roots rolled by hand from `getServerRoot()` — framework `delete` (which also resolved the WRONG root, the D8 Arc 1 defect missed at that site) and `skills-sync`. Carries a satisfied-exception check that fired on its own first exemption | P1.7    | `--self-test` + a positive control stripping the guard from `gate-file-writer.ts`; in `validate:all` (50)   |

**Receipt correction (2026-08-30): the commit mapping was crossed, in both directions.** P1.7 named
`233b2bf2` as though that commit were P1.7 alone, and P1.8 named no commit at all. Re-measured
against `git show --stat`:

- `233b2bf2` carries P1.7 **and** the first half of P1.8 — `validate-contained-resource-writes.js`,
  its `run-validation-suite.js` registration, and the HTTP `create_category` site the enumeration
  found. The commit did more than its row claimed.
- P1.8's remaining two findings — framework `delete` (which also resolved the WRONG root, the D8
  Arc 1 defect missed at that site) and `skills-sync` — landed in `b6e66d6f`, whose subject is
  copy-on-write (P1.2). That commit also did more than its subject claimed.

Neither fix is wrong and no work is missing; what was wrong is that the ledger could not lead a
reader from either row to the code. Kept as a correction rather than a silent rewrite, because the
reusable part is the shape: **an enumeration gate and the per-site fixes it finds tend to land in
one commit**, so a row owning the gate and a row owning the sites will compete for it and one goes
unattributed — unless the receipt is written from `git show --stat` rather than from memory.

**Gate P1**: a prompt authored from a worktree survives that worktree's removal, and
`server/resources/prompts` has no `.gitignore`.

### Tier P1.5-X — cut over to the personal library

Executes P1.5. Composed 2026-08-29 after establishing how the server is actually launched:
`claude --plugin-dir <repo>`, which reads `.mcp.json` at the plugin root. That file is **tracked and
shipped**, so it must not name a personal path — the cutover removes its resources line rather than
editing it, and the personal root moves to machine-local configuration.

Measured basis: with `MCP_RESOURCES_PATH` absent the server resolves `packageRoot/resources` and
serves the same 39 bundled prompts, so the line is redundant for dev and installed users alike.
With it supplied from the environment it serves 119 over the bundled base.

**Two consumers, not one.** The Claude Code plugin server is STDIO and per-conversation; the
`claude-prompts-catalog` systemd unit is long-lived HTTP on :9090. They cannot be one process — a
STDIO server has no port to share — so "one server" is unavailable, and the goal is one LIBRARY read
by both. Each keeps its own machine-local env source.

**Ordering is load-bearing**: every env flip is blocked on P1.5a. Main's `dist/` predates P1.0a, so
pointing any consumer at the personal store before rebuilding serves 84 prompts and silently drops
the 39 bundled ones.

| #     | St                                                                                                                                                                   | Change                                                                                                                                                                                                                                                                                                                                                                | Depends     | Verification                                                          |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------- |
| P1.5a | ✓ (verified 2026-08-29 · main's rebuilt `dist/` serves 120 against an external resources root, where the pre-rebuild binary served 81)                               | Landed `feat/settability-parity` on main by fast-forward and rebuilt `server/dist/`. **Not pushed** — ahead of `origin/main`, owner reviews pushes per push                                                                                                                                                                                                           | —           | done: same env, same binary path, 81 → 120                            |
| P1.5b | ✓ (verified 2026-08-29 · with no override the server resolves the identical directory and serves the identical count; a control with the old explicit value matches) | Removed the redundant resources path. ⚠ **Edited the wrong file first**: `.mcp.json` is a RENDERED projection of canonical `mcp.json` (`scripts/render-targets.json`), caught by `validate:render-drift`. Corrected at the source and re-rendered                                                                                                                     | P1.5a       | done: 120 from `<plugin>/server/resources/prompts` either way         |
| P1.5c | ✓ (verified 2026-08-29 · `ANTHROPIC_DEFAULT_HAIKU_MODEL` from `settings.json` is ABSENT in the `claude` process and PRESENT in both MCP subprocesses)                | `~/.claude/settings.json` `env` is the mechanism — Claude Code injects it into MCP subprocess environments rather than into its own. ⚠ The row called this unanswerable without a restart; it was answerable immediately by tracing a value already sitting in that block. `MCP_RESOURCES_PATH` added there, machine-local                                            | P1.5b       | done: propagation traced through a live subprocess                    |
| P1.5d | ✓ (verified 2026-08-29 · service active, its `/proc/<pid>/environ` names the personal root, and it serves a personal-only prompt over HTTP)                          | Catalog points at the same library via `~/.config/claude-prompts-catalog.env`. Also DELETED the unit's redundant `Environment=MCP_RESOURCES_PATH`, so precedence no longer depends on unit line order — the same shape as P1.5b                                                                                                                                       | P1.5a       | done: `action_plan` returns 200, a bogus id returns 404               |
| P1.5e | ✓ (verified 2026-08-29 · restarted plugin server logs 120 over the bundled base; a personal-only and a bundled-only prompt both resolve through the live tool)       | Restarted both consumers and verified each. Plugin server pids are new, carry `MCP_RESOURCES_PATH` injected from `settings.json`, and report `resource_root` as the personal store. `action_plan` (personal-only) and `create_framework` (category `examples`, absent from the personal store) both inspect successfully — a replacement would have failed the second | P1.5c P1.5d | done: both consumers, both directions                                 |
| P1.5f | ✓ (verified 2026-08-29 · zero tracked CONFIG or SOURCE files name a personal path)                                                                                   | Writeback. ⚠ **Falsifier was unsatisfiable as authored**: 14 tracked files already contain `/home/minipuft`, all `plans/**` reference prose quoting measured sessions, which is legitimate and permanent. Rescoped to config and source, where the count is 0                                                                                                         | P1.5e       | `git ls-files \| rg -l` for the path, minus `plans/`, returns nothing |

**Gate P1.5-X — PASSED (verified 2026-08-29).** Both consumers serve the personal library merged
over the bundled base, and no config or source file in the repo names a personal path. Proven in
both directions through the live tool, not only from logs: a personal-only prompt and a bundled-only
prompt each resolve, so the merge is distinguishable from a replacement.

**Filed, not done** — whether the :9090 catalog has a live consumer at all. No connections were
observed on it, and t3code's own tests reference a discovered endpoint on a different port
(`127.0.0.1:41000`) with `promptSourceId: "claude-prompts"`. That is a t3code integration question,
not a resource-surface one; P1.5d points the service at the right library either way.

### P1.5g — merge identity (found while proving P1.6 is 1:1)

| #     | St                                                                                                                   | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Depends | Verification                                                                                          |
| ----- | -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------- |
| P1.5g | ✓ (verified 2026-08-29 · served id sets identical with and without the originals: 120 = 120, zero lost, zero gained) | `mergePromptResults` keyed `convertedPrompts` on **display name**, which nothing makes unique. Three collide in this catalog once a personal library is overlaid: `Content Analysis`, `Deep Analysis`, `Initial Scan`. An overlay prompt therefore EVICTED an unrelated bundled prompt sharing its label — a personal `analysis/initial_scan` removed the bundled `examples/deep_analysis/initial_scan` from the served catalog. `promptsData` keys on id and still reported both, so the startup count looked right while the tool could not resolve the prompt. Re-keyed on `category/id` | —       | `tests/unit/runtime/prompt-merge-identity.test.ts` — 3 assertions, all 3 red against the reverted key |

**This is what made P1.6 safe.** Before it, deleting the originals dropped `examples/deep_analysis/initial_scan` from the served set while every count still read plausibly. The check that caught it compares served ID SETS between the current tree and the post-deletion shape, not counts — a count comparison would have read 120 → 119 and been dismissed as the expected duplicate collapsing.

**Gate P1 — PASSED (verified 2026-08-29).** A prompt authored from any checkout now lives in
`~/.claude/resources/prompts`, outside every worktree, so it survives that worktree's removal; and
`server/resources/prompts` has no `.gitignore`, with on-disk and tracked counts equal at 39.

**P1 Arc 2 complete (2026-08-30).** P1.1–P1.4 are ✓, plus P1.7–P1.8 which the arc found rather
than planned. Those rows asked how WRITES behave once a resource is resident — the question the
storage model made reachable but did not answer.

**Validation is 49/50, and the missing one is not this plan's.**
`validate:plan-row-tracking` fails on 19 unstamped rows in
`plans/features/mid-chain-unknown-surfacing-2026-08-20.md`. Confirmed pre-existing at HEAD by
stashing. Deliberately NOT fixed here: that plan was committed 15 minutes before this arc's first
measurement by a concurrent session working in this same checkout, and editing an active session's
plan is the shared-HEAD hazard `CLAUDE.md` names. It is recorded rather than absorbed, because a
gate nobody can pass blocks every future tier — if that session does not stamp them, the next
session in this plan should.

---

## P2 — Settability verbs

The matrix's rows 2, 3, 4 and 5b, plus SF-2. D1 established these are **one missing verb**
("remove"), not four defects; fixing them separately would produce four conventions for one verb.

| #    | St                                                                                                                              | Change                                                                                                                                                                                                                | Depends   | Verification                                                                         |
| ---- | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------ |
| P2.1 | ☐ (as of 2026-08-27 · flips when `unset: ['system_message']` deletes `system-message.md` and drops the `systemMessageFile` key) | `unset: [keys]` parameter, reusing the `suppliedKeys` write-scope machinery. Closes rows 2 and 4 together and dissolves row 3's "empty array means no change"                                                         | —         | an update sending `unset` removes the key; `tools: []` still means "set to empty"    |
| P2.2 | ☐ (as of 2026-08-27 · flips when a delete preview runs without `confirm: true`)                                                 | `action: 'preview'` replaces `dry_run`, which is **removed**. Preview is not in `DESTRUCTIVE_ACTIONS`, so non-destructiveness is structural rather than a flag someone can get backwards — which is how SF-2 happened | —         | preview reaches dispatch with no `confirm`; `rg dry_run` returns zero                |
| P2.3 | ☐ (as of 2026-08-27 · flips when `tools: []` clears the on-disk id list AND a dropped id's `tools/{id}/` directory is removed)  | `tool_operation: 'add'\|'remove'` with directory deletion on explicit removal only, behind `confirm`. A narrowed `tools` array unbinds without deleting                                                               | P2.1      | a removal deletes the directory; a narrowed array does not                           |
| P2.4 | ☐ (as of 2026-08-27 · flips when an `update`-at-index operation exists and `'replace'` is gone from the enum)                   | `chain_step_operation: 'update'` at index; **remove** the vestigial `'replace'` no-op at `validation.ts:443`                                                                                                          | —         | a single step field edits without resending the array; `rg "'replace'"` returns zero |
| P2.5 | ☐ (as of 2026-08-27 · flips when `[Unreleased]` names both removals under a breaking heading)                                   | CHANGELOG breaking entries for `dry_run` and `'replace'`, per CONTRIBUTING §Breaking Changes                                                                                                                          | P2.2 P2.4 | `[Unreleased]` carries both                                                          |

**Gate P2**: the tool can express "remove this" for every clearable field, and preview is reachable
without confirming the thing being previewed.

---

## P3 — Styles as a resource type, and the systemMessage triage

Owner-proposed 2026-08-27. The matrix's §15 holds the measurement; the ruling was **not** "styles
replace `systemMessage`" but "`systemMessage` keeps only what is prompt-specific; anything
role-shaped becomes a style", with a testable criterion: _if the text would read identically for any
prompt in its category, it is a style._

| #    | St                                                                                                               | Change                                                                                                                                             | Depends   | Verification                                                        |
| ---- | ---------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------- |
| P3.1 | ☐ (as of 2026-08-27 · flips when a `resource_manager` call writes a `style.yaml`)                                | `resource_type: 'style'` — lifecycle / discovery / versioning processors, schema, contract, router, mirroring the gate handler (~1,160 lines)      | —         | create/update/delete/inspect a style through the tool               |
| P3.2 | ☐ (as of 2026-08-27 · flips when the four `guidance/*` prompts carry no `systemMessageFile` and behave the same) | Pilot: retire the four `guidance/{reasoning,analytical,creative,procedural}` system messages, which restate the four shipped styles by name        | P3.1 P2.1 | the four prompts lose the key; their rendered guidance is unchanged |
| P3.3 | ☐ (as of 2026-08-27 · flips when `rg -c systemMessageFile` over the corpus returns 29)                           | Promote the remaining style-shaped system messages (23 under 40 words) into styles and strip them via `unset`, keeping the 45 prompt-specific ones | P3.2      | count drops to 29; no prompt loses per-prompt framing               |

**Gate P3**: a style is authorable through the tool, and no `systemMessage` remains that would read
identically for any sibling in its category.

**Explicitly not in scope**: removing `systemMessage` from `PromptYamlSchema`. It is a breaking
change to a Public API Contract surface, and the 12 substantial messages (`strategicImplement` at
1,347 words) have nowhere else to live.

---

## P4 — Deferred surface gaps

Carried from the matrix with their original falsifiers intact. Each is independently shippable;
none blocks another.

| #    | St                                                                                                                | Change                                                                                                                                                                                                               | Verification                                                             |
| ---- | ----------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| P4.1 | ☐ (as of 2026-08-27 · flips when `framework_gates` appears in the contract with an element shape)                 | **SF-1** — `framework_gates` is hard-required by `FrameworkDraftValidator` but undeclared; its only published shape is an example inside an error response. Declaring it is **narrowing, not breaking**              | the contract names it; a caller can read its shape without a failed call |
| P4.2 | ☐ (as of 2026-08-27 · flips when a forced write failure leaves no new version row)                                | **SF-3** — framework `update` calls `recordEditResult` (`:160`) before `writeFrameworkFiles` (`:176`), so a failed write leaves a history row for an edit that never landed                                          | a forced failure leaves the ledger unchanged                             |
| P4.3 | ☐ (as of 2026-08-27 · flips when the deletion guard derives its set from the registry)                            | **SF-4** — `builtInFrameworks = ['cageerf','react','5w1h','scamper']` literal at `:244`, already wrong since `focus`, `liquescent`, `radiant` and `verify` also ship. The handbook forbids hardcoded framework lists | deleting a shipped framework not in the old literal is refused           |
| P4.4 | ☐ (as of 2026-08-27 · flips when a create/update call can author `severity`)                                      | Gate `severity` / `enforcementMode` / `gate_type` are in the loader schema but undeclared on `GateManagerInput`, so every tool-authored gate takes loader defaults silently                                          | a gate created through the tool carries a non-default `severity`         |
| P4.5 | ☐ (as of 2026-08-27 · flips when the 11 fields appear in `resourceManagerInputSchema`)                            | Framework's 11 "advanced" fields ride the outer `.passthrough()` — settable but undiscoverable to any client reading the schema                                                                                      | the fields are declared with descriptions                                |
| P4.6 | ☐ (as of 2026-08-27 · flips when `inspect format:'json'` returns the literal `arguments` and `chainSteps` arrays) | `inspect` never surfaces `arguments[].validation`/`defaultValue` or step `inputMapping`/`outputMapping`/`retries`/`visibility`/`delegation`, so a full-array rewrite reconstructed from it silently drops fields     | a round-trip through inspect loses nothing                               |
| P4.7 | ☐ (as of 2026-08-27 · flips when a tool call writes a `category.yaml`)                                            | `category` resource type — `CategorySchema` has zero writer in `src/`, so `category.yaml` is hand-authored today, which the MCP-Tooling-Only rule forbids                                                            | a category is authorable through the tool                                |
| P4.8 | ☐ (as of 2026-08-27 · flips when `user_message_template_file` or `system_message_file` resolves in `src/`)        | `create_prompt`'s `prompt_builder` emits parameter names `resource_manager` never reads, so file-referenced authoring through the guided workflow hard-fails validation                                              | the guided workflow completes in file-reference mode                     |

| P4.9 | ☐ (as of 2026-08-30 · flips when a prompt that fails schema validation can be repaired through `resource_manager`) | **A resource cannot be repaired by the only tool allowed to author it, once its defect stops it loading.** `update` resolves the target through the registry, and a prompt dropped at load has no registry entry — so `resource_manager` answers `Prompt not found` for a file sitting on disk, and the sole remaining repair path is the hand-editing the MCP-Tooling-Only rule forbids. Found 2026-08-30 while ruling P5.1: three invalid prompts had to be edited directly because no tool could reach them. `validate:prompts` (P5.11) now makes them detectable, which sharpens the gap rather than closing it — the operator is told precisely what is broken and still has no supported way to fix it | an on-disk prompt with a schema error is repaired through the tool |

**Gate P4**: no field is required-but-undeclared, and no shipped resource kind is unauthorable.

---

## P5 — Corrections and hygiene

Findings from the Arc 1 execution that bind future work rather than belonging to it.

| #    | St                                                                                                                                                                            | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Verification                                                                                                                                                        |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P5.1 | ⚠ ☐ (as of 2026-08-30 · flips when `validate:prompts --root ~/.claude/resources/prompts` exits 0)                                                                             | ⚠ **Two of the row's premises were false, and the location moved.** P1.5/P1.6 relocated the personal library to `~/.claude/resources`, so 10 of the 11 defects are no longer repo files at all — but the 11th is: `knowledge-capture/practice_capture` is TRACKED and carried two gates that had never loaded. Dropped gates measured **8**, not 5, across 6 prompts. All 8 are now repaired (2 repo + 6 personal) and re-measure at zero. The three invalid prompts are NOT mechanical repairs: each authors chain steps in a vocabulary `ChainStepSchema` rejects (`userMessage`/`dependsOn`, `name`/`prompt`), and `general/resume_variant_build` is a content-free stub duplicating a real id — owner decisions, tracked as P5.10. The durable half is `validate:prompts` (P5.11) | `validate:prompts` exits 0 on the bundled tree (39, green); zero `Dropped inline gate` on a live start                                                              |
| P5.2 | ✓ (verified 2026-08-29 · the line reads `120 (16 categories, 3 invalid)` and 123 files on disk − 3 invalid = 120 served)                                                      | Inventory names its subtractions. `CategoryPromptsResult.invalid` carries the per-root load-failure count to `formatResourceInventory`, which drops zero-valued reasons so a healthy start stays quiet. **Two of the row's own numbers were wrong**: the 5 "duplicated ids" were cross-ROOT, which is the overlay contract rather than a subtraction, and the served count was 119 only because `mergePromptResults` keyed `promptsData` on bare `id` while keying `convertedPrompts` on `category/id` — the same defect fixed on the sibling array a day earlier, still live here. Both arrays now key on `category/id` and both report 120                                                                                                                                          | the line reads `120 (16 categories, 3 invalid)`, and 123 − 3 = 120                                                                                                  |
| P5.3 | ✓ (verified 2026-08-29 · `grep -rn '\.\.\.process\.env' tests/e2e` returns only `child-env.ts`; e2e 163 passing)                                                              | `spawnMcpServer` was one of FIVE spawn sites spreading `...process.env`, each scrubbing a different subset — one dropped the two jest markers, one also dropped `NODE_OPTIONS`, one blanked `MCP_RUNTIME_ROOT` and no other path var. All five now route through `tests/e2e/helpers/child-env.ts`, which additionally scrubs `MCP_WORKSPACE`/`MCP_RESOURCES_PATH`/`MCP_RUNTIME_ROOT`/`MCP_CONFIG_PATH`: an inherited resources path made a child read the developer's personal catalog instead of the fixture                                                                                                                                                                                                                                                                         | `validate:hermetic-child-env` fails a sixth site; self-test + a positive control reintroducing a spread both exercised                                              |
| P5.4 | ✓ (verified 2026-08-30 · the headline is chosen AFTER verification; a failed one opens `❌ **Prompt Created, but post-write verification FAILED**` with the reason on line 2) | **Verified 2026-08-30, and the answer was “yes, but”.** The body opened `✅ **Prompt Created**` and DID carry `❌ **Post-write verification failed**` — at line 14, under the success headline. `isError: true` was correct and the ORDERING was the defect. Fixed by composing the headline after verification instead of appending a correction beneath it: there is no ordering in which an appended qualifier outranks the line above it. Extracted to `utils/mutation-headline.ts` — choosing which claim a response leads with is a decision, and it is testable without the orchestrator's context. The headline still says the write happened, because it did; what failed is verification                                                                                    | `mutation-headline.test.ts` — 7 cases, both branches; `resource-path-containment.e2e.test.ts` asserts a passing create's first line carries `✅`                    |
| P5.5 | ✓ (verified 2026-08-30 · `verify:claims` 105/105 green leaves the package tree empty; reverting the helper leaks 14 files and the guard exits 1)                              | ⚠ **The row named the symptom and prescribed the wrong fix.** Cleanup was never missing — ISOLATION was. `startServerWithHttp` defaulted `MCP_RESOURCES_PATH` to the package tree, and that outranks `MCP_WORKSPACE` in `PathResolver.getResourcesPath()` (priority 1 vs 2), so the suite's "isolated workspace" server read and WROTE the package tree while believing otherwise. It leaked on a fully PASSING run (105/105), not only on failure, because the workspace OVERLAY resolves separately — the one fixture the scenarios assert on was found while every mutation landed in the wrong tree. A cleanup hook would have hidden that and left isolation broken. A caller-supplied workspace now suppresses the default                                                      | `package-resources-guard` jest globalSetup/globalTeardown — diffs the package tree across the run; a leak fails an otherwise-green run (measured: 1 passed, exit 1) |
| P5.6 | ✓ (verified 2026-08-29 · a live start warns `duplicate prompt id 'deep_analysis'` and `'resume_variant_build'`, each naming both files)                                       | `ResourceIndexer` records every id it overwrites and `reportShadowedResources` logs it by PATH, not count. `sameCategory` picks the severity: same category and id is one resource being overridden by precedence (debug), the id alone is two resources where one becomes unreachable (warn). Keying on root instead would have filed all 18 cross-root matches as benign                                                                                                                                                                                                                                                                                                                                                                                                            | `resource-indexer-roots.test.ts` — a same-id/two-category pair warns naming both paths, with a no-duplicates positive control                                       |

| P5.7 | ✓ (verified 2026-08-29 · a live STDIO start indexes 121 prompts incl. `strategicImplement`, 25 gates, 8 frameworks, 4 styles — previously 78/0/0/0) | **The index and the served catalog were two independent derivations of one question, and nothing compared them.** `ResourceIndexer` walked ONE root, two levels deep, keyed on the YAML `id:` field; the loaders walk every root to any depth and path-qualify nested ids. Result: 119 served, 78 indexed, and since `resource_index` is what every Python hook reads, `>>strategicImplement` answered "Unknown prompt" for a prompt `prompt_engine` executes. Gates, frameworks and styles were indexed as ZERO, so `get_valid_styles_from_db` returned empty. Fixed at all three: roots come from the runtime via `resourceRoots` (infra may not import runtime), the scan recurses to `MAX_SCAN_DEPTH`, and ids derive from the path below the category as `yaml-prompt-loader` does. `formatIndexReconciliation` now compares the two at startup and names the disagreement in both directions — the gate this class lacked | `resource-indexer-roots.test.ts` — 6 assertions; 3 mutations (single-root, depth 2, sameCategory forced true) each kill tests, restored green |
| P5.8 | ✓ (verified 2026-08-29 · 4 new e2e cases; a single-root mutation in the indexer fails all 4, restored green) | **The gate for P5.7's class.** A startup warning is not a gate — nobody reads a log. `bundled-resource-fallback.e2e.test.ts` now reads back the `resource_index` the booted server wrote and asserts it against the served catalog: bundled AND workspace prompts present, gates/frameworks/styles non-zero, a chain step under its qualified id, and — the case that closes the class rather than the instances — that the server's OWN reconciliation reports no disagreement, which fails on causes nobody has thought of yet. Finding this required fixing P5.3 first: the test was reading the developer's personal store, so its `> 1` assertion passed either way | mutation-verified; runs in CI via `test:e2e` on the full route |
| P5.9 | ✓ (verified 2026-08-30 · a create with `My Category` returns `isError: false`, writes `my-category/`, and carries no `mismatched: category`) | A create with a spaced category ALWAYS reported failure. The slug is written to disk and `loader.ts:186` then overwrites `category` with the directory-derived id, so verification compared the reloaded slug against the AUTHORED value and a flawless write returned `❌ Post-write verification failed (mismatched: category)`. Normalized on BOTH sides — the transform is idempotent, so a symmetric normalization cannot drift into asserting which side is already slugged. The duplicated slug expression (2 inline copies, a 3rd about to be written) is now `slugifyCategoryDirectory` | `resource-path-containment.e2e.test.ts` — mutation: removing the category normalization fails the case, restored green |
| P5.10 | ☐ (as of 2026-08-30 · flips when `validate:prompts --root ~/.claude/resources/prompts` exits 0 — 2 of 3 done, only the empty stub is left and its disposition is a delete decision) | **Owner ruled 2026-08-30: align to modern standards.** Two of three are done. The modern shape is one sub-prompt per step in its own nested directory, referenced as `promptId: <parent>/<step>` — measured across all 5 bundled chains, none of which carries inline step content. `resume/resume_variant_build`'s six `name`/`prompt` steps and `general/test_gate_chain`'s three `userMessage`/`dependsOn` steps are now nested sub-prompts (9 new directories); all 9 resolve on a live load and both chains validate. `dependsOn` was dropped rather than translated: it is not in `ChainStepSchema`, and on a strictly linear chain the sequence already IS the dependency, so it carried nothing the order did not. `inlineGateIds` is schema and was kept. **Still open**: `general/resume_variant_build`, a content-free stub — empty description, 0-byte `user-message.md`, `name` equal to `id` — duplicating the id of the real `resume/` chain and never once loaded. There is nothing to align; the only coherent action is deletion, which is destructive and needs a word | `validate:prompts` against the personal root reports 1 error, down from 28 across 3 files; a live load serves all 9 nested steps and 0 dropped gates |
| P5.11 | ✓ (verified 2026-08-30 · `--self-test` passes both directions; the bundled tree is clean at 39; the personal root reports 28 schema errors across exactly 3 files and 0 dropped gates) | **The gate P5.1's class lacked.** `validate:frameworks` has validated framework YAML since frameworks shipped; prompts had no equivalent, so a schema-invalid prompt was one `[ERROR]` line in a log and a malformed gate one `[WARN]` — both with exit 0 and a quietly smaller catalog. `validate:prompts` runs the loader's OWN `validatePromptYaml` and `normalizeInlineGateDefinitions` rather than reimplementing either, so it cannot drift into accepting what the server drops, and it takes `--root` because 10 of the 11 measured defects live outside any checkout. It reports the FULL error set where the log showed only the first: after the kebab-case fix, `test_gate_chain` still had 3 unrecognized-key errors the runtime log never mentioned | in `validate:all` (51); `validate:prompts:self-test` asserts a valid prompt is not reported alongside two that must be |

| P5.12 | ☐ (as of 2026-08-31 · flips when `validate:prompts` reports zero deferred convention exemptions) | **The id convention, codified — and the two ids that violate it.** Owner ruled 2026-08-31: adopt the convention as MEASURED rather than change it. `shared/utils/resource-ids.ts` is now its sole owner and states it: **ids that appear in the `>>` / `-->` command grammar are snake_case; every other id is kebab-case.** The exception is principled, not historical — `command-tokenizer.ts` splits on `/-->|==>|\+|\?/`, so a hyphen inside a prompt id makes `>>a-->b` ambiguous; no other id type enters that grammar. Measured basis: prompt ids 92 snake / 1 kebab / 2 camel · categories 5 kebab / 1 snake · gate ids 25/26 kebab · chain step, workflow node and unknown ids kebab and schema-enforced. **Deferred half**: `development/strategicImplement` (bundled and TRACKED) and `general/diagnosisCard` are camelCase. Renaming is a four-repository arc, not a prompt edit — ~252 references: `opencode-prompts` 107, this repo's `plans/` 77, `~/.claude/skills` 14 (it is also a global skill name), this repo's src/tests/hooks/docs/resources 50, `gemini-prompts` 3. Both are stamped exemptions in `validate:prompts` naming this row, with a satisfied-exception check that fails once either stops needing one | `validate:prompts` reports `0 deferred convention exemption(s)`; `>>strategic_implement` resolves and no reference to the old spelling survives outside CHANGELOG |
| P5.13 | ☐ (as of 2026-08-31 · flips when `validatePromptId` rejects a camelCase id and the suite stays green) — BLOCKED on P5.12 | Tighten `validatePromptId` (`^[a-zA-Z][a-zA-Z0-9_-]*$`) and `validateCategoryName` (`^[a-zA-Z0-9][a-zA-Z0-9_-]*$`) to the convention patterns, rejecting the wrong shape at the API boundary rather than only reporting it. Ruled 2026-08-31 and deliberately NOT done in the same change: both regexes currently ACCEPT the two camelCase ids, and `strategicImplement` ships in the bundled tree — tightening now fails the server's own catalog at load. It is also a narrowing of a Public API Contract surface and needs its own breaking-change entry | a create with a camelCase id is refused by name; `validate:all` green |
| P5.14 | ✗ KILLED (2026-08-31 · revives when three determined repairs accumulate) | **A `--fix` autofixer for `validate:prompts`, considered and declined.** The line for auto-repair is that the correct output must be fully DETERMINED by the input. Sorted against the eleven defects this arc actually measured, that set is empty: the eight dropped gates needed a `guidance` string written (authoring), the three invalid prompts needed chain steps re-authored into sub-prompts (authoring), the stub needs a deletion decision, and the convention violations need renames — which the fixer must refuse by construction, since a rename is only safe if every reference moves with it and today's own measurement puts that at 252 references across 4 repositories. Building it now would ship a feature with no cases, which `cleanup-standards.md` prices as a parallel system with a nicer name. Recorded rather than dropped: the revive condition is three determined repairs, matching the Pattern Maturity Model's threshold | a fourth `validate:prompts` defect class arrives whose repair is determined |
| P5.15 | ✓ (verified 2026-08-31 · one owner in `shared/utils/resource-ids.ts`; `typecheck` green; the parser's strip proven unreachable before removal) | **Two implementations of prompt-id normalization, unified.** `normalizePromptId` and an inline chain in `command-parser.ts:372` differed by one `.replace(/[^a-z0-9_]/g, '')`. Measured divergence: they disagree on nested chain-step ids — `deep_analysis/initial_scan` → `deep_analysisinitial_scan` under the parser's version — and the draft service compares that output against every loaded prompt id, nested ones included. The strip turned out to be **unreachable**: the capture group feeding it is `([a-zA-Z][a-zA-Z0-9_-]*)`, which already excludes every character it removes. Dropped rather than adopted — the constraint belongs at the boundary that already enforces it, and a second guard standing where the first one holds only makes the two copies disagree on inputs neither can receive. Placed in `shared/` (L0) because `engine/` may not value-import `mcp/`; same rationale `node-order.ts` records | `resource-ids.test.ts`; `rg` finds one definition of each |

**Gate P5 — PASSED for the repo, one row open (verified 2026-08-30).** Three probes that reported
success for work that did not happen are closed: a create that wrote correctly and reported failure
(P5.9), a failed mutation whose first line claimed success (P5.4), and a green 105/105 suite writing
into the tree it believed it was isolated from (P5.5). Each is now held by a gate rather than by
care — a package-tree diff across every jest run, and `validate:prompts` running the loader's own
schema. P5.10 stays open: it is the owner's content, and the decision is authoring, not repair.

---

## Phase order

P1 and P2 are independent and may run in either order. P3.1 is independent; P3.2–P3.3 need P2.1
(`unset`) as their instrument, because the handbook forbids hand-editing under `server/prompts/**`.
P4 and P5 are unordered.

**P1 before P2.3** is the one real edge: P2.3 deletes directories under `resources/prompts/`, and
reviewing that diff is meaningless while 83 prompts are invisible to git (the D6 argument, scoped to
the row it actually reaches).
