---
title: "Primitive rework — implementation notes"
date: 2026-09-23
status: active
plan: plans/technical-debt/primitive-rework-2026-09-23.md
tags: [gates, chains, execution, mcp-tools]
---

# Implementation Notes

Deviations, rulings on open questions, and probe output for
`plans/technical-debt/primitive-rework-2026-09-23.md`.

## Rulings

| id  | date       | ruling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | 2026-09-23 | Planner: the review map persists in the run's residual document (`run-registry.ts` 368–369, field map 505), not in a new column — no schema bump. Measured: neither `run-registry.ts` nor `sqlite-engine.ts` contains `pending_review_json`; `SCHEMA_VERSION` is 31.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R2  | 2026-09-23 | Owner: every slice of this plan is push, PR and merge, one PR per slice, on a green full suite and a clean merge state; breaking changes and force pushes still stop for the owner.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| R3  | 2026-09-23 | Owner (OQ1, OQ3): the `pendingGateReview` projection is deleted inside this initiative once the reader count is zero (row 3.6's validator refuses new writers and unlisted readers until then); the record type is renamed `GateReview`, with `PendingGateReview` kept as a type alias in the stamped exception list until the readers migrate.                                                                                                                                                                                                                                                                                                                                                                                             |
| R4  | 2026-09-23 | Owner (OQ4): removing a declared-but-unread parameter from one command's declared list while the name stays declared on the tool is not breaking (the #366 precedent); removing a name from the tool entirely still needs an owner ruling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| R5  | 2026-09-23 | Planner, on the row 4.1 analysis (row 4.2 triage): findings 1–7 and 9–12 open as rows P6.1–P6.11 below in the plan; finding 8's MCP-vs-cpm rollback key names is an owner call (P6.8 stamped); the two-writer `version_history` relationship is KILLED for this arc (cross-process design, documented as accepted); the `dr` handoff's deviations 40 and 31 open as row 3.8; the inline-gate-bound-to-A-appears-on-C finding opens as P6.12. Schema changes in P6.1/P6.10 touch ephemeral/derived tables only and are safe under a `SCHEMA_VERSION` bump, provided the Python `run_status IS NULL` branch is deleted in the same PR.                                                                                                        |
| R6  | 2026-09-23 | Planner, on the `cv` handoff: row 1.2's premise was false — `buildInlineGateDefinition` produces an intermediate `InlineGateDefinition`; the real third converter was `TemporaryGateRegistry.convertToLightweightGate` (hardcoded retry/activation, six fields carried). 1.2 re-cut to route that converter through `toGateDefinition` (temporary gates keep `severity`/`gate_type` absent); row 1.5 opened for carrying an inline gate's `enforcement_mode` through normalizer → registrar → `TemporaryGateDefinition` (measured five drop sites; spelling `enforcement_mode`, matching the neighbouring inline keys). The converter takes no `opts`: both live paths stamp `sourceRoot`.                                                  |
| R7  | 2026-09-23 | Owner, on the `bd` handoff: `execution_hint` (prompt create/validate) and `persist` (framework switch) are removed from `resource_manager` — breaking — and `common:reload` is split into one reload command per type so category reload's `id` is refused by per-action ownership rather than a hand-written refusal. Both measured boundaries (resource_manager: inside the processor the handler hands `args` to; prompt_engine: from the registration allowlist through the executor to any reader under `engine/execution`, OQ2) are accepted; the brief's boundaries would have hidden defects. `skip_version` declared on the three updates that read it.                                                                            |
| R8  | 2026-09-23 | Planner, on the 3.1 handoff: `nodeId` is required on every review write; the fallback key derivation (the run's last node, which can collide with a detached node's review) is deleted in 3.3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| R9  | 2026-09-23 | Planner, on the 3.2 handoff: in the `exhausted` phase a verdict is refused for every review kind; only `gate_action` retry/skip/abort or `cancel` moves the review (closes the P4.164 shape: a FAIL past the budget no longer counts).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| R10 | 2026-09-23 | Planner, on the 3.2 handoff: a detached review's FAIL is decided by its own gates through `resolveEnforcementMode` (no `'blocking'` constant); row 3.8 keeps only the stage-19 grading half.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| R11 | 2026-09-23 | Planner, on the 3.3 handoff: `RetryConfig`, `ReviewOutcome`, `ActionResult` and their barrel re-exports are deleted (knip 643 → 637, never raised); the run counters stay on `recordGateReviewOutcome` until row 3.4 shrinks it to the counters; a deferred PASS now counts in `gatesFiredCount`, and a verdict with no open review on a run past its end is refused by name (CHANGELOG bullet); the two writers that could reach the R8 throw go to row 3.5. The one answering path lives in the verdict processor, not the authority, because five harnesses drive verdicts with no authority on the context.                                                                                                                             |
| R12 | 2026-09-23 | Planner, on the 3.4 handoff: knip is satisfied by typing history entries `satisfies GateReviewHistoryEntry`, never by an export; P4.157 is re-cut as row 3.9 — `advanceStep` stops completing the run, one completion point after grading, `detachedNodesHoldingRun` widened to unreported ∪ open reviews and renamed, `isRunComplete`'s view clause deleted.                                                                                                                                                                                                                                                                                                                                                                               |
| R13 | 2026-09-23 | Planner, on the 3.5 handoff: `DEFAULT_GATE_RETRY_CONFIG` is un-exported; the capture hold stays on the run's step review (`currentStepReview`), stamped to flip when one review per node is kept (row 3.10), because `writeReview` still evicts every other non-detached review.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| R14 | 2026-09-23 | Planner, on the 3.9 handoff: the `writeReview` eviction is deleted (row 3.10); the capture hold reads the target node's review or any open review on an earlier node; a verdict still routes by trailer node (R10). Stage 17's early exits get completion asked once after the stage loop (row 3.11) rather than a fourth completion site. Deviations from 3.8 (stage 16 + `pipeline-builder.ts` edits; grading from the landing, not `answerReview`) and 3.6 (exceptions keyed by file + function + kind, not line) are accepted.                                                                                                                                                                                                          |
| R15 | 2026-09-23 | Planner, on the 3.11 handoff: a pending shell verification holds the run open the way an open review does (`nodesHoldingRunOpen`), and stage 13's "already complete" short-circuit reads that hold — one hold derivation, not a second (row 3.13). Until it lands, `chain/complete` arrives on the final step's bounce call, which the 3.11 e2e pins. The 3.10 out-of-list edit to `gate-verdict-processor.ts` (a named `clearReview`) and 3.11's kept stage-16 asks are accepted.                                                                                                                                                                                                                                                          |
| R16 | 2026-09-23 | Planner, on the 3.12 and 3.13 handoffs: with several step reviews open the SHOWN review is the earliest one opened (`resolveShownReview`, one resolver for stage 13, stage 20 and the hooks key); a bare verdict is still refused naming every open node. A stage-19 call that captured no step reads no review and grades nothing (row 3.14). Abort after the attempts run out CANCELS the run (pinned). The shell hold covers inline `:: verify:` only until `sourceGateIds` has a writer or is deleted (P6.28). Out-of-list edits accepted: `review-target.ts` (3.12), `chain-execution.ts` (3.13).                                                                                                                                      |
| R17 | 2026-09-23 | Planner, on the 3.14 handoff: R16's "a call that captured no step grades nothing" means it READS NO REVIEW; a `user_response` call that captured no step still evaluates the guards and warns (rows 3.5/P4.111 pin it), and does not exit before evaluating. The ESLint ceiling lowered by the row (three rules, every count down) is accepted.                                                                                                                                                                                                                                                                                                                                                                                             |
| R18 | 2026-09-24 | Owner: a slice's plan writeback lands in that slice's PR — the planner commits rows, rulings, deviations and the `## Now` block onto the slice branch after the last handoff and before the boundary suite; receipts cite the PR number, never a squash SHA; no docs PR per slice. A single initiative PR merged at the end was rejected: it loses per-slice review and bisect, keeps `main` and the served `dist` stale, mixes breaking slices with fixes, and accumulates the CHANGELOG conflicts sibling merges create. Measured: Tier 3 shipped six code PRs and six docs PRs for six slices, each docs PR waiting only for a squash SHA. The planner prompt (`strategic_implement`) records the rule in its steps 9 and 11 in this PR. |
| R19 | 2026-09-24 | Planner, cutting Evolution slice 1: a resume that carries no `user_response` captures nothing and advances nothing; it may only answer a review (`gate_verdict`/`gate_action`, routed by `resolveReviewTarget`), apply a `remainder`, resolve an interrupt, or cancel — and because it captures nothing it needs no `HANDOFF RESULT` trailer even at a delegated node. The trailer is evidence for a capture; the guarantee-B bypass is closed by the first half of the rule, not by refusing the call. P6.22 was added to the slice as the foundation of P6.15 and P6.17.                                                                                                                                                                  |
| R20 | 2026-09-24 | Planner, on the P6.22 and P6.15 handoffs: R19 applies to every verdict polarity. A bare BLOCKING FAIL on an unanswered step keeps today's behaviour (opens the review, spends an attempt, advances nothing — the P4.116 control); a bare non-blocking FAIL drops its deferred advance but keeps its record and warning (P6.35, pulled into the slice once P6.15's measurement showed the advance live under the relaxed admission).                                                                                                                                                                                                                                                                                                         |
| R21 | 2026-09-24 | Planner, on the P6.17 handoff: a remainder stays entitled only to an open blocking unknown or the synthetic review (OQ-3); re-planning a delegated workflow mid-run declares the unknown first. P6.17's authored premise ("cannot be re-planned mid-run") was the admission refusal, fixed by P6.15. `remainder` is not a resolution verb and stays out of the hook's accepted set. Out-of-list edits accepted: three test files (P6.22), the flipped pins (P6.15).                                                                                                                                                                                                                                                                         |
| R22 | 2026-09-24 | Planner, cutting Evolution slice 2: a prompt that declares `gateConfiguration.framework_gates: false` declares NO required sections — its render carries no graded-sections block, no declaration is recorded for its node, and the phase guard grades nothing for it; the framework's guidance text may still inject. Addendum on the P6.16 handoff: a forcing framework modifier restores the framework's GATES (the veto binds up to `prompt-config` rank) but not the sections — a modifier is a gate dial, not a render dial.                                                                                                                                                                                                          |
| R23 | 2026-09-24 | Planner, cutting Evolution slice 2: a PreToolUse whose payload identifies a subagent is not the session that rendered the brief; `delegation-enforce.py` allows it silently and leaves the state untouched, and a subagent's own Agent call does not clear the parent's state. The discriminator was to be measured, not mirrored — it is `agent_id`, present only inside a subagent call. Other hooks change only when measured to misfire (P6.42, P6.43 cut).                                                                                                                                                                                                                                                                             |
| R24 | 2026-09-24 | Planner, cutting Evolution slice 3: `gate_action: "skip"` on an exhausted review accepts the step's captured answer as-is and advances past it (its `step_complete` fires on that call); with no captured answer it is refused by name (R19). `retry` unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| R25 | 2026-09-24 | Planner: `chain/step_complete` fires when the run advances past a step, not when its answer is captured — one emission point; a captured-but-held step emits nothing until its review passes or is skipped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| R26 | 2026-09-24 | Planner: a review's attempt count belongs to its node; a non-blocking FAIL with no answer spends an attempt and the review stays open at that count, exhausting like a blocking review past the budget.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| R27 | 2026-09-24 | Planner: a verdict arriving with no captured step records against the node whose review it answered, with the review outcome — never a `completed` row for the current step.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| R28 | 2026-09-24 | Planner, completing R17: a `user_response` call that captured no step warns but never opens a review — nothing to grade against or hold.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| R29 | 2026-09-24 | Planner, cutting Evolution slice 4: a step whose render declared a shell verification is HELD by that pending check as a step under review is held — the capture returns no advance, stage 17 runs the command on the same call, a pass advances the step through the one advance and emission path, a fail bounces with the run still on the step; `skip` releases the hold with the captured answer and is refused by name without one (the R24 shape). Addendum on the node A handoff: the hold lives in the one advance owner (`applyDeferredAdvance`), because a bounced answer sent with a PASS moves through the verdict's advance, not the capture's.                                                                               |
| R30 | 2026-09-24 | Planner, cutting Evolution slice 4: a session is visible to hooks exactly while its status is non-terminal and `!isRunComplete(session)` — the one derivation the server uses — on both the TypeScript projection and the Python reader; no schema bump for it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

## Pre-flight (Step 2)

domain pass · layer pass · naming pass · complexity pass · size pass (`chain-operator-executor.ts`
at its ceiling: reads move out) · service pass · defined pass · contracts pass · pattern pass ·
reuse-scope pass · persistence pass (ephemeral residual document) · lib-api n/a · lib-version n/a ·
failures 0 · compound none.

## Verify-Paths (Step 3)

Raw output measured on `main` `1161853f` from `server/`:

```
$ rg -c "pendingGateReview" src --glob '!**/*.test.ts' | sort -t: -k2 -nr | head -6
src/modules/chains/manager.ts:15
src/engine/execution/operators/chain-operator-executor.ts:11
src/engine/gates/services/gate-verdict-processor.ts:7
src/shared/types/chain-session.ts:4
src/modules/chains/run-registry.ts:4
src/mcp/tools/system-control/handlers/session-action-handler.ts:3
$ rg -ln "pendingGateReview" src --glob '!**/*.test.ts' | wc -l
17
$ rg -n "pending_review_json|pendingGateReview" src/modules/chains/run-registry.ts
62:  pendingGateReview?: unknown;
368:  if (session.pendingGateReview !== undefined)
369:    residual.pendingGateReview = session.pendingGateReview;
505:    ['pendingGateReview', 'pendingGateReview'],
$ rg -n "SCHEMA_VERSION = |pending_review_json" src/infra/database/sqlite-engine.ts
381:const SCHEMA_VERSION = 31;
$ rg -n "toLightweightGate|private toLightweight|buildInlineGateDefinition" src/engine/gates/core/gate-loader.ts src/engine/gates/registry/gate-provider-adapter.ts src/modules/prompts/yaml-prompt-loader.ts
gate-loader.ts:284:  private toLightweightGate(definition: LoadedGateDefinition): LightweightGateDefinition {
gate-provider-adapter.ts:150:  private toLightweight(definition: LoadedGateDefinition): LightweightGateDefinition {
yaml-prompt-loader.ts:201:function buildInlineGateDefinition(definition: Record<string, unknown>): InlineGateDefinition {
$ rg -n "KNOWN_PROVIDER_GAPS" tests/unit/gates/registry/gate-provider-converter-parity.test.ts
36:const KNOWN_PROVIDER_GAPS = ['sourceRoot'];
$ wc -l scripts/validate-system-control-parameter-reads.js scripts/validate-scoped-framework-reads.js
483 246
$ wc -l src/engine/execution/operators/chain-operator-executor.ts
1413   (ESLint max-lines counts 1002/1000)
```

Every other path in the plan's §Verified paths was checked with `ls -la`, `wc -l` and `rg -n`; no
ENOENT, no shim.

## Evolution analysis (row 4.1)

Measured on `rework/evolution-analysis` cut from `origin/main` `1161853f` (2026-09-23). Every
probe below was run from `server/` unless the command names another root. No source was edited.

### Positive control — the method fires on a known case

`enforcement_mode` before #372 (`8910a63d`):

- `git grep -n "resolveEnforcementMode(" 8910a63d~1 -- server/src` → 2 call sites
  (`gate-verdict-processor.ts:316,561`), both reading `context.state.gates.enforcementMode`.
- `git grep -n "enforcementMode" 8910a63d~1 -- server/src/engine/gates/services/gate-enhancement-service.ts`
  → the only writers of that field set a constant (`:301` `isSinglePrompt ? 'advisory' : 'blocking'`,
  `:532` `'blocking'`). The declared `gate.yaml` key had 55 mentions across 36 files and reached
  neither decision: a writer-without-reader the seam-1 probe finds by counting readers of the
  decision, not mentions of the name.
- Post-#372 the same probe → 3 call sites, `gate-enhancement-service.ts:383` feeding
  `stepEnforcement`. The probe distinguishes the two trees.

Null-result controls used below: the seam-2 probe (`rg -n "process\.(env|cwd|pid)|^let |getInstance\(|globalThis"`)
returns 6 hits on `modules/skills-sync` and 2 on `engine/frameworks`, so a 0 on another module
is a measured absence.

### `server/src/engine/frameworks` (30 files)

| seam                        | probe                                                                                                                                                                                                                                             | count              | finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | fork                                                 | proposed row or kill                                                                                                                                                                                                                      |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| writer-without-reader       | per declared field of `types/framework-types.ts`: `rg -n "\b<field>\b" src --glob '!src/engine/frameworks/types/**'`; per export of `types/semantic-integration-types.ts`: `rg -n "\b<name>\b" src tests` excluding the file and `types/index.ts` | 7 fields + 6 types | Write-only fields: `qualityGuidance` and `structureGuidance` (`definitions/generic-framework-guide.ts:118,102`), `enhancementMetadata` (`:213`, `utils/template-enhancer.ts:117`), `selectionReason` (`framework-manager.ts:415`), `applicableTypes` (`:629`), `stepValidation`/`stepEnhancements` (`utils/step-generator.ts:170-207`, guide `:177-178`) — every src mention is an object-literal write; tests construct them as fixtures. All 6 exports of `semantic-integration-types.ts` have 0 consumers (the `semantic` module went in #366)                                                                         | surplus                                              | ROW: delete the 6 types, the 7 fields and their producers (`createEnhancementMetadata`, `getSelectionReason`, `getApplicableTypes`); check = knip baseline drops, `rg` per name → 0                                                       |
| ambient scope               | `rg -n "process\.(env\|cwd\|pid)\|^let \|getInstance\(\|globalThis" src/engine/frameworks`                                                                                                                                                        | 2                  | `definitions/runtime-framework-loader.ts:583 let defaultLoader` — a singleton whose config comes from whichever caller supplies it first; 6 no-arg consumers (`definitions/registry.ts:98`, `mcp/tools/tool-description-overlays.ts:132`, `system-control/handlers/framework-action-handler.ts:95,149,229`, `prompt-engine/core/pipeline-builder.ts:216,244`). `framework-state-store.ts:303 SqliteEngine.getInstance` is a library global — n/a. Same shape ×3 siblings: `gates/core/gate-definition-loader.ts:513`, `formatting/core/style-definition-loader.ts:518`, `automation/core/script-definition-loader.ts:526` | design gap                                           | ROW (class of 4): loader built once at the composition root and injected; delete `getDefault*Loader`/`reset*Loader`; check = `rg "^let default\w*Loader" src` → 0                                                                         |
| missing state machine       | `rg -n "status ===\|\?\? null\|!= null\|!== null" src/engine/frameworks`                                                                                                                                                                          | 2                  | `phase-guards/criteria.ts:252`, `framework-state-store.ts:338` — both null checks on loaded data, no lifecycle inferred                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | —                                                    | none found after that probe                                                                                                                                                                                                               |
| cross-stage implicit timing | `rg -n -i "must (run\|be called)\|call order\|before (calling\|any)" src/engine/frameworks`; `rg -n "getDefaultRuntimeLoader\(\{" tests`                                                                                                          | 1                  | `runtime-framework-loader.ts:596-606`: config must arrive on the FIRST `getDefaultRuntimeLoader` call; the comment names `module-initializer.ts:219` as the only config-passing caller (now `:366` — stale). No test pins the order (1 integration site passes config; unit tests `reset` between)                                                                                                                                                                                                                                                                                                                        | live-and-wrong                                       | folds into the seam-2 row above (same primitive)                                                                                                                                                                                          |
| two answers                 | `rg -n "'cageerf'\|'react'\|'5w1h'\|'scamper'" src --glob '!**/shipped-frameworks.ts'`; `rg -n "getCombinedText" src`                                                                                                                             | 3                  | (a) `engine/gates/judge/judge-resource-collector.ts:82-85 isFrameworkPromptId` hardcodes the four ids — a workspace framework is never a "framework prompt" to the judge; handbook: "never hardcode framework lists". (b) `utils/framework-detection.ts:14-25 FRAMEWORK_INDICATORS` — 10 literal sentences, a second per-framework list. (c) `getCombinedText` byte-identical at `types/framework-types.ts:418` (protected, 0 `this.` callers) and `utils/compliance-validator.ts:193` (exported, 2 callers)                                                                                                              | (a) live-and-wrong · (b) design gap · (c) superseded | ROW (a): the collector asks `frameworkManager.getFramework(id)`; ROW (c): delete the protected copy. KILL (b): the indicators are prose the detector matches, not an id list — revives if a workspace framework's guidance is re-injected |

### `server/src/modules/prompts` (17 files; the registry)

| seam                        | probe                                                                                                                                                                                                 | count | finding                                                                                                                                                                                                                                                                                                                | fork       | proposed row or kill                                                                                           |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------- |
| writer-without-reader       | per field of `ConvertedPrompt` (31, declared `engine/execution/types.ts`) and of `modules/prompts/types.ts` (8): `rg -n "\.<field>\b" src` excluding the declaring file; `rg -n "\bPromptFile\b" src` | 1     | Lowest `ConvertedPrompt` counts are real readers (`produces` 3 → `gates/utils/artifact-kinds.ts:132`, `fromArgument` 5 → `:134`). `PromptFile` (`types.ts:42-48`) has 0 constructors and 0 readers — the `.title` hit is `guide.ts:166` on another type                                                                | surplus    | KILL-sized: delete `PromptFile`                                                                                |
| ambient scope               | `rg -n "process\.\|^let \|getInstance\(\|globalThis\|CLAUDE_PROJECT_DIR" src/modules/prompts`                                                                                                         | 0     | —                                                                                                                                                                                                                                                                                                                      | —          | none found after that probe (control: 6 hits on `modules/skills-sync`)                                         |
| missing state machine       | `rg -n "exportedPromptIds\.has\|registeredIdsFor\(.*\)\.has\|livePrompts\.get" src/modules/prompts/registry.ts`                                                                                       | 2     | One prompt's registration is spread over three sets (`registry.ts:53 registeredPromptIds`, `:54 exportedPromptIds`, `:56 livePrompts`) but only 2 sites decide from them (`:143`, `:177`) — under the "several call sites" bar                                                                                         | —          | none found after that probe (2 < 3)                                                                            |
| cross-stage implicit timing | `rg -n "Precedence is the call order" src/modules/prompts`; `rg -ln "mergePromptResults" tests`                                                                                                       | 1     | `prompt-root-loader.ts:56`: bundle → primary → overlays precedence is the argument order of `loadPromptsAcrossRoots`; pinned by `tests/unit/runtime/prompt-merge-identity.test.ts`                                                                                                                                     | design gap | KILL: a test pins it; an ordered `PromptRootSet` type is not worth a slice — revives if a fourth root is added |
| two answers                 | `rg -o "(resolve\|find\|lookup\|normalize)\w*Prompt\w*\(" src`; `rg -n "discoverCategoryDirectories\|readdirSync\(.*prompts" src`                                                                     | 0     | `resolveChainStepPromptId` (`chain-step-resolution.ts:61`) classifies membership, `workflow-ir/{compiler,validator}.ts lookupPrompt` is injected, `resource-ids.ts:70 normalizePromptId` normalizes — three questions, not three answers. Category discovery has one owner (`category-maintenance.ts:36`, 4 consumers) | —          | none found after those probes                                                                                  |

### `server/src/modules/chains` (3 files) + `run-registry.ts`

| seam                        | probe                                                                                                                                                                            | count  | finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | fork                                                                                       | proposed row or kill                                                                                                                                                                                                          |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| writer-without-reader       | per column of `chain_runs`/`chain_run_nodes`/`chain_sessions` (12 probed): `rg -n "\b<col>\b" src ../hooks --glob '!src/infra/database/sqlite*'` then `\| rg -i "select\|where"` | 2      | `chain_runs.base_chain_id`: INSERT `run-registry.ts:202` + index `sqlite-engine.ts:1291`, 0 SELECT/WHERE anywhere; the comment at `run-registry.ts:13` says "stored as a column because a query needs it" — no query does. `chain_sessions.run_number`: INSERT `manager.ts:535`, projected by `v_execution_status` (`sqlite-engine.ts:1328`), 0 readers of the projection in src or `hooks/lib` (`chain_id` already carries `#runNumber`, `chain-id-codec.ts:68`)                                                                                                                  | `base_chain_id` surplus (the comment is the live-and-wrong part) · `run_number` superseded | ROW: drop both columns + the index, rewrite the comment, schema v32; check = the per-column probe → 0                                                                                                                         |
| ambient scope               | `rg -n "process\.(pid\|env\|cwd)" src/modules/chains`                                                                                                                            | 1      | `manager.ts:238-239` reads `process.pid` once into `serverPid`/`pidScope` and carries it as `StateStoreOptions` — a value, not ambient                                                                                                                                                                                                                                                                                                                                                                                                                                             | —                                                                                          | none found after that probe                                                                                                                                                                                                   |
| missing state machine       | `rg -c "isTerminalRunStatus\(\|runStatus \?\? 'working'\|currentNodeId (===\|!==) null" src/modules/chains/manager.ts`; same outside                                             | 15 + 7 | `runStatus` is an explicit union (`chain-execution.ts:63`) but `undefined` is a fourth phase defaulted to `'working'` at 4 sites (`manager.ts:569,968,1101,1151`), and "finished" is derived two ways — terminal status OR `currentNodeId === null` (`chain-session.ts:345-350`; `manager.ts:1703` tests both). 8 `isTerminalRunStatus` sites in `manager.ts`, 6 in `run-registry.ts`, 1 in `13-session-stage.ts`. The Python reader mirrors the fourth phase: `hooks/lib/db_reader.py:349-350` treats `run_status IS NULL` as active ("legacy")                                   | live-and-wrong                                                                             | ROW: `runStatus` non-optional at type and column (`NOT NULL DEFAULT 'working'`), one `isRunFinished` predicate, delete the Python NULL branch; check = `rg "runStatus \?\? " src` → 0 and `rg "run_status IS NULL" hooks` → 0 |
| cross-stage implicit timing | `rg -n -i "must (run\|be called) inside\|same transaction" src/modules/chains`; `rg -n "inTransaction\|transactionDepth" src/infra/database/sqlite-engine.ts`                    | 2 / 0  | `run-registry.ts:40` "Must run inside the caller's transaction"; `manager.ts:526` "Must be called inside an active transaction". `SqliteEngine` exposes no in-transaction accessor, so neither site can assert it; the only pin is `manager.ts:499 db.beginTransaction()` sitting 27 lines above                                                                                                                                                                                                                                                                                   | design gap                                                                                 | ROW: `SqliteEngine` tracks depth at `beginTransaction`/`commit` and exposes `inTransaction`; both sites throw when false; check = a unit test calling each outside a transaction goes red                                     |
| two answers                 | `rg -n "resolveTenantId\(" src`; `rg -n "TERMINAL_RUN_STATUSES" hooks server/src`                                                                                                | 3 + 2  | Three functions named `resolveTenantId` with three signatures: `execution-record-store.ts:254 (scope?)`, `versioning/version-history-service.ts:157 ()`, `cli-shared/version-history-scope.ts:53 (dbPath)` — all end in `resolveContinuityScopeId`, each with its own fallback rule (handbook: `tenant_id` carries three meanings). `TERMINAL_RUN_STATUSES` is restated in `hooks/lib/db_reader.py:469` against `shared/types/chain-session.ts:338`, and each side's test restates the literal again (`test_db_reader.py:439`, `chain-session-store.test.ts:306`) — no parity test | live-and-wrong                                                                             | ROW: one `resolveTenantId(scope)` beside `resolveContinuityScopeId`; the Python parity test is the hooks row below                                                                                                            |

### `server/src/modules/versioning` (12 files)

| seam                        | probe                                                                                                                                            | count                     | finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | fork                                                              | proposed row or kill                                                                                                                                                                                                     |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| writer-without-reader       | per field of `types.ts` (14): `rg -n "\.<field>\b\|\b<field>:" src` excluding the file; `rg -n "\bRollbackResult\b" src ../cli/src`              | 0 fields                  | Lowest: `saved_version` written `cli-shared/version-history.ts:780`, read `cli/src/commands/rollback.ts:205,244`. Every field has a reader. `RollbackResult` itself (`types.ts:71`) is consumed only by `cli-shared` (3 sites) — the MCP path never returns it (moved to seam 5)                                                                                                                                                                                                                                                                                                                                                                                                                                                  | —                                                                 | none found after that probe                                                                                                                                                                                              |
| ambient scope               | seam-2 probe on `src/modules/versioning` plus `'default'`                                                                                        | 0                         | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | —                                                                 | none found after that probe (control: skills-sync 6)                                                                                                                                                                     |
| missing state machine       | `rg -n "status ===\|\?\? null\|!= null\|!== null\|=== null" src/modules/versioning`                                                              | 8                         | All `row === null` / `?? null` on absent rows (`version-history-service.ts:117,298,374-375,463,646,754,757`); no lifecycle inferred                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —                                                                 | none found after that probe                                                                                                                                                                                              |
| cross-stage implicit timing | `rg -n -i "same transaction\|must (run\|be called)" src/modules/versioning`                                                                      | 1                         | `version-history-service.ts:457` "inside the same transaction identifies exactly the row just written" — pinned by `insertAndPrune` (`:354`) wrapping both statements                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —                                                                 | none found (pinned by shape)                                                                                                                                                                                             |
| two answers                 | `rg -n "INSERT INTO version_history\|FROM version_history" src`; `rg -n "^export (function\|const) \w+" src/modules/versioning/projections/*.ts` | 2 writers · 1 of 4 shapes | (a) Two writers of `version_history`: `VersionHistoryService.saveVersion` (`:216`) and `cli-shared/version-history.ts recordResourceWrite/rollbackVersion` (`:488,:692`), documented as "the accepted second writer" (`:213,:260`), each with its own tenant resolver (chains seam 5) and its own rollback vocabulary — cpm returns `saved_version`/`restored_version`, the four MCP processors return `version:` (`prompt-versioning-processor.ts:433` adds `restored_version` alone). (b) `projections/{gate,framework,category}-snapshot.ts` each export `*_SNAPSHOT_PROJECTED_KEYS` + `*_REQUIRED_SNAPSHOT_FIELDS` + `project*Snapshot`; `prompt-snapshot.ts` exports `SNAPSHOT_PRESERVED_FIELDS` + `canonicalPromptSnapshot` | (a) design gap (separate process by charter) · (b) live-and-wrong | KILL (a) for this arc — revives if the CLI gains an in-process path. ROW (b): prompt projection adopts the `SnapshotContract` shape (`snapshot-contract.ts:75`); check = each `projections/*.ts` exports the three names |

### `server/src/modules/skills-sync` (3 files)

| seam                        | probe                                                                                                                                     | count | finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | fork                                              | proposed row or kill                                                                                                                                                      |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| writer-without-reader       | per field of `SkillsSyncRunReport`/`SkillsSyncOutput`/`SkillsSyncFailure`: `rg -n "\.<field>\b" src --glob '!src/modules/skills-sync/**'` | 0     | Lowest `drift` 1 → `mcp/tools/skills-sync.ts:354`; `writtenByClient`/`pruned` 2. Every field read                                                                                                                                                                                                                                                                                                                                                                                          | —                                                 | none found after that probe                                                                                                                                               |
| ambient scope               | `rg -n "process\.(env\|cwd\|pid)\|^let \|getInstance\(\|globalThis\|HOME" src/modules/skills-sync`                                        | 6     | `service.ts:74 MCP_SERVER_ROOT`, `:86 cwd`, `:138 process.env[name]`, `:926 HOME`, `:948 MCP_WORKSPACE`, `:968 cwd`. `resolveProjectRoot(paths)` (`:947`) RECEIVES `SkillsSyncPaths.workspace` — `PathResolver`'s answer via `runtime/skills-sync-paths.ts:44` — and reads `process.env['MCP_WORKSPACE']` first anyway, so a `--workspace` flag or an `MCP_RESOURCES_PATH` overlay makes the carried and ambient answers disagree; `locateSkillsSyncPackageRoot` (`:73`) is the same shape | live-and-wrong                                    | ROW: `resolveProjectRoot` reads `paths.workspace ?? paths.packageRoot`; package root and home dir arrive in `SkillsSyncPaths`; check = the seam-2 probe on the module → 0 |
| missing state machine       | seam-3 probe on `src/modules/skills-sync`                                                                                                 | 35    | Counted, not classified — unprobed at the bound                                                                                                                                                                                                                                                                                                                                                                                                                                            | —                                                 | unprobed                                                                                                                                                                  |
| cross-stage implicit timing | `rg -n -i "precedence\|lowest precedence first" src/modules/skills-sync/service.ts`                                                       | 2     | `:108,:132` prose; `SkillsSyncPaths.sourceRoots` (`:123`) carries the order as an array — the pin is the type                                                                                                                                                                                                                                                                                                                                                                              | —                                                 | none found (pinned by shape)                                                                                                                                              |
| two answers                 | `rg -n "export (async )?function \w*(resolve\|find\|locate)\w*(Workspace\|Root)\w*\(" server/src cli/src hooks`                           | 4     | "Which directory is the workspace" is answered by `runtime/paths.ts PathResolver`, `skills-sync/service.ts:947 resolveProjectRoot`, `cli/src/lib/workspace.ts:26 resolveWorkspace`, `hooks/lib/workspace.py:17 get_workspace_root` (a 5-branch env chain no TS resolver has). cli and hooks are separate processes by charter                                                                                                                                                              | in-process one live-and-wrong · others design gap | folds into the seam-2 row (skills-sync); KILL the cross-process ones — revives if `PathResolver`'s precedence changes without the two mirrors                             |

### `server/src/cli-shared` (20 files) + `cli/`

| seam                        | probe                                                                                                                                                    | count  | finding                                                                                                                                                                                                                                                                                                                                                                                          | fork                                                            | proposed row or kill                                                                                             |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| writer-without-reader       | per export: `rg -n "\b<name>\b" server/src cli/src --glob '!server/src/cli-shared/**'` → 27 at 0; then the same inside `cli-shared` excluding `index.ts` | 1      | 26 of the 27 are internal to `cli-shared`; `validateResourceDocument` (`resource-validation.ts:15`) has 0 callers in src or `cli/`, 3 in tests                                                                                                                                                                                                                                                   | surplus                                                         | KILL-sized: delete it (knip baseline drops)                                                                      |
| ambient scope               | `rg -n "process\.(env\|cwd)\|homedir\(\|MCP_WORKSPACE" server/src/cli-shared cli/src`                                                                    | 5 + 16 | `version-history-location.ts:56-57 resolveStateDbPath(resourceDir)` reads `MCP_RUNTIME_ROOT`/`MCP_WORKSPACE` inside a library function whose caller (`version-history.ts`) already received the workspace from the command; `cli/src/lib/workspace.ts:27` reads `MCP_WORKSPACE`/`HOME`/`cwd` once (the right place); `cli/src/cli.ts` repeats "(default: MCP_WORKSPACE or cwd)" 16× in help text | design gap (env is the CLI's input, but it should be read once) | ROW: `resolveStateDbPath` takes the resolved runtime root; check = `rg "process\.env" server/src/cli-shared` → 0 |
| missing state machine       | seam-3 probe on `server/src/cli-shared`                                                                                                                  | 45     | `version-history.ts` 14: `'error' in opened` ×3 (`:149,:498,:710`) on `openStateDb`'s `{db}\|{error}` union, `available.status === 'refused'\|'ready'` ×3 (`:739-761`) — discriminated unions, explicit phases                                                                                                                                                                                   | —                                                               | none found after that probe                                                                                      |
| cross-stage implicit timing | `rg -n -i "checkpoint\|holds the connection open" server/src/cli-shared/version-history.ts`                                                              | 3      | `:168,:173`: the DB stays open across the file write — enforced by `recordCheckpointedWrite` taking the write as a callback (`:510,:756`)                                                                                                                                                                                                                                                        | —                                                               | none found (pinned by shape)                                                                                     |
| two answers                 | `rg -n "export function resolveResourceDir" cli/src server/src/cli-shared`                                                                               | 2      | `cli/src/lib/workspace.ts:46` (which dir holds type T, with the legacy `<workspace>/<type>/` fallback that `runtime/paths.ts` owns) vs `cli-shared/resource-scaffold.ts:294` (which dir a resource id lives in) — a homonym answering two questions, plus the cli one re-derives the legacy-layout rule                                                                                          | design gap                                                      | ROW (small): rename the cli one `locateTypeDir`; the legacy rule is separate-process by charter — KILL that half |

### `server/src/infra/hooks` (2 files) + `hooks/*.py` + `hooks/lib`

| seam                        | probe                                                                                                                                                                                                                  | count          | finding                                                                                                                                                                                                                                                                                                                                                                                                                             | fork                             | proposed row or kill                                                                                                                                                                    |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| writer-without-reader       | per `HookRegistry` method: `rg -n "\.<emit\|register>\(" src --glob '!src/infra/hooks/**'` (13 methods)                                                                                                                | 0              | Every emitter has ≥1 src caller (`prompt-execution-pipeline.ts:446-468`, `gate-verdict-processor.ts:807-843`, `step-capture-service.ts:300`, `manager.ts:1209-1211`); the 3 `register*` are consumed by `telemetry/hook-observer.ts:95-97`. Python reads `run_status`, `current_step`, `total_steps`, `last_activity` from `chain_sessions` — `run_number` reaches it for nobody (chains seam 1)                                    | —                                | none found after that probe                                                                                                                                                             |
| ambient scope               | `rg -n "os\.environ\|getcwd\|getpid" hooks/*.py hooks/lib/*.py`                                                                                                                                                        | 29             | `workspace.py` 9 (five env vars in one precedence chain `:29-51`, two more for the db path `:121`), `ralph-stop.py` 7, `config_loader.py` 6, `cli_spawner.py` 5. Hooks do not inherit the server env by charter — env IS the input                                                                                                                                                                                                  | design gap                       | KILL: documented in the handbook §Environment (paths) — revives if the server persists its resolved roots for hooks to read                                                             |
| missing state machine       | `rg -n "run_status IS NULL" hooks/lib/db_reader.py`                                                                                                                                                                    | 1              | `:349-350` NULL read as active — the same fourth phase as chains seam 3                                                                                                                                                                                                                                                                                                                                                             | live-and-wrong                   | folds into the chains seam-3 row                                                                                                                                                        |
| cross-stage implicit timing | `rg -n "same transaction" server/src/modules/chains/manager.ts`                                                                                                                                                        | 1              | `chain_sessions` is rebuilt in the transaction that writes `chain_runs` (`manager.ts:499-526`), so a `PostToolUse` hook never reads a half-written projection. `ralph-*` inter-hook sequencing unprobed                                                                                                                                                                                                                             | —                                | none found; ralph sequencing unprobed                                                                                                                                                   |
| two answers                 | `rg -n "TERMINAL_RUN_STATUSES\s*=" hooks/lib`; `rg -n "'-->'\|'==>'\|'::'\|'>>'" hooks/lib/operators.py server/src/engine/execution/parsers`; `rg -n "DEFAULT_CONFIG\|Matches the shipped" hooks/lib/config_loader.py` | 3 vocabularies | Terminal statuses restated in Python (`db_reader.py:469`); operator tokens defined in `operators.py:100` and three TS parsers (`command-parser.ts:662`, `symbolic-operator-parser.ts:561`, `append-command-parser.ts:162-166`); `config_loader.py:30` hand-copies the shipped `hooks` defaults ("Matches the shipped server/config.json") and re-parses `config.json(c)` (`:142-205`). No test reads both sides of any of the three | live-and-wrong (drift is silent) | ROW: `hooks/tests/test_ts_parity.py` reading the TS literals (or a server-emitted `hook-contract.json`) for the three vocabularies; the check is the test going red on a one-sided edit |

### Ranking (sites affected × client-observable)

| #   | finding                                                                                   | sites                | client can observe                                                          | proposal           |
| --- | ----------------------------------------------------------------------------------------- | -------------------- | --------------------------------------------------------------------------- | ------------------ |
| 1   | chains seam 3 — `runStatus` fourth phase + two "finished" derivations, mirrored in Python | 22 TS + 1 py         | yes: a NULL-status run reads as active in `prompt-suggest`                  | ROW (chains)       |
| 2   | chains/versioning seam 5 — three `resolveTenantId`                                        | 3 defs, 9 call sites | yes: history scoping differs by path                                        | ROW (chains)       |
| 3   | skills-sync seam 2 — env read over the carried `SkillsSyncPaths`                          | 6                    | yes: project-scope export lands under the env workspace, not the served one | ROW (skills-sync)  |
| 4   | frameworks seam 5(a) — judge hardcodes four framework ids                                 | 1                    | yes: a workspace framework is absent from the judge's context               | ROW (frameworks)   |
| 5   | hooks seam 5 — three Python/TS vocabularies with no parity test                           | 3                    | only on drift                                                               | ROW (hooks)        |
| 6   | frameworks seam 2/4 — order-dependent loader singleton, ×4 siblings                       | 6 + 3                | no (test hazard)                                                            | ROW (class of 4)   |
| 7   | chains seam 4 — transaction requirement nothing asserts                                   | 2                    | no, until a caller forgets                                                  | ROW (chains)       |
| 8   | versioning seam 5(b) — one of four snapshot projections off-shape                         | 1 of 4               | no                                                                          | ROW (small)        |
| 9   | frameworks seam 1 — 7 write-only fields + 6 dead types                                    | 13                   | no                                                                          | ROW (delete)       |
| 10  | chains seam 1 — `base_chain_id`, `run_number` unread                                      | 2 columns            | no                                                                          | ROW (schema v32)   |
| 11  | cli-shared seam 2 — env read inside `resolveStateDbPath`                                  | 1                    | no                                                                          | ROW (small)        |
| 12  | `PromptFile`, `validateResourceDocument`, protected `getCombinedText`                     | 3                    | no                                                                          | KILL-sized deletes |

**Unprobed at the bound**: skills-sync seam 3 (35 null sites counted, not classified); `ralph-*`
inter-hook ordering; `cli/src` own exports for seam 1; the `hooks/lib` seam-1 pass beyond
`db_reader.py`; every seam on the three sibling `defaultLoader` modules (finding 6 names them by
shape only).

## Deviations

- DEV-P6-8 (slice 4): the shell hold lives in `applyDeferredAdvance` (the one advance owner), not the capture, and stage 17 reaches it by injection (`pipeline-builder.ts` +1); the render snapshot saves no `nodeId` so `chain_id` + `skip` cannot release an unanswered step; two callers that relied on the render running the check were updated (`shell-verify-final-step-complete.e2e`, `conformance/chain-lifecycle.yaml`); the Python reader had two converters, not one, and no `held` flag was added (a non-terminal row at `total + 1` already means held; the blob key set is pinned); R30 also makes a run on its last step with nothing pending visible (two tests rewritten); the held-run text came from stage 18 (`renderHeldRun`), edited out of list, and P6.30 is ~116 lines against ~30; the P6.30 driven case lives in the P6.34 e2e.
- DEV-P6-7 (slice 3): P6.21 edited stage 16 (+9, sole caller of `handleGateAction`) and fixed the blocked reply as the P6.23 class; P6.24's one-advance-site premise was false (two sites) and stage 16 applies the capture's returned advance; P6.38's clear lived in `review-lifecycle.ts`, edited as the attempt count's owner; P6.36 threaded the answered node through `verdictDetection` (type in `internal-state.ts`) and dropped an unused parameter through stage 16; P6.33's premise was false on this tree (a fallback already declined the review) and landed as a restructure with regression pins and no CHANGELOG bullet. A skipped step records status `failed` (no `skipped` in the vocabulary; a contract change not made here).
- DEV-P6-5 (P6.16): `framework_gates` had one reader (the gate-set resolver's veto); the sections were built in two places (`chain-operator-executor.ts`, `response-assembler.ts:1192`), the second outside the list — one function now gates both; no prompt text claimed grading. DEV-P6-6 (P6.18): the brief's `transcript_path` discriminator was disproved by the positive control (it is the parent's inside a subagent); `agent_id` is the field; the tests' payload builder gained the measured fields.
- DEV-P6-1 (P6.22): the anchor was false — stage 13 opens the current step's review on every gated resume before stage 16, so the deferred entry is never reached under shipped defaults; `answerVerdict` extracted (cognitive 21/15) → ~68 lines vs ~40; three test files outside the list rewritten. DEV-P6-2 (P6.15): the empty-reply refusal text, `hasCapturedOutput` and the `replyWasEmpty` branch deleted with their last caller; two pins flipped. DEV-P6-3 (P6.35): dropped the advance rather than refusing, so every bare FAIL records alike; the delegated control cannot check the warning (a "Chain complete" reply omits it — P6.37). DEV-P6-4 (P6.17): zero source lines; twin (b) reads "unknown node id" as an edge endpoint and carries the worker's reply on purpose; two commits so only twin (a) rides the mutated path.
- DEV-T3-17 (3.15): the reader also skips when the call captured nothing (a held run's re-answer is never captured, `step-capture-service.ts:94`), because node equality alone re-graded that text and reopened the review (M2); control (b) is a one-line re-answer since a sectioned one passes under both fixes; ESLint baseline lowered by one.
- DEV-T3-16 (3.14): twin (b) as briefed cannot go red in the pipeline — when the captured node has a review, stage 13 shows that same review; the twin lives in the unit test where shown and captured can differ. Twin (d)'s `:100` exit covers verdict-only calls, not a `user_response` call that captured no step (R17).
- DEV-T3-14 (3.12): the brief's "publish nothing when ambiguous" was probed and strands the client (the step re-renders with no mention of owed verdicts); `resolveShownReview` added in `review-target.ts` instead; the class was closed by deleting the node-less API (22 test files touched; tests-typecheck baseline lowered 343 → 342). DEV-T3-15 (3.13): the snapshot type lives in `chain-execution.ts`, not `chain-session.ts`; a re-run that captures no step needed a fallback to the previous save's node (found by driving, killed by its own mutant); stages 13 and 16 untouched because `isRunComplete` reading false was enough; the docs sentence scopes the hold to inline verification (P6.28).
- DEV-T3-12 (3.10): twin (a)'s control did not reproduce on `c67708e5` (the old hold already stopped on any open review; the eviction is unreachable in the harness; the run stands on N+1 when N's phase-guard review opens); the row's real defect was the unnamed clear-after-answer, found by twin (b); `gate-verdict-processor.ts` edited out of list and `clearReview` added to the store. DEV-T3-13 (3.11): the brief's expected result ("`chain/complete` on the call that clears the bounce") cannot occur — stage 13 ends every call once the run is past its last node; stage 16's two asks kept; comment-only edits in `overview.md`, `chain-session.ts`, `manager.ts`, one e2e and one mock.
- DEV-T3-8 (3.8): stage 19 never runs on the call that lands a late report (`landDetachedReport` sets the response; the pipeline stops at the first response), so `gradeLateReport` is called from stage 16 and stage 19 is built first in `pipeline-builder.ts`; `withoutStructuralFinding` lives in stage 19 beside `PHASE_GUARD_GATE_ID`. DEV-T3-9 (3.9): the completion point is stage 20, not 22 (stage 21 always sets the response); `18-execution-stage.ts` doc comment corrected; the capture hold was NOT flipped (R13/R14). DEV-T3-10 (3.6): the validator keys exceptions by file + enclosing function/type + kind; `run-registry.ts`, `manager.ts` and `sqlite-engine.ts` were edited because the validator's first run listed them; the `PendingGateReview` alias stays (20 files); two `@deprecated` markers that waited on an impossible condition now flip when `setPendingGateReview` has no `src` caller. DEV-T3-11 (3.7): the new e2e drives 3 of 4 flows (the detached flow stays in `detached-review-at-report.e2e`); the `gate_action` table cell in mcp-tools is unchanged (table reflow); `docs/guides/gates.md` §Escalation replacement is in the handoff, not applied. Slice-2 workflow: run #1 cancelled after 3.4/3.5 (delegated-node deadlock, P6.15–P6.17); run #2 carried 3.8 → 3.9 → 3.6 → 3.7.
- DEV-T1-1 (R6): row 1.2 premise false; re-cut. DEV-T2-1 (R7): both adapter boundaries differ from the brief, measured. DEV-T2-2: `validate:tool-parameter-reads` is 1,391 lines (row 2.6). DEV-T3-1: `PendingGateReview` is `Omit<GateReview, identity> & Partial<identity>`, not a plain alias (five sites build it without identity). DEV-T3-2: the `pendingGateReview` view is the run's one non-detached review, not the literal `reviews[currentNodeId]` (phase-guard and final-step reviews grade a node the run left). DEV-T3-3: `detachedGateReviews` stays as a stamped read-only view until 3.5. DEV-T3-4: no barrel export for the 3.2 decisions (knip). DEV-T3-5: `resolveReviewTarget` takes `nodeIds`; outcomes add `reopened`/`refused`. DEV-T3-6: the one answering path lives in the processor (R11). DEV-T3-7: `authority.resolveAction` deleted; two e2e-found defects fixed inside 3.3 (a PASS carrying N+1's answer did not advance N+1; stage 13 opened a review on a run held past its last node).
