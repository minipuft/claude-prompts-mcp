id: prompt_builder
name: Prompt Builder Adapter
description: >-
  Maps a complete authoring draft to resource_manager action validate; canonical
  validation remains server-owned.
script: script.py
runtime: python
timeout: 30000
enabled: true
execution:
  trigger: schema_match
  confirm: false
  strict: true
