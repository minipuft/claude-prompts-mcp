{
  "schemaVersion": 1,
  "generatedAt": "2026-09-22T19:24:34.778Z",
  "total": 9,
  "methods": [
    "EnhancedLogger.initLogFile",
    "ExecutionRecordStore.queryByChain",
    "ExecutionRecordStore.queryBySession",
    "GateAccumulator.getBySource",
    "GateAccumulator.getEntry",
    "GateAccumulator.has",
    "GateStateStore.recordValidation",
    "PendingConfirmationTracker.clear",
    "PendingConfirmationTracker.hasPending"
  ],
  "reasons": {
    "GateStateStore.recordValidation": "kept per its own doc comment (gate-state-store.ts): the only caller, LightweightGateSystem.validateContent, was deleted with GateValidator when every criteria type moved to a pipeline stage, leaving getSystemHealth's totalValidations/successRate/lastValidationTime fields with no producer. Deleting the writer without the health fields would leave a reader describing a number nothing computes; revives when 20-gate-review-stage.ts wires in ground-truth results, or gets killed together with the health fields in a follow-up row (R36, P4.52, 2026-09-17).",
    "GateAccumulator.getBySource": "sole test-observation window for add()'s live dedup/priority-override contract at entry-source granularity; getAll() (live, id-only) can't express it (P4.52/R36)",
    "GateAccumulator.getEntry": "sole test-observation window for a single entry's live retryLimit metadata written by add(); getAll() (live, id-only) can't express it (P4.52/R36)",
    "GateAccumulator.has": "sole test-observation window for add()'s live existence-after-dedup contract; kept alongside getEntries/getEntry/getBySource for the same reason (P4.52/R36)",
    "EnhancedLogger.initLogFile": "reached only through a duck-typed cast the static walker can't resolve: runtime/context.ts calls `(logger as any).initLogFile()` behind a `typeof ... === 'function'` guard, for every logger `createLogger()` returns (P4.52, 2026-09-20).",
    "PendingConfirmationTracker.hasPending": "sole non-destructive test-observation window for pending-confirmation state: checkAndClearPending clears on match (single-use), so hasPending is the only way tests assert a pending confirmation exists without consuming it — 10 call sites in pending-confirmation-tracker.test.ts (P4.52, 2026-09-20).",
    "PendingConfirmationTracker.clear": "has its own dedicated behavioral test (multi-entry clear + hasPending verification), not merely incidental teardown; kept as tested capability alongside hasPending (P4.52, 2026-09-20).",
    "ExecutionRecordStore.queryByChain": "sole test-observation window for the `WHERE chain_id = ?` predicate and its tenant scoping (AC4/AC5, execution-record-store.integration.test.ts); the only production readers (analytics-action-handler.ts, execution-history-action-handler.ts) call queryRecent(), which never filters by chain_id or session_id in JS — verified, not just assumed. Both write and query sides are documented as intentional in-process consumers in this class's own header (P4.52, 2026-09-20).",
    "ExecutionRecordStore.queryBySession": "sole test-observation window for the `WHERE session_id = ?` predicate and its tenant scoping (AC3/AC4/AC5, execution-record-store.integration.test.ts); see ExecutionRecordStore.queryByChain — same class, same finding (P4.52, 2026-09-20)."
  }
}
