{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/minipuft/claude-prompts-mcp/server/config.schema.json",
  "title": "Claude Prompts MCP Configuration",
  "description": "Configuration for the Claude Prompts MCP server",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference for IDE validation"
    },

    "server": {
      "type": "object",
      "additionalProperties": false,
      "description": "Server identity and transport settings",
      "properties": {
        "name": {
          "type": "string",
          "default": "claude-prompts",
          "description": "Server name reported to MCP clients"
        },
        "version": {
          "type": "string",
          "default": "1.0.0",
          "description": "DEPRECATED — settable via `cpm config set server.version` and reported by GET /health, but unrelated to the package version. Scheduled for removal (plan OQ-2); read the package version instead."
        },
        "transport": {
          "type": "string",
          "enum": ["stdio", "streamable-http", "both"],
          "default": "stdio",
          "description": "Transport protocol: 'stdio' for CLI tools (Claude Code), 'streamable-http' for web clients, 'both' for either"
        },
        "port": {
          "type": "integer",
          "default": 9090,
          "description": "Port for the Streamable HTTP transport (ignored for stdio)"
        }
      }
    },

    "prompts": {
      "type": "object",
      "additionalProperties": false,
      "description": "Prompt loading and registration",
      "properties": {
        "directory": {
          "type": "string",
          "default": "resources/prompts",
          "description": "Directory containing prompt definitions (relative to server root)"
        },
        "registerWithMcp": {
          "type": "boolean",
          "default": true,
          "description": "Register prompts as MCP resources for client discovery"
        }
      }
    },

    "identity": {
      "type": "object",
      "additionalProperties": false,
      "description": "Identity and client-profile resolution for scope isolation and delegation routing",
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["permissive", "locked"],
          "default": "permissive",
          "description": "Identity precedence mode"
        },
        "allowPerRequestOverride": {
          "type": "boolean",
          "default": true,
          "description": "Allow request claims to override launch defaults based on transport policy"
        },
        "launchDefaults": {
          "type": "object",
          "additionalProperties": false,
          "description": "Launch-time defaults used when request identity/profile metadata is missing",
          "properties": {
            "organizationId": {
              "type": "string",
              "description": "Default organization scope"
            },
            "workspaceId": {
              "type": "string",
              "description": "Default workspace scope"
            },
            "clientFamily": {
              "type": "string",
              "enum": [
                "claude-code",
                "codex",
                "gemini",
                "opencode",
                "cursor",
                "unknown"
              ],
              "description": "Authoritative launch-level client family for delegation routing"
            },
            "clientId": {
              "type": "string",
              "description": "Authoritative launch-level client identifier"
            },
            "clientVersion": {
              "type": "string",
              "description": "Authoritative launch-level client version"
            },
            "delegationProfile": {
              "type": "string",
              "enum": [
                "task_tool_v1",
                "spawn_agent_v1",
                "gemini_subagent_v1",
                "opencode_agent_v1",
                "cursor_agent_v1",
                "neutral_v1"
              ],
              "description": "Authoritative launch-level delegation profile"
            }
          }
        }
      }
    },

    "frameworks": {
      "type": "object",
      "additionalProperties": false,
      "description": "Frameworks (CAGEERF, ReACT, etc.) that guide LLM behavior. Controls three injection types: system-prompt (framework phases), gate-guidance (quality criteria), and style-guidance (response formatting).",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable framework system"
        },
        "dynamicToolDescriptions": {
          "type": "boolean",
          "default": true,
          "description": "Adapt MCP tool descriptions based on active framework"
        },
        "defaultFramework": {
          "type": "string",
          "default": "CAGEERF",
          "description": "Framework a scope falls back to when it has no persisted state row. Set per project to pin a repo to one framework (e.g. 'RADIANT' for design work). A runtime framework switch overrides this for that scope and persists."
        },
        "systemPromptFrequency": {
          "type": "integer",
          "default": 2,
          "minimum": 1,
          "description": "Inject framework guidance every N chain steps. Higher values reduce token usage at the cost of less frequent framework reinforcement."
        },
        "systemPromptTarget": {
          "type": "string",
          "enum": ["steps", "gates", "both"],
          "default": "steps",
          "description": "Where to inject framework guidance: 'steps' (normal execution), 'gates' (gate reviews), or 'both'"
        },
        "gateGuidanceFrequency": {
          "type": "integer",
          "default": 0,
          "minimum": 0,
          "description": "Inject gate criteria every N chain steps. 0 = first step only (default). Gate review steps always receive guidance regardless of this setting."
        },
        "gateGuidanceTarget": {
          "type": "string",
          "enum": ["steps", "gates", "both"],
          "default": "both",
          "description": "Where to inject gate criteria: 'steps' (normal execution), 'gates' (gate reviews), or 'both'"
        },
        "styleGuidance": {
          "type": "boolean",
          "default": true,
          "description": "Include response formatting guidance in prompts"
        },
        "styleGuidanceFrequency": {
          "type": "integer",
          "default": 0,
          "minimum": 0,
          "description": "Inject style guidance every N chain steps. 0 = first step only (default). Set to 1 for every step."
        },
        "styleGuidanceTarget": {
          "type": "string",
          "enum": ["steps", "gates", "both"],
          "default": "steps",
          "description": "Where to inject style guidance: 'steps' (normal execution), 'gates' (gate reviews), or 'both'"
        }
      }
    },

    "gates": {
      "type": "object",
      "additionalProperties": false,
      "description": "Quality gates for validating LLM outputs",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable the gate validation system"
        },
        "directory": {
          "type": "string",
          "default": "resources/gates",
          "description": "Directory containing gate definitions"
        },
        "frameworkGates": {
          "type": "boolean",
          "default": true,
          "description": "Apply framework-specific quality gates"
        },
        "enforcePendingVerdict": {
          "type": "boolean",
          "description": "DEPRECATED — accepted by `cpm config set` but read by no runtime path. Scheduled for removal (plan OQ-3)."
        },
        "evaluation": {
          "type": "object",
          "additionalProperties": false,
          "description": "Judge evaluation defaults. Gates with evaluation.mode 'judge' use context-isolated review via delegation.",
          "properties": {
            "defaultMode": {
              "type": "string",
              "enum": ["self", "judge"],
              "default": "self",
              "description": "Default evaluation mode for all gates: 'self' (same-context review) or 'judge' (context-isolated delegation)"
            },
            "defaultModel": {
              "type": "string",
              "description": "Default model hint for judge sub-agents (e.g., 'haiku' for cheap evaluation)"
            },
            "strict": {
              "type": "boolean",
              "default": true,
              "description": "Default strict mode: use failure-first framing for judge evaluation"
            }
          }
        },
        "harnessCovers": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
          },
          "default": [],
          "description": "Reminder subjects this installation's harness already covers (its own rules or hooks). A reminder gate whose `subject` is listed is not rendered; checks are never suppressed. Copy spellings from the Subject column of resources/gates/_index.md."
        },
        "reminderTokenBudget": {
          "type": "integer",
          "minimum": 0,
          "default": 800,
          "description": "Estimated tokens of reminder guidance rendered per dispatch. Reminders over the budget render as one line each, in priority order; nothing is dropped. Checks are outside the budget."
        }
      }
    },

    "verification": {
      "type": "object",
      "additionalProperties": false,
      "description": "Ralph Loops: autonomous verification via shell commands",
      "properties": {
        "inContextAttempts": {
          "type": "integer",
          "default": 3,
          "minimum": 0,
          "description": "Fix attempts within current context before spawning isolation. Set to 0 for immediate isolation."
        },
        "isolation": {
          "type": "object",
          "additionalProperties": false,
          "description": "Context isolation spawns a fresh Claude CLI to fix issues",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Enable spawning isolated Claude instances after in-context attempts fail"
            },
            "maxBudget": {
              "type": "number",
              "default": 1.0,
              "minimum": 0.01,
              "description": "Maximum cost (USD) per isolation spawn"
            },
            "timeout": {
              "type": "integer",
              "default": 300,
              "minimum": 30,
              "description": "Timeout in seconds for each isolation spawn"
            },
            "permissionMode": {
              "type": "string",
              "enum": ["delegate", "ask", "deny"],
              "default": "delegate",
              "description": "How spawned instance handles permission requests"
            }
          }
        }
      }
    },

    "versioning": {
      "type": "object",
      "additionalProperties": false,
      "description": "Prompt version control",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Track prompt versions"
        },
        "autoVersion": {
          "type": "boolean",
          "default": true,
          "description": "Auto-create versions on prompt changes"
        },
        "maxVersions": {
          "type": "integer",
          "default": 50,
          "minimum": 1,
          "description": "Maximum versions to retain per prompt"
        },
        "max_versions": {
          "type": "integer",
          "minimum": 1,
          "description": "DEPRECATED — the spelling `cpm config set` writes today; read as `maxVersions`. Scheduled for removal when the CLI writes camelCase (plan row 2.2)."
        },
        "auto_version": {
          "type": "boolean",
          "description": "DEPRECATED — the spelling `cpm config set` writes today; read as `autoVersion`. Scheduled for removal when the CLI writes camelCase (plan row 2.2)."
        }
      }
    },

    "phaseGuards": {
      "type": "object",
      "additionalProperties": false,
      "description": "Phase guards: deterministic structural validation of LLM output against framework phase definitions (zero LLM cost)",
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["enforce", "warn", "off"],
          "default": "enforce",
          "description": "Enforcement mode: 'enforce' blocks advancement until structural requirements pass, 'warn' logs advisory warnings, 'off' disables phase guards"
        },
        "maxRetries": {
          "type": "integer",
          "default": 2,
          "minimum": 0,
          "maximum": 10,
          "description": "Maximum retry attempts before phase guard review expires (enforce mode only)"
        }
      }
    },

    "execution": {
      "type": "object",
      "additionalProperties": false,
      "description": "Prompt execution behavior",
      "properties": {
        "judge": {
          "type": "boolean",
          "default": true,
          "description": "Enable %judge modifier for framework comparison"
        }
      }
    },

    "hooks": {
      "type": "object",
      "additionalProperties": false,
      "description": "Claude Code hook behavior (affects prompt-suggest.py output)",
      "properties": {
        "expandedOutput": {
          "type": "boolean",
          "default": false,
          "description": "Show detailed multi-line output instead of compact single-line. Useful for debugging or verbose mode."
        }
      }
    },

    "telemetry": {
      "type": "object",
      "additionalProperties": false,
      "description": "OpenTelemetry observability: tracing, metrics, and attribute policy. Separate from resources.observability (MCP resource toggles).",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Master switch for the telemetry subsystem. When false, no OTel SDK is initialized."
        },
        "mode": {
          "type": "string",
          "enum": ["off", "traces", "full"],
          "default": "off",
          "description": "Export mode: 'off' (disabled), 'traces' (spans/events only), 'full' (spans + OTel metrics)"
        },
        "exporterEndpoint": {
          "type": "string",
          "default": "http://localhost:4318",
          "description": "OTLP HTTP exporter endpoint URL"
        },
        "samplingRate": {
          "type": "number",
          "default": 1.0,
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Head sampling rate for traces (0.0–1.0). 1.0 = sample everything, 0.0 = sample nothing."
        },
        "attributePolicy": {
          "type": "object",
          "additionalProperties": false,
          "description": "Controls which attributes are included in trace spans and events for data safety.",
          "properties": {
            "businessContext": {
              "type": "boolean",
              "default": true,
              "description": "Include safe business-context attributes (cpm.prompt.id, cpm.execution.mode, etc.)"
            },
            "rawCommands": {
              "type": "boolean",
              "default": false,
              "description": "Include raw command text in traces. WARNING: may contain sensitive data."
            },
            "rawResponses": {
              "type": "boolean",
              "default": false,
              "description": "Include raw user responses in traces. WARNING: may contain sensitive data."
            },
            "allowlist": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Custom attribute names to explicitly include in traces"
            }
          }
        }
      }
    },

    "analysis": {
      "type": "object",
      "additionalProperties": false,
      "deprecated": true,
      "description": "DEPRECATED - no runtime path reads this section. It is still parsed so an existing config.json keeps loading, and is scheduled for removal in the next major. The gate service that consumed it and the LLM side client behind it were both deleted; for model-graded gate evaluation use the '%judge' modifier or gates.evaluation.defaultMode. ConfigManager.getAnalysisConfig() still returns the parsed value and ConfigManager warns once at load when the section is present.",
      "properties": {
        "semanticAnalysis": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "llmIntegration": {
              "type": "object",
              "additionalProperties": false,
              "deprecated": true,
              "description": "DEPRECATED with its parent section - every member is parsed and defaulted, and none is consulted. Modelled on LLMIntegrationConfig in src/shared/types/core-config.ts. There is deliberately no 'mode' member: the CLI used to accept a 'mode' of 'on'|'off' here that no reader consulted. That spelling is gone from both tool surfaces, and ConfigManager still adopts it into 'enabled' for configs already carrying one so the deprecation notice names a single spelling.",
              "properties": {
                "enabled": {
                  "type": "boolean",
                  "default": false,
                  "description": "Enable LLM-backed semantic analysis. Changing this requires a restart."
                },
                "apiKey": {
                  "type": ["string", "null"],
                  "default": null,
                  "description": "API key for the LLM provider"
                },
                "endpoint": {
                  "type": ["string", "null"],
                  "default": null,
                  "description": "Custom endpoint URL; the provider is auto-detected from the URL"
                },
                "model": {
                  "type": "string",
                  "default": "gpt-4",
                  "description": "Model name used for analysis requests"
                },
                "maxTokens": {
                  "type": "integer",
                  "default": 1000,
                  "minimum": 100,
                  "description": "Maximum tokens per analysis request"
                },
                "temperature": {
                  "type": "number",
                  "default": 0.1,
                  "minimum": 0,
                  "description": "Temperature for analysis requests"
                }
              }
            }
          }
        }
      }
    },

    "logging": {
      "type": "object",
      "additionalProperties": false,
      "description": "Server logging configuration",
      "properties": {
        "level": {
          "type": "string",
          "enum": ["error", "warn", "info", "debug"],
          "default": "info",
          "description": "Minimum log level"
        },
        "directory": {
          "type": "string",
          "default": "./logs",
          "description": "Log file directory"
        }
      }
    },

    "resources": {
      "type": "object",
      "additionalProperties": false,
      "description": "MCP resources configuration. Master switch must be enabled for any resources to register.",
      "properties": {
        "registerWithMcp": {
          "type": "boolean",
          "default": false,
          "description": "Master switch: register resources with MCP protocol (default: false for token efficiency)"
        },
        "prompts": {
          "type": "object",
          "additionalProperties": false,
          "description": "Prompt resources (resource://prompt/...)",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Enable prompt resources"
            },
            "defaultRegistration": {
              "type": "boolean",
              "description": "DEPRECATED — accepted by `cpm config set` but read by no runtime path. Scheduled for removal (plan OQ-3)."
            }
          }
        },
        "gates": {
          "type": "object",
          "additionalProperties": false,
          "description": "Gate resources (resource://gate/...)",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Enable gate resources"
            }
          }
        },
        "frameworks": {
          "type": "object",
          "additionalProperties": false,
          "description": "Framework resources (resource://framework/...)",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Enable framework resources"
            }
          }
        },
        "observability": {
          "type": "object",
          "additionalProperties": false,
          "description": "Observability resources (sessions and metrics)",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Enable observability resources"
            },
            "sessions": {
              "type": "boolean",
              "default": true,
              "description": "Enable session resources (resource://session/...)"
            },
            "metrics": {
              "type": "boolean",
              "default": true,
              "description": "Enable metrics resources (resource://metrics/...)"
            }
          }
        },
        "logs": {
          "type": "object",
          "additionalProperties": false,
          "description": "Logs resource for debugging and observability",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Enable logs resource (resource://logs/)"
            },
            "maxEntries": {
              "type": "integer",
              "default": 500,
              "minimum": 50,
              "maximum": 5000,
              "description": "Maximum log entries to retain in memory"
            },
            "defaultLevel": {
              "type": "string",
              "enum": ["error", "warn", "info", "debug"],
              "default": "info",
              "description": "Minimum level to buffer (entries below this level are not stored)"
            }
          }
        }
      }
    },

    "advanced": {
      "type": "object",
      "additionalProperties": false,
      "description": "Internal settings (most users won't need to change)",
      "properties": {
        "sessions": {
          "type": "object",
          "additionalProperties": false,
          "description": "Chain session lifecycle management",
          "properties": {
            "timeoutMinutes": {
              "type": "integer",
              "default": 1440,
              "description": "Idle session timeout (default: 24 hours)"
            },
            "reviewTimeoutMinutes": {
              "type": "integer",
              "default": 30,
              "description": "Gate review timeout"
            },
            "cleanupIntervalMinutes": {
              "type": "integer",
              "default": 5,
              "description": "Background cleanup frequency"
            }
          }
        }
      }
    }
  },
  "additionalProperties": false
}
