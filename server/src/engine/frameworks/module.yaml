schemaVersion: 1
id: engine-frameworks
kind: domain
lifecycle: canonical
description: Loads, validates, selects, and applies reasoning frameworks.
children: internal
owns:
  - capability: Framework selection
    symbol: FrameworkManager
  - capability: Framework validity
    symbol: FrameworkManager
