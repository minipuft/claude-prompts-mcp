version: 1
runtime: 'container'
build:
  dockerfile: 'src/Dockerfile'
  context: '.'
startCommand:
  type: 'http'
  command: ['node', 'dist/index.js', '--transport=streamable-http', '--quiet']
  configSchema:
    type: 'object'
    properties: {}
