{
  "version": "1.2.0",
  "lastUpdated": "2025-02-15T00:00:00Z",
  "tools": {
    "prompt_engine": {
      "description": "🚀 PROMPT ENGINE — Metadata-driven execution surface tuned for LLM clients.\n\n**Operators (always available):**\n• Chain `-->` composes steps while preserving `chain_id`\n• Framework `@` temporarily injects a methodology (e.g., `@ReACT`)\n• Gate `::` adds inline validation criteria\n\n**Gate review answers:** When Stage 5 pauses for human validation, resume with `gate_verdict: \"GATE_REVIEW: PASS|FAIL - reason\"` plus the original `chain_id`.\n\n**Guides-on-demand:** Type `>>guide <topic>` (or `prompt_engine({\"command\": \">>guide gate controls\"})`) to surface usage patterns, temporary gate schemas, and full parameter catalogs without memorizing payloads.\n\nResponses echo gate scope and requested quality gates so you can reason about validation at a glance.",
      "shortDescription": "🔥 Guide-aware execution of prompts, templates, and chains",
      "category": "execution",
      "parameters": {
        "command": "Prompt or chain command. Supports symbolic operators (`-->`, `@`, `::`). Use JSON when arguments span multiple lines.",
        "chain_id": "Resume token returned in responses (e.g., `chain-demo#2`). Reuse with the same command to continue a chain.",
        "user_response": "Result for the previous step; required when advancing chains.",
        "gate_verdict": "Answer to a pending gate review in the format `GATE_REVIEW: PASS|FAIL - reason`. Use this field instead of stuffing the verdict into `user_response`.",
        "execution_mode": "Force how the pipeline executes when semantic detection guesses wrong: `prompt`, `template`, `chain`, or `auto`.",
        "api_validation": "Manual gate-review handshake (defaults off). Set true only when you plan to send `gate_verdict` so the chain pauses without forcing retries.",
        "gate_scope": "Where validation applies: `execution`, `session`, `chain`, or `step`.",
        "quality_gates": {
          "type": "array",
          "description": "List of gate IDs to enforce. Combine with `>>guide gate controls` for the latest catalog."
        },
        "custom_checks": {
          "type": "array",
          "description": "Inline validation definitions: [{\"name\": \"production-ready\", \"description\": \"Include tests\"}, …]"
        },
        "temporary_gates": {
          "type": "array",
          "description": "Full gate definitions (id, criteria, severity). If an ID matches an existing gate, it automatically reuses that gate instead of duplicating it."
        },
        "force_restart": "Create a new chain execution (increments chain ID). Use `command'.",
        "timeout": "Execution timeout in milliseconds.",
        "options": "Bag of execution-specific options forwarded to downstream processors."
      },
      "frameworkAware": {
        "enabled": "🚀 PROMPT ENGINE [FRAMEWORK-ENABLED]: Responses include methodology-specific coaching plus guide entries filtered by the active framework.",
        "disabled": "🚀 PROMPT ENGINE [FRAMEWORK-DISABLED]: Executes without methodology overlays while still supporting metadata-driven `>>guide` output.",
        "parametersEnabled": {
          "execution_mode": "Framework-aware override hints are surfaced via the guide."
        },
        "parametersDisabled": {
          "execution_mode": "Standard override behavior."
        }
      }
    },
    "prompt_manager": {
      "description": "🧭 PROMPT MANAGER — Lifecycle + discovery hub with inline guidance.\n\n• `action:\"guide\" goal:\"gate config\"` ranks canonical verbs and calls out high-risk ones.\n• Legacy verbs (`create_prompt`, `create_template`) stay blocked unless `allow_legacy:true`; the guide suggests modern replacements automatically.\n• Responses embed lifecycle warnings, issue summaries, and telemetry-backed hints so an LLM can correct requests without doc hunting.",
      "shortDescription": "Manage prompts with guide-backed lifecycle cues",
      "category": "management",
      "parameters": {
        "action": "Supported: create, update, delete, list, reload, analyze_type, migrate_type, analyze_gates, suggest_temporary_gates, guide.",
        "allow_legacy": "Set true to temporarily unlock deprecated verbs (not recommended).",
        "goal": "Optional goal text consumed by the guide action for targeting suggestions.",
        "include_legacy": "When true, guide output includes deprecated or high-risk verbs.",
        "id": "Prompt identifier required for lifecycle and analysis actions.",
        "name": "Friendly name for create/update paths.",
        "description": "Human-readable summary stored with the prompt.",
        "user_message_template": "Main prompt body (Nunjucks friendly). Required for create.",
        "system_message": "Optional system message injected at run time.",
        "content": "Full JSON payload for advanced create/update scenarios.",
        "category": "Label for organization and list filtering.",
        "arguments": "Structured argument definitions for prompts that expect typed inputs.",
        "gate_configuration": "Explicit gate configuration applied to the prompt.",
        "search_query": "Filter syntax for the list action."
      },
      "frameworkAware": {
        "enabled": "🧭 PROMPT MANAGER [FRAMEWORK-ENABLED]: Guide messaging and validations reflect the active methodology.",
        "disabled": "🧭 PROMPT MANAGER [FRAMEWORK-DISABLED]: Standard lifecycle handling with metadata-backed guides."
      }
    },
    "system_control": {
      "description": "⚙️ SYSTEM CONTROL — Framework, gate, and diagnostics cockpit.\n\n• `>>help` routes here and renders the same metadata-driven guide as `system_control action:\"guide\"`.\n• Use `action:\"framework\"` for switch/list/enable/disable, `action:\"gates\"` for discovery, `action:\"analytics\"` for planned telemetry surfaces.\n• Responses reiterate the active methodology and gate state so downstream LLMs stay aware of context.",
      "shortDescription": "Manage frameworks, gates, analytics, and maintenance",
      "category": "system",
      "parameters": {
        "action": "Top-level command: status, framework, gates, analytics, config, maintenance, guide.",
        "operation": "Sub-command for the selected action (e.g., framework switch/list).",
        "framework": "Framework identifier when switching (CAGEERF, ReACT, 5W1H, SCAMPER).",
        "topic": "Guide focus filter (e.g., \"gates\" or \"analytics\").",
        "reason": "Audit-friendly explanation for sensitive operations.",
        "confirm": "Boolean acknowledgment for destructive actions.",
        "include_history": "Include recorded history when supported.",
        "include_metrics": "Request detailed analytics in status responses.",
        "show_details": "Expand verbose payloads for list/status commands."
      },
      "frameworkAware": {
        "enabled": "⚙️ SYSTEM CONTROL [FRAMEWORK-ENABLED]: Guide + routing automatically reflect the active methodology catalog.",
        "disabled": "⚙️ SYSTEM CONTROL [FRAMEWORK-DISABLED]: Outputs omit methodology guidance but keep gate/system summaries."
      }
    }
  }
}
