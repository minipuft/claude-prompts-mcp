/**
 * File system and category management operations
 */
import { OperationResult, FileOperationResult, PromptManagerDependencies } from '../core/types.js';
/**
 * File system operations for prompt management
 */
export declare class FileOperations {
    private logger;
    private configManager;
    constructor(dependencies: Pick<PromptManagerDependencies, 'logger' | 'configManager'>);
    /**
     * Update prompt implementation (shared by create/update)
     */
    updatePromptImplementation(promptData: any): Promise<OperationResult>;
    /**
     * Delete prompt implementation
     */
    deletePromptImplementation(id: string): Promise<OperationResult>;
    /**
     * Create or update prompt file
     */
    createOrUpdatePromptFile(promptData: any, effectiveCategory: string, promptsFile: string): Promise<FileOperationResult>;
    /**
     * Validate file system state
     */
    validateFileSystemState(): Promise<{
        valid: boolean;
        issues: string[];
        stats: Record<string, number>;
    }>;
    /**
     * Backup prompt files
     */
    backupPrompts(): Promise<{
        backupPath: string;
        fileCount: number;
    }>;
    /**
     * Get file system statistics
     */
    getFileSystemStats(): Promise<{
        totalCategories: number;
        totalPrompts: number;
        totalFiles: number;
        diskUsage: number;
    }>;
}
