{
  "name": "claude-prompts-root",
  "version": "4.0.0",
  "private": true,
  "description": "Root package for git hooks and workspace management",
  "//workspaces": "server/ is deliberately NOT a workspace — it keeps its own lockfile and CI installs it with `npm ci --prefix server`.",
  "workspaces": [
    "cli"
  ],
  "scripts": {
    "prepare": "husky || true",
    "mcpb:validate": "mcpb validate manifest.json",
    "sync": "bash scripts/sync-to-cache.sh",
    "dev": "npm run sync && echo 'Restart Claude Code to apply hook changes'",
    "dev:claude": "bash scripts/dev-claude.sh",
    "dev:claude:resume": "bash scripts/dev-claude.sh --resume",
    "renovate:request-run": "node scripts/request-renovate-run.js",
    "renovate:request-run:self-test": "node scripts/request-renovate-run.js --self-test"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "2.1.2",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7"
  },
  "dependencies": {
    "graphviz": "^0.0.9"
  },
  "//overrides": "tmp: GHSA-ph9p-34f9-6g65/GHSA-52f5-9888-hmc6 — @anthropic-ai/mcpb (latest) pins @inquirer→external-editor→tmp@0.0.33; drop when mcpb's chain moves past tmp 0.2.6",
  "overrides": {
    "tmp": "^0.2.6"
  }
}
