{
  "claude-prompts": {
    "command": "node",
    "args": ["${CLAUDE_PLUGIN_ROOT}/server/dist/index.js", "--transport=stdio"],
    "env": {
      "MCP_WORKSPACE": "${CLAUDE_PLUGIN_ROOT}",
      "MCP_RESOURCES_PATH": "${CLAUDE_PLUGIN_ROOT}/server/resources"
    }
  }
}
